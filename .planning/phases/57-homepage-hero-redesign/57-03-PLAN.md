---
phase: 57-homepage-hero-redesign
plan: 03
type: execute
wave: 3
depends_on: [57-02]
files_modified:
  - src/app/page.tsx
autonomous: true
requirements: [HERO-04]

must_haves:
  truths:
    - "Solutions section uses BentoGrid + BentoCard with Phase 56 surface tokens"
    - "Results section has no bg-muted, no blur orbs, no scale-105, no animationDelay inline styles"
    - "Free Tools section cards have no hover scale transforms, no accent-underline decorators"
    - "All section h2 elements use text-section-title (not text-responsive-2xl)"
    - "No h2 wraps content in a text-accent span"
    - "No accent underline absolute spans under any h2"
    - "Final CTA uses Button variant=accent (not variant=default)"
    - "Final CTA h2 has no text-accent span coloring"
    - "All sections use py-section on bg-background — no alternating muted backgrounds"
    - "Free Tools section arrow SVGs replaced with lucide ArrowRight — no group-hover:translate-x-1"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Fully redesigned homepage with consistent section rhythm"
      contains: "text-section-title"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/ui/bento-grid.tsx"
      via: "BentoGrid + BentoCard components in Solutions section"
      pattern: "BentoGrid|BentoCard"
    - from: "src/app/page.tsx"
      to: "src/app/globals.css"
      via: "text-section-title, py-section, surface-raised, border-border tokens"
      pattern: "text-section-title|py-section|surface-raised"
---

<objective>
Apply section rhythm and anti-pattern removal to all remaining homepage sections: Solutions, Results, Free Tools, and Final CTA.

Purpose: Delivers HERO-04 — deliberate whitespace-only vertical rhythm, consistent section h2 tokens, elimination of all hover transforms, blur orbs, and alternating backgrounds.
Output: A fully redesigned page.tsx where every section is consistent, token-based, and transform-free.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-homepage-hero-redesign/57-CONTEXT.md
@.planning/phases/57-homepage-hero-redesign/57-RESEARCH.md
@.planning/phases/57-homepage-hero-redesign/57-02-SUMMARY.md
@src/app/page.tsx
@src/components/ui/bento-grid.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Solutions Grid with BentoGrid + apply section rhythm</name>
  <files>src/app/page.tsx</files>
  <action>
Replace the Solutions Grid section with a BentoGrid layout. Then apply section rhythm fixes to the Results section and Free Tools section headers.

**Solutions Grid section — full replacement:**

Replace the entire `<section className="py-section px-4 sm:px-6">` Solutions block (currently a `grid lg:grid-cols-3` of Card components) with:

```tsx
{/* Solutions Section */}
<section className="py-section px-4 sm:px-6">
  <div className="container-wide">
    <div className="text-center mb-content-block">
      <h2 className="text-section-title text-foreground mb-comfortable text-balance">
        How We Solve Your Biggest Problems
      </h2>
      <p className="text-lead text-muted-foreground max-w-3xl mx-auto">
        Three ways we help businesses go from struggling to scaling.
      </p>
    </div>

    <BentoGrid className="auto-rows-auto">
      <BentoCard
        name="Ship Features Faster"
        description="Launch new features in days, not months. We handle the entire technical stack — React/Next.js, API architecture, and 99.9% uptime."
        Icon={Code2}
        href={ROUTES.CONTACT}
        cta="Get Started"
        background={<div className="absolute inset-0" aria-hidden="true" />}
        className="col-span-3 md:col-span-2 bg-surface-raised border-border shadow-sm"
      />
      <BentoCard
        name="Fix Revenue Leaks"
        description="Stop losing leads to broken processes. Automate everything that slows you down — CRM integration, lead scoring, real-time analytics."
        Icon={Settings}
        href={ROUTES.CONTACT}
        cta="Learn More"
        background={<div className="absolute inset-0" aria-hidden="true" />}
        className="col-span-3 md:col-span-1 bg-surface-raised border-border shadow-sm"
      />
      <BentoCard
        name="Scale Without Breaking"
        description="Handle 10x growth without rebuilding. We future-proof your tech from day one — performance audits, infrastructure planning, cost optimization."
        Icon={BarChart3}
        href={ROUTES.CONTACT}
        cta="See How"
        background={<div className="absolute inset-0" aria-hidden="true" />}
        className="col-span-3 bg-surface-raised border-border shadow-sm"
      />
    </BentoGrid>
  </div>
</section>
```

Note: `bg-surface-raised` and `border-border` override BentoCard's hardcoded `bg-background` and `rgba()` box-shadow. `shadow-sm` provides subtle elevation. `auto-rows-auto` overrides `auto-rows-[22rem]` since cell content height varies.

**Results section — anti-pattern removal:**

Find the `<section className="relative py-section px-4 sm:px-6 bg-muted overflow-hidden">` and apply these changes:

1. Section wrapper: remove `bg-muted overflow-hidden relative` — change to `py-section px-4 sm:px-6`
2. Remove the three absolute background divs entirely:
   - `<div className="absolute inset-0 bg-primary/5" />`
   - `<div className="absolute top-1/4 left-1/4 w-96 h-96 bg-accent/5 rounded-full blur-3xl" />`
   - `<div className="absolute bottom-1/4 right-1/4 w-96 h-96 bg-info-text/5 rounded-full blur-3xl" />`
3. Section h2: Replace the entire `<h2>` block (which wraps "Proven Impact" in `text-accent relative inline-block` with an accent underline `<span>`) with:
   ```tsx
   <h2 className="text-section-title text-foreground mb-comfortable text-balance">
     Proven Impact
   </h2>
   ```
4. The supporting paragraph: Replace `<span className="text-accent font-semibold">measurable results</span>` inside it with plain text: `measurable results`
5. Results grid cards: For each result card, remove:
   - `style={{ animationDelay: \`${index * 100}ms\` }}` from the outer div
   - `group-hover:transform group-hover:scale-105` from Card className
   - The `Card hover` prop — change `<Card variant="glassLight" size="lg" hover ...>` to `<Card variant="glassLight" size="lg" ...>`
   - The top accent line div: `<div className="absolute top-0 left-1/2 transform -translate-x-1/2 w-16 h-1 bg-accent rounded-b-full"></div>`
   - The glow overlay div: `<div className="absolute inset-0 bg-accent/5 rounded-lg opacity-0 group-hover:opacity-100 ...">`
   - The `absolute -inset-4 bg-accent/10 ...` background div around the metric text
   - The `group-hover:text-accent` transition class on the metric text
   - The `group-hover:text-foreground` transition class on the label
   - Keep only: metric value with `text-4xl font-black text-foreground font-mono`, label with `text-lg font-bold text-muted-foreground`, period badge with `text-sm text-muted-foreground font-medium px-3 py-1 bg-muted/50 rounded-full border border-border/50`
   - The connecting dots divs between cards: remove entirely (decorative)
6. Bottom accent text: Replace `<span className="text-accent font-semibold">growing businesses</span>` with plain text
7. Remove the `mb-24` on the header div — use `mb-content-block`

**Free Tools section — h2 and hover fixes:**

1. Section h2: Replace the `<h2>` block (wraps "Free Business Tools" in accent span with underline) with:
   ```tsx
   <h2 className="text-section-title text-foreground mb-comfortable text-balance">
     Free Business Tools
   </h2>
   ```
2. Remove `mt-heading` from the supporting paragraph — it follows `mb-comfortable` on h2
3. For each of the 3 tool link-cards, on the `<Card>` element:
   - Remove `hover` prop
   - Remove `hover:transform hover:scale-105` from className
   - Remove `transform-gpu` from className
   - Keep: `variant="glassLight" size="lg" className="relative border border-accent/20 hover:border-accent/40 transition-smooth"`
   - Remove the top accent line div: `<div className="absolute top-0 left-1/2 transform -translate-x-1/2 w-16 h-1 bg-accent rounded-b-full"></div>`
   - Remove icon container hover class: `group-hover:bg-primary/20` — keep `bg-primary/10`
   - Change the "Try Calculator" / "Get Estimate" / "Analyze Site" link from a custom SVG arrow to lucide ArrowRight:
     Replace the `<svg ...>...</svg>` with `<ArrowRight className="w-5 h-5" aria-hidden="true" />` — no `group-hover:translate-x-1 transition-transform`

Note: `mb-20` on the Tools section header — replace with `mb-content-block` to match other sections.
  </action>
  <verify>
    <automated>bun test tests/unit/homepage.test.tsx 2>&1 | grep -E "(pass|fail|✓|✗)" | head -20</automated>
    <manual>Solutions section shows 3 bento grid cards. Results section is on bg-background (no muted background). Results cards do not scale on hover. No blur orbs visible.</manual>
    <sampling_rate>After this task</sampling_rate>
  </verify>
  <done>Solutions section uses BentoGrid with surface-raised cards. Results section has no bg-muted, no blur orbs, no animationDelay. Free Tools section has no scale transforms. All section h2s use text-section-title.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Final CTA section and unused import cleanup</name>
  <files>src/app/page.tsx</files>
  <action>
Fix the Final CTA section and clean up remaining anti-patterns and unused imports.

**Final CTA section fixes:**

Find `<section className="py-section px-4 sm:px-6">` containing `<Card variant="glassSection"...>`:

1. Replace the h2 block (currently wraps "Your competitors..." and `<span className="block text-accent mt-4">Why don't you?</span>`) with:
   ```tsx
   <h2 className="text-section-title text-foreground mb-content-block max-w-4xl mx-auto text-balance">
     Your competitors ship faster. Why don&apos;t you?
   </h2>
   ```
2. Icon container: keep `<Rocket className="w-10 h-10 text-accent" />` inside `<div className="w-20 h-20 rounded-2xl bg-primary/10 flex items-center justify-center mx-auto mb-comfortable">` — this is fine (not a decorative effect, it's meaningful)
3. Primary CTA Button: change `variant="default"` to `variant="accent"`, change `size="lg"` to `size="xl"`
4. Secondary CTA Button: change `variant="outline"` to `variant="ghost"`, change `size="lg"` to `size="xl"`

**Unused import cleanup:**

After all section edits, audit the import block at the top of page.tsx. Remove any icon imports that are no longer used:
- `Clock` — was used in hero trust indicators (removed in Plan 02). Remove if not used elsewhere.
- `Users` — was used in hero trust indicators (removed in Plan 02). Remove if not used elsewhere.
- `Rocket` — used in Final CTA icon container. Keep.
- `TrendingUp`, `Calculator`, `Zap` — used in Free Tools section. Keep.
- `Code2`, `Settings`, `BarChart3` — used in BentoGrid Solutions section. Keep.
- `ArrowRight` — used in hero CTAs and Final CTA. Keep.

Remove the `solutions` and `results` arrays at the top of `HomePage` if the BentoGrid approach in Task 1 removes the `solutions.map()` call. The `results` array may still be mapped — keep if so.

After this task, run the full check suite:
```
bun run typecheck
bun test tests/unit/homepage.test.tsx
bun test tests/
```

Fix any TypeScript errors before marking done. Common issues:
- BentoCard `className` prop is typed as `string` (required, not optional) — always pass it
- If `bg-surface-raised` is not a recognized Tailwind class, use `bg-[var(--color-surface-raised)]`
- If `border-border` renders duplicated (`border border-border`), that's correct — `border` sets the border-width, `border-border` sets the color
  </action>
  <verify>
    <automated>bun test tests/unit/homepage.test.tsx && bun run typecheck 2>&1 | tail -5</automated>
    <manual>Final CTA has two buttons: solid amber (accent) and ghost. h2 is plain foreground text, no accent span. All sections use consistent py-section spacing.</manual>
    <sampling_rate>After this task</sampling_rate>
  </verify>
  <done>Final CTA buttons use variant=accent and variant=ghost at size=xl. No unused icon imports remain. All 10 homepage unit tests pass. TypeScript check clean. Full bun test tests/ suite passes.</done>
</task>

</tasks>

<verification>
bun test tests/unit/homepage.test.tsx
bun run typecheck
bun test tests/
bun run build 2>&1 | tail -10
</verification>

<success_criteria>
All 10 homepage unit tests pass (HERO-01 through HERO-04 fully covered). TypeScript clean. Full test suite passes. Build succeeds. No section uses bg-muted. No element has scale-105, blur-3xl, or animationDelay. All section h2s use text-section-title. BentoGrid is in the Solutions section.
</success_criteria>

<output>
After completion, create `.planning/phases/57-homepage-hero-redesign/57-03-SUMMARY.md`
</output>

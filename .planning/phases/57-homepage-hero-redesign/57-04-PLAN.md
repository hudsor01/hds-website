---
phase: 57-homepage-hero-redesign
plan: 04
type: execute
wave: 4
depends_on: [57-03]
files_modified: []
autonomous: false
requirements: [HERO-01, HERO-02, HERO-03, HERO-04]

must_haves:
  truths:
    - "User confirms hero section is visually distinctive and premium on dark background"
    - "User confirms headline hierarchy is clear — large h1 above smaller muted supporting text"
    - "User confirms primary CTA is visually unmissable (solid amber)"
    - "User confirms sections feel deliberately spaced with no visual clutter"
    - "User confirms no hover transforms or animation effects anywhere"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Fully redesigned homepage — verified by user"
  key_links: []
---

<objective>
Human visual verification of the complete Phase 57 homepage redesign.

Purpose: Automated tests confirm structure and classes. This checkpoint confirms the visual result actually matches the premium, purposeful aesthetic the user specified — the subjective quality no assertion can capture.
Output: User approval (or feedback for correction) on the complete redesigned homepage.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-homepage-hero-redesign/57-CONTEXT.md
@.planning/phases/57-homepage-hero-redesign/57-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification: Complete homepage redesign</name>
  <action>Run dev server and verify all visual aspects of the redesigned homepage match Phase 57 requirements.</action>
  <what-built>
Complete homepage redesign including:
- Full-width centered typographic hero on dark solid background (bg-background-dark) with optional grid-pattern-minimal texture
- h1 using text-page-title class — plain foreground color, no accent spans, no subtitle line
- Supporting paragraph in text-lead text-muted-foreground
- Primary CTA: Button variant=accent (solid amber, size=xl)
- Secondary CTA: Button variant=ghost (size=xl)
- Solutions section replaced with BentoGrid using BentoCard — surface-raised cards with border-border
- Results section: bg-background (not muted), no blur orbs, no scale animations, no animationDelay
- Free Tools section: cards without hover scale, no accent underline decorators, ArrowRight icons
- All section h2s: text-section-title, plain text-foreground — no accent spans, no underline decorators
- Final CTA: Button variant=accent + variant=ghost at size=xl, h2 plain foreground
  </what-built>
  <how-to-verify>
1. Start dev server: `bun run dev`
2. Open: http://localhost:3000

**Hero (HERO-01, HERO-02, HERO-03):**
- Background is visibly dark (distinct from rest of page)
- h1 is very large, bold, plain white/foreground — no yellow/amber accent text in the headline
- Supporting text below h1 is visibly smaller and in muted gray
- Two buttons: one solid amber (primary), one ghost/transparent (secondary) — visually distinct
- No terminal card visible. No blur orb halos visible.

**Section rhythm (HERO-04):**
- Sections separated by whitespace only — no alternating background colors
- Solutions section shows 3 cards in bento grid arrangement (2+1 on desktop)
- Results section is on the same background as surrounding sections — no muted/gray background
- Free Tools section cards do not lift/scale on hover
- All section h2 elements appear visually consistent in size and weight

**Final CTA:**
- Two buttons: solid amber (Get Your Free Roadmap), ghost (See Proven Results First)
- h2 text is plain foreground — no yellow/amber colored portion

**Hover states:**
- Hovering any card shows border color change only — no lifting, scaling, or glow
  </how-to-verify>
  <verify>bun run dev — load http://localhost:3000 and inspect visually</verify>
  <done>User visually approves the complete homepage redesign.</done>
  <resume-signal>Type "approved" if the redesign looks correct. Describe any specific issues (section name + problem) if anything needs adjustment.</resume-signal>
</task>

</tasks>

<verification>
User visual approval of redesigned homepage.
</verification>

<success_criteria>
User approves the visual result. All requirements HERO-01 through HERO-04 visually confirmed. Phase 57 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/57-homepage-hero-redesign/57-04-SUMMARY.md`
</output>

---
phase: 60-content-page-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/about/page.tsx
  - src/app/contact/page.tsx
autonomous: true
requirements:
  - PAGE-02
  - PAGE-04

must_haves:
  truths:
    - "About page hero has grid-pattern-subtle + hero-spotlight overlays wrapping content in relative z-10"
    - "About page has a testimonials section with 2 hardcoded Card variant='testimonial' cards"
    - "Contact page hero has grid-pattern-subtle + hero-spotlight overlays"
    - "Contact page two-column layout has form on LEFT and contact info on RIGHT"
    - "Contact page right column shows email address from BUSINESS_INFO"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "About page with hero overlays + testimonials section"
      contains: "grid-pattern-subtle"
    - path: "src/app/contact/page.tsx"
      provides: "Contact page with hero overlays + form-left layout"
      contains: "ContactForm"
  key_links:
    - from: "src/app/about/page.tsx"
      to: "src/components/ui/card.tsx"
      via: "Card variant='testimonial'"
      pattern: "variant=\"testimonial\""
    - from: "src/app/contact/page.tsx"
      to: "BUSINESS_INFO"
      via: "import from @/lib/constants/business"
      pattern: "BUSINESS_INFO.email"
---

<objective>
Add hero grid/spotlight overlays to About and Contact pages, add hardcoded testimonials to About, and restructure Contact page to form-left / info-right two-column layout.

Purpose: Both pages currently have the hero section wrapper but are missing the two overlay divs that give all pages the consistent dark-with-gradient hero treatment. Contact page column order is inverted from the locked decision.
Output: Updated about/page.tsx (hero overlays + testimonials), updated contact/page.tsx (hero overlays + column flip)
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-content-page-polish/60-CONTEXT.md
@.planning/phases/60-content-page-polish/60-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

Hero overlay pattern (from src/app/page.tsx — proven pattern):
```tsx
<section className="relative overflow-hidden bg-background">
  <div
    className="absolute inset-0 grid-pattern-subtle dark:grid-pattern-dark pointer-events-none"
    aria-hidden="true"
  />
  <div
    className="hero-spotlight absolute inset-0 pointer-events-none"
    aria-hidden="true"
  />
  <div className="relative z-10 container-wide px-4 sm:px-6 pt-28 pb-16 sm:pt-32 sm:pb-20 text-center">
    {/* hero content */}
  </div>
</section>
```

Card variant="testimonial" required props (from src/components/ui/card.tsx):
```typescript
{
  variant: "testimonial"
  testimonialId: number   // 1 as const, 2 as const
  name: string
  company: string
  role: string
  content: string
  rating: number          // 5 as const
  service: string
  highlight?: string
}
```

BUSINESS_INFO (from src/lib/constants/business.ts):
```typescript
export const BUSINESS_INFO = {
  name: 'Hudson Digital Solutions',
  email: 'hello@hudsondigitalsolutions.com',
  location: { city: 'Dallas', state: 'Texas', stateCode: 'TX' },
  links: { website: 'https://hudsondigitalsolutions.com' }
} as const
```

Contact page current structure (IMPORTANT — column order to flip):
- Current: Left col = hero text + "What Happens Next" card + trust badges; Right col = form
- Required: Left col = form (inside surface-raised card); Right col = hero text + "What Happens Next" + contact info (email/hours) + trust badges
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hero overlays and testimonials section to About page</name>
  <files>src/app/about/page.tsx</files>
  <action>
Make two targeted changes to `src/app/about/page.tsx`:

**1. Add hero overlay divs to the hero section**
The hero section currently is:
```tsx
<section className="relative overflow-hidden bg-background">
  <div className="container-wide px-4 sm:px-6 pt-28 pb-16 sm:pt-32 sm:pb-20 text-center">
    ...content...
  </div>
</section>
```

Add the two overlay divs immediately after the opening `<section>` tag, before the container div. Wrap the container div in `relative z-10`:
```tsx
<section className="relative overflow-hidden bg-background">
  <div
    className="absolute inset-0 grid-pattern-subtle dark:grid-pattern-dark pointer-events-none"
    aria-hidden="true"
  />
  <div
    className="hero-spotlight absolute inset-0 pointer-events-none"
    aria-hidden="true"
  />
  <div className="relative z-10 container-wide px-4 sm:px-6 pt-28 pb-16 sm:pt-32 sm:pb-20 text-center">
    ...existing content unchanged...
  </div>
</section>
```

**2. Add testimonials section**
Add a `const testimonials` array at the top of the file (after imports, before the `export default function` line). Use identical data to Plan 01's testimonials array:

```typescript
const testimonials = [
  {
    testimonialId: 1 as const,
    name: 'Sarah Mitchell',
    company: 'Bright Spark Consulting',
    role: 'Founder',
    content: 'Our lead volume doubled in the first month after launch. The automation alone saves us 12 hours a week.',
    rating: 5 as const,
    service: 'Website Development + Automation',
    highlight: '2x lead volume'
  },
  {
    testimonialId: 2 as const,
    name: 'Marcus Holt',
    company: 'Gulf Coast Roofing',
    role: 'Operations Manager',
    content: 'We went from manually following up on every quote to having it all run automatically. Game changer.',
    rating: 5 as const,
    service: 'Business Automation',
    highlight: 'Zero manual follow-ups'
  }
] satisfies Array<{
  testimonialId: number
  name: string
  company: string
  role: string
  content: string
  rating: number
  service: string
  highlight: string
}>
```

Add import for `Card` at the top: `import { Card } from '@/components/ui/card'`

Insert testimonials section in the JSX **between the Core Values section and the Call to Action section** (second to last section, before the closing CTA):

```tsx
{/* Testimonials Section */}
<section className="py-section-sm px-4 sm:px-6">
  <div className="container-wide">
    <div className="text-center mb-10">
      <p className="text-xs font-semibold uppercase tracking-widest text-accent mb-3">
        Client Results
      </p>
      <h2 className="text-section-title text-foreground mb-comfortable text-balance">
        What Our Clients Say
      </h2>
      <p className="text-lead text-muted-foreground max-w-2xl mx-auto">
        Real businesses. Real results.
      </p>
    </div>
    <div className="grid md:grid-cols-2 gap-6">
      {testimonials.map(t => (
        <Card
          key={t.testimonialId}
          variant="testimonial"
          testimonialId={t.testimonialId}
          name={t.name}
          company={t.company}
          role={t.role}
          content={t.content}
          rating={t.rating}
          service={t.service}
          highlight={t.highlight}
        />
      ))}
    </div>
  </div>
</section>
```

Do NOT modify any other sections. The Story, Expertise, Founder, Values, and CTA sections remain unchanged.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint 2>&1 | tail -10</automated>
  </verify>
  <done>
    - about/page.tsx hero section has grid-pattern-subtle + hero-spotlight overlay divs
    - about/page.tsx has a testimonials section with 2 Card variant="testimonial" cards
    - TypeScript and Biome checks pass
    - All existing sections (Story, Expertise, Founder, Values, CTA) unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Contact page — add hero overlays and flip column order</name>
  <files>src/app/contact/page.tsx</files>
  <action>
Make two targeted changes to `src/app/contact/page.tsx`:

**1. Add hero overlay divs to the hero section**
The hero section currently wraps content directly in a container div without overlays. Add the grid + spotlight overlays:

```tsx
<section className="relative overflow-hidden bg-background">
  <div
    className="absolute inset-0 grid-pattern-subtle dark:grid-pattern-dark pointer-events-none"
    aria-hidden="true"
  />
  <div
    className="hero-spotlight absolute inset-0 pointer-events-none"
    aria-hidden="true"
  />
  <div className="relative z-10 container-wide px-4 sm:px-6 pt-28 pb-16 sm:pt-32 sm:pb-20">
    {/* two-column grid */}
  </div>
</section>
```

**2. Flip the two-column layout — form LEFT, contact info RIGHT**
The current layout has: Left = hero text + "What Happens Next" + trust badges; Right = form.
The locked decision requires: Left = form; Right = contact info + "What Happens Next".

Restructure the grid to:

```tsx
<div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">
  {/* LEFT: Form */}
  <div className="rounded-xl border border-border bg-surface-raised p-8 hover:border-border-strong transition-colors">
    <div className="mb-8">
      <p className="text-xs font-semibold uppercase tracking-widest text-accent mb-3">
        Free Strategy Call
      </p>
      <h1 className="text-page-title text-foreground leading-tight text-balance">
        Book Your Free Strategy Call
      </h1>
      <p className="mt-4 text-lead text-muted-foreground">
        See exactly where your website is losing customers — and how to fix it. No sales pitch. No commitment.
      </p>
    </div>
    <Suspense fallback={<ContactFormSkeleton />}>
      <ContactForm />
    </Suspense>
    {/* Trust badges */}
    <div className="mt-6 pt-6 border-t border-border">
      <div className="flex flex-col sm:flex-row justify-center items-center gap-4 text-xs text-muted-foreground">
        <div className="flex items-center gap-2">
          <div className="w-3 h-3 bg-success-text rounded-full" />
          <span>No sales pitch</span>
        </div>
        <div className="flex items-center gap-2">
          <div className="w-3 h-3 bg-accent rounded-full" />
          <span>2-hour response time</span>
        </div>
        <div className="flex items-center gap-2">
          <div className="w-3 h-3 bg-info rounded-full" />
          <span>Proven success stories</span>
        </div>
      </div>
    </div>
  </div>

  {/* RIGHT: Contact Info + What Happens Next */}
  <div className="space-y-8">
    {/* What Happens Next card — keep existing content verbatim */}
    <div className="rounded-xl border border-border bg-surface-raised p-8 hover:border-border-strong transition-colors space-y-6">
      <h3 className="text-h3 text-foreground">What Happens Next?</h3>
      {/* ... existing 3 steps unchanged ... */}
    </div>

    {/* Contact Info */}
    <div className="rounded-xl border border-border bg-surface-raised p-8 hover:border-border-strong transition-colors space-y-6">
      <h3 className="text-h3 text-foreground">Get in Touch</h3>
      <div className="space-y-4">
        <div className="flex items-start gap-4">
          <div className="w-10 h-10 rounded-full bg-accent/10 border border-accent/20 flex items-center justify-center shrink-0">
            <Mail className="w-4 h-4 text-accent" aria-hidden="true" />
          </div>
          <div>
            <p className="font-semibold text-foreground text-sm">Email</p>
            <a
              href={`mailto:${BUSINESS_INFO.email}`}
              className="text-xs text-muted-foreground hover:text-accent transition-colors"
            >
              {BUSINESS_INFO.email}
            </a>
          </div>
        </div>
        <div className="flex items-start gap-4">
          <div className="w-10 h-10 rounded-full bg-accent/10 border border-accent/20 flex items-center justify-center shrink-0">
            <Clock className="w-4 h-4 text-accent" aria-hidden="true" />
          </div>
          <div>
            <p className="font-semibold text-foreground text-sm">Business Hours</p>
            <p className="text-xs text-muted-foreground">Monday – Friday, 9am – 6pm CT</p>
            <p className="text-xs text-muted-foreground">Response within 2 hours</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
```

Add imports at the top:
- `import { Mail } from 'lucide-react'` (add to existing lucide-react import)
- `import { BUSINESS_INFO } from '@/lib/constants/business'`

The h1 moves into the left column (above the form) since the right column no longer has the hero heading. This keeps the page-title on the left, paired with the form it leads into.

The map section below the grid remains unchanged.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint && bun run test:e2e:fast -- --grep "contact" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - contact/page.tsx hero section has grid-pattern-subtle + hero-spotlight overlays
    - Form (ContactForm component) renders in the LEFT column
    - Right column shows "What Happens Next" card + contact info with BUSINESS_INFO.email
    - TypeScript and Biome checks pass
    - Contact form E2E tests still pass (form is still present and functional)
  </done>
</task>

</tasks>

<verification>
Run full verification after both tasks:
1. `bun run typecheck` — no TypeScript errors
2. `bun run lint` — no Biome errors
3. `bun run test:e2e:fast -- --grep "about|contact"` — content-page tests for About and Contact pass
4. `bun run build` — build succeeds
</verification>

<success_criteria>
- about/page.tsx: hero has grid/spotlight overlays, testimonials section present with 2 cards
- contact/page.tsx: hero has grid/spotlight overlays, form on left column, contact info on right
- TypeScript strict mode: no errors
- Biome: no errors
- Build succeeds
- Contact form functional tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/60-content-page-polish/60-02-SUMMARY.md`
</output>

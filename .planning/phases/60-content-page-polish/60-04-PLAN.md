---
phase: 60-content-page-polish
plan: "04"
type: execute
wave: 2
depends_on:
  - "60-01"
  - "60-02"
  - "60-03"
files_modified: []
autonomous: false
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04

must_haves:
  truths:
    - "All 4 content pages render with dark hero + grid/spotlight treatment matching homepage"
    - "Services page shows testimonial cards and has no 'use client' directive"
    - "About page shows testimonial cards"
    - "Contact page shows form on left, contact info on right in the two-column layout"
    - "Location slug page shows testimonial cards"
    - "All E2E content-page tests pass"
  artifacts:
    - path: "e2e/content-pages.spec.ts"
      provides: "Full content-page structural test coverage"
    - path: "src/app/services/page.tsx"
      provides: "Server Component services page with metadata + testimonials"
    - path: "src/app/about/page.tsx"
      provides: "About page with hero overlays + testimonials"
    - path: "src/app/contact/page.tsx"
      provides: "Contact page with hero overlays + form-left layout"
    - path: "src/app/locations/[slug]/page.tsx"
      provides: "Location slug page with hero overlays + testimonials"
    - path: "src/app/locations/page.tsx"
      provides: "Location index page with hero overlays"
  key_links: []
---

<objective>
Final verification checkpoint: confirm all content page changes look correct in the browser, the full E2E test suite is green, and the build passes.

Purpose: Visual QA — automated tests verify structure but cannot verify that the hero spotlight looks correct, the testimonial cards render with proper styling, and the contact two-column layout renders well on mobile and desktop.
Output: User-approved visual sign-off; full test suite confirmation
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full verification suite</name>
  <files></files>
  <action>
Run the complete verification pipeline in order:

1. `bun run typecheck` — must exit 0
2. `bun run lint` — must exit 0
3. `bun run build` — must succeed (confirms Services metadata export works, no bundle errors)
4. `bun test tests/` — unit tests must all pass
5. `bun run test:e2e:fast` — all E2E tests must pass (including new content-pages.spec.ts)

If any check fails, diagnose and fix before the human verification checkpoint.

Report results:
- Typecheck: pass/fail + error count
- Lint: pass/fail + error count
- Build: pass/fail + JS bundle sizes per page (confirm services page bundle decreased after removing 'use client')
- Unit tests: pass count
- E2E tests: pass count, fail count, specific failures if any
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint && bun run build && bun test tests/ && bun run test:e2e:fast 2>&1 | tail -30</automated>
  </verify>
  <done>All checks pass: typecheck clean, lint clean, build succeeds, unit tests green, E2E tests green including new content-pages.spec.ts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 4 content pages</name>
  <files></files>
  <action>Pause for human visual review of all 4 content pages in the browser. Run `bun run dev` and verify each page listed in how-to-verify below.</action>
  <verify>
    <automated>bun run test:e2e:fast -- --grep "content-pages" 2>&1 | tail -10</automated>
    <manual>Visit all 5 URLs listed below and confirm visual quality matches the requirements.</manual>
  </verify>
  <done>User types "approved" confirming all 4 content pages render correctly with the dark hero treatment, testimonial cards, and correct contact layout.</done>
  <what-built>
    Phase 60 content page polish — all 4 pages (Services, About, Contact, Locations) updated with:
    - Dark hero sections with grid-pattern + spotlight overlay treatment matching homepage
    - Services: converted from 'use client' to Server Component, added metadata export, added testimonials
    - About: hero overlays + testimonials section added
    - Contact: hero overlays + form moved to left column, contact info on right
    - Location slug: hero overlays + testimonials section added
    - Location index: hero overlays added
  </what-built>
  <how-to-verify>
    Run the dev server: `bun run dev`

    1. Visit http://localhost:3000/services
       - Hero has dark background with visible grid texture and amber spotlight glow
       - Page h1 "Everything Your Business Needs Online" is clearly visible
       - Below the Process section: "What Our Clients Say" heading with 2 testimonial cards
       - Closing CTA section at bottom

    2. Visit http://localhost:3000/about
       - Hero has dark background with visible grid texture and amber spotlight glow
       - Eyebrow "About Us" + h1 "Built Around Your Business" visible
       - Scroll down past Core Values: "What Our Clients Say" heading with 2 testimonial cards
       - Closing CTA section at bottom

    3. Visit http://localhost:3000/contact
       - Hero has dark background with visible grid texture and amber spotlight glow
       - TWO COLUMNS on desktop (lg breakpoint):
         - LEFT column: form card with h1 "Book Your Free Strategy Call" + form fields
         - RIGHT column: "What Happens Next?" card + "Get in Touch" card with email address
       - On mobile: stacks vertically (form first, info second)

    4. Visit http://localhost:3000/locations/dallas-tx
       - Hero has dark background with visible grid texture and amber spotlight glow
       - h1 tagline visible with MapPin badge above it
       - Stats grid (3 columns) with amber accent bars
       - Services section
       - "What Our Clients Say" heading with 2 testimonial cards
       - Neighborhoods grid
       - Closing CTA card

    5. Visit http://localhost:3000/locations
       - Hero has dark background with visible grid texture and amber spotlight glow
       - City grid and CTA section below look unchanged

    Confirm: All pages feel visually consistent with each other and with the homepage (http://localhost:3000).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Final state after human approval:
- All automated checks pass (typecheck, lint, build, unit tests, E2E)
- User has visually confirmed all 4 pages render correctly
- Phase 60 requirements PAGE-01 through PAGE-04 fully satisfied
</verification>

<success_criteria>
- All automated checks pass
- User has approved visual quality of all 4 content pages
- Content pages feel visually consistent with homepage hero treatment
- PAGE-01, PAGE-02, PAGE-03, PAGE-04 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/60-content-page-polish/60-04-SUMMARY.md`
</output>

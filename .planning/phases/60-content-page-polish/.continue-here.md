---
phase: 60-content-page-polish
task: in_progress
status: in_progress
last_updated: 2026-03-03T01:57:05Z
---

<current_state>
Phase 60 has extensive UI polish complete — homepage CTA redesign, theme switcher removal, global divider cleanup, pricing removal, env var fix, map debug log cleanup. All changes are STAGED but NOT committed (user rejected the commit to add more work). Three new tasks were requested before committing.
</current_state>

<completed_work>

- Newsletter section: eyebrow "Stay in the loop", heading text-section-title, button variant="accent"
- Theme switcher fully removed: ThemeToggle.tsx deleted, Navbar (desktop+mobile), ClientProviders, next-themes uninstalled, THEME storage key removed
- Homepage closing CTA redesigned: merged newsletter + CTA into one unified section with compact NewsletterSignup variant
- Outline button matched hero: border-2 border-foreground/25 hover:border-accent
- Section uses bg-surface-raised (no border-t) for clean transition to footer
- Removed ALL section-level border dividers from subpages: testimonials, locations/[slug], showcase/[slug], tools, contact
- Removed testimonial card border-t divider (replaced with mt-auto pt-4)
- Removed customer-facing pricing: ServicesGrid ($5k/$8k/$2k), Card pricing prop+render, FAQ dollar amounts rewritten, category renamed "Services & Pricing" -> "Services"
- Removed 2 pricing tests from service-card.test.tsx
- Fixed WebVitalsReporting.tsx: env.NODE_ENV -> process.env.NODE_ENV (server env accessed in client component)
- Removed 3 debug console.warn from USServiceAreaMap.tsx
- Lint + typecheck + tests all passing

</completed_work>

<remaining_work>

### 1. Showcase hero typewriter effect
Add a typewriter animation to the showcase page hero that cycles between "Real Projects." and "Real Results." — type, pause, erase, type the other, repeat indefinitely.

### 2. Global typography and spacing consistency audit
User reports inconsistent spacing throughout the project after all the recent updates. Need to go page-by-page and ensure consistent use of spacing tokens (py-section, py-section-sm, mb-comfortable, mb-heading, etc.) and typography classes (text-section-title, text-lead, text-h3, text-h4, eyebrow pattern).

### 3. Commit all changes
All current work is staged but not committed. Once the typewriter effect and spacing audit are done, commit everything together.

### 4. Mark phase 60 complete
node ~/.claude/get-shit-done/bin/gsd-tools.cjs phase complete 60

</remaining_work>

<decisions_made>

- Merged newsletter + CTA into one section: two back-to-back conversion sections were competing
- compact NewsletterSignup variant: just intro line + form, no eyebrow/heading, default button (not accent) — one orange button per viewport
- bg-surface-raised WITHOUT border-t: homepage uses whitespace + background shifts for section transitions, not dividers
- Removed ALL section-level dividers site-wide: consistent with homepage pattern, stats grids with gap-px bg-border/30 rounded-2xl already create visual containment
- Pricing removed per user direction: fixed-price anchoring hurts consultative businesses, redirects to Cost Estimator tool or strategy call
- process.env.NODE_ENV in client components: Next.js inlines this at build time, unlike @t3-oss/env-nextjs server vars
- Dark mode is dead (next-themes removed, defaultTheme was "light"): 23 files still have dark: prefixes — harmless dead code for future cleanup

</decisions_made>

<blockers>
None. All changes passing lint/typecheck/build/tests.
</blockers>

<context>
Phase 60 is the FINAL phase of v4.0 UI Redesign milestone. The user is particular about visual consistency — they caught divider inconsistencies across subpages that I initially missed, and pricing that should have been removed earlier. The remaining typewriter effect is a new feature for the showcase hero. The spacing audit is a global polish pass. All staged files are ready; just need the two remaining tasks before final commit.

Files currently staged (git add done):
- src/app/page.tsx, contact/page.tsx, faq/FaqClient.tsx, layout.tsx
- src/app/locations/[slug]/page.tsx, showcase/[slug]/page.tsx, testimonials/page.tsx, tools/page.tsx
- src/components/forms/NewsletterSignup.tsx, layout/Navbar.tsx, testimonial-card.tsx
- src/components/ui/ServicesGrid.tsx, card.tsx, ThemeToggle.tsx (deleted)
- src/components/utilities/USServiceAreaMap.tsx, WebVitalsReporting.tsx
- src/lib/constants/storage-keys.ts, src/providers/ClientProviders.tsx
- tests/unit/service-card.test.tsx, package.json, bun.lock
</context>

<next_action>
1. Build the typewriter effect component for showcase hero ("Real Projects." / "Real Results." cycling)
2. Do a page-by-page spacing and typography audit (all pages, not just homepage)
3. Stage WebVitalsReporting.tsx (was modified after the git add)
4. Commit everything, mark phase 60 complete
</next_action>

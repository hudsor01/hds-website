---
phase: 60-content-page-polish
task: in_progress
status: in_progress
last_updated: 2026-03-03T01:10:40Z
---

<current_state>
Homepage closing section redesigned: newsletter + CTA merged into one unified section. Needs visual verification in browser before committing. All other phase 60 work (newsletter eyebrow/heading/button, theme switcher removal) is done and lint/typecheck clean.
</current_state>

<completed_work>

- Newsletter section variant: added eyebrow "Stay in the loop", heading changed to text-section-title, button changed to variant="accent"
- Theme switcher fully removed: ThemeToggle.tsx deleted, removed from Navbar (desktop + mobile), ThemeProvider removed from ClientProviders.tsx, next-themes package uninstalled, THEME storage key removed, suppressHydrationWarning removed from html tag
- Homepage closing CTA redesigned: merged newsletter + final CTA into one unified section with clear hierarchy
- New `compact` variant added to NewsletterSignup component: just intro line + form, no eyebrow/heading, default button (not accent)
- Outline button matched to hero treatment: border-2 border-foreground/25 hover:border-accent
- Section uses bg-surface-raised border-t border-border to bridge to dark footer
- Lint + typecheck + build all passing

</completed_work>

<remaining_work>

### 1. Visual verification of merged closing section
User needs to see the merged CTA+newsletter section in browser and confirm it flows correctly. Previous iterations were rejected for not matching the homepage's visual language.

### 2. Clean up dead dark: prefixes (optional/future)
23 files still have dark: Tailwind prefixes that are now dead code since next-themes was removed. The site defaults to light mode (ThemeProvider had defaultTheme="light"). These are harmless but could be cleaned up in a future pass.

### 3. Verify USServiceAreaMap renders + remove debug console.warn
From original .continue-here — check map on contact page, remove any debug logs from USServiceAreaMap.tsx.

### 4. Commit all changes
Once visual verification passes: git add specific files and commit.

### 5. Mark phase complete
node ~/.claude/get-shit-done/bin/gsd-tools.cjs phase complete 60

</remaining_work>

<decisions_made>

- Merged newsletter + CTA into one section: two back-to-back conversion sections were competing visually and diluting each other
- New "compact" variant for NewsletterSignup: renders just a soft intro line ("Not ready yet? Get weekly insights instead.") + form inline, no eyebrow/heading — avoids duplicate section structure
- Subscribe button uses variant="default" (not accent) in compact variant: only one orange button per viewport — "Get a Free Strategy Call" is the unmistakable primary action
- Section uses bg-surface-raised + border-t: creates visual bridge between white content and dark footer
- Outline button matches hero: border-2 border-foreground/25 hover:border-accent for consistency
- py-section (not py-section-sm): closing statement gets breathing room
- Newsletter divider: mt-12 pt-10 border-t border-border/60 separates primary CTA from newsletter soft-ask
- Dark mode is now dead (next-themes removed, defaultTheme was "light"): dark: prefixes are inert, .dark CSS block unused — harmless dead code for now
- Homepage visual language: full-width bands only, no contained cards (cards are for subpages), hero is the only section with overlays

</decisions_made>

<blockers>
None. All changes passing lint/typecheck/build.
</blockers>

<context>
Phase 60 is the FINAL phase of v4.0 UI Redesign milestone. Plans 01-04 all have SUMMARY files. The remaining work is cosmetic polish from the previous .continue-here plus the homepage CTA redesign the user requested this session. The user is particular about visual consistency — they caught that subpage CTA card patterns don't belong on the homepage, and that two competing CTA sections break flow. They want to see the merged section visually before approving.
</context>

<next_action>
1. Run dev server, screenshot the bottom of the homepage showing Tools -> Closing CTA -> Footer flow
2. Present to user for visual approval
3. If approved: check USServiceAreaMap, commit everything, mark phase 60 complete
4. If not approved: iterate on the closing section based on feedback
</next_action>

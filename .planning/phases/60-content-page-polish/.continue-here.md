---
phase: 60-content-page-polish
task: post-execution-polish
status: in_progress
last_updated: 2026-03-02T21:31:50.358Z
---

<current_state>
Phase 60 execution is COMPLETE (all 4 plans done, ROADMAP updated). However, after visual review of the contact page and services page, the user identified 6 UI issues to fix. These are NOT gap closure — they are direct polish tasks to implement before marking the phase verified/approved.

The verifier returned `human_needed` (11/11 automated checks passed). The user reviewed screenshots and provided feedback. We read the relevant files (Navbar.tsx, ContactForm.tsx, contact/page.tsx) right before context ran out.

Branch: `gsd/phase-60-content-page-polish`
</current_state>

<completed_work>
- Phase 60 all 4 plans executed and committed
- Wave 1: 60-01 (E2E + Services Server Component), 60-02 (About + Contact hero), 60-03 (Location pages)
- Wave 2: 60-04 (verification — fixed 2 stale unit tests, 408/408 pass)
- All SUMMARY.md files created, ROADMAP updated
- Verifier run: human_needed (11/11 automated pass)
- User provided visual feedback on contact page and services testimonials
- Files already read: Navbar.tsx, ContactForm.tsx, src/app/contact/page.tsx
</completed_work>

<remaining_work>
**6 UI fixes to implement (user approved, implement comprehensively):**

1. **Add "Contact" to navbar** — Add `{ name: 'Contact', href: ROUTES.CONTACT }` as last item in the `navigation` array in `src/components/layout/Navbar.tsx` (line 17-23). Also add to mobile menu navigation array (same file, line 138). The existing "Talk to Sales" link in the right CTA area can be removed or repurposed since Contact will be in the nav.

2. **Fix form input background inconsistency** — The `<select>` dropdowns in ContactForm render with browser-native styling (different bg from text inputs). Need to find the `SelectField` component (likely in `src/components/ui/field.tsx` or similar) and apply `bg-background` + unified border styling. Read `src/components/ui/field.tsx` first.

3. **Remove colored dot trust badges** — In `src/app/contact/page.tsx` lines 90-105, remove the entire trust badges div (the `<div className="mt-6 pt-6 border-t border-border">` block with 3 dot+text items: "No sales pitch", "2-hour response time", "Proven success stories").

4. **Replace Google Map section with Service Area section** — In `src/app/contact/page.tsx` lines 206-228, replace the entire Map Section with a Service Area section. Design: dark/subtle bg section, heading "We Serve Clients Nationwide", subtext "Primarily serving TX, FL, GA, OK and surrounding states — fully remote for all projects.", then a grid of city/state chips (pull from existing location data or hardcode top 8-10 cities). Remove the `GoogleMap` dynamic import and `MapSkeleton` function (lines 23-29). Also remove `Clock` import if no longer used after checking.

5. **Fix "Florida" copy bug** — In `src/app/contact/page.tsx` line 217: change "Located in the heart of Florida's tech corridor, ready to serve clients worldwide." — this is inside the map section being replaced in #4 above, so it's handled automatically.

6. **Fix testimonial avatar circle** — In the `Card variant="testimonial"` rendering, there's an empty placeholder circle. Find `src/components/ui/card.tsx` and look for the testimonial variant. Replace the empty circle with an initials avatar: extract first letter of first name + first letter of last name from the `author` prop, display in a `bg-accent/10 text-accent font-bold` circle. If no name available, remove circle entirely.

**Files to modify:**
- `src/components/layout/Navbar.tsx` — add Contact nav item
- `src/components/ui/field.tsx` (or wherever SelectField lives) — fix select bg
- `src/app/contact/page.tsx` — remove dot badges, replace map section, fix copy
- `src/components/ui/card.tsx` — fix testimonial avatar (initials or remove)
</remaining_work>

<decisions_made>
- Contact page IS reachable from navbar (add it) — standard UX for service businesses
- Replace Google Maps entirely with Service Area chips/grid — better aesthetic, no iframe quality issues
- Testimonial avatar: use initials avatar (bg-accent/10, text-accent, font-bold) — not remove
- Colored dot badges: remove entirely (no replacement needed)
- Trust signal info ("2-hour response time" etc.) already exists in the "What Happens Next" right column — not lost
</decisions_made>

<blockers>
- E2E tests couldn't run: port 3001 occupied by heic-to-jpeg-converter dev server. Run `bun run test:e2e:fast` after stopping that server. Not a code blocker.
- Phase verification pending user visual approval (6 fixes are the condition for approval)
</blockers>

<context>
Phase 60 is functionally complete — all content pages have hero overlays and testimonials. These 6 fixes are polish items the user identified during visual review. They are cosmetic/UX improvements, not functional gaps. After implementing, re-run `bun run typecheck && bun run lint && bun run build && bun test tests/` to verify clean, then tell user to visually approve and mark phase complete with `node gsd-tools.cjs phase complete 60`.

The card.tsx testimonial variant has an empty `<div>` or `<span>` acting as avatar placeholder — it needs to use initials from the author name prop.

All work is on branch `gsd/phase-60-content-page-polish`.
</context>

<next_action>
Start with: Read `src/components/ui/field.tsx` and `src/components/ui/card.tsx` in parallel to understand the SelectField and testimonial card implementations, then implement all 6 fixes in sequence. Commit each logical group atomically. Run typecheck + lint + build + unit tests. Then report to user for final visual approval.
</next_action>

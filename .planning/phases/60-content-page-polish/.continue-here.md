---
phase: 60-content-page-polish
task: in_progress
status: in_progress
last_updated: 2026-03-03T00:43:03Z
---

<current_state>
Two immediate tasks remain before phase is complete:
1. Newsletter section still doesn't match site UI (heading too small, button wrong color, no eyebrow)
2. Remove the theme switcher (sun/moon icon in navbar) — user wants single UI only
</current_state>

<completed_work>

- Root hydration crash fixed: logger.ts dynamic import for db.ts
- global-error.tsx, error.tsx, ErrorBoundary.tsx, WebVitalsReporting.tsx: removed server imports
- USServiceAreaMap.tsx: fixed CSS color syntax (oklch relative color)
- Homepage: removed availability badge, redesigned CTA section (full-width dark band)
- Footer: middle-dot separator changed to slash
- Newsletter section: Card removed, new 'section' variant added (bg-surface-raised band)
- Production notifications: newsletter/calculator/testimonial all notify hello@hudsondigitalsolutions.com
- All commits clean, lint+typecheck passing

</completed_work>

<remaining_work>

### 1. Newsletter section — final 3-line fix
File: src/components/forms/NewsletterSignup.tsx (section variant render block)
- Add eyebrow BEFORE h2: `<p className="text-xs font-semibold uppercase tracking-widest text-accent mb-3">Stay in the loop</p>`
- Change h2 class from `text-2xl font-bold tracking-tight text-foreground mb-3` → `text-section-title text-foreground mb-3 text-balance`
- Change Button to `variant="accent"`

### 2. Remove theme switcher
User wants single UI, no light/dark toggle.
Find the theme toggle in navbar (sun/moon icon) and remove it.
Likely in src/components/layout/Navbar.tsx — grep for "theme" or "ThemeToggle" or sun/moon icon.
Also check src/providers/ClientProviders.tsx for ThemeProvider — may need to remove too.

### 3. Verify map on contact page
After all fixes, visually confirm US map renders, then remove console.warn debug logs from USServiceAreaMap.tsx.

### 4. Mark phase complete
node ~/.claude/get-shit-done/bin/gsd-tools.cjs phase complete 60

</remaining_work>

<decisions_made>
- Dynamic import in logger.ts: defers db.ts module eval to call time, keeps logger safe for client components
- Newsletter section variant: no Card, section provides background, content is centered text+form
- Single UI theme: user confirmed no light/dark toggle needed
</decisions_made>

<blockers>
None. All changes passing lint/typecheck.
RESEND_API_KEY must be set in env for emails to send.
</blockers>

<context>
Phase 60 is 90% done. The hydration bug is fully resolved at the root. The remaining work is cosmetic polish: newsletter section heading/button/eyebrow treatment, and removing the theme switcher button from navbar. Three targeted edits each.
</context>

<next_action>
1. Edit src/components/forms/NewsletterSignup.tsx section variant: add eyebrow, fix h2 class to text-section-title, set Button variant="accent"
2. Find and remove theme switcher from navbar
3. lint + typecheck + commit
</next_action>

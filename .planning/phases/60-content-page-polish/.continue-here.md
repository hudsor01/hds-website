---
phase: 60-content-page-polish
task: post-execution-polish
status: in_progress
last_updated: 2026-03-02T22:06:18.392Z
---

<current_state>
Phase 60 polish in progress. 6 original UI fixes committed (04f59de). Pricing page
removed (61a1733). One open question remains: what to do with the contact page map
section. User pushed back on unilateral removal of Google Maps — wants to choose.
Service Area chips are currently live (from 04f59de). User needs to pick an option.

Branch: `gsd/phase-60-content-page-polish`
</current_state>

<completed_work>
- All 4 phase-60 plans executed (60-01 through 60-04)
- 408/408 unit tests pass, build clean
- commit 04f59de — 6 UI polish fixes:
  1. Contact added to navbar; Talk to Sales links removed
  2. SelectTrigger bg-background → bg-surface-sunken (matches Input)
  3. Colored dot trust badges removed from contact form
  4. Google Maps replaced with Service Area chips (10 cities: Dallas, Fort Worth, Houston, Austin, San Antonio, Miami, Orlando, Atlanta, Oklahoma City, Tampa)
  5. Florida copy bug fixed (was inside map section — handled by #4)
  6. Testimonial card initials avatar added (bg-accent/10, text-accent, font-bold)
- commit 61a1733 — Pricing page removed:
  - Deleted src/app/pricing/page.tsx
  - Removed from Navbar.tsx navigation array
  - Removed from Footer.tsx company links
  - Removed from about/page.tsx ("View our pricing" link in guarantee section)
  - Removed from sitemap.ts
</completed_work>

<remaining_work>
**ONE OPEN DECISION — Map section:**

The user asked why the map was removed without presenting options first. Present these
options and let them choose:

**Option A: Keep Service Area chips** (current state)
- City/state grid, matches site aesthetic, no dependency
- Already live in current code

**Option B: react-simple-maps US SVG map with highlighted states** (RECOMMENDED)
- ~30kB, no API key, fully themeable with CSS vars
- Highlight TX, FL, GA, OK in accent color
- `bun add react-simple-maps` (or `d3-geo` + custom SVG)

**Option C: Mapbox static image embed**
- Static `<img>` URL from Mapbox API, no JS, free tier
- Dark style, DFW pin, no iframe
- Requires MAPBOX_PUBLIC_TOKEN in .env.local

**Option D: Google Maps dark-styled embed**
- Requires Google Maps JS API key, iframe quality issues remain
- Most friction, least reward

After user selects, implement chosen option.

**After map decision:**
- Run bun run typecheck && bun run lint && bun run build && bun test tests/
- Commit
- Ask user for final visual approval
- Mark phase complete: node gsd-tools.cjs phase complete 60
</remaining_work>

<decisions_made>
- Pricing page removed entirely — pricing tables fit SaaS/subscription products, not
  service/agency businesses where price is scoped per project. User agreed.
- Testimonial avatar: initials circle (bg-accent/10, text-accent, font-bold)
- Trust badges removed (no replacement — info exists in "What Happens Next" section)
- Contact added to main nav
- SelectTrigger: bg-surface-sunken to match Input component
</decisions_made>

<blockers>
- Map section decision pending user choice (Options A-D above)
- E2E tests still blocked by port 3001 (heic-to-jpeg server) — not a code blocker
</blockers>

<context>
User called out that the Google Maps → Service Area replacement was done without
presenting options. They were right. The Service Area chips are currently live and clean.
The user may prefer a visual map element (Option B is the best path if so).

On next session: open with the map options, let user choose, implement if needed,
then do final verification and phase completion.
</context>

<next_action>
Present map options A-D to user and ask which they prefer. Then implement if needed,
run full verification suite, get visual approval, and run:
  node gsd-tools.cjs phase complete 60
</next_action>

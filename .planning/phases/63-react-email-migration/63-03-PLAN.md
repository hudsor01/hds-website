---
phase: 63-react-email-migration
plan: "03"
type: execute
wave: 3
depends_on: ["63-01", "63-02"]
files_modified:
  - src/emails/calculator-results.tsx
  - src/emails/ttl-calculator-results.tsx
  - src/emails/scheduled-drip.tsx
  - src/app/api/calculators/submit/route.ts
  - src/app/actions/ttl-calculator.ts
  - src/lib/scheduled-emails.ts
autonomous: true
requirements:
  - REMAIL-COMPLEX-01

must_haves:
  truths:
    - "Three complex React Email components exist for results emails and the drip campaign"
    - "Each component uses BRAND for all color references — zero hex literals beyond white/neutral grays"
    - "All three send sites pass `react:` to Resend (no raw `html:`)"
    - "Phase 63 exit gate met: zero raw `html:` send calls remain in src/app/ or src/lib/"
  artifacts: []
---

<objective>
Migrate the three complex emails: calculator results (gradient header + value boxes + CTA), TTL calculator results (multi-section table layout), and scheduled drip campaign (dynamic content blocks with conditional sections).

These are the largest existing email templates. Each one preserves the existing layout and copy as JSX components, with all color references sourced from BRAND.

Purpose: Close out the React Email migration. Phase 63 exit gate: zero raw `html:` send calls anywhere in the codebase.
Output: 3 components + 3 send-site updates + visual sign-off for each.
</objective>

<execution_context>
Standard GSD execute-plan flow.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-react-email-migration/63-CONTEXT.md
@.planning/phases/63-react-email-migration/63-RESEARCH.md
@.planning/phases/63-react-email-migration/63-01-SUMMARY.md
@.planning/phases/63-react-email-migration/63-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CalculatorResults component</name>
  <files>src/emails/calculator-results.tsx, src/app/api/calculators/submit/route.ts</files>
  <action>
Read `src/app/api/calculators/submit/route.ts` lines 310-348 to understand the existing structure: gradient header bar, three value-display boxes (label + big number), a body-text section with a key insight callout, and a CTA button.

Author `src/emails/calculator-results.tsx`:

Props: takes the calculator type, the input fields, the computed result values (typically 3-4 numeric outputs), and the lead's email for follow-up.

Layout (preserve from existing):
- Header: gradient `linear-gradient(to right, BRAND.primary, BRAND.primaryDark)` if `primaryDark` is in BRAND from phase 61 (if not, use BRAND.primary as a solid bg). BRAND.primaryDark may NOT exist in the generated module yet — if globals.css doesn't define `--color-primary-dark`, this becomes a follow-up: add the token to globals.css FIRST (and let the codegen regenerate brand.ts), then reference here.
- Three value boxes: row of three Sections, each with a small uppercase label and a large BRAND.primary number
- Insight callout: subtle bg (BRAND.surfaceMuted or `#f9fafb` if not in BRAND), 4px BRAND.primary left border
- CTA: BrandButton linking to /contact
- Footer: BrandFooter

Use `<Row>` and `<Column>` from react-email for the value boxes layout.

Update `src/app/api/calculators/submit/route.ts` line 310 area: replace raw HTML send with `react: <CalculatorResults {...} />`.
  </action>
  <verify>
    <automated>grep -cE "html: " src/app/api/calculators/submit/route.ts</automated>
    <manual>Count should be 0 (admin migrated in 63-02; results migrated here).</manual>
  </verify>
  <done>Component built; send site migrated; zero raw HTML in file.</done>
</task>

<task type="auto">
  <name>Task 2: TTLCalculatorResults component</name>
  <files>src/emails/ttl-calculator-results.tsx, src/app/actions/ttl-calculator.ts</files>
  <action>
Read `src/app/actions/ttl-calculator.ts` lines 306-407. This is a multi-section TTL (Total Tax Liability) calculator result email with:
- Branded header
- Total TTL summary line
- Itemised breakdown table
- Share-results CTA button

Author `src/emails/ttl-calculator-results.tsx` mirroring the existing structure. Use BRAND.primary for the header bg, accent text, and CTA. Use react-email's `<Row>`, `<Column>`, `<Section>`, `<Text>` for the table-like layout.

Update the server action: replace the long raw HTML template-literal block with a `react: <TTLCalculatorResults {...} />` send call. Most of the prose helper functions (formatCurrency, escape) likely move into the component as inline transforms, OR remain at the action level and pass the formatted strings as props. Recommend the latter (keep the action's data shaping; component just renders).
  </action>
  <verify>
    <automated>grep -cE "html: " src/app/actions/ttl-calculator.ts</automated>
    <manual>Count should be 0.</manual>
  </verify>
  <done>Component built; action send site migrated.</done>
</task>

<task type="auto">
  <name>Task 3: ScheduledDrip component</name>
  <files>src/emails/scheduled-drip.tsx, src/lib/scheduled-emails.ts</files>
  <action>
Read `src/lib/scheduled-emails.ts` to understand the drip-campaign structure. Each send may have:
- A subject + preview line
- A dynamic body (markdown or HTML content from the database)
- An optional CTA section
- A footer with unsubscribe + contact

Use react-email's `<Markdown>` component if the body is markdown. Use BrandFooter with `unsubscribeUrl` populated.

Author `src/emails/scheduled-drip.tsx` accepting the email payload props (subject, preview, bodyMarkdown, cta?, recipientEmail).

Update `src/lib/scheduled-emails.ts` send sites (lines 255, 281, 282, 288 area): each one switches to `react: <ScheduledDrip {...} />`. If the file dispatches multiple drip variants from one function, the component takes a `variant` prop OR each variant gets its own component (executor's call based on what reads cleanest).
  </action>
  <verify>
    <automated>grep -cnE "html: |#0891b2" src/lib/scheduled-emails.ts</automated>
    <manual>Both counts should be 0.</manual>
  </verify>
  <done>Drip migrated; cyan gone; tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Final exit-gate verification</name>
  <action>
Run the phase-level exit gate:

```bash
grep -rn "@react-email/render\|@react-email/components" src/ package.json
grep -rnE "#0891b2|#06b6d4|#0e7490" src/app/api/ src/app/actions/ src/lib/scheduled-emails.ts src/lib/contact-service.ts
grep -rn "html: '" src/app/api/ src/app/actions/ src/lib/scheduled-emails.ts src/lib/contact-service.ts
```

ALL three commands must return ZERO matches. If any of them produce hits, the migration is incomplete — investigate and resolve before declaring this plan done.

If the only remaining `html:` matches are in legitimately non-email contexts (e.g., a JSON shape with an `html` field, or React's raw-HTML-injection prop used elsewhere), document the false-positive in SUMMARY with the file:line and the reason it's not an email send.
  </action>
  <verify>
    <automated>grep -rn "html: '" src/app/api/ src/app/actions/ src/lib/scheduled-emails.ts src/lib/contact-service.ts 2>&1 | head -5</automated>
  </verify>
  <done>All three exit-gate greps clean.</done>
</task>

<task type="manual">
  <name>Task 5: Smoke-test each migrated path</name>
  <action>
Trigger each flow in dev:
1. Submit a calculator form → calculator results email
2. Submit a TTL calculator → TTL results email
3. Trigger a scheduled drip dispatch (manual trigger via the existing admin/cron endpoint OR by simulating the cron in dev)

For each, confirm the email arrives with slate-blue brand, intact layout (gradient header still gradient, value boxes still aligned, etc.), and content matches the prior raw-HTML version. Sign off in SUMMARY.

If the calculator-results email needs `BRAND.primaryDark` and that token isn't in `BRAND` yet (because globals.css doesn't define `--color-primary-dark`), add it to globals.css FIRST, regenerate brand.ts (`bun run brand:generate`), then complete this task.
  </action>
  <verify>
    <manual>All three complex emails verified in modern client.</manual>
  </verify>
  <done>End-to-end verified for all three.</done>
</task>

</tasks>

<verification>
1. `bun run typecheck && bun run lint && bun run test:unit && bun run build` all pass
2. `grep -rnE "#0891b2|#06b6d4|#0e7490" src/` returns ZERO matches (combined with phase 61 + 62 exit gates — this is the milestone-level brand-cyan elimination gate)
3. `grep -rn "html: '" src/app/api/ src/app/actions/ src/lib/scheduled-emails.ts src/lib/contact-service.ts` returns ZERO matches
4. `grep -rn "@react-email/render" src/ package.json` returns ZERO matches
5. Three complex emails visually verified
6. PHASE 63 EXIT GATE: every transactional email is a React Email component sourcing colors from the BRAND codegen
</verification>

<success_criteria>
- Three complex components built
- Three send sites migrated
- Phase 63 exit gate met
- All checks green
- Visual confirmation captured for all three
</success_criteria>

<output>
After completion, create `.planning/phases/63-react-email-migration/63-03-SUMMARY.md` AND `.planning/phases/63-react-email-migration/63-VERIFICATION.md` (consolidates plans 01-03; confirms exit gate).
</output>

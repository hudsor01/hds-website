---
phase: 63-react-email-migration
plan: "02"
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - src/emails/contact-admin-notification.tsx
  - src/emails/testimonial-admin-notification.tsx
  - src/emails/calculator-admin-notification.tsx
  - src/emails/newsletter-admin-notification.tsx
  - src/lib/contact-service.ts
  - src/app/api/testimonials/submit/route.ts
  - src/app/api/calculators/submit/route.ts
  - src/app/api/newsletter/subscribe/route.ts
autonomous: true
requirements:
  - REMAIL-ADMIN-01

must_haves:
  truths:
    - "Four admin-notification React Email components exist in src/emails/, each consuming BRAND and reusing brand-layout/brand-heading"
    - "Each of the four send sites uses `react: <Component />` instead of `html: '<raw HTML>'`"
    - "Zero raw cyan hex remains in any of the four migrated files"
    - "Each migrated send site preserves all original send options (from, to, subject, replyTo, etc.)"
  artifacts: []
---

<objective>
Migrate the four simple admin-notification emails. Each is a single-paragraph notification triggered by a user action (contact form, testimonial submit, calculator submit, newsletter subscribe) and sent to the business inbox.

Purpose: Bulk-migrate the simple end of the email surface using the patterns established in 63-01.
Output: 4 React Email components + 4 send-site updates.
</objective>

<execution_context>
Standard GSD execute-plan flow.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-react-email-migration/63-CONTEXT.md
@.planning/phases/63-react-email-migration/63-RESEARCH.md
@.planning/phases/63-react-email-migration/63-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactAdminNotification</name>
  <files>src/emails/contact-admin-notification.tsx, src/lib/contact-service.ts</files>
  <action>
Author `src/emails/contact-admin-notification.tsx`:

Component takes the contact-form fields as props (name, email, phone?, company?, message). Renders:
- BrandHeading "New Contact Form Submission"
- Section with labelled fields (Name: ..., Email: ..., Message: ...)
- BrandFooter (no unsubscribe — admin email)

Then update `src/lib/contact-service.ts` line 219 area: replace the raw HTML construction with `react: <ContactAdminNotification {...fields} />` in the `resend.emails.send` call.

Preserve every field in the existing escapeHtml() pattern by passing the raw values to the JSX (React auto-escapes child text). KEEP the existing `from`, `to`, `subject`, `replyTo` fields exactly as they were.
  </action>
  <verify>
    <automated>grep -nE "html: |#0891b2" src/lib/contact-service.ts | head -5</automated>
  </verify>
  <done>Component exists; send site uses react: prop; zero cyan; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: TestimonialAdminNotification</name>
  <files>src/emails/testimonial-admin-notification.tsx, src/app/api/testimonials/submit/route.ts</files>
  <action>
Same pattern. Component takes testimonial fields (name, company, role, content, rating, etc.) as props. Renders the same labelled-section structure.

Update `src/app/api/testimonials/submit/route.ts` line 95 area: replace raw HTML send with `react: <TestimonialAdminNotification {...fields} />`.
  </action>
  <verify>
    <automated>grep -nE "html: |#0891b2" src/app/api/testimonials/submit/route.ts | head -5</automated>
  </verify>
  <done>Migrated; cyan gone; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: CalculatorAdminNotification</name>
  <files>src/emails/calculator-admin-notification.tsx, src/app/api/calculators/submit/route.ts</files>
  <action>
Same pattern, but careful — `src/app/api/calculators/submit/route.ts` has TWO different emails (admin notification around line 201, AND a results email around lines 310-348). This plan migrates ONLY the admin notification (line 201). The results email is plan 63-03.

Component takes calculator-submission fields (calculator type, lead name, lead email, key inputs/outputs).

Update only the admin send call. Leave the results-email send call untouched — plan 03 handles it.
  </action>
  <verify>
    <automated>grep -cE "html: " src/app/api/calculators/submit/route.ts</automated>
    <manual>Count of `html:` should drop by exactly 1 (admin migrated; results still raw HTML for plan 03).</manual>
  </verify>
  <done>Admin notification migrated; results email left for plan 03.</done>
</task>

<task type="auto">
  <name>Task 4: NewsletterAdminNotification</name>
  <files>src/emails/newsletter-admin-notification.tsx, src/app/api/newsletter/subscribe/route.ts</files>
  <action>
Same pattern. Component takes the newsletter-subscribe fields (email, attribution if any). Renders a "New Newsletter Subscriber" notification.

Update `src/app/api/newsletter/subscribe/route.ts` line 117 area (the admin notification that follows the welcome email). Plan 01 already migrated the welcome email; this finishes the file.

After this task, the entire `src/app/api/newsletter/subscribe/route.ts` should have ZERO raw `html:` send calls — both emails (welcome + admin) are React Email components.
  </action>
  <verify>
    <automated>grep -cE "html: " src/app/api/newsletter/subscribe/route.ts</automated>
    <manual>Count should be 0 (both emails migrated).</manual>
  </verify>
  <done>File fully migrated; no raw HTML in send calls.</done>
</task>

<task type="manual">
  <name>Task 5: Smoke-test each migrated path</name>
  <action>
Trigger each of the four flows in dev mode:
1. Submit a contact form → contact admin email
2. Submit a testimonial → testimonial admin email
3. Submit a calculator → calculator admin email (NOT the results email)
4. Subscribe to newsletter → admin notification (welcome already verified in 63-01)

Confirm each admin email arrives with slate-blue brand, intact layout, escaped user content. Sign off in SUMMARY.
  </action>
  <verify>
    <manual>All four flows trigger emails that render correctly.</manual>
  </verify>
  <done>End-to-end verified.</done>
</task>

</tasks>

<verification>
1. `bun run typecheck && bun run lint && bun run test:unit && bun run build` all pass
2. `grep -rnE "#0891b2|#06b6d4|#0e7490" src/lib/contact-service.ts src/app/api/testimonials/submit/route.ts src/app/api/newsletter/subscribe/route.ts` returns zero matches
3. `grep -cE "html: " src/app/api/newsletter/subscribe/route.ts` returns 0
4. `grep -cE "html: " src/app/api/calculators/submit/route.ts` returns 1 (results email — plan 03 territory)
5. Four real emails verified
</verification>

<success_criteria>
- Four admin-notification components built and wired
- Three of four files fully cyan-free (calculators/submit still has results-email cyan, plan 03)
- Real emails verified
- All checks green
</success_criteria>

<output>
After completion, create `.planning/phases/63-react-email-migration/63-02-SUMMARY.md`.
</output>

---
phase: 05-admin-ops
plan: 06
type: execute
wave: 3
depends_on: [05-02, 05-03, 05-04, 05-05]
files_modified:
  - src/app/admin/(coming-soon)/leads/page.tsx
  - src/app/admin/(coming-soon)/newsletter/page.tsx
  - src/app/admin/(coming-soon)/emails/page.tsx
autonomous: true
requirements: [P05]
must_haves:
  truths:
    - "Navigating to /admin/leads, /admin/newsletter, /admin/emails resolves to the real Wave-2 routes — not the coming-soon stubs."
    - "The `src/app/admin/(coming-soon)/` directory no longer exists in the working tree."
    - "Every dynamic admin page from Wave 2 conforms to the Next 16 cacheComponents requirement: `generateStaticParams()` placeholder + `await connection()` + `<Suspense>`."
    - "`bun run build` succeeds with the Wave-2 routes in the route table and zero parallel-pages collision warnings."
  artifacts: []
  key_links:
    - from: "Sidebar /admin/leads link (Phase 03 Sidebar.tsx unchanged)"
      to: "src/app/admin/leads/page.tsx (Plan 05-02)"
      via: "Next.js App Router resolution after the stub is deleted"
      pattern: "src/app/admin/leads/page\\.tsx exists AND src/app/admin/\\(coming-soon\\)/leads/page\\.tsx does not"
---

<objective>
Delete the 3 Phase-05 coming-soon stubs preserved by Phase 04 (`src/app/admin/(coming-soon)/{leads,newsletter,emails}/page.tsx`) and remove the now-empty `(coming-soon)` route group directory. After this plan, every Wave-2 route resolves to its real page; the Sidebar links continue to work without any Sidebar edit.

Purpose: With Wave 2 complete, the stub pages are dead code. Next.js App Router will throw "parallel pages" build errors if both a stub and a real page resolve to the same URL, so the stubs MUST be removed before/at the same time the real pages land. The Wave 2 plans intentionally create their own routes without touching `(coming-soon)/`, so the parallel-pages collision lands during Wave 3 build verification and is fixed by this plan's deletions.

Output: 3 file deletions + 1 empty-directory removal. No code added.

NOTE on Next 16 `cacheComponents` requirement: Each Wave 2 plan already builds its dynamic `[id]/page.tsx` route with `generateStaticParams() { return [{ id: '__build_placeholder__' }] }` + `await connection()` + `<Suspense>` (Phase 04 Wave 3 lesson, applied as part of every Wave 2 detail page in this phase). This plan ONLY verifies that pattern is present in all 4 detail routes; it does NOT add it (Wave 2 already shipped it). If a Wave 2 plan failed to include the pattern, see "Recovery" below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/05-admin-ops/05-CONTEXT.md
@.planning/phases/04-admin-content-crud/04-SUMMARY.md
@src/app/admin/(coming-soon)/leads/page.tsx
@src/app/admin/(coming-soon)/newsletter/page.tsx
@src/app/admin/(coming-soon)/emails/page.tsx
@src/app/admin/showcase/[id]/edit/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 3 coming-soon stubs and remove the empty directory</name>
  <read_first>
    - src/app/admin/(coming-soon)/leads/page.tsx (confirm it is still the simple stub from Phase 03)
    - src/app/admin/(coming-soon)/newsletter/page.tsx (same)
    - src/app/admin/(coming-soon)/emails/page.tsx (same)
    - .planning/phases/04-admin-content-crud/04-SUMMARY.md (analog: Phase 04 Plan 04-05 deleted the showcase/blog/testimonials stubs the same way)
  </read_first>
  <files>src/app/admin/(coming-soon)/leads/page.tsx, src/app/admin/(coming-soon)/newsletter/page.tsx, src/app/admin/(coming-soon)/emails/page.tsx</files>
  <action>
    Run from the project root:

    ```
    rm src/app/admin/\(coming-soon\)/leads/page.tsx
    rm src/app/admin/\(coming-soon\)/newsletter/page.tsx
    rm src/app/admin/\(coming-soon\)/emails/page.tsx
    rmdir src/app/admin/\(coming-soon\)/leads
    rmdir src/app/admin/\(coming-soon\)/newsletter
    rmdir src/app/admin/\(coming-soon\)/emails
    rmdir 'src/app/admin/(coming-soon)'
    ```

    After deletion, the route group directory `(coming-soon)/` should be gone entirely (Phase 04 already removed the showcase/blog/testimonials children).

    Verify:
    ```
    ls 'src/app/admin/(coming-soon)/' 2>&1 | grep -q 'No such file' && echo OK
    ```

    Stage the deletions (parentheses in the path need quoting for git too):
    ```
    git add 'src/app/admin/(coming-soon)/leads/page.tsx' 'src/app/admin/(coming-soon)/newsletter/page.tsx' 'src/app/admin/(coming-soon)/emails/page.tsx'
    git status  # confirm only deletions of those 3 files plus the implicit directory removal
    ```

    NOTHING else gets staged in this commit. NO other file is modified by this plan.

    Protected paths (DO NOT modify): every file enumerated in CONTEXT.md §7 plus the Wave-2 files just shipped by Plans 05-02..05-05 (the cleanup must not touch those — that's a different agent's territory).

    Recovery (only if the Wave 2 build verification flagged a parallel-pages collision OR a missing cacheComponents pattern):
    - If `bun run build` produces "You cannot have two parallel pages that resolve to the same path" pointing at one of the 3 Wave-2 routes, the stub for that route did not get deleted in Task 1. Re-run the rm step for the missing file.
    - If `bun run build` produces a partial-prerender Neon error mentioning a `/admin/leads/[id]`-style route, that route's Wave-2 page is missing the `generateStaticParams() / await connection() / <Suspense>` pattern. DO NOT add it here — open a new follow-up plan (e.g. 05-06.1) that edits ONLY the affected Wave-2 file and re-runs verification. This plan does not modify Wave-2 files.
  </action>
  <verify>
    <automated>bun run build &amp;&amp; test ! -d 'src/app/admin/(coming-soon)' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/admin/(coming-soon)/` directory does not exist.
    - `src/app/admin/(coming-soon)/leads/page.tsx` does not exist.
    - `src/app/admin/(coming-soon)/newsletter/page.tsx` does not exist.
    - `src/app/admin/(coming-soon)/emails/page.tsx` does not exist.
    - `bun run build` exit 0.
    - Build route table contains `/admin/leads`, `/admin/leads/[id]`, `/admin/leads/calculator`, `/admin/leads/calculator/[id]`, `/admin/newsletter`, `/admin/newsletter/[id]`, `/admin/emails`, `/admin/emails/[id]`.
    - Build route table does NOT contain any `(coming-soon)` segment.
    - No parallel-pages collision warning in the build output.
    - `git status --porcelain` shows EXACTLY 3 deletions (the stub files); nothing else modified.
  </acceptance_criteria>
  <done>The coming-soon stubs are gone, the directory is removed, the build succeeds, and the route table shows the real Wave-2 routes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Next 16 cacheComponents pattern present in all 4 dynamic detail routes</name>
  <read_first>
    - src/app/admin/leads/[id]/page.tsx (Plan 05-02 shipped this — verify pattern present)
    - src/app/admin/leads/calculator/[id]/page.tsx (Plan 05-03 shipped this)
    - src/app/admin/newsletter/[id]/page.tsx (Plan 05-04 shipped this)
    - src/app/admin/emails/[id]/page.tsx (Plan 05-05 shipped this)
    - src/app/admin/showcase/[id]/edit/page.tsx (canonical Phase-04 reference — what good looks like)
  </read_first>
  <files></files>
  <action>
    Read-only verification — no files modified by this task. Confirms the Wave-2 detail routes carry the cacheComponents pattern Phase 04 established.

    For each of the 4 detail routes, run:
    ```
    grep -l "generateStaticParams" src/app/admin/leads/\[id\]/page.tsx \
                                   'src/app/admin/leads/calculator/[id]/page.tsx' \
                                   'src/app/admin/newsletter/[id]/page.tsx' \
                                   'src/app/admin/emails/[id]/page.tsx'
    ```
    Should print all 4 file paths.

    ```
    grep -l "await connection" src/app/admin/leads/\[id\]/page.tsx \
                                'src/app/admin/leads/calculator/[id]/page.tsx' \
                                'src/app/admin/newsletter/[id]/page.tsx' \
                                'src/app/admin/emails/[id]/page.tsx'
    ```
    Should print all 4 file paths.

    ```
    grep -l "Suspense" src/app/admin/leads/\[id\]/page.tsx \
                       'src/app/admin/leads/calculator/[id]/page.tsx' \
                       'src/app/admin/newsletter/[id]/page.tsx' \
                       'src/app/admin/emails/[id]/page.tsx'
    ```
    Should print all 4 file paths.

    ```
    grep -l "__build_placeholder__" src/app/admin/leads/\[id\]/page.tsx \
                                    'src/app/admin/leads/calculator/[id]/page.tsx' \
                                    'src/app/admin/newsletter/[id]/page.tsx' \
                                    'src/app/admin/emails/[id]/page.tsx'
    ```
    Should print all 4 file paths.

    If any file fails any grep: the Wave-2 plan that created it did not follow the contract. Record the gap in the SUMMARY for this plan and open a follow-up plan to fix the specific file. DO NOT edit Wave-2 files from this plan.

    Also verify the 4 LIST pages have the pattern (less strict — list pages need `await connection()` + `<Suspense>` but not `generateStaticParams()` because they are not dynamic segments):
    ```
    grep -l "await connection" src/app/admin/leads/page.tsx \
                                src/app/admin/leads/calculator/page.tsx \
                                src/app/admin/newsletter/page.tsx \
                                src/app/admin/emails/page.tsx
    ```
    Should print all 4. Same check for `Suspense`. If any miss: open follow-up plan.

    Protected paths: same as Task 1.
  </action>
  <verify>
    <automated>bun run build</automated>
  </verify>
  <acceptance_criteria>
    - All 4 detail routes contain `generateStaticParams`, `await connection`, `Suspense`, `__build_placeholder__`.
    - All 4 list routes contain `await connection` and `Suspense`.
    - `bun run build` exit 0 with the full route table and no prerender errors.
  </acceptance_criteria>
  <done>The cacheComponents contract is verified across all 8 Wave-2 admin routes. Any failure surfaces as a follow-up plan rather than an edit from this plan.</done>
</task>

</tasks>

<verification>
After both tasks:
- `bun run build` exit 0.
- `bun run lint && bun run typecheck` exit 0.
- `src/app/admin/(coming-soon)/` does not exist.
- Build route table contains the 8 Wave-2 admin routes and no `(coming-soon)` segment.
- `git status --porcelain` shows EXACTLY the 3 stub deletions and nothing else.
</verification>

<success_criteria>
Coming-soon stubs deleted, directory removed, build green, cacheComponents pattern present on all Wave-2 detail routes. Wave 4 verification can now run a clean diff against `main` and confirm no protected file moved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-ops/05-06-SUMMARY.md` documenting:
- The 3 stub deletions and the empty-directory removal.
- The cacheComponents verification result (all 8 routes pass / any failures).
- Build route table excerpt showing the 8 new routes.
</output>

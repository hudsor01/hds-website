---
phase: 05-admin-ops
plan: 07
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06]
files_modified:
  - .planning/phases/05-admin-ops/05-07-VERIFICATION.md
autonomous: false
requirements: [P05]
must_haves:
  truths:
    - "All 13 automated gates pass (lint, typecheck, unit tests, build, em-dash sweep, en-dash sweep, protected-files byte-equal, requireAdminSession defense-in-depth count, revalidatePath count, no console.*, no process.env.X, no any types, cron endpoint byte-equal)."
    - "The operator-facing smoke checklist for all 4 surfaces is captured in 05-07-VERIFICATION.md (execution deferred to operator)."
    - "Phase 02/03/04 surface diff vs main is empty."
    - "`src/app/api/process-emails/route.ts` diff vs main is empty."
    - "Public-route surface diff vs main is empty: src/app/{contact,unsubscribe,newsletter}/*, src/app/api/{contact,newsletter,blog}/*."
  artifacts:
    - path: .planning/phases/05-admin-ops/05-07-VERIFICATION.md
      provides: "Evidence log of all 13 automated gates + the deferred operator smoke checklist"
  key_links: []
---

<objective>
Run every automated gate against the merged Wave-1-through-Wave-3 work and capture the evidence in `05-07-VERIFICATION.md`. Produce the manual operator smoke checklist for the 4 surfaces (deferred to the operator pre-PR per the Phase 03 / 04 precedent).

Purpose: Gate the PR. If any gate fails, this plan records the failure and exits non-autonomous; the operator decides whether to open a follow-up plan or split the failure into a separate phase.

Output: One new VERIFICATION.md file. No code changes.

`autonomous: false` because Gate 14 (operator smoke) requires human action — same shape as Phase 03 Plan 03-06 and Phase 04 Plan 04-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/05-admin-ops/05-CONTEXT.md
@.planning/phases/04-admin-content-crud/04-SUMMARY.md
@.planning/phases/03-admin-shell-and-dashboard/03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all 13 automated gates and capture evidence in VERIFICATION.md</name>
  <read_first>
    - .planning/phases/04-admin-content-crud/04-SUMMARY.md (canonical analog — Phase 04 ran 13 of the same gates; mirror its evidence format)
    - .planning/phases/05-admin-ops/05-CONTEXT.md §7 (constraints — every "do not violate" item maps to a gate)
    - .planning/phases/05-admin-ops/05-CONTEXT.md §8 (verification list — these gates implement that list verbatim)
  </read_first>
  <files>.planning/phases/05-admin-ops/05-07-VERIFICATION.md</files>
  <action>
    Run each gate and append the result (command + output) to `.planning/phases/05-admin-ops/05-07-VERIFICATION.md`. Use the structure Phase 04's `04-06-VERIFICATION.md` established (if it does not exist on disk, mirror the layout described in `04-SUMMARY.md`).

    **Gate 1 — Lint**
    ```
    bun run lint
    ```
    Expect exit 0 and zero diagnostics. Record the file-count line.

    **Gate 2 — Typecheck**
    ```
    bun run typecheck
    ```
    Expect exit 0.

    **Gate 3 — Unit tests**
    ```
    bun run test:unit
    ```
    Expect exit 0. Record the pass/fail counts (Phase 04 baseline was 563 pass / 0 fail; Phase 05 adds no test files per plan design, so the count should be unchanged at 563).

    **Gate 4 — Build**
    ```
    bun run build
    ```
    Expect exit 0. Capture the route-table excerpt showing:
    - /admin/leads
    - /admin/leads/[id]
    - /admin/leads/calculator
    - /admin/leads/calculator/[id]
    - /admin/newsletter
    - /admin/newsletter/[id]
    - /admin/emails
    - /admin/emails/[id]
    Confirm NO `(coming-soon)` segment in the route table.

    **Gate 5 — Em-dash sweep on changed files**
    Build the file list:
    ```
    CHANGED_FILES=$(git diff --name-only main...HEAD | grep -E '^src/' | tr '\n' ' ')
    echo "Checking: $CHANGED_FILES"
    grep -rnE '—' $CHANGED_FILES
    ```
    Expect zero matches.

    **Gate 6 — En-dash sweep on changed files**
    ```
    grep -rnE '–' $CHANGED_FILES
    ```
    Expect zero matches.

    **Gate 7 — Protected Phase 02/03/04 files byte-equal to main**
    For each path enumerated in CONTEXT.md §7 "Constraints" subsection, run:
    ```
    git diff main -- <path>
    ```
    Expected empty diff. Pre-built list (paste verbatim into VERIFICATION.md as a checklist):
    - `src/lib/auth/admin.ts`
    - `src/lib/auth/index.ts`, `src/lib/auth/client.ts`, `src/lib/auth/get-session.ts`
    - `src/components/auth/*` (run `git diff main -- 'src/components/auth/'`)
    - `src/app/auth/*`
    - `src/app/api/auth/[...all]/route.ts`
    - `proxy.ts`
    - `src/lib/admin/dashboard-queries.ts`
    - `src/lib/admin/auth.ts`
    - `src/lib/admin/form-data.ts`
    - `src/lib/admin/db-errors.ts`
    - `src/lib/admin/slugify.ts`
    - `src/lib/admin/showcase-queries.ts`
    - `src/lib/admin/blog-queries.ts`
    - `src/lib/admin/testimonials-queries.ts`
    - `src/lib/schemas/admin-showcase.ts`
    - `src/lib/schemas/admin-blog.ts`
    - `src/lib/schemas/admin-testimonials.ts`
    - `src/components/admin/Sidebar.tsx`
    - `src/components/admin/Topbar.tsx`
    - `src/components/admin/Forbidden.tsx`
    - `src/components/admin/FormFieldSet.tsx`
    - `src/components/admin/DeleteButton.tsx`
    - `src/components/admin/PublishToggle.tsx`
    - `src/components/admin/ResourceListPage.tsx`
    - `src/components/admin/widgets/` (directory)
    - `src/app/admin/layout.tsx`
    - `src/app/admin/page.tsx`
    - `src/app/admin/dashboard/page.tsx`
    - `src/app/admin/showcase/` (directory)
    - `src/app/admin/blog/` (directory)
    - `src/app/admin/testimonials/` (directory)
    - `src/app/api/process-emails/route.ts`
    - `src/app/api/blog/` (directory — n8n Bearer route)
    - `src/app/api/contact/route.ts`
    - `src/app/api/newsletter/route.ts`
    - `src/app/contact/` (directory)
    - `src/app/newsletter/` (directory)
    - `src/app/unsubscribe/` (directory)
    - `src/lib/scheduled-emails.ts`
    - `src/lib/contact-service.ts`
    - `src/lib/resend-client.ts`
    Record per-path PASS / FAIL.

    **Gate 8 — Phase 05 stubs removed**
    ```
    test ! -e 'src/app/admin/(coming-soon)/leads/page.tsx' && echo OK
    test ! -e 'src/app/admin/(coming-soon)/newsletter/page.tsx' && echo OK
    test ! -e 'src/app/admin/(coming-soon)/emails/page.tsx' && echo OK
    test ! -d 'src/app/admin/(coming-soon)' && echo OK
    ```
    Expect 4 OK lines.

    **Gate 9 — `requireAdminSession` defense-in-depth count per action file**
    ```
    grep -c 'await requireAdminSession()' src/app/admin/leads/actions.ts
    grep -c 'await requireAdminSession()' src/app/admin/leads/calculator/actions.ts
    grep -c 'await requireAdminSession()' src/app/admin/newsletter/actions.ts
    grep -c 'await requireAdminSession()' src/app/admin/emails/actions.ts
    ```
    Expected minimums: leads >= 4, calculator >= 3, newsletter >= 3, emails >= 3.

    **Gate 10 — `revalidatePath` count per action file**
    ```
    grep -c 'revalidatePath' src/app/admin/leads/actions.ts
    grep -c 'revalidatePath' src/app/admin/leads/calculator/actions.ts
    grep -c 'revalidatePath' src/app/admin/newsletter/actions.ts
    grep -c 'revalidatePath' src/app/admin/emails/actions.ts
    ```
    Expected minimums: leads >= 4, calculator >= 3, newsletter >= 3, emails >= 3.

    **Gate 11 — No `console.*` in any new file**
    ```
    git diff --name-only main...HEAD | grep -E '^src/' | xargs grep -nE 'console\.(log|warn|error|debug|info)' 2>/dev/null
    ```
    Expect zero matches.

    **Gate 12 — No `process.env.X` in any new file** (env must go through `@/env`)
    ```
    git diff --name-only main...HEAD | grep -E '^src/' | xargs grep -nE 'process\.env\.' 2>/dev/null
    ```
    Expect zero matches.

    **Gate 13 — No `any` types in any new file**
    ```
    git diff --name-only main...HEAD | grep -E '^src/' | xargs grep -nE ': any|<any>|as any\b' 2>/dev/null
    ```
    Expect zero matches.

    **Gate 13b — `/api/process-emails/route.ts` explicitly byte-equal to main** (also covered by Gate 7 but call it out for emphasis per CONTEXT.md §5.4)
    ```
    git diff main -- src/app/api/process-emails/route.ts
    ```
    Expect empty.

    For each gate, write a row in the VERIFICATION.md table:
    ```
    | Gate | Description | Command | Result |
    |------|-------------|---------|--------|
    | 1 | Lint | bun run lint | PASS (334 files, 0 diagnostics) |
    | ... |
    ```

    If any gate FAILS, set autonomous to false in this plan's status and STOP. Document the failure in VERIFICATION.md, write the conclusion section pointing the operator at the failing gate, and open a follow-up plan instead of patching from this plan.

    Protected paths: this plan only writes `.planning/phases/05-admin-ops/05-07-VERIFICATION.md`. NO other file should be modified by this plan. If a gate fails, fix it in a follow-up plan, not here.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-admin-ops/05-07-VERIFICATION.md &amp;&amp; grep -c '^| ' .planning/phases/05-admin-ops/05-07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-admin-ops/05-07-VERIFICATION.md` exists.
    - All 13 gates have a row in the gates table.
    - For each gate, the file records command + result (PASS or FAIL with the diagnostic snippet).
    - If all gates PASS, the file's "Conclusion" section reads "Ready for operator smoke + PR".
    - If any gate FAILS, the file's "Conclusion" section names the failing gate and recommends a follow-up plan.
    - No src/ files modified by this plan (`git diff --name-only main...HEAD -- 'src/'` returns the same list as before this plan).
  </acceptance_criteria>
  <done>Evidence log written. Either all gates PASS and the operator can proceed to manual smoke, or a failure is documented and a follow-up plan is recommended.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator manual smoke checklist for all 4 surfaces</name>
  <what-built>
    The 4 admin ops surfaces shipped by Plans 05-02 through 05-05:
    - /admin/leads (list + detail + 4 mutations)
    - /admin/leads/calculator (list + detail + 3 mutations)
    - /admin/newsletter (list + detail + 3 mutations)
    - /admin/emails (list + detail + 3 mutations, with 4 stat cards)
  </what-built>
  <how-to-verify>
    Run `bun run dev` (port 3001) and, signed in as the admin user, perform every step below. If any step fails or shows visual regression, record it in VERIFICATION.md "Operator Smoke" section and STOP — open a follow-up plan to fix.

    **Leads surface (/admin/leads)**
    1. Visit http://localhost:3001/admin/leads. Page renders inside the admin shell with the sidebar's "Leads" item highlighted.
    2. Filter chips show: All, New, Contacted, Qualified, Closed. Default state is "All" highlighted (`aria-current="page"`).
    3. Table shows the most recent ≤200 contact submissions OR the empty-state copy "No leads yet."
    4. Click a status chip (e.g. "New"). Page reloads with `?status=new` in URL; only matching rows appear; "New" chip is now highlighted.
    5. Click "All" chip. `?status=` clears; full list returns.
    6. Click the "Calculator leads" button (top-right). Lands on /admin/leads/calculator.
    7. Back to /admin/leads. Click a lead row's name to open detail.
    8. Detail page renders: header (name/email/company/status badge), Contact info <dl>, Status section (4 chip-buttons; current status highlighted), Touchpoints section (or empty state), Notes section with textarea + existing notes, Danger section with Delete button.
    9. Click a different status chip in the Status section. Page reloads with the new status; chip highlighting updates.
    10. Type a note into the textarea and click "Add note". The note appears at the top of the list with the admin's email as createdBy and a timestamp.
    11. Click "Delete" on a note. The note disappears immediately (no confirm; that's intentional for low-stakes deletes).
    12. Click "Delete lead". Confirm dialog appears. Click OK. Lands on /admin/leads; the lead is gone; lead_attribution + lead_notes also gone via CASCADE.

    **Calculator leads surface (/admin/leads/calculator)**
    13. Filter chips show: All, Hot, Warm, Cold. Status filter works via query param.
    14. Click a row. Detail page renders: header, Lead <dl>, Inputs <pre>, Results <pre>, Conversion <dl> + action row (Mark contacted button OR Mark converted form OR "No further actions"), Attribution <dl>, Danger Delete button.
    15. If the lead is not contacted: click "Mark contacted". `contacted` becomes Yes; the form is replaced with the Mark converted form.
    16. Type "100" into the Conversion value field and click "Mark converted". `converted` becomes Yes; conversionValue shows $100.
    17. Click "Delete calculator lead". Confirm; lands on /admin/leads/calculator; row is gone.

    **Newsletter surface (/admin/newsletter)**
    18. Filter chips show: All, Active, Unsubscribed, Bounced. Filter works.
    19. Click a row. Detail page renders: header (email/name/status badge), Subscription <dl>, Actions section showing ONE button based on status (Unsubscribe / Re-subscribe / "No mutations available"), Danger Delete button.
    20. If subscriber is Active: click "Unsubscribe". Status flips to Unsubscribed; unsubscribedAt is now set; the button now says "Re-subscribe".
    21. Click "Re-subscribe". Status flips back to Active; unsubscribedAt is cleared.
    22. Click "Delete subscriber". Confirm; lands on /admin/newsletter; row is gone (GDPR hard delete).

    **Emails surface (/admin/emails)**
    23. Page renders 4 stat cards (Pending, Sent, Failed, Cancelled) above the filter row. Counts are accurate (cross-check against a `SELECT status, count(*) FROM scheduled_emails GROUP BY status` if curious).
    24. Filter chips show: All, Pending (count), Sent (count), Failed (count), Cancelled (count). Filter works.
    25. Click a row. Detail page renders: header (recipient/status badge/scheduledFor), Email <dl> with variables <pre>, Delivery <dl> with error <pre> if present, Actions section, Danger Delete button.
    26. For a Pending row: both "Retry now" and "Cancel" buttons appear.
    27. For a Failed row WITH retryCount < maxRetries: "Retry now" appears. Click it. Status flips to Pending, scheduledFor is now, error cleared.
    28. For a Failed row WITH retryCount >= maxRetries: instead of Retry button, "Retry limit reached." text appears.
    29. For a Sent row: "Already sent. No actions available." appears.
    30. For a Cancelled row: "Already cancelled. Delete below if no longer needed." appears.
    31. Click "Delete email". Confirm; lands on /admin/emails; row is gone.

    **Cross-cuts**
    32. Sign out via AccountMenu. Visit /admin/leads directly. Bounced to /auth/sign-in.
    33. Try to invoke a Server Action via the UI from an incognito window (cookie cleared). The action should redirect to /auth/sign-in (defense in depth — `requireAdminSession` first in every action).
    34. Em-dash / en-dash by-eye sweep on every rendered surface above. Zero unexpected dashes.
    35. No browser-console errors at any step.

    If all 35 steps pass: write "Operator smoke: PASS (35/35)" into VERIFICATION.md and proceed to PR. If any step fails: write the failure into the Operator Smoke section of VERIFICATION.md and STOP for triage.
  </how-to-verify>
  <resume-signal>Type "approved" if all 35 steps pass, or describe the failures.</resume-signal>
</task>

</tasks>

<verification>
After all tasks (gate 1-13 + operator smoke):
- VERIFICATION.md exists with all 13 gates recorded.
- Operator has either approved or recorded a failure.
- `git diff --name-only main...HEAD` shows ONLY the expected Phase 05 files (the 5 new files per Wave-2 plan × 4 plans = 20 + 2 shared = 22 created src files, + 3 stub deletions, + the 7 PLAN.md files, + 7 SUMMARY.md files, + 1 VERIFICATION.md, + STATE.md / ROADMAP.md updates if Plan 06 / 07 wrote them).
</verification>

<success_criteria>
All 13 automated gates green. Operator-smoke checklist captured. VERIFICATION.md committed. Phase 05 ready for PR.
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-ops/05-07-SUMMARY.md` documenting:
- Aggregate gate results (13/13 PASS or list of failures).
- Operator smoke result (deferred / approved / failed-with-detail).
- Recommended PR title and squash commit message, modeled after Phase 04:
  ```
  feat(admin): ops - leads, calculator-leads, newsletter, emails with Server Actions
  ```
- Phase 05 ROADMAP update note: status `complete (7/7)`, mark all 7 plans `[x]`, milestone v4 status `complete (4/4)`.
</output>

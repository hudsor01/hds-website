---
phase: 05-admin-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/StatusFilterBar.tsx
  - src/components/admin/StatusBadge.tsx
autonomous: true
requirements: [P05]
must_haves:
  truths:
    - "A status filter row of chip buttons can be reused across all 4 admin ops list pages (leads, calculator leads, newsletter, emails) without per-page CSS."
    - "A status badge renders any string status value with a color from the OKLCH token palette and never falls back to a hardcoded hex."
    - "The active filter chip is announced to assistive tech via aria-current=\"page\" (matches the breadcrumb/nav idiom)."
    - "Filter chip click navigates via GET request — no client JS required."
  artifacts:
    - path: src/components/admin/StatusFilterBar.tsx
      provides: "Reusable server-rendered status filter chip row"
      exports: ["StatusFilterBar", "StatusFilterOption"]
    - path: src/components/admin/StatusBadge.tsx
      provides: "Reusable server-rendered status pill with token color mapping"
      exports: ["StatusBadge"]
  key_links:
    - from: "Future Wave 2 list pages"
      to: "StatusFilterBar"
      via: "<StatusFilterBar baseHref=\"/admin/leads\" current={status} options={...} />"
      pattern: "import.*StatusFilterBar.*from.*@/components/admin/StatusFilterBar"
    - from: "Future Wave 2 list + detail pages"
      to: "StatusBadge"
      via: "<StatusBadge status={row.status} />"
      pattern: "import.*StatusBadge.*from.*@/components/admin/StatusBadge"
---

<objective>
Build the two new shared UI primitives every Wave 2 plan depends on: a server-rendered `<StatusFilterBar />` that renders a `<form method="get">` chip row keyed off a `?status=` query param, and a server-rendered `<StatusBadge />` that maps status strings to OKLCH token colors.

Purpose: Wave 2 (leads, calculator-leads, newsletter, emails) ships four near-identical list+detail surfaces. Both primitives are extracted here so each Wave 2 slice imports them rather than duplicating chip CSS or badge color logic.

Output: Two new files under `src/components/admin/`. No tests (pure rendering; visual coverage during Wave 4 operator smoke is sufficient — Phase 04's `DeleteButton`/`PublishToggle`/`ResourceListPage` shipped without unit tests for the same reason).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/05-admin-ops/05-CONTEXT.md
@src/components/admin/DeleteButton.tsx
@src/components/admin/PublishToggle.tsx
@src/components/admin/ResourceListPage.tsx
@src/components/admin/FormFieldSet.tsx

<interfaces>
<!-- These are the exports the Wave 2 plans will consume. Define them here so executors don't have to guess shapes. -->

From src/components/admin/StatusFilterBar.tsx (NEW — create in this plan):
```typescript
export type StatusFilterOption = {
  value: string | null  // null = "All" chip; clears the query param
  label: string         // user-facing chip text ("All", "New", "Contacted", ...)
  count?: number        // optional count rendered after the label as " (12)"
}

export interface StatusFilterBarProps {
  baseHref: string                    // e.g. "/admin/leads" — the GET form action target
  current: string | null              // current value of ?status; null = "All"
  options: StatusFilterOption[]       // chips left-to-right; first is conventionally "All"
  legend?: string                     // sr-only group label, defaults to "Filter by status"
}

export function StatusFilterBar(props: StatusFilterBarProps): JSX.Element
```

From src/components/admin/StatusBadge.tsx (NEW — create in this plan):
```typescript
export interface StatusBadgeProps {
  status: string | null | undefined   // accepts nullable for DB columns that allow NULL
  // No size or variant prop — one canonical style. Add variants only if a Wave 2 page needs them.
}

export function StatusBadge(props: StatusBadgeProps): JSX.Element
```
</interfaces>

<status_color_palette>
<!-- These token names must match what's actually defined in src/app/globals.css. -->
<!-- If a token name does not exist, use the closest existing token (e.g. text-foreground / bg-surface-base). -->
<!-- DO NOT introduce hardcoded hex / rgb values per CLAUDE.md "no hardcoded colors". -->

Lead statuses:        new=primary, contacted=accent, qualified=success, closed=muted
Newsletter statuses:  active=success, unsubscribed=muted, bounced=destructive
Email statuses:       pending=warning, sent=success, failed=destructive, cancelled=muted
Calculator quality:   hot=destructive, warm=warning, cold=muted
Default / unknown:    muted-foreground on bg-surface-base
</status_color_palette>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatusFilterBar — server-component chip row driven by ?status query param</name>
  <read_first>
    - src/components/admin/PublishToggle.tsx (analog: tiny `<form>` posting to a server endpoint; mirror the inline `<form>` idiom)
    - src/components/admin/ResourceListPage.tsx (analog: shared admin surface, server component, no `'use client'`)
    - src/components/admin/FormFieldSet.tsx (analog: aria-* patterns used in this project)
    - .planning/phases/05-admin-ops/05-CONTEXT.md §3 "Filter pattern" block
    - CLAUDE.md "Styling" section — confirm `text-foreground`, `text-muted-foreground`, `bg-surface-base`, `bg-surface-raised`, `border-border`, `text-accent-text` are the available tokens used in the existing primitives
  </read_first>
  <files>src/components/admin/StatusFilterBar.tsx</files>
  <action>
    Create `src/components/admin/StatusFilterBar.tsx`. NO `'use client'` directive — this is a server component.

    Imports: `import type { JSX } from 'react'` (only if your TS config doesn't have the global JSX namespace — Next 16 + React 19 typically does not need this; if `JSX.Element` resolves, no import needed).

    Export `StatusFilterOption` and `StatusFilterBarProps` types exactly as shown in the `<interfaces>` block above.

    Component body: render a single `<form method="get" action={baseHref}>` wrapping a `<fieldset>` whose `<legend className="sr-only">` is `{legend ?? 'Filter by status'}`. Inside the fieldset render a flex row (`className="flex flex-wrap items-center gap-2"`) of one `<button>` per `options` entry.

    Per chip:
    - When `option.value === null` (the "All" chip): `<button type="submit" name="status" value="">` — submitting with an empty value clears `?status` on the URL via the browser default (empty value omits the param in the query string when name is present; if the browser includes it as `?status=`, use a different approach: render the All chip as `<button type="submit" formAction={baseHref}>` with NO `name` attribute. Pick whichever approach actually clears the param — verify by running the page locally during Wave 4 smoke).
    - Otherwise: `<button type="submit" name="status" value={option.value}>`.
    - Active state: `current === option.value` (treat both as null when option.value is null). Active chip gets `aria-current="page"` AND className includes the "filled" variant; inactive chips use the "outline" variant.
    - Class shape — model after `PublishToggle.tsx` and the action-cell links in `src/app/admin/showcase/page.tsx`:
      - Inactive: `inline-flex items-center gap-2 px-3 py-1.5 rounded-md text-sm font-medium text-muted-foreground border border-border bg-surface-raised hover:bg-surface-base transition-smooth`
      - Active (add to inactive base): replace `text-muted-foreground` with `text-foreground` and `bg-surface-raised` with `bg-surface-base`; keep the border for visual weight
    - Label rendering: `{option.label}{typeof option.count === 'number' ? ` (${option.count})` : ''}` — note this is a TEMPLATE LITERAL inside JSX `{}`. The space character is a regular ASCII space, the parens are ASCII `(` and `)`. NO em-dash, NO en-dash anywhere.

    File header comment (mirror `ResourceListPage.tsx` style, one paragraph): explain that this is the Phase-05 status filter primitive shared by `/admin/leads`, `/admin/leads/calculator`, `/admin/newsletter`, `/admin/emails`; that it uses a GET form so the round-trip works with no client JS; that `aria-current="page"` marks the active chip.

    No new dependencies. No use of `usePathname`, `useRouter`, or any client-only API.

    DO NOT touch any other file. DO NOT touch any Phase 02 / 03 / 04 file (`src/lib/auth/*`, `src/components/auth/*`, `src/app/auth/*`, `proxy.ts`, `src/lib/auth/admin.ts`, `src/lib/admin/dashboard-queries.ts`, `src/components/admin/{Sidebar,Topbar,Forbidden,FormFieldSet,DeleteButton,PublishToggle,ResourceListPage}.tsx`, `src/components/admin/widgets/*`, `src/app/admin/layout.tsx`, `src/app/admin/page.tsx`, `src/app/admin/dashboard/page.tsx`, anything under `src/app/admin/{showcase,blog,testimonials}/`, anything under `src/lib/admin/{showcase,blog,testimonials}-queries.ts` or `src/lib/schemas/admin-{showcase,blog,testimonials}.ts`). DO NOT touch any public surface (`src/app/{contact,unsubscribe,newsletter}/*`, `src/app/api/{contact,newsletter,process-emails}/*`).
  </action>
  <verify>
    <automated>bun run lint &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/admin/StatusFilterBar.tsx` exists.
    - File does NOT contain `'use client'`.
    - File exports `StatusFilterBar`, `StatusFilterOption`, `StatusFilterBarProps`.
    - File contains `<form method="get"` (case-sensitive on `method="get"`).
    - File contains `aria-current="page"` (exact substring).
    - File contains NO em-dash (—, U+2014) and NO en-dash (–, U+2013) anywhere in any string literal or template literal.
    - File contains NO `import.*useState` / `useEffect` / `usePathname` / `useRouter`.
    - File contains NO hardcoded `#` hex color, NO `rgb(`, NO `oklch(` (use Tailwind tokens).
    - `bun run lint` exit 0.
    - `bun run typecheck` exit 0.
  </acceptance_criteria>
  <done>The file compiles, lints clean, and the export surface matches the interfaces block above so Wave 2 plans can import it directly without further modification.</done>
</task>

<task type="auto">
  <name>Task 2: StatusBadge — server-component status pill with token color mapping</name>
  <read_first>
    - src/components/admin/widgets/RecentLeadsPanel.tsx (if it exists — analog for any existing status-rendering pattern in this codebase)
    - src/app/admin/showcase/page.tsx (analog: how the "Yes/No" / status cells render inside the admin list tables — keep visual weight consistent)
    - CLAUDE.md "Styling" section — confirm available semantic tokens
    - src/app/globals.css (search for the token names referenced in the status_color_palette block above; substitute the nearest existing token where a name does not resolve)
  </read_first>
  <files>src/components/admin/StatusBadge.tsx</files>
  <action>
    Create `src/components/admin/StatusBadge.tsx`. NO `'use client'` — server component.

    Export `StatusBadgeProps` and `StatusBadge` exactly as shown in the `<interfaces>` block.

    Inside the file, define a private const map from status string -> Tailwind class string. Cover all values the Wave 2 plans will pass in (Phase-05 CONTEXT.md sections 5.1-5.4):

    ```
    const STATUS_CLASSES: Record<string, string> = {
      // leads
      new:          'text-foreground bg-surface-base border-border',           // neutral / first-touch
      contacted:    'text-accent-text bg-surface-base border-border',
      qualified:    'text-success bg-success/10 border-success/30',
      closed:       'text-muted-foreground bg-surface-raised border-border',
      // newsletter
      active:       'text-success bg-success/10 border-success/30',
      unsubscribed: 'text-muted-foreground bg-surface-raised border-border',
      bounced:      'text-destructive bg-destructive/10 border-destructive/30',
      // emails
      pending:      'text-warning bg-warning/10 border-warning/30',
      sent:         'text-success bg-success/10 border-success/30',
      failed:       'text-destructive bg-destructive/10 border-destructive/30',
      cancelled:    'text-muted-foreground bg-surface-raised border-border',
      // calculator-lead quality (treated as status for badge purposes)
      hot:          'text-destructive bg-destructive/10 border-destructive/30',
      warm:         'text-warning bg-warning/10 border-warning/30',
      cold:         'text-muted-foreground bg-surface-raised border-border',
    }
    const DEFAULT_CLASS = 'text-muted-foreground bg-surface-raised border-border'
    ```

    IMPORTANT — token availability: `text-success`, `text-warning`, `text-destructive`, `text-accent-text`, `text-foreground`, `text-muted-foreground`, `bg-surface-base`, `bg-surface-raised`, `border-border` are the tokens this project already uses (confirmed via Phase 03 widgets and `ResourceListPage.tsx`). If `text-warning` / `bg-warning/10` does not resolve in `src/app/globals.css`, fall back to `text-foreground bg-surface-base border-border` for that specific row and add a one-line code comment noting the fallback. DO NOT introduce a new color token in this plan.

    Component body: pick `STATUS_CLASSES[status ?? '']` with `DEFAULT_CLASS` fallback. Render:

    ```jsx
    <span
      className={`inline-flex items-center px-2 py-0.5 rounded-md text-xs font-medium border ${cls}`}
    >
      {status ?? 'unknown'}
    </span>
    ```

    Both `null` and `undefined` inputs render the literal text `unknown` with the default class.

    File header comment (mirror `ResourceListPage.tsx` style): explain that this primitive is consumed by Phase-05 list and detail pages to keep status coloring consistent across the four ops surfaces, and that the status->class map is the only place to change a color.

    DO NOT touch any other file. DO NOT touch any of the protected paths listed in Task 1.
  </action>
  <verify>
    <automated>bun run lint &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/admin/StatusBadge.tsx` exists.
    - File does NOT contain `'use client'`.
    - File exports `StatusBadge` and `StatusBadgeProps`.
    - File contains a status->class map covering AT LEAST the keys: `new`, `contacted`, `qualified`, `closed`, `active`, `unsubscribed`, `bounced`, `pending`, `sent`, `failed`, `cancelled`, `hot`, `warm`, `cold`.
    - File renders the literal string `unknown` for `null` / `undefined` input (grep `'unknown'` or `"unknown"`).
    - File contains NO em-dash and NO en-dash.
    - File contains NO hardcoded hex color, NO `rgb(`, NO `oklch(` literal.
    - `bun run lint` exit 0.
    - `bun run typecheck` exit 0.
  </acceptance_criteria>
  <done>The file compiles, lints clean, and Wave 2 list/detail pages can `import { StatusBadge } from '@/components/admin/StatusBadge'` without any further work.</done>
</task>

</tasks>

<verification>
After both tasks land:
- `bun run lint` exits 0.
- `bun run typecheck` exits 0.
- `bun run build` should still pass (these files are unused until Wave 2 imports them; Next does not fail on unused exports).
- Em/en-dash sweep on the two new files returns zero:
  ```
  grep -nE '[—–]' src/components/admin/StatusFilterBar.tsx src/components/admin/StatusBadge.tsx
  ```
- Protected-files diff returns empty for every Phase 02/03/04 file enumerated in `<action>` Task 1.
</verification>

<success_criteria>
Both primitives shipped, no Phase 02/03/04 file changed, no public surface changed, no new deps. Wave 2 plans can consume `StatusFilterBar` and `StatusBadge` exactly per the `<interfaces>` block.
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-ops/05-01-SUMMARY.md` documenting:
- The exported types (paste the interfaces block verbatim so Wave 2 consumers have a canonical reference).
- The status->class map (paste the const declaration verbatim so Wave 2 consumers know which keys are pre-registered).
- Any token substitutions made (e.g. "`text-warning` did not resolve in globals.css; cancelled/warm rows fall back to `text-foreground bg-surface-base`").
</output>
</content>
</invoke>
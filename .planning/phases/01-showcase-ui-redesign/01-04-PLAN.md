---
phase: 01-showcase-ui-redesign
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified: []
autonomous: false
requirements: [SHOWCASE-VERIFY]
must_haves:
  truths:
    - "bun run lint passes against HEAD"
    - "bun run typecheck passes against HEAD"
    - "bun run build completes successfully"
    - "The 4 changed files (card.tsx, showcase/page.tsx, and any new files this phase created) contain zero em-dash and zero en-dash characters"
    - "Loading /showcase locally shows: hero with typewriter, 4-stat bar, section header, full-width jirah-shop featured card with image, 3-col support grid with images, inline CTA, closing CTA"
    - "Network tab shows the 4 showcase JPGs served as WebP (Next.js Image conversion)"
  artifacts:
    - path: ".planning/phases/01-showcase-ui-redesign/01-04-verification-report.md"
      provides: "Pass/fail evidence for each verification gate"
      contains: "lint output, typecheck output, build output, em/en-dash grep output, human visual checklist"
  key_links: []
---

<objective>
End-to-end verification of the phase: code passes lint/typecheck/build, copy is em/en-dash free, the rendered page matches the spec.

Purpose: Catch regressions before the PR is opened. Bundle all phase-level gates into one plan so the executor knows when the phase is shippable.

Output: A verification report at `.planning/phases/01-showcase-ui-redesign/01-04-verification-report.md` capturing the exact output of each gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-showcase-ui-redesign/01-CONTEXT.md
@.planning/phases/01-showcase-ui-redesign/01-01-SUMMARY.md
@.planning/phases/01-showcase-ui-redesign/01-02-SUMMARY.md
@.planning/phases/01-showcase-ui-redesign/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated gates (lint, typecheck, build, em/en-dash grep)</name>
  <read_first>
    - .planning/phases/01-showcase-ui-redesign/01-CONTEXT.md (section 10: "Verification")
    - .planning/phases/01-showcase-ui-redesign/01-03-SUMMARY.md
  </read_first>
  <files>.planning/phases/01-showcase-ui-redesign/01-04-verification-report.md</files>
  <action>
    Create `.planning/phases/01-showcase-ui-redesign/01-04-verification-report.md` and run each of the following commands from the project root, capturing exit code and a short summary of output (last 20 lines for build) under a dedicated heading for each gate:

    1. `bun run lint` — capture exit code; if non-zero, list the failing files/rules verbatim
    2. `bun run typecheck` — capture exit code; if non-zero, list the failing diagnostics verbatim
    3. `bun run build` — capture exit code and the final summary block (route table); confirm `/showcase` appears in the route output
    4. Em/en-dash grep across files this phase touched:
       ```
       grep -nE "—|–" src/components/ui/card.tsx src/app/showcase/page.tsx
       ```
       Expected: zero matches in any line that this phase introduced or modified. Pre-existing matches elsewhere in card.tsx that this phase did not touch are NOT failures for this plan — record them in the report under "Pre-existing em/en-dash (out of scope)" if any exist.
    5. Confirm the 4 image files still exist:
       ```
       ls -la public/images/showcase/ink37-tattoos.jpg public/images/showcase/tenantflow.jpg public/images/showcase/revops-portfolio.jpg public/images/showcase/jirah-shop.jpg
       ```

    Use the GSD logger or simply append to the report file with `>>` redirection — your call.

    If ANY gate fails (non-zero exit, or em/en-dash match in modified code), STOP and report the failure rather than continuing. The verification plan does not fix issues; it surfaces them. If failures exist, the user will reopen plans 01-02 or 01-03 to address them.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-showcase-ui-redesign/01-04-verification-report.md &amp;&amp; grep -q "bun run lint" .planning/phases/01-showcase-ui-redesign/01-04-verification-report.md &amp;&amp; grep -q "bun run typecheck" .planning/phases/01-showcase-ui-redesign/01-04-verification-report.md &amp;&amp; grep -q "bun run build" .planning/phases/01-showcase-ui-redesign/01-04-verification-report.md</automated>
  </verify>
  <acceptance_criteria>
    - Report file exists and has a section per gate (lint, typecheck, build, em/en-dash grep, image presence)
    - Each section records the actual exit code
    - `bun run lint` exit code is 0
    - `bun run typecheck` exit code is 0
    - `bun run build` exit code is 0 AND the build output includes a successfully compiled `/showcase` route
    - `grep -nE "—|–"` against `src/components/ui/card.tsx` and `src/app/showcase/page.tsx` shows zero matches in code introduced by this phase (record any pre-existing matches separately, do not block)
    - All 4 showcase JPGs are present at the expected paths
  </acceptance_criteria>
  <done>All automated gates green, verification report captures evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification on local dev server</name>
  <what-built>
    A redesigned `/showcase` page with:
    - Hero (typewriter preserved)
    - 4-stat bar showing `4+` projects
    - New section header (eyebrow "Featured", H2 "Four small businesses. One thing in common.", lead)
    - Full-width featured card for jirah-shop with image on left, content on right
    - 3-col grid of 3 support cards (Ink 37, TenantFlow, RevOps), each with image header
    - New inline CTA between grid and closing CTA
    - Existing closing CTA preserved
  </what-built>
  <how-to-verify>
    1. Run `bun run dev` and open http://localhost:3000/showcase (or the configured port).
    2. Confirm the hero typewriter cycles between "Real Projects." and "Real Results." with a blinking caret.
    3. Confirm the stats bar shows `4+ Projects Delivered`.
    4. Confirm the section header reads "Featured" / "Four small businesses. One thing in common." (second clause in accent amber) / lead copy.
    5. Confirm the featured card displays the jirah-shop screenshot, NOT a placeholder or broken image. Image should be cropped to 4:3 and anchored to the top.
    6. Confirm the support grid shows 3 cards in a row at desktop width, with the Ink 37, TenantFlow, and RevOps screenshots loading.
    7. Confirm the inline CTA "Want your business on this page?" appears between the grid and the closing CTA, with the accent button and the trust signal line (three middle dots, NOT em-dashes).
    8. Confirm the closing CTA "Ready to create your success story?" still appears at the bottom unchanged.
    9. Resize the viewport to ~375px wide. Confirm: featured card stacks vertically (image on top, content below), support grid collapses to a single column, no horizontal overflow, no clipped images.
    10. Open DevTools Network tab, hard reload, filter by `showcase`. Confirm all 4 images are served as `.webp` (Next.js Image conversion) and return 200.
    11. Tab through the page with the keyboard. Confirm focus rings appear on the two hero CTAs, every project card, the inline CTA button, and the two closing CTAs.
    12. Reply with "approved" if all 11 checks pass. If any fail, describe what's broken so the relevant plan (01-02 for card, 01-03 for layout, 01-01 for data) can be reopened.
  </how-to-verify>
  <read_first>
    - .planning/phases/01-showcase-ui-redesign/01-CONTEXT.md (sections 3 and 8)
  </read_first>
  <acceptance_criteria>
    - User replies "approved" after running through the 11 visual checks
    - OR user reports specific failures and the team reopens the relevant earlier plan
  </acceptance_criteria>
  <resume-signal>Reply "approved" or list specific failures</resume-signal>
  <done>Human has visually confirmed the redesigned showcase page matches the spec in section 3 of CONTEXT.md.</done>
</task>

</tasks>

<verification>
- Verification report exists and captures all gate outputs
- Human checkpoint approved
- All `bun run lint`, `typecheck`, `build` exit codes recorded as 0
- All 4 images served as WebP confirmed via DevTools
</verification>

<success_criteria>
- Phase is shippable: code passes all gates, page renders per spec, no copy violations, no broken images, mobile layout is clean
- The phase can now move to PR review and merge
</success_criteria>

<output>
After completion, create `.planning/phases/01-showcase-ui-redesign/01-04-SUMMARY.md` capturing:
- Final pass/fail for each gate (lint, typecheck, build, copy, visual)
- Pointer to the verification report
- Recommended commit message structure for the PR (one commit per plan, or squashed)
- Any follow-up issues to log against future phases (e.g. captured Playwright script utility from CONTEXT section 11)
</output>

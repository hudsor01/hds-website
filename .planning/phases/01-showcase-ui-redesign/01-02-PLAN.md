---
phase: 01-showcase-ui-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/card.tsx
autonomous: true
requirements: [SHOWCASE-CARD]
must_haves:
  truths:
    - "ProjectCardProps accepts an optional imageUrl (string | null) and an optional imageAlt (string)"
    - "When imageUrl is present and falsy-checked, the project card renders a Next.js <Image> header at the top of the card; when absent, the card renders the existing accent-bar header with no visual regression"
    - "The image area renders at 4:3 aspect ratio when featured=true and 16:9 otherwise"
    - "Overlay pills (Featured / Case Study / Portfolio / category eyebrow) sit on top of the image when imageUrl is present; they remain in the body row when imageUrl is absent"
    - "imageAlt defaults to `${title} homepage` when not provided"
    - "Only the featured card sets priority on its Image; all other cards lazy-load"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "Updated project card variant with optional image header"
      contains: "imageUrl?: string | null, imageAlt?: string in ProjectCardProps; <Image ... /> conditional render branch"
  key_links:
    - from: "Card variant=\"project\""
      to: "next/image"
      via: "import Image from 'next/image' and conditional <Image> render"
      pattern: "import Image from 'next/image'"
    - from: "imageUrl prop"
      to: "rendered <Image src={imageUrl}> with correct sizes/aspect"
      via: "conditional branch inside Project Card block"
      pattern: "imageUrl &&"
---

<objective>
Extend the existing `Card` component's `variant="project"` branch to support an optional image header without breaking any current consumer.

Purpose: The redesigned page (plan 03) needs image-led cards for all 4 projects. The card variant must be backward compatible — any caller that does NOT pass `imageUrl` should get the existing accent-bar layout exactly as it renders today. The change is purely additive.

Output: Updated `src/components/ui/card.tsx` with new optional props and a conditional image-header render branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-showcase-ui-redesign/01-CONTEXT.md
@src/components/ui/card.tsx
@src/lib/showcase.ts
@src/app/globals.css

<interfaces>
Current ProjectCardProps in src/components/ui/card.tsx:75-88:

```ts
interface ProjectCardProps extends Omit<BaseCardProps, 'variant'> {
  variant: 'project'
  id: string
  slug: string
  title: string
  description: string
  category: string
  industry?: string
  showcaseType?: 'quick' | 'detailed'
  featured?: boolean
  stats?: Record<string, string>
  tech_stack: string[]
  externalLink?: string | null
}
```

Existing wrapper at card.tsx:389-412 already applies `md:col-span-2` when `featured`, and wraps the card in `<a>` (external) or `<Link>` (internal). Keep that wrapper logic — the image goes INSIDE `cardContent`.

Existing `card.tsx` is a `'use client'` file. Next.js `<Image>` is safe in client components.

Available utility classes (from globals.css):
- `.hover-lift`, `.card-hover-glow` (used via `cardVariants({ hover: true })`)
- `.glass-card`, `.glass-card-light`
- `.text-accent-text` (WCAG-safe accent color for small text on light bg)
- `.text-h3`, `.text-h4`, `.text-eyebrow`

Accent token: `--color-accent: oklch(0.72 0.16 55)` (amber). Use `text-accent` for large or bold text; `text-accent-text` for small/medium body copy.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional image props and image-header render branch to project card</name>
  <read_first>
    - src/components/ui/card.tsx (the entire file — pay attention to lines 75-88 for the props and 286-413 for the project card branch)
    - .planning/phases/01-showcase-ui-redesign/01-CONTEXT.md (sections 5.1 and 7)
    - src/app/globals.css (lines 290-345 for glass/grid utilities; 488-499 for typography)
  </read_first>
  <files>src/components/ui/card.tsx</files>
  <action>
    Modify `src/components/ui/card.tsx` as follows:

    1. Add `import Image from 'next/image'` at the top of the imports block (after other third-party imports, before local imports).

    2. Extend `ProjectCardProps` (currently at lines 75-88) with two new optional fields:
       - `imageUrl?: string | null`
       - `imageAlt?: string`
       Place them right after `externalLink` so the diff is minimal.

    3. In the Project Card branch (currently starting around line 287 `if ('variant' in props && props.variant === 'project')`), destructure the two new fields from props alongside the existing destructure. Compute `const resolvedAlt = imageAlt ?? `${title} homepage`` and `const hasImage = Boolean(imageUrl)`.

    4. Inside the `cardContent` JSX (currently the `<div className={cn(cardVariants(...), 'h-full overflow-hidden')}>` at lines 305-386), replace the existing `{/* Top accent bar */}<div className="h-1 bg-accent" />` line with a conditional:

       - When `hasImage` is true: render a `<div className="relative w-full {aspect-class} overflow-hidden bg-muted">` where `{aspect-class}` is `aspect-[4/3]` for `featured` and `aspect-video` (16/9) for non-featured. Inside it render `<Image src={imageUrl!} alt={resolvedAlt} fill className="object-cover object-top" sizes={featured ? '(min-width: 1024px) 60vw, 100vw' : '(min-width: 1024px) 33vw, 100vw'} priority={featured} />`. Layer the overlay pills (Featured / Case Study or Portfolio) ABSOLUTELY positioned over the top-left of the image area (e.g. `<div className="absolute top-3 left-3 flex items-center gap-2">`).

       - When `hasImage` is false: render the existing `<div className="h-1 bg-accent" />` so legacy callers see no change.

    5. In the existing content `<div className="card-padding-lg flex flex-col gap-4">` block, the "Category row" (currently lines 317-338) ALREADY contains the eyebrow + Case Study pill + Featured pill. When `hasImage` is true, those pills must NOT also render inside the body — they live ONLY on the image overlay. Implement this by wrapping the existing in-body pill row in `{!hasImage && (...)}` and keeping the eyebrow category text by itself (or wrapping the whole row in the conditional — your judgment based on what reads cleanest in the diff). The H3 title, description, metric row, tech stack, and "View Project" hint MUST remain in the body regardless of `hasImage`.

    6. Use `text-accent-text` (not `text-accent`) for the overlay pills' text color when they sit on the image to ensure WCAG contrast on varied backgrounds. Pill backgrounds: keep the same `bg-accent text-accent-foreground` for Featured (high-contrast filled chip) and `bg-card/90 border border-border` for Case Study/Portfolio.

    7. Do NOT introduce any em-dash or en-dash in comments or JSX text. Use commas, periods, or `to`.

    8. Run `bun run lint` and `bun run typecheck` and fix any errors introduced by the change.
  </action>
  <verify>
    <automated>grep -q "imageUrl?: string | null" src/components/ui/card.tsx &amp;&amp; grep -q "imageAlt?: string" src/components/ui/card.tsx &amp;&amp; grep -q "import Image from 'next/image'" src/components/ui/card.tsx &amp;&amp; grep -qE "aspect-\\[4/3\\]|aspect-video" src/components/ui/card.tsx &amp;&amp; bun run typecheck &amp;&amp; bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `ProjectCardProps` includes the two new optional fields exactly as named (`imageUrl`, `imageAlt`)
    - The project card branch destructures both and computes a default `imageAlt` of `${title} homepage`
    - The conditional render branch exists: `hasImage === true` produces an `<Image>` header with `fill`, `object-cover object-top`, `priority` only when `featured`; `hasImage === false` produces the original accent bar
    - Aspect ratio is `aspect-[4/3]` when `featured`, `aspect-video` otherwise
    - The in-body Category/Case Study/Featured pill row is suppressed when `hasImage` is true so they only appear once (on the overlay)
    - H3 title, description, metric row, and tech stack still render in the card body in BOTH branches
    - `bun run typecheck` exits 0
    - `bun run lint` exits 0
    - `grep -nE "—|–" src/components/ui/card.tsx` returns no matches in any change introduced by this task (existing matches elsewhere in the file are out of scope)
  </acceptance_criteria>
  <done>Card component compiles, lints, and supports both image-led and accent-bar project card layouts via the new optional `imageUrl` prop without breaking existing consumers.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- `bun run lint` passes
- Visual smoke check is deferred to plan 04; this plan ships the component change only
- Existing callers of `<Card variant="project" ... />` that do NOT pass `imageUrl` render identically to before (no visual regression)
</verification>

<success_criteria>
- `src/components/ui/card.tsx` has the additive imageUrl/imageAlt support
- The component is ready to be consumed by plan 03's rewritten `/showcase` page
- All existing card variants (`service`, `pricing`, `testimonial`, default) are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/01-showcase-ui-redesign/01-02-SUMMARY.md` capturing:
- Final shape of the new ProjectCardProps fields
- The decision on how the overlay pill row was suppressed when hasImage is true (which conditional pattern was used)
- Any edge cases discovered (e.g. did `object-top` need a fallback for portrait sources?)
</output>

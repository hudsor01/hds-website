---
phase: 01-showcase-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [SHOWCASE-DATA]
must_haves:
  truths:
    - "The showcase Postgres table has exactly 4 published rows that will render on /showcase"
    - "All 4 published rows have a non-null imageUrl pointing to a JPG that exists under public/images/showcase/"
    - "The jirah-shop row is featured=true and has the lowest displayOrder of any published row, so it renders first"
    - "The three pre-existing rows (Ink 37 Tattoos, TenantFlow, RevOps Consultant Portfolio) keep their existing slugs, titles, descriptions, technologies, and metrics; only imageUrl is added"
  artifacts:
    - path: ".planning/phases/01-showcase-ui-redesign/01-01-data-changes.md"
      provides: "Audit trail of SQL run against Neon (slug lookups + UPDATE + INSERT) so the data change is reproducible"
      contains: "SELECT lookup output, the exact UPDATE statements, the exact INSERT statement, post-change SELECT verification"
  key_links:
    - from: "showcase.imageUrl column"
      to: "public/images/showcase/{slug}.jpg"
      via: "file path string in DB row"
      pattern: "/images/showcase/(ink37-tattoos|tenantflow|revops-portfolio|jirah-shop)\\.jpg"
---

<objective>
Populate the production Neon `showcase` table so the redesigned page has 4 image-backed rows to render.

Purpose: The UI work in plans 02 and 03 renders whatever is in the DB. Until JirahShop exists as a row and the 3 existing rows have `imageUrl` populated, the redesigned page will render placeholder layouts or a 3-card grid with no featured card. This plan is the data prerequisite.

Output: 3 UPDATEs (imageUrl), 1 INSERT (jirah-shop), and a markdown audit file at `.planning/phases/01-showcase-ui-redesign/01-01-data-changes.md` capturing the actual SQL that was run plus before/after SELECT output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-showcase-ui-redesign/01-CONTEXT.md
@src/lib/schemas/showcase.ts
@src/lib/showcase.ts

<interfaces>
From src/lib/schemas/showcase.ts — relevant columns on the `showcase` table:

```ts
slug: text — NOT NULL UNIQUE
title: text — NOT NULL
description: text — NOT NULL
clientName: text
industry: text
projectType: text
category: text
showcaseType: text — defaults 'quick'; use 'detailed' for case studies
technologies: jsonb (string[]) — defaults []
metrics: jsonb (Record<string, string>) — defaults {}
imageUrl: text — nullable
externalLink: text
featured: boolean — defaults false
published: boolean — defaults false (MUST be true for the row to render)
displayOrder: integer — defaults 0; rows are ordered asc(displayOrder), desc(createdAt)
```

Live query the showcase data layer uses (src/lib/showcase.ts:103-107):
```ts
db.select().from(showcase)
  .where(eq(showcase.published, true))
  .orderBy(asc(showcase.displayOrder), desc(showcase.createdAt))
```
</interfaces>

**Image files already on disk (verified):**
- `public/images/showcase/ink37-tattoos.jpg` (352K)
- `public/images/showcase/tenantflow.jpg` (311K)
- `public/images/showcase/revops-portfolio.jpg` (270K)
- `public/images/showcase/jirah-shop.jpg` (189K)
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm JirahShop row copy and metrics with the user</name>
  <read_first>
    - .planning/phases/01-showcase-ui-redesign/01-CONTEXT.md (section 4: "Data Layer Changes")
  </read_first>
  <decision>What final values go into the new `jirah-shop` row for: `title`, `description` (one to two sentences, no em/en-dash), `technologies` (real stack — CONTEXT marks Shopify/Liquid/Klaviyo/Hydrogen as placeholders), and `metrics` (exactly 3 entries as a Record&lt;string,string&gt;, e.g. `{ "Conversion rate": "+18%", "Page weight": "-42%", "Time to ship": "3 wks" }`)?</decision>
  <context>
    The other fields are fixed by the spec:
    - slug: 'jirah-shop'
    - clientName: 'JirahShop'
    - industry: 'E-Commerce'
    - projectType: 'Online Store'
    - category: 'E-Commerce'
    - showcaseType: 'detailed'
    - featured: true
    - published: true
    - externalLink: 'https://www.shopatjirah.com/'
    - imageUrl: '/images/showcase/jirah-shop.jpg'

    The 4 placeholder fields above are content decisions only the user can make. Ask in a single message; do not invent.

    Em/en-dash ban applies to `description` — verify the user's answer contains no `—` or `–` before proceeding.
  </context>
  <options>
    <option id="user-provides">User supplies final title, description, technologies array, metrics record</option>
    <option id="use-placeholders-and-flag">Insert with CONTEXT placeholders, leave a TODO in the data-changes audit file, ship the UI, refine later</option>
  </options>
  <resume-signal>Reply with the 4 values, OR say "use placeholders" to proceed with CONTEXT defaults</resume-signal>
  <acceptance_criteria>
    - User has supplied values for all 4 fields, OR explicitly chose `use-placeholders-and-flag`
    - The chosen `description` contains zero `—` and zero `–` characters
    - The chosen `technologies` is a JSON array of strings
    - The chosen `metrics` is a JSON object with exactly 3 string-valued entries
  </acceptance_criteria>
  <done>Final field values for the jirah-shop row are recorded in the agent's working notes and ready for Task 3's INSERT.</done>
</task>

<task type="auto">
  <name>Task 2: Look up live showcase rows and write UPDATE statements</name>
  <read_first>
    - .planning/phases/01-showcase-ui-redesign/01-CONTEXT.md
    - src/lib/schemas/showcase.ts
  </read_first>
  <files>.planning/phases/01-showcase-ui-redesign/01-01-data-changes.md</files>
  <action>
    Run a SELECT against the production Neon `showcase` table to discover the live `slug`, current `imageUrl`, current `displayOrder`, and current `featured` flag for every published row. Use the Neon MCP tool `mcp__Neon__run_sql` against project `soft-bush-38066584`, database `neondb`, branch `br-rough-shape-afdj4aqj` (defaults from user MEMORY.md; do NOT hardcode credentials).

    Query exactly:
    ```sql
    SELECT slug, title, image_url, display_order, featured, published
    FROM showcase
    WHERE published = true
    ORDER BY display_order ASC, created_at DESC;
    ```

    From the result set, match each row to one of the 3 known projects by `title` (Ink 37 Tattoos, TenantFlow, RevOps Consultant Portfolio) and capture its actual slug. The slugs in MEMORY/code may NOT match these exact strings — do not guess.

    Then derive the exact 3 UPDATE statements (one per existing row), each of the form:
    ```sql
    UPDATE showcase SET image_url = '/images/showcase/{file}.jpg', updated_at = NOW() WHERE slug = '{live_slug}';
    ```
    mapping:
    - Ink 37 Tattoos -> `/images/showcase/ink37-tattoos.jpg`
    - TenantFlow -> `/images/showcase/tenantflow.jpg`
    - RevOps Consultant Portfolio -> `/images/showcase/revops-portfolio.jpg`

    Also compute the new `displayOrder` for the jirah-shop INSERT as `(MIN(display_order across published rows)) - 1`. If the min is 0, use -1; if negative, keep going negative. Record this number.

    Write all of this — the SELECT output, the 3 derived UPDATE statements, and the computed jirah-shop displayOrder — into `.planning/phases/01-showcase-ui-redesign/01-01-data-changes.md` under a `## Pre-change snapshot` and `## Planned UPDATEs` heading. Do NOT execute the UPDATEs yet.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-showcase-ui-redesign/01-01-data-changes.md &amp;&amp; grep -q "## Pre-change snapshot" .planning/phases/01-showcase-ui-redesign/01-01-data-changes.md &amp;&amp; grep -q "## Planned UPDATEs" .planning/phases/01-showcase-ui-redesign/01-01-data-changes.md &amp;&amp; grep -cE "UPDATE showcase SET image_url = '/images/showcase/(ink37-tattoos|tenantflow|revops-portfolio)\\.jpg'" .planning/phases/01-showcase-ui-redesign/01-01-data-changes.md | grep -q '^3$'</automated>
  </verify>
  <acceptance_criteria>
    - SELECT result for all currently published rows is recorded verbatim in the audit file
    - The 3 derived UPDATE statements appear in the audit file with the 3 correct image paths, each addressing a slug discovered in the SELECT (not a guessed slug)
    - The new displayOrder value for the jirah-shop row is recorded and is strictly less than the MIN(display_order) observed in the SELECT
    - No UPDATE has been executed yet against the DB
  </acceptance_criteria>
  <done>Audit file contains pre-change snapshot, the 3 planned UPDATE statements with verified live slugs, and the computed displayOrder for the jirah-shop INSERT.</done>
</task>

<task type="auto">
  <name>Task 3: Execute UPDATEs and INSERT, then verify post-state</name>
  <read_first>
    - .planning/phases/01-showcase-ui-redesign/01-01-data-changes.md (the audit file produced by Task 2)
    - .planning/phases/01-showcase-ui-redesign/01-CONTEXT.md (section 4)
  </read_first>
  <files>.planning/phases/01-showcase-ui-redesign/01-01-data-changes.md</files>
  <action>
    Using `mcp__Neon__run_sql` against project `soft-bush-38066584`, database `neondb`, branch `br-rough-shape-afdj4aqj`:

    1. Execute the 3 UPDATE statements recorded in the audit file (Task 2's "## Planned UPDATEs" section). Run them one at a time so any failure is isolated.

    2. Execute exactly one INSERT for the new jirah-shop row using the field values confirmed in Task 1 plus the fixed fields from CONTEXT. The INSERT must set:
       - `slug = 'jirah-shop'`
       - `title`, `description`, `technologies` (cast `::jsonb`), `metrics` (cast `::jsonb`) — from Task 1
       - `client_name = 'JirahShop'`, `industry = 'E-Commerce'`, `project_type = 'Online Store'`, `category = 'E-Commerce'`
       - `showcase_type = 'detailed'`
       - `featured = true`, `published = true`
       - `display_order = {value computed in Task 2}`
       - `external_link = 'https://www.shopatjirah.com/'`
       - `image_url = '/images/showcase/jirah-shop.jpg'`
       - `published_at = NOW()`, `created_at = NOW()`, `updated_at = NOW()`

    3. Run this verification SELECT and capture the output:
       ```sql
       SELECT slug, title, image_url, featured, published, display_order
       FROM showcase
       WHERE published = true
       ORDER BY display_order ASC, created_at DESC;
       ```

    4. Append to the audit file under `## Executed statements` (verbatim SQL run) and `## Post-change snapshot` (the SELECT output above).
  </action>
  <verify>
    <automated>grep -q "## Executed statements" .planning/phases/01-showcase-ui-redesign/01-01-data-changes.md &amp;&amp; grep -q "## Post-change snapshot" .planning/phases/01-showcase-ui-redesign/01-01-data-changes.md &amp;&amp; grep -q "jirah-shop" .planning/phases/01-showcase-ui-redesign/01-01-data-changes.md</automated>
  </verify>
  <acceptance_criteria>
    - Post-change SELECT shows exactly 4 published rows
    - Every one of the 4 published rows has a non-null `image_url` matching the pattern `/images/showcase/{ink37-tattoos|tenantflow|revops-portfolio|jirah-shop}.jpg`
    - The jirah-shop row appears first in the result set (lowest display_order)
    - The jirah-shop row has `featured = true`, `published = true`, `showcase_type = 'detailed'`
    - Pre-existing rows' `title`, `description`, `technologies`, `metrics`, `slug`, `featured`, `display_order` are unchanged (compare to Task 2 snapshot — only `image_url` and `updated_at` should differ)
    - Audit file shows both the executed SQL and the verification SELECT output
  </acceptance_criteria>
  <done>Production Neon has 4 image-backed published showcase rows with jirah-shop featured and first in sort order; the change is reproducible from the audit file.</done>
</task>

</tasks>

<verification>
- All 3 tasks complete
- Audit file `.planning/phases/01-showcase-ui-redesign/01-01-data-changes.md` exists and contains all 4 sections (snapshot, planned, executed, post-change)
- Post-change SELECT in the audit file shows 4 rows, all with image_url set, jirah-shop first
- No code files modified (this is a data-only plan)
</verification>

<success_criteria>
- `/showcase` page, when next rendered against this DB, will receive 4 ShowcaseItem rows from `getShowcaseItems()`, each with a populated `imageUrl`, with the jirah-shop row appearing first by `displayOrder`
- The audit file lets the change be reapplied to another Neon branch by reading the file top-to-bottom
</success_criteria>

<output>
After completion, create `.planning/phases/01-showcase-ui-redesign/01-01-SUMMARY.md` capturing:
- Live slugs of the 3 existing rows (resolved during Task 2)
- The exact displayOrder assigned to jirah-shop
- Total published row count after the change
- Pointer to the audit file
</output>

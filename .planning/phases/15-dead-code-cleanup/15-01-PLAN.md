---
phase: 15-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/notifications.ts
  - src/lib/help-articles.ts
  - src/lib/logger.ts
  - src/types/logger.ts
  - src/emails/contact-welcome.tsx
  - src/lib/ttl-calculator/calculator.ts
  - src/types/ttl-types.ts
  - src/app/(public)/actions/ttl-calculator.tsx
  - src/components/calculators/Calculator.tsx
  - tests/unit/calculator-store.test.ts
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "The dangling 'Test notification endpoints' JSDoc no longer exists in notifications.ts"
    - "HelpArticle no longer declares or maps order_index (no backing DB column)"
    - "logger group/groupEnd/table no-ops are gone from BOTH the impl and the Logger interface; the class still implements Logger (no TS2420)"
    - "contact-welcome PARAGRAPH_STYLE.whiteSpace is retained, with a comment marking it intentional inert-but-defensive styling"
    - "processingFees is fully removed from the TTLResults contract and every consumer site, including the unit test assertion"
    - "Zero user-visible behavior change: lint, typecheck, unit tests, and build all pass"
  artifacts:
    - path: "src/lib/notifications.ts"
      provides: "Notification helpers with no trailing dead JSDoc"
      contains: "notifyNewLead"
    - path: "src/lib/help-articles.ts"
      provides: "HelpArticle interface + mapper without order_index"
      contains: "interface HelpArticle"
    - path: "src/lib/logger.ts"
      provides: "BaseLogger without group/groupEnd/table no-ops"
      contains: "class BaseLogger"
    - path: "src/types/logger.ts"
      provides: "Logger interface without group/groupEnd/table members"
      contains: "interface Logger"
    - path: "src/emails/contact-welcome.tsx"
      provides: "Retained whiteSpace with intentional-defensive comment"
      contains: "whiteSpace: 'pre-wrap'"
    - path: "src/types/ttl-types.ts"
      provides: "TTLResults without processingFees"
      contains: "interface TTLResults"
  key_links:
    - from: "src/lib/logger.ts (class BaseLogger)"
      to: "src/types/logger.ts (interface Logger)"
      via: "implements contract"
      pattern: "implements Logger"
    - from: "src/components/calculators/Calculator.tsx"
      to: "src/types/ttl-types.ts (TTLResults)"
      via: "ttl: TTLResults state + comparison copy"
      pattern: "ttl: TTLResults"
---

<objective>
Remove genuinely dead/dangling code surfaced by the v6 audit (CLEAN-01, CLEAN-02, CLEAN-03) and clarify the one intentionally-retained item so it stops reappearing in future audits. Pure subtraction plus a single comment tightening. No behavior change.

Purpose: The site's code should do exactly what its names and signatures promise. A dangling JSDoc stub, a phantom field with no backing column, three unused logger no-ops, and a never-rendered always-zero result field are all dead surface that misleads future readers and audits.

Output: Ten files edited (eight pure-deletion sites + one comment tightening + one test assertion drop). All gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dead-code-cleanup/15-CONTEXT.md
@.planning/phases/15-dead-code-cleanup/15-RESEARCH.md
@.planning/phases/15-dead-code-cleanup/15-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Verified line numbers and identifiers extracted from the codebase. -->
<!-- Executor edits these exact sites; no codebase exploration needed. -->

CLEAN-01 — src/lib/notifications.ts (last lines, file ends at 391):
  Lines 388-391 are a dangling JSDoc block with NO function under it:
    /**
     * Test notification endpoints
     */
  notifyNewLead ends at line 386. There is no symbol after the comment.

CLEAN-02 — src/lib/help-articles.ts:
  Line 23: `order_index: number` inside the exported `HelpArticle` interface.
  Line 88: `order_index: 0` inside `mapHelpArticle`.
  The `help_articles` pgTable (src/lib/schemas/content.ts) has NO order_index column;
  the Drizzle row type does not carry it. Zero readers repo-wide.

CLEAN-03a — TWO FILES (the TS2420 trap):
  src/lib/logger.ts lines 321-333 (on `class BaseLogger`):
    group(_label: string): void { /* No-op */ }
    groupEnd(): void { /* No-op */ }
    table(_data: unknown): void { /* No-op */ }
  src/types/logger.ts lines 71-76 (on `interface Logger`):
    // Grouping (for development)
    group(label: string): void
    groupEnd(): void
    // Table display (for development)
    table(data: unknown): void
  `class BaseLogger implements Logger` and `interface ServerLogger extends Logger`.
  Deleting ONLY the bodies => TS2420 "incorrectly implements interface 'Logger'".
  Both files MUST change together.

CLEAN-03b — src/emails/contact-welcome.tsx:
  PARAGRAPH_STYLE (lines 25-31) sets `whiteSpace: 'pre-wrap' as const`; applied on
  the <Text> at line 47. Existing comment lines 19-24 already explains it is inert
  today. KEEP the property. Tighten the comment to label it intentional
  defensive styling (currently inert) so future audits do not re-flag it.

CLEAN-03c — processingFees chain (6 occurrences, 5 files; all ttl is typed `TTLResults`):
  src/lib/ttl-calculator/calculator.ts:99  `const processingFees = 0`
  src/lib/ttl-calculator/calculator.ts:114 `processingFees,` in the returned object
  src/types/ttl-types.ts:6                 `processingFees: number` (required field on TTLResults)
  src/app/(public)/actions/ttl-calculator.tsx:40  `processingFees: z.number().optional(),`
  src/components/calculators/Calculator.tsx:146    `processingFees: calculationResults?.ttlResults?.processingFees ?? 0,`
  tests/unit/calculator-store.test.ts:52   `processingFees: 50,` (in a TTLResults literal, asserted via toEqual)
  Consumer-component typing: Calculator.tsx:38, ResultsPanel.tsx:36, ComparisonView.tsx:15
  all type the comparison `ttl` as `TTLResults` (NOT an inline shape listing processingFees),
  and neither ResultsPanel nor ComparisonView renders a processing-fee row. Dropping the
  field from TTLResults + the assignment is clean; do NOT add a UI assertion for it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure deletions (CLEAN-01, CLEAN-02, CLEAN-03a two-file) + CLEAN-03b comment tightening</name>
  <files>src/lib/notifications.ts, src/lib/help-articles.ts, src/lib/logger.ts, src/types/logger.ts, src/emails/contact-welcome.tsx</files>
  <read_first>
    - src/lib/notifications.ts (lines 380-391, the dangling JSDoc at EOF)
    - src/lib/help-articles.ts (lines 16-27 interface, 81-92 mapper)
    - src/lib/logger.ts (lines 320-334, the three no-op bodies on BaseLogger)
    - src/types/logger.ts (lines 65-77, the Logger interface members)
    - src/emails/contact-welcome.tsx (lines 17-50, PARAGRAPH_STYLE + its comment + usage)
    - .planning/phases/15-dead-code-cleanup/15-CONTEXT.md (LOCKED dispositions)
    - .planning/phases/15-dead-code-cleanup/15-RESEARCH.md (per-item safety verdicts + the TS2420 trap)
  </read_first>
  <action>
    CLEAN-01: In src/lib/notifications.ts, delete the trailing dangling JSDoc block (`/** Test notification endpoints */`, the file's last 4 lines). The file must end cleanly with the closing brace of notifyNewLead plus a single trailing newline. Do NOT implement a phantom sendTestNotification (YAGNI per CONTEXT D CLEAN-01).

    CLEAN-02: In src/lib/help-articles.ts, delete `order_index: number` from the `HelpArticle` interface and delete `order_index: 0` from the `mapHelpArticle` return object. There is no `order_index` column on the `help_articles` table and zero readers; this is a fabricated permanently-constant field (per CLEAN-02).

    CLEAN-03a (TWO FILES, do both in this task to avoid TS2420): In src/lib/logger.ts, delete the three no-op method bodies on `BaseLogger` — `group`, `groupEnd`, `table` — along with their leading blank line. In src/types/logger.ts, delete the matching three signatures plus their two `// Grouping` / `// Table display` comment lines from the `Logger` interface. Because `class BaseLogger implements Logger`, removing only the bodies would trigger TS2420; both files must change together (per CLEAN-03a). Do NOT introduce `any`; leave the remaining typed signatures (`time`, `timeEnd`, `clearContext`, etc.) untouched.

    CLEAN-03b (KEEP + clarify, no removal): In src/emails/contact-welcome.tsx, do NOT remove `whiteSpace: 'pre-wrap'` from PARAGRAPH_STYLE. Tighten the existing comment (lines 19-24) so it reads as intentional defensive styling that is currently inert — state plainly that it preserves intra-paragraph soft line breaks for future templates, is a no-op today because templates separate paragraphs with a double newline, and must not be removed. Keep the comment dash-free (no em/en-dash), no emoji.
  </action>
  <verify>
    <automated>! grep -n "Test notification endpoints" src/lib/notifications.ts && ! grep -n "order_index" src/lib/help-articles.ts && ! grep -nE "^\s*(group|groupEnd|table)\b" src/lib/logger.ts && ! grep -nE "^\s*(group|groupEnd|table)\(" src/types/logger.ts && grep -n "whiteSpace: 'pre-wrap'" src/emails/contact-welcome.tsx && bun run typecheck</automated>
  </verify>
  <done>
    The dangling JSDoc is gone from notifications.ts; `order_index` appears nowhere in help-articles.ts; the three logger no-op methods are gone from BOTH logger.ts and the Logger interface in types/logger.ts; `bun run typecheck` is green (proves no TS2420); `whiteSpace: 'pre-wrap'` is still present in contact-welcome.tsx with a tightened intentional-defensive comment.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove vestigial processingFees across the 5 sites (CLEAN-03c)</name>
  <files>src/lib/ttl-calculator/calculator.ts, src/types/ttl-types.ts, src/app/(public)/actions/ttl-calculator.tsx, src/components/calculators/Calculator.tsx, tests/unit/calculator-store.test.ts</files>
  <read_first>
    - src/lib/ttl-calculator/calculator.ts (lines 90-119, the const + returned field)
    - src/types/ttl-types.ts (lines 1-10, the TTLResults interface)
    - src/app/(public)/actions/ttl-calculator.tsx (lines 35-44, the ttlResults Zod schema)
    - src/components/calculators/Calculator.tsx (lines 35-42 state typing, 137-150 comparison copy)
    - src/components/calculators/ResultsPanel.tsx (lines 30-40, ttl typed as TTLResults, no processing-fee row)
    - src/components/calculators/ComparisonView.tsx (lines 12-22, ttl typed as TTLResults, no processing-fee row)
    - tests/unit/calculator-store.test.ts (lines 44-63, the TTLResults literal asserted via toEqual)
    - .planning/phases/15-dead-code-cleanup/15-RESEARCH.md (CLEAN-03c verdict: read-but-never-rendered; removal clean if the one test is updated)
  </read_first>
  <action>
    Remove the vestigial always-0, never-rendered `processingFees` field at all 6 occurrences across these 5 files (research-confirmed read-but-never-rendered; the real fee is already folded into registrationFees via PROCESSING_FEE in calculateRegistrationFee, so this carries no value):

    1. src/lib/ttl-calculator/calculator.ts: delete `const processingFees = 0` (and its `// Processing fees ...` comment line) and remove `processingFees,` from the returned TTLResults object.
    2. src/types/ttl-types.ts: drop `processingFees: number` from the `TTLResults` interface.
    3. src/app/(public)/actions/ttl-calculator.tsx: drop the `processingFees: z.number().optional(),` line from the `ttlResults` Zod object.
    4. src/components/calculators/Calculator.tsx: drop the `processingFees: ... ?? 0,` copy line (~line 146) inside the comparison-vehicle `ttl` object. The comparison state is typed `ttl: TTLResults`; once the field leaves the interface this line must go or TS flags an excess property.
    5. tests/unit/calculator-store.test.ts: drop the `processingFees: 50,` line from the `TTLResults` literal so the `toEqual` assertion still matches the new contract.

    Do NOT touch ResultsPanel.tsx or ComparisonView.tsx — they type `ttl: TTLResults` and render no processing-fee row, so they update transitively with the interface and need no edit. Do NOT add any UI/DOM assertion for a processing-fee label (it was never rendered; per RESEARCH Pitfall 2). Removal has zero user-visible effect.
  </action>
  <verify>
    <automated>! grep -rn "processingFees" src/ tests/ e2e/ && bun run typecheck && bun test tests/unit/calculator-store.test.ts</automated>
  </verify>
  <done>
    `processingFees` appears nowhere in src/, tests/, or e2e/; `bun run typecheck` is green (no dangling reference, no excess-property error); the calculator-store unit test passes against the trimmed TTLResults contract.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full phase gate</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/15-dead-code-cleanup/15-VALIDATION.md (gate chain + per-requirement verification map)
    - .planning/phases/15-dead-code-cleanup/15-CONTEXT.md (Specifics: gates)
  </read_first>
  <action>
    Run the full phase gate chain and confirm no net-new failures. The documented pre-existing baseline is 21 failures in tests/unit/homepage.test.tsx + tests/unit/navigation.test.tsx (cross-file RTL pollution; they pass in isolation, 0 net-new — see STATE.md "What just happened"). This phase touches none of those files, so the failure count must not increase. If any NEW failure appears that references a symbol this phase removed (notifications JSDoc, order_index, a logger no-op, or processingFees), fix it before considering the gate passed. Do NOT use GNU `timeout` (unavailable on this macOS shell per RESEARCH Environment Availability); rely on the Bash tool's own timeout.
  </action>
  <verify>
    <automated>bun run lint && bun run typecheck && bun run test:unit && bun run build</automated>
  </verify>
  <done>
    `bun run lint`, `bun run typecheck`, and `bun run build` all pass clean; `bun run test:unit` shows no net-new failures beyond the documented pre-existing homepage/navigation pollution (21), and no failure references any symbol removed by this phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced or removed. This phase only deletes dead code and tightens one developer comment; no input parsing, no network surface, no auth path, no data flow changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | TS interface/impl coupling (logger.ts <-> types/logger.ts) | mitigate | Edit BOTH files in Task 1; `bun run typecheck` gate proves `BaseLogger implements Logger` still holds (no TS2420). Not a security threat, tracked as a build-integrity risk. |

No new threat surface: this phase is pure deletion plus one documentation comment, with no behavior change. No package installs (no `T-15-SC` supply-chain checkpoint needed — RESEARCH Package Legitimacy Audit: not applicable).
</threat_model>

<verification>
- `! grep -n "Test notification endpoints" src/lib/notifications.ts` (CLEAN-01)
- `! grep -n "order_index" src/lib/help-articles.ts` (CLEAN-02)
- logger no-ops gone from BOTH src/lib/logger.ts AND src/types/logger.ts; `bun run typecheck` green proves no TS2420 (CLEAN-03a)
- `whiteSpace: 'pre-wrap'` still present in src/emails/contact-welcome.tsx with a tightened intentional-defensive comment (CLEAN-03b)
- `! grep -rn "processingFees" src/ tests/ e2e/` + `bun run typecheck` + calculator-store test passes (CLEAN-03c)
- Full gate: `bun run lint && bun run typecheck && bun run test:unit && bun run build` all green, no net-new test failures
</verification>

<success_criteria>
- All three dead symbols (dangling JSDoc, phantom order_index, three logger no-ops) are gone and the build stays green.
- `whiteSpace` is retained and documented as intentional defensive styling.
- `processingFees` is fully removed from the TTLResults contract and every consumer, including the unit test assertion.
- Zero user-visible behavior change; lint, typecheck, unit tests (no net-new failures), and build all pass.
- CLEAN-01, CLEAN-02, CLEAN-03 satisfied.
</success_criteria>

<output>
Create `.planning/phases/15-dead-code-cleanup/15-01-SUMMARY.md` when done
</output>

---
phase: 11-paystub-tax-accuracy
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - src/lib/paystub-calculator/states-utils.ts
  - src/lib/paystub-calculator/validation.ts
  - src/lib/paystub-calculator/state-tax-calculations.ts
  - tests/state-tax-calculations.test.ts
  - tests/paystub-validation.test.ts
  - tests/paystub-federal-tax.test.ts
autonomous: true
requirements: [PAYSTUB-01, PAYSTUB-03, PAYSTUB-05, PAYSTUB-06, PAYSTUB-07, PAYSTUB-08]

must_haves:
  truths:
    - "getIncomeTaxStates() returns exactly CA, NY, IL, PA, MA derived from getSupportedIncomeTaxStateCodes(), not from a minus-no-tax allow-list"
    - "Every selectable income-tax state has bracket data and vice versa (bidirectional parity)"
    - "validatePaystubInputs rejects a tax year not present in the data (e.g. 2024) and accepts 2025, via getSupportedTaxYears() membership (not the hardcoded 2020..currentYear+5 range)"
    - "The state 2-letter length check is retained; calculateStateTax keeps its defensive return 0, re-commented as a fallback for UI-unreachable input"
    - "Federal single golden case (gross 50000, single, 2025) computes 5914.00; SS wage base caps contributions at 176100; a >1M CA & MA case applies the surtax; an NY high-income case hits 10.9%"
    - "MA math-lock assertions are recomputed for flat 0.05 (no 0.0535 expected values remain)"
  artifacts:
    - path: "src/lib/paystub-calculator/states-utils.ts"
      provides: "getIncomeTaxStates() derived from getSupportedIncomeTaxStateCodes() intersected with states.json labels"
      contains: "getSupportedIncomeTaxStateCodes"
    - path: "src/lib/paystub-calculator/validation.ts"
      provides: "taxYear membership check against getSupportedTaxYears()"
      contains: "getSupportedTaxYears"
    - path: "tests/state-tax-calculations.test.ts"
      provides: "Bidirectional parity + exact-set + corrected 2025 surtax/MA value tests; re-documented defensive cases"
      contains: "parity"
    - path: "tests/paystub-federal-tax.test.ts"
      provides: "Federal 2025 golden number + SS base 176100 cap test + getSupportedTaxYears()==[2025] + documented fallback"
      contains: "getSupportedTaxYears"
    - path: "tests/paystub-validation.test.ts"
      provides: "Rejected-year (2024) + accepted-year (2025) cases for PAYSTUB-03"
      contains: "taxYear"
  key_links:
    - from: "src/lib/paystub-calculator/states-utils.ts"
      to: "src/lib/paystub-calculator/state-tax-data.ts"
      via: "import getSupportedIncomeTaxStateCodes"
      pattern: "getSupportedIncomeTaxStateCodes"
    - from: "src/lib/paystub-calculator/validation.ts"
      to: "src/lib/paystub-calculator/tax-data.ts"
      via: "import getSupportedTaxYears"
      pattern: "getSupportedTaxYears"
    - from: "tests/state-tax-calculations.test.ts"
      to: "src/lib/paystub-calculator/states-utils.ts"
      via: "import getIncomeTaxStates for parity test"
      pattern: "getIncomeTaxStates"
---

<objective>
Wire the data-derived helpers from Wave 1 into the income-tax state list and the year validator, re-comment the defensive state-tax guard, and write/recompute the full unit-test matrix that locks the 2025 corrections.

Purpose: This plan closes the two drift bugs in code (independent state allow-list; hardcoded year range) by deriving from the Wave 1 single sources of truth (`getSupportedIncomeTaxStateCodes`, `getSupportedTaxYears`), and it owns ALL test files so the golden numbers are computed once against the final 2025 data. After Wave 1 keyed the data to 2025 and changed MA to 0.05 + SS base to 176100, the pre-existing MA and federal assertions are stale; this plan recomputes them and adds the new 2025 value-assertion tests (federal single 11925 ceiling, SS cap 176100, CA & MA >$1M surtax, NY 10.9%) plus the bidirectional parity test and the rejected/accepted-year tests.

Output: data-derived `states-utils.ts`, data-derived year validation in `validation.ts`, a re-commented defensive guard in `state-tax-calculations.ts`, and three test files (`state-tax-calculations.test.ts`, `paystub-validation.test.ts`, new `paystub-federal-tax.test.ts`) all green against the 2025 data. This plan depends on 11-01 (federal: `getSupportedTaxYears`, SS base 176100, federal calc fn) and 11-02 (state: `getSupportedIncomeTaxStateCodes`, surtax bands, MA 0.05). It owns no file in `files_modified` of 11-01/11-02, so there is no within-wave overlap; it runs in Wave 2 after both Wave 1 plans complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md
@.planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md
@.planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md
@.planning/phases/11-paystub-tax-accuracy/11-VALIDATION.md
@.planning/phases/11-paystub-tax-accuracy/11-01-SUMMARY.md
@.planning/phases/11-paystub-tax-accuracy/11-02-SUMMARY.md

<interfaces>
<!-- Verified contracts the executor needs. Extracted from the codebase 2026-06-02 + the Wave 1 SUMMARYs. -->

From src/lib/paystub-calculator/tax-data.ts (11-01):
- `getSupportedTaxYears(): number[]` returns data-backed years sorted descending (after 11-01: [2025])
- SS wage base is 176100; federal brackets are official 2025

From src/lib/paystub-calculator/tax-calculations.ts (NOT modified):
- `calculateFederalTax(grossPay, filingStatus, ytdGross, year?)` - NOTE arg order: gross, filingStatus, ytdGross. Walks 2025 federal brackets against cumulative `ytdGross + grossPay`.
- `calculateSocialSecurity(grossPay, ytdGross, year?)` - caps contributions at the SS wage base (176100 after 11-01).

From src/lib/paystub-calculator/state-tax-data.ts (11-02):
- `getSupportedIncomeTaxStateCodes(): string[]` returns CA, NY, IL, PA, MA (unioned from stateTaxDataByYear keys)
- `calculateStateTax(grossPay, ytdGross, state, filingStatus, year?)` (state-tax-calculations.ts) walks the 2025 state brackets the same incremental way; CA MHS surtax (over 1000000 -> 13.3%) and MA 4% surtax (over 1083150 -> 9%) are encoded as top brackets.

From src/lib/paystub-calculator/states-utils.ts (current):
- `const NO_INCOME_TAX_CODES = ['AK', 'FL', 'NV', 'SD', 'TN', 'TX', 'WA', 'WY']` (line 5) UNCHANGED
- `const NO_INCOME_TAX_STATES_CACHE = statesData.states.filter(state => NO_INCOME_TAX_CODES.includes(state.value))` (lines 6-8) the filter-against-a-code-set shape to mirror
- `const INCOME_TAX_STATES_CACHE = statesData.states.filter(state => !NO_INCOME_TAX_CODES.includes(state.value))` (lines 9-11) REPLACE this derivation only
- `getNoIncomeTaxStates()` (lines 16-18) UNCHANGED; `getIncomeTaxStates()` (lines 23-25) returns the income cache
- `statesData.states` shape: `{ value: string; label: string }`

From src/lib/paystub-calculator/validation.ts (current):
- `validatePaystubInputs(params): ValidationResult` (line 8); hardcoded year range lines 45-49 (`currentYear` + `< 2020 || > currentYear + 5`) REPLACE
- membership-check analog already present: `validFrequencies.includes(...)` (lines 51-60), `validFilingStatuses.includes(...)` (lines 62-72); state 2-letter length check (lines 74-77) KEEP

From src/lib/paystub-calculator/state-tax-calculations.ts (current):
- defensive guard lines 11-14: `if (!stateBrackets) { return 0 // Unknown state -> assume no income tax }` KEEP behavior, change ONLY the comment; bracket loop lines 16-62 DO NOT TOUCH

From tests (current):
- tests/state-tax-calculations.test.ts: imports `calculateStateTax` (line 2); math-lock assertions IL 247.5 (line 13), PA 122.8 (line 35), MA 107/267.5 (lines 47/57 at the OLD 0.0535 rate, MUST recompute to 100/250), CA/NY `> 0` (lines 95/100/107); defensive TX/FL cases 61-79; unknown `'XX'` 82-89; second TX block in `States with no income tax` 111-116
- tests/paystub-validation.test.ts: `validBase` fixture (lines 18-27) with `taxYear: 2024` (UPDATE the fixture to 2025 so the baseline is valid); assertion-shape analog `throws on invalid pay frequency` (lines 51-71) with the `if (!validBase) { return }` guard
</interfaces>

<golden_values>
<!-- Precomputed against the final 2025 data. Each is hand-verifiable; cite the arithmetic in a comment. -->
- Federal single, gross 50000, ytd 0, 2025: 0.10*11925 + 0.12*(48475-11925) + 0.22*(50000-48475) = 1192.50 + 4386.00 + 335.50 = 5914.00. Call: calculateFederalTax(50000, 'single', 0, 2025) -> 5914.00.
- Federal single 10% ceiling moved: at gross exactly 11925 (ytd 0, single) the whole slice is 10% -> 1192.50 (under the old 11000 ceiling the 925 over would have been 12%); asserting 1192.50 proves the 11925 ceiling.
- SS cap at 176100: calculateSocialSecurity(176100, 0, 2025) = 176100*0.062 = 10918.20 (full base). calculateSocialSecurity(10000, 170000, 2025) = (176100-170000)*0.062 = 6100*0.062 = 378.20 (proves cap is 176100, not 168600; under the old base this would have been 0).
- MA flat below threshold: calculateStateTax(2000, 10000, 'MA', 'single') = 2000*0.05 = 100. calculateStateTax(5000, 80000, 'MA', 'single') = 5000*0.05 = 250.
- MA 4% surtax over 1083150: calculateStateTax(100000, 1000000, 'MA', 'single'): cumulative 1000000->1100000; 83150 at 5% (to 1083150) + 16850 at 9% (over) = 4157.50 + 1516.50 = 5674.00.
- CA MHS surtax over 1000000: calculateStateTax(50000, 1000000, 'CA', 'single'): cumulative 1000000->1050000, all over 1000000 -> 0.133 -> 50000*0.133 = 6650.00.
- NY 10.9% top bracket: calculateStateTax(1000000, 25000000, 'NY', 'single'): cumulative 25000000->26000000, all over 25000000 -> 0.109 -> 1000000*0.109 = 109000.00.
</golden_values>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive getIncomeTaxStates() from the data + data-derived year validation + re-comment defensive guard (PAYSTUB-01, PAYSTUB-03)</name>
  <files>src/lib/paystub-calculator/states-utils.ts, src/lib/paystub-calculator/validation.ts, src/lib/paystub-calculator/state-tax-calculations.ts</files>
  <read_first>
    - src/lib/paystub-calculator/states-utils.ts (file under edit; NO_INCOME_TAX filter shape lines 5-18, income cache lines 9-11)
    - src/lib/paystub-calculator/validation.ts (file under edit; hardcoded range lines 45-49; membership analog 51-72; state check 74-77)
    - src/lib/paystub-calculator/state-tax-calculations.ts (file under edit; defensive guard lines 11-14)
    - .planning/phases/11-paystub-tax-accuracy/11-02-SUMMARY.md (confirms getSupportedIncomeTaxStateCodes export name)
    - .planning/phases/11-paystub-tax-accuracy/11-01-SUMMARY.md (confirms getSupportedTaxYears returns [2025])
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (PAYSTUB-01 derive list lines 26-30; PAYSTUB-03 derive-from-data year validation lines 37-40; test contract on defensive return 0 lines 59-60)
    - .planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md (states-utils target lines 87-115; membership-check idiom lines 159-192; re-comment lines 196-208)
  </read_first>
  <behavior>
    - getIncomeTaxStates().map(s => s.value).sort() === ['CA','IL','MA','NY','PA']; getNoIncomeTaxStates() returns the 8 NO_INCOME_TAX_CODES states unchanged.
    - validatePaystubInputs({ ...validBase, taxYear: 2024 }) -> isValid:false, errors.taxYear defined; taxYear 2025 -> isValid:true. Error message lists supported years (e.g. "Tax year must be one of: 2025"), no dash.
    - calculateStateTax(5000, 25000, 'XX', 'single') still returns 0 (behavior unchanged; comment documents it as UI-unreachable defensive fallback).
  </behavior>
  <action>
    Three edits:

    1. states-utils.ts (PAYSTUB-01): add `import { getSupportedIncomeTaxStateCodes } from './state-tax-data'`, build `const SUPPORTED_INCOME_TAX_CODES = new Set(getSupportedIncomeTaxStateCodes())` at module scope, and REPLACE the `INCOME_TAX_STATES_CACHE` derivation (lines 9-11) so it filters `statesData.states` against that Set (mirror the `NO_INCOME_TAX_STATES_CACHE` filter shape). Keep `getIncomeTaxStates()` returning the cache. Leave `NO_INCOME_TAX_CODES` and `getNoIncomeTaxStates()` UNCHANGED (Pitfall 2: the `selectedState || 'TX'` default depends on TX staying a no-tax code).

    2. validation.ts (PAYSTUB-03): add `import { getSupportedTaxYears } from './tax-data'`. REPLACE the hardcoded year block (lines 45-49: the `currentYear` computation and the `< 2020 || > currentYear + 5` check) with `const supportedYears = getSupportedTaxYears()` and `if (!supportedYears.includes(params.taxYear)) { errors.taxYear = \`Tax year must be one of: ${supportedYears.join(', ')}\` }`, mirroring the existing `validFrequencies.includes(...)` idiom. Leave the state 2-letter length check (lines 74-77) intact.

    3. state-tax-calculations.ts: change ONLY the comment on the defensive guard (line 13) from `// Unknown state -> assume no income tax` to a comment documenting it as a defensive fallback for input the UI can no longer produce (CONTEXT.md:60 discretion: keep `return 0`). Do NOT touch the bracket loop or the guard behavior.

    No em-dash/en-dash. No `any`. Tabs, single quotes, no semicolons (Biome).
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/business-website && grep -q 'getSupportedIncomeTaxStateCodes' src/lib/paystub-calculator/states-utils.ts && grep -q 'getSupportedTaxYears' src/lib/paystub-calculator/validation.ts && [ "$(grep -v '^[[:space:]]*//' src/lib/paystub-calculator/validation.ts | grep -c '2020')" -eq 0 ] && grep -q 'return 0' src/lib/paystub-calculator/state-tax-calculations.ts && ! grep -q 'Unknown state -> assume no income tax' src/lib/paystub-calculator/state-tax-calculations.ts && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `getSupportedIncomeTaxStateCodes` imported/used in states-utils.ts; `getSupportedTaxYears` imported/used in validation.ts.
    - Hardcoded range removed: `grep -v '^[[:space:]]*//' src/lib/paystub-calculator/validation.ts | grep -c '2020'` == 0.
    - Defensive guard intact, comment changed: `return 0` present and `Unknown state -> assume no income tax` absent in state-tax-calculations.ts.
    - `bun run typecheck && bun run lint` pass; no U+2014/U+2013 in the three files.
  </acceptance_criteria>
  <done>getIncomeTaxStates() derives from the bracket data; year validation is a membership check against getSupportedTaxYears(); the defensive guard is re-commented (behavior unchanged); typecheck + lint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: State-tax tests - parity, exact-set, recompute MA to 0.05, CA/MA/NY surtax golden values, re-document defensive cases (PAYSTUB-01, PAYSTUB-06/07/08 lock)</name>
  <files>tests/state-tax-calculations.test.ts</files>
  <read_first>
    - tests/state-tax-calculations.test.ts (file under edit; MA assertions at OLD 0.0535 lines 39-59; defensive TX/FL 61-79; XX 82-89; CA/NY > 0 92-109; second TX block 111-116)
    - src/lib/paystub-calculator/state-tax-data.ts (the 2025 values from 11-02; read to confirm exact arrays behind the golden numbers)
    - src/lib/paystub-calculator/state-tax-calculations.ts (read-only; confirm the slice math so expected values are correct)
    - .planning/phases/11-paystub-tax-accuracy/11-02-SUMMARY.md (the exact CA/NY/MA arrays + surtax encodings written)
    - .planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md (proposed parity test + golden examples lines ~642-661; Pitfall 3 test-meaning; Pitfall 5 recompute MA)
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (test contract lines 59-62; value-assertion examples line 52)
  </read_first>
  <behavior>
    - Parity: `[...new Set(getIncomeTaxStates().map(s => s.value))].sort()` equals `[...new Set(getSupportedIncomeTaxStateCodes())].sort()`.
    - Exact set: `getIncomeTaxStates().map(s => s.value).sort()` equals `['CA','IL','MA','NY','PA']`.
    - MA flat: calculateStateTax(2000, 10000, 'MA', 'single') == 100; calculateStateTax(5000, 80000, 'MA', 'single') == 250.
    - MA surtax: calculateStateTax(100000, 1000000, 'MA', 'single') == 5674.00 (83150 at 5% + 16850 at 9%).
    - CA surtax: calculateStateTax(50000, 1000000, 'CA', 'single') == 6650.00 (all over 1000000 at 13.3%).
    - NY 10.9%: calculateStateTax(1000000, 25000000, 'NY', 'single') == 109000.00 (all over 25000000 at 10.9%).
    - Defensive: calculateStateTax(5000, 25000, 'XX', 'single') and the TX/FL cases still return 0.
  </behavior>
  <action>
    Update tests/state-tax-calculations.test.ts (per CONTEXT.md test contract + the precomputed `<golden_values>`):

    1. ADD imports `getIncomeTaxStates` from `@/lib/paystub-calculator/states-utils` and `getSupportedIncomeTaxStateCodes` from `@/lib/paystub-calculator/state-tax-data`.

    2. ADD `describe('PAYSTUB-01: dropdown <-> data parity')` with (a) an `it` asserting deduped sorted `getIncomeTaxStates()` values equal deduped sorted `getSupportedIncomeTaxStateCodes()`; (b) an `it` asserting `getIncomeTaxStates().map(s => s.value).sort()` equals `['CA','IL','MA','NY','PA']`.

    3. RECOMPUTE the two existing MA assertions (lines 47 and 57) from `gross * 0.0535` (107 / 267.5) to `gross * 0.05` (100 / 250) and fix their comments (no dash). Update the describe label / 5.35% comment to reflect flat 5.0%.

    4. ADD `describe('Corrected 2025 surtax boundaries')` with value-assertion `it` cases using the precomputed `<golden_values>`: MA over-1083150 (expect 5674.00 via `toBeCloseTo(5674, 2)`), CA over-1000000 single (expect 6650.00), NY into-10.9% (expect 109000.00). Use concrete numbers via `toBeCloseTo(expected, 2)`, never `> 0`.

    5. RE-DOCUMENT the defensive cases: update comments on the TX case (62-69), FL case (71-78), unknown `'XX'` case (82-89), and the second TX case in `States with no income tax` (111-116) to state they exercise the DEFENSIVE fallback for UI-unreachable input, not user-facing "graceful" behavior. KEEP every assertion. Do NOT change the IL 247.5, PA 122.8, or CA/NY `> 0` math-lock assertions (they stay green).

    No em-dash/en-dash. No `any`.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/business-website && bun test tests/state-tax-calculations.test.ts && [ "$(grep -c '0.0535' tests/state-tax-calculations.test.ts)" -eq 0 ] && grep -q '5674' tests/state-tax-calculations.test.ts && grep -q '6650' tests/state-tax-calculations.test.ts && grep -q '109000' tests/state-tax-calculations.test.ts && bun run lint && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun test tests/state-tax-calculations.test.ts -t "parity"` passes; exact-set test passes.
    - Surtax golden cases pass: `5674`, `6650`, `109000` all present and asserted via `toBeCloseTo`.
    - No stale MA assertion: `grep -c '0.0535' tests/state-tax-calculations.test.ts` == 0.
    - Full file green (IL 247.5, PA 122.8 math-lock still pass).
    - `bun run lint && bun run typecheck` pass; no U+2014/U+2013.
  </acceptance_criteria>
  <done>Parity + exact-set lock PAYSTUB-01; MA assertions recomputed to flat 0.05; new value-assertion tests lock the CA MHS, MA 4%, and NY 10.9% 2025 boundaries; defensive cases re-documented; lint + typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Federal 2025 golden + SS cap 176100 test, getSupportedTaxYears()==[2025], documented fallback; rejected/accepted-year validation tests (PAYSTUB-05, PAYSTUB-02, PAYSTUB-03)</name>
  <files>tests/paystub-federal-tax.test.ts, tests/paystub-validation.test.ts</files>
  <read_first>
    - src/lib/paystub-calculator/tax-data.ts (11-01 values; read to confirm getSupportedTaxYears + getTaxDataForYear)
    - src/lib/paystub-calculator/tax-calculations.ts (read-only; calculateFederalTax(gross, filingStatus, ytdGross, year?) and calculateSocialSecurity(gross, ytdGross, year?) signatures + the SS cap logic)
    - tests/state-tax-calculations.test.ts (read-only; the bun:test describe/it/toBeCloseTo style to mirror)
    - tests/paystub-validation.test.ts (file under edit; validBase fixture 18-27 with taxYear 2024 -> update to 2025; assertion shape 51-71 with the `if (!validBase) { return }` guard)
    - .planning/phases/11-paystub-tax-accuracy/11-01-SUMMARY.md (federal calc fn name + that getSupportedTaxYears returns [2025])
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (PAYSTUB-03 rejected/accepted year test; PAYSTUB-05 value-assertion examples line 52)
    - .planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md (paystub-validation fixture + assertion shape lines 298-330)
  </read_first>
  <behavior>
    - getSupportedTaxYears() returns [2025].
    - calculateFederalTax(50000, 'single', 0, 2025) == 5914.00; calculateFederalTax(11925, 'single', 0, 2025) == 1192.50 (proves the 11925 ceiling, not 11000).
    - calculateSocialSecurity(176100, 0, 2025) == 10918.20; calculateSocialSecurity(10000, 170000, 2025) == 378.20 (proves the 176100 cap; would be 0 under the old 168600 base).
    - getTaxDataForYear(2024) returns the same TaxData (federal single first-bracket limit) as getTaxDataForYear(2025) (documented Math.max fallback, asserted as defense-in-depth, not user-facing).
    - validatePaystubInputs({ ...validBase, taxYear: 2024 }) -> isValid:false, errors.taxYear defined; validatePaystubInputs(validBase with taxYear 2025) -> isValid:true.
  </behavior>
  <action>
    Two test files (per the precomputed `<golden_values>` and the PAYSTUB-03 test contract):

    1. CREATE tests/paystub-federal-tax.test.ts (new bun:test file in `tests/`):
       - Import `getSupportedTaxYears`, `getTaxDataForYear` from `@/lib/paystub-calculator/tax-data`; `calculateFederalTax`, `calculateSocialSecurity` from `@/lib/paystub-calculator/tax-calculations`.
       - `describe('PAYSTUB-02: supported tax years')`: `it` asserting `getSupportedTaxYears()` equals `[2025]`.
       - `describe('PAYSTUB-05: federal 2025 brackets')`: `it` asserting `calculateFederalTax(50000, 'single', 0, 2025)` is `toBeCloseTo(5914, 2)` with a comment showing the arithmetic; `it` asserting `calculateFederalTax(11925, 'single', 0, 2025)` is `toBeCloseTo(1192.5, 2)` (locks the 11925 ceiling).
       - `describe('PAYSTUB-05: SS wage base 2025')`: `it` asserting `calculateSocialSecurity(176100, 0, 2025)` is `toBeCloseTo(10918.2, 2)`; `it` asserting `calculateSocialSecurity(10000, 170000, 2025)` is `toBeCloseTo(378.2, 2)` (locks the 176100 cap).
       - `describe('getTaxDataForYear defensive fallback')`: `it` asserting `getTaxDataForYear(2024)` returns the same federal single first-bracket limit as `getTaxDataForYear(2025)`, with a comment documenting it as the UI-unreachable Math.max defense.

    2. UPDATE tests/paystub-validation.test.ts:
       - Change the `validBase` fixture `taxYear: 2024` to `taxYear: 2025` (lines 18-27) so the baseline is the valid year.
       - ADD `it('rejects a tax year with no backing data')` using `{ ...validBase, taxYear: 2024 }` -> `expect(validation.isValid).toBe(false)` + `expect(validation.errors.taxYear).toBeDefined()`, with the `if (!validBase) { return }` guard.
       - ADD `it('accepts the supported tax year 2025')` -> `expect(validatePaystubInputs(validBase).isValid).toBe(true)`.

    No em-dash/en-dash. No `any`.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/business-website && bun test tests/paystub-federal-tax.test.ts tests/paystub-validation.test.ts && grep -q '5914' tests/paystub-federal-tax.test.ts && grep -q '10918' tests/paystub-federal-tax.test.ts && grep -q '2025' tests/paystub-federal-tax.test.ts && bun run lint && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun test tests/paystub-federal-tax.test.ts tests/paystub-validation.test.ts` passes (supported-years==[2025], federal golden 5914, SS cap 10918.20/378.20, fallback, rejected-2024/accepted-2025).
    - Golden numbers present: `5914`, `10918` in the federal test file.
    - `toBeCloseTo` used (concrete golden assertions, not `> 0`).
    - validBase fixture is 2025; the rejected case uses 2024.
    - `bun run lint && bun run typecheck` pass; no U+2014/U+2013.
  </acceptance_criteria>
  <done>A federal test file locks getSupportedTaxYears()==[2025], the 2025 federal single golden number 5914.00, the SS base 176100 cap, and the documented fallback; the validation test rejects 2024 and accepts 2025; lint + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale nuqs URL -> validatePaystubInputs | A restored `?year=2024`/`?year=2023` reaches `validatePaystubInputs` (invoked in `use-paystub-validation` and inside `calculatePaystubTotals`); nuqs `parseAsInteger` passes it through unchanged, so this validator is the only year gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | `validatePaystubInputs` consuming a restored `?year=` param | mitigate | Replace the hardcoded `2020..currentYear+5` range with a membership check against `getSupportedTaxYears()` (Task 1). An unbacked year (e.g. 2024) now produces a validation error instead of silently flowing to getTaxDataForYear's Math.max fallback. Rejected/accepted-year tests lock it (Task 3). |
| T-11-06 | Information disclosure | `getIncomeTaxStates()` drift / `calculateStateTax` defensive `return 0` presented as a real $0 | mitigate | Derive the income-tax list from `getSupportedIncomeTaxStateCodes()` (Task 1); bidirectional parity test prevents drift (Task 2); the guard is re-commented as UI-unreachable (Task 1). The state-side stale-URL gate (PAYSTUB-10) lands in 11-04. |
| T-11-SC | Tampering | npm/bun installs | accept | No package installs in this phase (RESEARCH "## Package Legitimacy Audit": Not applicable, zero new deps). |

Input-validation / data-correctness scope only. No auth, session, network, crypto, secret, or persistence surface. The state 2-letter length check (V5 control) is retained.
</threat_model>

<verification>
- `bun run test:unit` fully green (this is the Wave-2 merge gate; it confirms the Wave 1 data + the recomputed/added tests all agree).
- `bun run lint && bun run typecheck` no `any`, imports resolve, no dash violations.
</verification>

<success_criteria>
- getIncomeTaxStates() derives exactly CA, NY, IL, PA, MA from the data; bidirectional parity test passes.
- Year validation rejects 2024, accepts 2025, via getSupportedTaxYears() membership; hardcoded range gone; state 2-letter check retained.
- Defensive guard re-commented (behavior unchanged).
- Federal single golden (5914.00), SS cap (176100), CA & MA >$1M surtax, NY 10.9%, and MA flat-0.05 recompute all pass.
- `bun run test:unit` green.
</success_criteria>

<output>
Create `.planning/phases/11-paystub-tax-accuracy/11-03-SUMMARY.md` when done. Record: the states-utils + validation edits, the comment change in state-tax-calculations.ts, the validBase fixture update to 2025, and all new/changed test case names so 11-04 can reference them.
</output>

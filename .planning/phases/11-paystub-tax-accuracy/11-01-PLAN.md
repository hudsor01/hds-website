---
phase: 11-paystub-tax-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/paystub-calculator/tax-data.ts
autonomous: true
requirements: [PAYSTUB-02, PAYSTUB-05]

must_haves:
  truths:
    - "Federal 2025 single brackets equal the official IRS Rev. Proc. 2024-40 values (10% ceiling 11925, 12% to 48475, 22% to 103350, 24% to 197300, 32% to 250525, 35% to 626350, 37% over)"
    - "All five federal schedules (single, marriedJoint, marriedSeparate, headOfHousehold, qualifyingSurvivingSpouse=marriedJoint) hold the official 2025 values"
    - "The Social Security wage base is 176100 (2025, up from 168600); SS rate 0.062, Medicare 0.0145, additional Medicare 0.009 and thresholds 200000/250000/125000 are unchanged"
    - "getSupportedTaxYears() returns [2025] (the only data-backed year after the 2024 entry and the 2025 clone are gone)"
    - "getTaxDataForYear baseline guard references 2025 (a year that still exists post re-key); the Math.max fallback is kept as defense-in-depth"
  artifacts:
    - path: "src/lib/paystub-calculator/tax-data.ts"
      provides: "Official 2025 federal tax data keyed 2025, SS base 176100, 2024 entry + 2025 clone removed, getSupportedTaxYears() helper"
      contains: "getSupportedTaxYears"
  key_links:
    - from: "src/lib/paystub-calculator/tax-data.ts"
      to: "getTaxDataForYear baseline guard"
      via: "taxDataByYear[2025] default guard"
      pattern: "taxDataByYear\\[2025\\]"
---

<objective>
Re-key the federal tax table to OFFICIAL 2025 (IRS Rev. Proc. 2024-40) for all five filing schedules, update the Social Security wage base to the 2025 figure ($176,100), delete the stale 2024 entry and the fake 2025 deep-clone so the single real entry is official 2025 data keyed 2025, move the getTaxDataForYear baseline guard from 2024 to 2025, and expose the supported-year set as a data-derived helper.

Purpose: Every paystub user hits the federal/SS/Medicare calc. The current table holds 2023 brackets mislabeled 2024 plus a deep-clone placeholder keyed 2025, and the SS wage base is the 2024 value. Replacing it with official 2025 values keyed 2025 makes the single real entry correct and current. Deriving the year set (`getSupportedTaxYears()`) is the single source of truth that downstream plans (validation in 11-03, the form dropdown in 11-04) consume so the year surface can never drift again.

Output: `tax-data.ts` with one official-2025 `taxDataByYear[2025]` entry (SS base 176100), the 2024 entry and 2025 clone deleted, the baseline guard at 2025, and an exported `getSupportedTaxYears()`. This plan exclusively owns `tax-data.ts`; 11-02 exclusively owns `state-tax-data.ts`, so 11-01 and 11-02 run in parallel in Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md
@.planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md
@.planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md
@.planning/phases/11-paystub-tax-accuracy/11-VALIDATION.md

<interfaces>
<!-- Verified contracts the executor needs. Extracted from the codebase 2026-06-02. No exploration required. -->

From src/lib/paystub-calculator/tax-data.ts (current):
- `const taxDataByYear: Record<number, TaxData>` (line 4) with a single `2024` entry (lines 5-65) holding 2023-era brackets mislabeled, `ssWageBase: 168600` (line 6).
- federalBrackets schedules inside 2024: single (lines 18-26, STALE), marriedJoint (27-35, STALE), marriedSeparate (36-44, STALE), headOfHousehold (45-53, STALE), qualifyingSurvivingSpouse (54-62, currently a copy of marriedJoint).
- A deep-clone placeholder `taxDataByYear[2025] = JSON.parse(JSON.stringify(taxDataByYear[2024])) as TaxData` (line 68, comment line 67) -> DELETE.
- `getTaxDataForYear(year?)` (lines 70-90): reads `defaultData = taxDataByYear[2024]` (line 75) and `throw new Error('Missing baseline tax data for 2024')` (line 78) -> both reference 2024 and MUST move to 2025. Keep the `Math.max(...availableYears)` fallback (lines 86-89) as defense-in-depth (unreachable from the UI after 11-03 validation rejects unbacked years).

From src/lib/paystub-calculator/tax-calculations.ts (NOT modified; the fan-out consumer):
- `getCurrentTaxData()` -> `getTaxDataForYear()` is called 4x (federal brackets, SS, Medicare, additional-Medicare threshold). Editing the federal brackets + SS base changes federal/SS output; Medicare values are unchanged so those outputs must not move.

From src/types/paystub.ts:
- `TaxData['federalBrackets']` keys: `single`, `marriedJoint`, `marriedSeparate`, `headOfHousehold`, `qualifyingSurvivingSpouse`. Each is an ascending array of `{ limit: number, rate: number }` terminating in `{ limit: Infinity }`.
</interfaces>

<official_2025_values>
<!-- TRANSCRIBE exactly from 11-RESEARCH.md "## Official 2025 Tables (TARGET YEAR)" -> "### Federal income tax brackets 2025" and "### FICA / Medicare 2025". Ascending upper limits; final band is { limit: Infinity, rate: 0.37 }. Do NOT use the 2024 reference section. -->

- single: 10% to 11925; 12% to 48475; 22% to 103350; 24% to 197300; 32% to 250525; 35% to 626350; 37% over (Infinity).
- marriedJoint (and qualifyingSurvivingSpouse, identical): 10% to 23850; 12% to 96950; 22% to 206700; 24% to 394600; 32% to 501050; 35% to 751600; 37% over (Infinity).
- marriedSeparate: 10% to 11925; 12% to 48475; 22% to 103350; 24% to 197300; 32% to 250525; 35% to 375800; 37% over (Infinity). (Same first six limits as single; ONLY the 35% ceiling differs: 375800 vs single 626350. RESEARCH Pitfall 7.)
- headOfHousehold: 10% to 17000; 12% to 64850; 22% to 103350; 24% to 197300; 32% to 250500; 35% to 626350; 37% over (Infinity). (Note HoH 32% ceiling is 250500, NOT 250525.)

Federal payroll constants 2025: ssWageBase 176100 (CHANGED from 168600); ssRate 0.062 (unchanged); medicareRate 0.0145 (unchanged); additionalMedicareRate 0.009 (unchanged); additionalMedicareThreshold single 200000 / marriedJoint 250000 / marriedSeparate 125000 / headOfHousehold 200000 / qualifyingSurvivingSpouse 250000 (statutory, unchanged).
</official_2025_values>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-key federal table to official 2025, set SS base 176100, delete the 2024 entry + 2025 clone, move the baseline guard to 2025 (PAYSTUB-05, PAYSTUB-02 source)</name>
  <files>src/lib/paystub-calculator/tax-data.ts</files>
  <read_first>
    - src/lib/paystub-calculator/tax-data.ts (file under edit; 2024 entry lines 5-65, federal schedules 18-62, SS/Medicare 6-16, 2025 clone 67-68, getTaxDataForYear 70-90)
    - .planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md "## Official 2025 Tables (TARGET YEAR)" -> "### Federal income tax brackets 2025" (all four status tables) and "### FICA / Medicare 2025" (SS base 176100). AUTHORITATIVE numeric source; do NOT transcribe from memory or from the 2024 reference section.
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (PAYSTUB-05 federal + SS base 176100, line 46; PAYSTUB-02 re-key to 2025 + delete 2024 entry and 2025 clone, lines 32-36; baseline-guard-to-2025 note)
    - .planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md (tax-data target shape: keep getTaxDataForYear fallback, lines 119-155)
  </read_first>
  <behavior>
    - federalBrackets.single 10% ceiling is 11925 (was 11000); 35% ceiling 626350; 37% over 626350.
    - federalBrackets.marriedJoint 10% ceiling 23850; 35% ceiling 751600. qualifyingSurvivingSpouse identical to marriedJoint.
    - federalBrackets.marriedSeparate 10% ceiling 11925; 35% ceiling 375800; 37% over 375800.
    - federalBrackets.headOfHousehold 10% ceiling 17000; 12% to 64850; 32% ceiling 250500; 35% ceiling 626350.
    - ssWageBase 176100; ssRate 0.062, medicareRate 0.0145, additionalMedicareRate 0.009, thresholds 200000/250000/125000 unchanged.
    - The only year key in taxDataByYear is 2025; getTaxDataForYear's baseline guard reads taxDataByYear[2025].
  </behavior>
  <action>
    Edit tax-data.ts per the transcribed official 2025 values in `<official_2025_values>`:

    1. Re-key the entry: change the `2024:` key to `2025:` and REPLACE all five federal schedule arrays with the official 2025 upper limits transcribed in `<official_2025_values>`: single, marriedJoint, marriedSeparate (35% ceiling 375800, distinct from single per Pitfall 7), headOfHousehold (32% ceiling 250500), and qualifyingSurvivingSpouse (identical to marriedJoint). Each array ascends in limit and terminates in `{ limit: Infinity, rate: 0.37 }`.

    2. Set `ssWageBase: 176100` (PAYSTUB-05; was 168600). Leave `ssRate: 0.062`, `medicareRate: 0.0145`, `additionalMedicareRate: 0.009`, and the entire `additionalMedicareThreshold` block UNCHANGED (single 200000, marriedJoint 250000, marriedSeparate 125000, headOfHousehold 200000, qualifyingSurvivingSpouse 250000 - statutory, not indexed).

    3. DELETE the 2025 clone: comment line 67 and `taxDataByYear[2025] = JSON.parse(JSON.stringify(taxDataByYear[2024])) as TaxData` line 68. After the re-key in step 1 the real 2025 entry replaces it; there is no longer any 2024 entry to clone from.

    4. In `getTaxDataForYear`, change `const defaultData = taxDataByYear[2024]` to `taxDataByYear[2025]` and the throw message `'Missing baseline tax data for 2024'` to reference 2025 (CONTEXT.md: confirm/update the baseline guard to a year that exists post re-key). KEEP the `Math.max(...availableYears)` fallback (lines 86-89) intact; add/keep a comment noting it is defense-in-depth, unreachable from the UI once 11-03 validation rejects unbacked years.

    No `any`. Tabs, single quotes, no semicolons (Biome).
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/business-website && grep -q 'ssWageBase: 176100' src/lib/paystub-calculator/tax-data.ts && grep -q 'limit: 11925' src/lib/paystub-calculator/tax-data.ts && grep -q 'limit: 626350' src/lib/paystub-calculator/tax-data.ts && grep -q 'limit: 375800' src/lib/paystub-calculator/tax-data.ts && grep -q 'limit: 751600' src/lib/paystub-calculator/tax-data.ts && grep -q 'taxDataByYear\[2025\]' src/lib/paystub-calculator/tax-data.ts && ! grep -q 'JSON.parse(JSON.stringify' src/lib/paystub-calculator/tax-data.ts && [ "$(grep -v '^[[:space:]]*//' src/lib/paystub-calculator/tax-data.ts | grep -c '2024')" -eq 0 ] && [ "$(grep -v '^[[:space:]]*//' src/lib/paystub-calculator/tax-data.ts | grep -c '168600')" -eq 0 ] && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ssWageBase: 176100` present; `168600` absent from non-comment lines.
    - Single 10% ceiling `limit: 11925`; single 35% ceiling `limit: 626350`; MFS 35% ceiling `limit: 375800` (proves MFS edited, not left as a single copy); MFJ 35% ceiling `limit: 751600`.
    - `grep -v '^[[:space:]]*//' ... | grep -c '2024'` == 0 (no 2024 key, no 2024 baseline reference, no 2024 throw message; comments excluded by the filter so header prose cannot self-invalidate the gate).
    - Deep-clone line gone (`JSON.parse(JSON.stringify` absent).
    - `taxDataByYear[2025]` referenced by the baseline guard.
    - `bun run typecheck` passes.
  </acceptance_criteria>
  <done>tax-data.ts holds exactly one official 2025 federal entry keyed 2025 for all five schedules (QSS = MFJ), SS base 176100, Medicare untouched, baseline guard at 2025, clone deleted, typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export getSupportedTaxYears() derived from the data table (PAYSTUB-02 source)</name>
  <files>src/lib/paystub-calculator/tax-data.ts</files>
  <read_first>
    - src/lib/paystub-calculator/tax-data.ts (line 73 already uses `Object.keys(taxDataByYear).map(Number)`)
    - .planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md "### `tax-data.ts`" Pattern 2 derivation idiom (lines 129-136)
    - .planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md "### Pattern 2: Derive valid year set" (getSupportedTaxYears example)
  </read_first>
  <behavior>
    - getSupportedTaxYears() returns exactly [2025] after the Task 1 re-key (descending sort; single element).
  </behavior>
  <action>
    Add an exported `getSupportedTaxYears(): number[]` returning `Object.keys(taxDataByYear).map(Number).sort((a, b) => b - a)` - mirroring the `Object.keys(taxDataByYear)` idiom already at line 73. This is the single source of truth for the selectable/valid year set; 11-03 validation and the 11-04 form dropdown import it. Do NOT add a parallel `SUPPORTED_TAX_YEARS` literal (anti-pattern: two parallel allow-lists). After the re-key this returns `[2025]`.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/business-website && grep -q 'export function getSupportedTaxYears' src/lib/paystub-calculator/tax-data.ts && bun -e "import('./src/lib/paystub-calculator/tax-data.ts').then(m => { const y = m.getSupportedTaxYears(); if (JSON.stringify(y) !== '[2025]') { throw new Error('expected [2025], got ' + JSON.stringify(y)); } })"</automated>
  </verify>
  <acceptance_criteria>
    - `getSupportedTaxYears` is exported.
    - Called at runtime it returns exactly `[2025]` (proves it is data-derived against the re-keyed table, not a hardcoded literal).
    - No `SUPPORTED_TAX_YEARS` const literal added.
  </acceptance_criteria>
  <done>getSupportedTaxYears() exported and returns [2025] from the live data table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale nuqs URL -> getTaxDataForYear | A restored `?year=2024`/`?year=2023` flows to getTaxDataForYear, which (by design, kept here) falls back via Math.max to the 2025 data. This plan ensures the fallback is documented and the baseline guard points at a year that still exists (2025); actual rejection of unbacked years lands in 11-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing/Information disclosure | `tax-data.ts` presenting 2023 federal brackets as the current year | mitigate | Replace all five federal schedules with the official 2025 IRS Rev. Proc. 2024-40 values and SS base 176100 (Task 1). Federal golden-number tests in 11-03 lock the correction. |
| T-11-02 | Information disclosure | `getTaxDataForYear` silently substituting another year's figures via Math.max | mitigate | The fallback is retained only as documented defense-in-depth and the baseline guard moves to 2025 so it cannot throw post re-key; year-membership validation in 11-03 makes it unreachable from the UI. |
| T-11-SC | Tampering | npm/bun installs | accept | No package installs in this phase (RESEARCH "## Package Legitimacy Audit": Not applicable, zero new deps). |
</threat_model>

<verification>
- `bun run typecheck` green.
- `getSupportedTaxYears()` returns `[2025]`.
- NOTE: federal golden-number unit tests are intentionally (re)written in 11-03 (which owns the test files) against the 2025 brackets + SS base 176100. After this plan the existing federal-encoding assertions may be RED until 11-03 recomputes them; that is expected at the Wave-1 boundary. Do NOT edit test files here (file ownership: 11-03 owns the tests).
</verification>

<success_criteria>
- One official 2025 federal entry keyed 2025 for all five schedules; SS base 176100; Medicare unchanged.
- 2024 entry and 2025 clone deleted; baseline guard at 2025; Math.max fallback kept.
- `getSupportedTaxYears()` exported and returns `[2025]`.
- `bun run typecheck` passes.
</success_criteria>

<output>
Create `.planning/phases/11-paystub-tax-accuracy/11-01-SUMMARY.md` when done. Record: the exact five federal 2025 schedules written, confirmation SS base is 176100 and Medicare untouched, the deleted 2024-entry + 2025-clone lines, the baseline-guard change to 2025, the `getSupportedTaxYears` export, and the federal calc function name so 11-03 can reference it in the golden test.
</output>

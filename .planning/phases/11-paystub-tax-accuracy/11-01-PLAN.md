---
phase: 11-paystub-tax-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/paystub-calculator/state-tax-data.ts
  - src/lib/paystub-calculator/states-utils.ts
  - src/lib/paystub-calculator/tax-data.ts
  - src/lib/paystub-calculator/validation.ts
  - src/lib/paystub-calculator/state-tax-calculations.ts
  - tests/state-tax-calculations.test.ts
  - tests/paystub-validation.test.ts
autonomous: true
requirements: [PAYSTUB-01, PAYSTUB-02, PAYSTUB-03, PAYSTUB-04]

must_haves:
  truths:
    - "getIncomeTaxStates() returns exactly CA, NY, IL, PA, MA (derived from the bracket data, not from a minus-no-tax allow-list)"
    - "No income-tax state without bracket data is in the selectable income-tax list"
    - "validatePaystubInputs rejects a tax year not present in taxDataByYear (e.g. 2023) with an errors.taxYear message"
    - "validatePaystubInputs accepts 2024 (the only data-backed year after the 2025 clone is deleted)"
    - "TX, FL, WA are absent from the income-tax bracket table; they resolve to 0 only via the defensive unknown-state fallback"
    - "Federal, Social Security, and Medicare math is unchanged after editing tax-data.ts (full unit suite stays green)"
  artifacts:
    - path: "src/lib/paystub-calculator/state-tax-data.ts"
      provides: "Income-tax bracket table without TX/FL/WA flat-0 rows + getSupportedIncomeTaxStateCodes() derivation helper"
      contains: "getSupportedIncomeTaxStateCodes"
    - path: "src/lib/paystub-calculator/states-utils.ts"
      provides: "getIncomeTaxStates() derived from getSupportedIncomeTaxStateCodes() intersected with states.json labels"
      contains: "getSupportedIncomeTaxStateCodes"
    - path: "src/lib/paystub-calculator/tax-data.ts"
      provides: "getSupportedTaxYears() helper; 2025 clone deleted so only 2024 is data-backed"
      contains: "getSupportedTaxYears"
    - path: "src/lib/paystub-calculator/validation.ts"
      provides: "taxYear membership check against getSupportedTaxYears()"
      contains: "getSupportedTaxYears"
    - path: "tests/state-tax-calculations.test.ts"
      provides: "Bidirectional parity test + exact-set assertion (PAYSTUB-01); re-documented defensive TX/FL/XX cases"
      contains: "parity"
    - path: "tests/paystub-validation.test.ts"
      provides: "Rejected-year case (2023) + accepted-year case (2024) for PAYSTUB-03"
      contains: "taxYear"
  key_links:
    - from: "src/lib/paystub-calculator/states-utils.ts"
      to: "src/lib/paystub-calculator/state-tax-data.ts"
      via: "import getSupportedIncomeTaxStateCodes"
      pattern: "getSupportedIncomeTaxStateCodes"
    - from: "src/lib/paystub-calculator/validation.ts"
      to: "src/lib/paystub-calculator/tax-data.ts"
      via: "import getSupportedTaxYears"
      pattern: "getSupportedTaxYears"
---

<objective>
Fix the paystub calculator's data/logic core so its selectable inputs match the data that actually exists. Derive the income-tax state list and the valid year set from the existing data tables (single source of truth), delete the redundant flat-0 TX/FL/WA rows and the fake 2025 clone, tighten year validation, and update the tests that codified the silent-$0 / silent-year-fallback as "graceful." This plan owns every change to the `src/lib/paystub-calculator/*` modules and the two paystub unit-test files.

Purpose: Make it structurally impossible for an income-tax state without bracket data, or a year without real data, to be accepted by the calculator. Net lines of code go DOWN, not up.
Output: Five edited lib modules + two updated test files. After this plan, `bun run test:unit` passes with the new parity and rejected-year assertions; the form layer (Plan 02) consumes the new `getSupportedTaxYears()` export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md
@.planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md
@.planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md
@.planning/phases/11-paystub-tax-accuracy/11-VALIDATION.md

<interfaces>
<!-- Verified contracts the executor needs. Extracted from the codebase 2026-06-01. No exploration required. -->

From src/lib/paystub-calculator/state-tax-data.ts (current):
- `type StateTaxBrackets = Record<string, TaxData['federalBrackets']>` (line 4)
- `const stateTaxDataByYear: Record<number, StateTaxBrackets>` (line 6) — keyed year -> stateCode -> brackets; only the 2024 key exists today
- Income-tax rows at lines 117-122: `IL`, `PA`, `MA` (flat, KEEP) and `TX`, `FL`, `WA` (flatBrackets(0), DELETE)
- `getStateTaxBrackets(state, year?)` (lines 126-136) — the existing data-derived lookup analog
- `flatBrackets(rate)` (lines 138-146) — hoisted factory; do not change

From src/lib/paystub-calculator/states-utils.ts (current):
- `const NO_INCOME_TAX_CODES = ['AK', 'FL', 'NV', 'SD', 'TN', 'TX', 'WA', 'WY']` (line 5) — UNCHANGED
- `getNoIncomeTaxStates()` (lines 16-18) — UNCHANGED; the exact filter-against-a-code-Set shape to mirror
- `getIncomeTaxStates()` (lines 23-25) returns `INCOME_TAX_STATES_CACHE` (lines 9-11), currently `states.json` minus `NO_INCOME_TAX_CODES` — REPLACE the cache derivation only
- `statesData.states` shape: `{ value: string; label: string }` (e.g. `{ value: 'CA', label: 'California' }`)

From src/lib/paystub-calculator/tax-data.ts (current):
- `const taxDataByYear: Record<number, TaxData>` (line 4) — 2024 real (lines 5-65)
- `taxDataByYear[2025] = JSON.parse(JSON.stringify(taxDataByYear[2024])) as TaxData` (line 68) — the fake clone, DELETE
- `getTaxDataForYear(year?)` (lines 70-90) — KEEP the `Math.max` fallback (lines 86-89) and the `defaultData = taxDataByYear[2024]` guard (lines 75-79); fans out to federal/SS/Medicare 4x in tax-calculations.ts

From src/lib/paystub-calculator/validation.ts (current):
- `validatePaystubInputs(params: PaystubCalculationParams): ValidationResult` (line 8)
- Hardcoded year range at lines 46-49: `if (params.taxYear < 2020 || params.taxYear > currentYear + 5)` — REPLACE
- Membership-check analog already in this file: `validFrequencies.includes(...)` (lines 52-60), `validFilingStatuses.includes(...)` (lines 63-72)
- State 2-letter length check (lines 74-77) — KEEP

From src/lib/paystub-calculator/state-tax-calculations.ts (current):
- `calculateStateTax(grossPay, ytdGross, state, filingStatus, year?)` (line 4)
- Defensive guard at lines 12-14: `if (!stateBrackets) { return 0 }` — KEEP, re-comment only
- Bracket loop (lines 16-62) — DO NOT TOUCH (YTD annualization is test-locked)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add data-derivation helpers and delete redundant data (PAYSTUB-01 source, PAYSTUB-02 source, PAYSTUB-04)</name>
  <files>src/lib/paystub-calculator/state-tax-data.ts, src/lib/paystub-calculator/states-utils.ts, src/lib/paystub-calculator/tax-data.ts</files>
  <read_first>
    - src/lib/paystub-calculator/state-tax-data.ts (file being modified; bracket table + getStateTaxBrackets derivation analog at lines 126-136)
    - src/lib/paystub-calculator/states-utils.ts (file being modified; getNoIncomeTaxStates filter-against-Set shape to mirror, lines 5-18)
    - src/lib/paystub-calculator/tax-data.ts (file being modified; Object.keys idiom at line 73, 2025 clone at line 68)
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (LOCKED decisions: PAYSTUB-01 derive-from-data; PAYSTUB-04 delete TX/FL/WA; discretion to delete 2025 clone, lines 26-54)
    - .planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md (in-file analogs + exact target shapes, lines 35-155)
  </read_first>
  <behavior>
    - getSupportedIncomeTaxStateCodes() returns ['CA','NY','IL','PA','MA'] in some order (union of stateTaxDataByYear codes across all years, after TX/FL/WA deletion)
    - getIncomeTaxStates() returns exactly the 5 states above as { value, label } objects sourced from states.json
    - getNoIncomeTaxStates() still returns the 8 NO_INCOME_TAX_CODES states (UNCHANGED)
    - getSupportedTaxYears() returns [2024] (only data-backed year after 2025 clone deletion), sorted descending
    - getTaxDataForYear(2024) returns the 2024 TaxData (federal math unchanged)
  </behavior>
  <action>
    Three edits implementing the single-source-of-truth derivation (per CONTEXT.md PAYSTUB-01/02/04, mirroring PATTERNS.md analogs):

    1. In `state-tax-data.ts`: DELETE the three lines `TX: flatBrackets(0)`, `FL: flatBrackets(0)`, `WA: flatBrackets(0)` (lines 120-122) inside the 2024 table, leaving `IL`/`PA`/`MA` flat rows intact (PAYSTUB-04). ADD a new exported `getSupportedIncomeTaxStateCodes(): string[]` that unions `Object.keys` of every `Object.values(stateTaxDataByYear)` year table into a `Set<string>` and returns the spread array (future-proofs for multi-year PAYSTUB-F1; today equals the 2024 keys = CA, NY, IL, PA, MA). Do NOT touch `getStateTaxBrackets`, the `flatBrackets` factory, or the CA/NY/IL/PA/MA bracket values.

    2. In `states-utils.ts`: REPLACE the `INCOME_TAX_STATES_CACHE` derivation (lines 9-11) so it intersects `statesData.states` with `getSupportedIncomeTaxStateCodes()` instead of computing `states.json` minus `NO_INCOME_TAX_CODES`. Add `import { getSupportedIncomeTaxStateCodes } from './state-tax-data'`. Build a `new Set(getSupportedIncomeTaxStateCodes())` at module scope and filter `statesData.states` against it (mirror the existing `NO_INCOME_TAX_STATES_CACHE` filter shape). Keep `getIncomeTaxStates()` body returning the cache. Leave `NO_INCOME_TAX_CODES` and `getNoIncomeTaxStates()` UNCHANGED (Pitfall 2: the `selectedState || 'TX'` default depends on TX staying a valid no-tax code).

    3. In `tax-data.ts`: DELETE the 2025 clone line `taxDataByYear[2025] = JSON.parse(...)` (line 68) and its comment (line 67) per CONTEXT.md:54 discretion (fake placeholder, YAGNI). ADD a new exported `getSupportedTaxYears(): number[]` that returns `Object.keys(taxDataByYear).map(Number).sort((a, b) => b - a)` (mirrors the existing `Object.keys(taxDataByYear)` usage at line 73). KEEP `getTaxDataForYear` exactly as-is including the `Math.max(...availableYears)` fallback and the `defaultData = taxDataByYear[2024]` guard (Pitfall 1: it fans out to federal/SS/Medicare in tax-calculations.ts; it stays as documented defense-in-depth, now unreachable from the UI).

    No `any` types; derive types from the existing tables. Tabs, single quotes, no semicolons (Biome).
  </action>
  <verify>
    <automated>bun test tests/state-tax-calculations.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "flatBrackets(0)" src/lib/paystub-calculator/state-tax-data.ts` returns nothing (TX/FL/WA rows deleted)
    - `grep -c "getSupportedIncomeTaxStateCodes" src/lib/paystub-calculator/state-tax-data.ts` >= 1 (helper exported)
    - `grep -n "getSupportedIncomeTaxStateCodes" src/lib/paystub-calculator/states-utils.ts` shows the import + Set usage
    - `grep -n "taxDataByYear\[2025\]" src/lib/paystub-calculator/tax-data.ts` returns nothing (clone deleted)
    - `grep -c "getSupportedTaxYears" src/lib/paystub-calculator/tax-data.ts` >= 1 (helper exported)
    - `bun run typecheck` passes (no `any`, no broken imports)
    - Existing flat/progressive math assertions in tests/state-tax-calculations.test.ts (IL 247.5, PA 122.8, MA 267.5, CA/NY > 0) stay green
  </acceptance_criteria>
  <done>TX/FL/WA flat-0 rows and the 2025 clone are deleted; `getSupportedIncomeTaxStateCodes()` and `getSupportedTaxYears()` are exported and consumed; `getIncomeTaxStates()` derives from the data table; typecheck passes; existing state-tax math assertions unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten year validation, re-comment defensive guard, add/update tests (PAYSTUB-03, PAYSTUB-01 lock, PAYSTUB-04 test meaning)</name>
  <files>src/lib/paystub-calculator/validation.ts, src/lib/paystub-calculator/state-tax-calculations.ts, tests/state-tax-calculations.test.ts, tests/paystub-validation.test.ts</files>
  <read_first>
    - src/lib/paystub-calculator/validation.ts (file being modified; hardcoded range lines 46-49; membership analog lines 52-72)
    - src/lib/paystub-calculator/state-tax-calculations.ts (file being modified; defensive guard lines 12-14)
    - tests/state-tax-calculations.test.ts (file being modified; defensive TX/FL/XX cases lines 61-90; math-lock assertions lines 5-58, 92-116)
    - tests/paystub-validation.test.ts (file being modified; validBase fixture lines 18-27; membership-failure assertion shape lines 51-71)
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (PAYSTUB-03 derive year validation; test contract lines 37-45)
    - .planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md (proposed parity test + rejected-year test, lines 245-264, 300-315; Pitfall 3 lines 193-196)
    - .planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md (test shapes to mirror, lines 254-330)
  </read_first>
  <behavior>
    - validatePaystubInputs({ ...validBase, taxYear: 2023 }) -> isValid:false with errors.taxYear defined
    - validatePaystubInputs(validBase with taxYear 2024) -> isValid:true
    - Parity test: [...new Set(getIncomeTaxStates().map(s => s.value))].sort() equals [...new Set(getSupportedIncomeTaxStateCodes())].sort()
    - Exact-set test: getIncomeTaxStates().map(s => s.value).sort() equals ['CA','IL','MA','NY','PA']
    - calculateStateTax(5000, 25000, 'XX', 'single') still returns 0 (defensive fallback, now documented as UI-unreachable)
  </behavior>
  <action>
    Four edits (per CONTEXT.md PAYSTUB-03 + test contract, mirroring the membership and test analogs in PATTERNS.md):

    1. In `validation.ts`: REPLACE the hardcoded year range (lines 46-49 — the `currentYear` computation + the `< 2020 || > currentYear + 5` check) with a membership check: `import { getSupportedTaxYears } from './tax-data'`, then `const supportedYears = getSupportedTaxYears()` and `if (!supportedYears.includes(params.taxYear)) { errors.taxYear = ... }` with a dash-free message such as `Tax year must be one of: ${supportedYears.join(', ')}` (PAYSTUB-03). This mirrors the existing `validFrequencies.includes(...)` / `validFilingStatuses.includes(...)` idiom in the same file. Leave the state 2-letter length check intact.

    2. In `state-tax-calculations.ts`: change ONLY the comment on the defensive guard (line 13) from `// Unknown state -> assume no income tax` to a comment documenting it as a defensive fallback for input the UI can no longer produce (per CONTEXT.md:53 discretion — keep `return 0`). Do NOT touch the bracket loop.

    3. In `tests/state-tax-calculations.test.ts`: ADD a new `describe('PAYSTUB-01: dropdown <-> data parity')` block with two `it` cases — bidirectional parity (`getIncomeTaxStates()` value set === `getSupportedIncomeTaxStateCodes()` set) and exact-set (`getIncomeTaxStates().map(s => s.value).sort()` === `['CA','IL','MA','NY','PA']`). Add the imports `getIncomeTaxStates` from `@/lib/paystub-calculator/states-utils` and `getSupportedIncomeTaxStateCodes` from `@/lib/paystub-calculator/state-tax-data`. UPDATE the comments on the existing TX/FL cases (lines 62-78), the `'XX'` unknown-state case (lines 82-89), and the second TX case in the `States with no income tax` describe block (lines 111-116) to document that they now exercise the DEFENSIVE fallback path (UI-unreachable), not user-facing "graceful" behavior (PAYSTUB-04 / Pitfall 3). Keep all assertions (they still pass). Do NOT change the flat/progressive math assertions.

    4. In `tests/paystub-validation.test.ts`: ADD two `it` cases mirroring the existing `throws on invalid pay frequency` shape (spread `validBase`, override one field, assert `isValid` + the error key, with the `if (!validBase) { return }` guard per CLAUDE.md curly-brace rule): (a) `rejects a tax year with no backing data` — `{ ...validBase, taxYear: 2023 }` -> `isValid:false`, `errors.taxYear` defined; (b) `accepts the supported tax year 2024` -> `validatePaystubInputs(validBase).isValid` is true.

    No em-dash/en-dash in test strings or messages. No `any` except the existing `as unknown as` cast pattern already used for invalid-frequency tests.
  </action>
  <verify>
    <automated>bun test tests/state-tax-calculations.test.ts tests/paystub-validation.test.ts && bun run lint && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `bun test tests/state-tax-calculations.test.ts -t "parity"` passes (bidirectional parity case green)
    - `bun test tests/state-tax-calculations.test.ts -t "CA, NY, IL, PA, MA"` passes (exact-set assertion green) — or the chosen test name containing the 5-state list
    - `bun test tests/paystub-validation.test.ts` passes including the new rejected-year (2023) and accepted-year (2024) cases
    - `grep -n "getSupportedTaxYears" src/lib/paystub-calculator/validation.ts` shows the import + membership usage
    - `grep -n "2020" src/lib/paystub-calculator/validation.ts` returns nothing (hardcoded range removed)
    - `bun run lint && bun run typecheck` pass
    - No U+2014/U+2013 in any new/edited string: `grep -nP "[\x{2013}\x{2014}]" src/lib/paystub-calculator/validation.ts tests/state-tax-calculations.test.ts tests/paystub-validation.test.ts` returns nothing
  </acceptance_criteria>
  <done>Year validation is a data-derived membership check; the defensive guard is re-commented (behavior unchanged); the parity + exact-set tests lock PAYSTUB-01 bidirectionally; the rejected-year/accepted-year tests lock PAYSTUB-03; the defensive TX/FL/XX cases are re-documented; lint + typecheck pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale nuqs URL -> form state | A bookmarked/shared `?year=2023` or `?state=AL` URL is restored into form state at `use-paystub-generator.ts:70` on mount. This is untrusted, user-controllable input that crosses into the calculation path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `validatePaystubInputs` (validation.ts) consuming a restored `?year=` param | mitigate | Replace the hardcoded `2020..currentYear+5` range with a membership check against `getSupportedTaxYears()` (Task 2). An unbacked year (e.g. 2023) now produces a validation error instead of silently flowing to `getTaxDataForYear`'s `Math.max` fallback. |
| T-11-02 | Spoofing/Information disclosure | `getTaxDataForYear` silently returning another year's figures presented as the requested year | mitigate | Validation rejects unbacked years upstream (T-11-01); the `Math.max` fallback is KEPT only as documented, UI-unreachable defense-in-depth (Task 1). No silent year substitution can reach a rendered figure. |
| T-11-03 | Tampering | `calculateStateTax` receiving an unsupported/crafted `?state=` (e.g. AL) | mitigate | Unsupported income-tax states are no longer selectable (PAYSTUB-01, Plan 02 form derives from data). The defensive `if (!stateBrackets) return 0` is kept and re-commented as UI-unreachable (Task 2); it prevents a crash without presenting a confident $0 as a real result reachable from the UI. The 2-letter state length check in validation.ts:74-77 is retained. |

All threats are input-validation only. No auth, session, network, crypto, secret, or persistence surface is touched by this phase. `Infinity` bracket limits live only in the static data table and are never user-controllable.
</threat_model>

<verification>
- `bun test tests/state-tax-calculations.test.ts tests/paystub-validation.test.ts` — all paystub unit cases green including new parity + rejected-year
- `bun run test:unit` — full suite green (federal/SS/Medicare regression guard after editing tax-data.ts; tax-calculations.ts has 4 fan-out call sites)
- `bun run lint && bun run typecheck` — no `any`, no broken imports, no dash violations
</verification>

<success_criteria>
- `getIncomeTaxStates()` returns exactly CA, NY, IL, PA, MA, derived from `stateTaxDataByYear`
- `validatePaystubInputs` rejects year 2023, accepts 2024
- TX/FL/WA flat-0 rows and the 2025 clone are deleted
- `getSupportedIncomeTaxStateCodes()` and `getSupportedTaxYears()` exported for the form layer (Plan 02)
- Full unit suite passes (no federal-calc regression)
- Net lines of code decreased
</success_criteria>

<output>
Create `.planning/phases/11-paystub-tax-accuracy/11-01-SUMMARY.md` when done. Record: exact lines deleted (TX/FL/WA rows, 2025 clone, hardcoded year range), the two new exported helper names, and the new test case names so Plan 02 and verification can reference them.
</output>

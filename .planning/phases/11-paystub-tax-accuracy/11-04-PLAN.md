---
phase: 11-paystub-tax-accuracy
plan: 04
type: execute
wave: 3
depends_on: ["11-03"]
files_modified:
  - src/components/paystub/PaystubForm.tsx
  - src/hooks/use-paystub-form.ts
  - src/hooks/use-paystub-generator.ts
  - src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx
  - src/app/(public)/tools/paystub-calculator/page.tsx
autonomous: true
requirements: [PAYSTUB-02, PAYSTUB-09, PAYSTUB-10]

must_haves:
  truths:
    - "The Tax Year dropdown offers only data-backed years (2025) rendered from getSupportedTaxYears(); no 2023 and no 2024 SelectItem exist"
    - "The default taxYear is 2025 (use-paystub-form.ts), consistent with the only data-backed year"
    - "The State Income Tax optgroup renders exactly the data-derived list (CA, NY, IL, PA, MA) via getIncomeTaxStates()"
    - "A URL-restored ?state= code is intersected with the supported state codes; an unsupported value (e.g. AL) does NOT reach the calculation, so the calculator never presents a silent defensive $0 for it"
    - "The paystub hero copy and page metadata describe the output as an estimate, drop the word accurate, reference the 2025 tax year, and contain no em/en-dash; metadata.description stays 120-160 chars"
  artifacts:
    - path: "src/components/paystub/PaystubForm.tsx"
      provides: "Tax Year dropdown rendered from getSupportedTaxYears(); no hardcoded 2023/2024 item"
      contains: "getSupportedTaxYears"
    - path: "src/hooks/use-paystub-form.ts"
      provides: "Default taxYear 2025"
      contains: "taxYear: 2025"
    - path: "src/hooks/use-paystub-generator.ts"
      provides: "URL-restored state intersected with supported codes (PAYSTUB-10)"
      contains: "setSelectedState"
    - path: "src/app/(public)/tools/paystub-calculator/page.tsx"
      provides: "Estimate-framed metadata.description (2025, no over-promise, no dash, 120-160 chars)"
      contains: "description:"
  key_links:
    - from: "src/components/paystub/PaystubForm.tsx"
      to: "src/lib/paystub-calculator/tax-data.ts"
      via: "import getSupportedTaxYears"
      pattern: "getSupportedTaxYears"
    - from: "src/hooks/use-paystub-generator.ts"
      to: "src/lib/paystub-calculator/states-utils.ts"
      via: "intersect restored state with getIncomeTaxStates + getNoIncomeTaxStates value sets"
      pattern: "getIncomeTaxStates|getNoIncomeTaxStates"
---

<objective>
Wire the data-derived helpers into the form UI, set the default year to 2025, harden the URL-state restore against unsupported state codes (PAYSTUB-10), reframe the marketing copy as a 2025 estimate, and run the full phase gate.

Purpose: Make the visible inputs and persisted-URL inputs match the locked-down 2025 data and stop the page from advertising accuracy the engine does not deliver. The Tax Year dropdown renders from `getSupportedTaxYears()` (only 2025); the default `taxYear` becomes 2025; the State Income Tax optgroup auto-narrows to CA/NY/IL/PA/MA via 11-03's derived list; a stale shared `?state=AL` URL is intersected against the supported codes so it can never reach the defensive `return 0` (PAYSTUB-10); and the hero/metadata copy reads as a 2025 estimate, dash-free. This is the final wave; it runs the full lint + typecheck + test:unit + build gate.

Output: data-driven year dropdown and default in `PaystubForm.tsx` + `use-paystub-form.ts`; state-intersect hardening in `use-paystub-generator.ts`; estimate-framed 2025 copy in `PaystubCalculatorClient.tsx` + `page.tsx`. Depends on 11-03 (consumes `getSupportedTaxYears`, the derived `getIncomeTaxStates`, and `getNoIncomeTaxStates`). It shares no file with 11-01/11-02/11-03, so there is no within-wave overlap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md
@.planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md
@.planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md
@.planning/phases/11-paystub-tax-accuracy/11-01-SUMMARY.md
@.planning/phases/11-paystub-tax-accuracy/11-02-SUMMARY.md
@.planning/phases/11-paystub-tax-accuracy/11-03-SUMMARY.md

<interfaces>
<!-- Verified contracts the executor needs. Extracted from the codebase 2026-06-02. -->

From src/lib/paystub-calculator/tax-data.ts (11-01):
- `getSupportedTaxYears(): number[]` returns data-backed years sorted descending (after Wave 1: [2025])

From src/lib/paystub-calculator/states-utils.ts (11-03):
- `getIncomeTaxStates(): { value: string; label: string }[]` derives from the bracket data (CA, NY, IL, PA, MA)
- `getNoIncomeTaxStates(): { value: string; label: string }[]` UNCHANGED (8 no-tax states). Together these two value sets are the full supported-state allow-list for the PAYSTUB-10 intersect (NO_INCOME_TAX_CODES is module-private, so use these public helpers).

From src/components/paystub/PaystubForm.tsx (current):
- 'use client' file CANNOT export metadata
- Existing imports `getIncomeTaxStates`, `getNoIncomeTaxStates` from `@/lib/paystub-calculator/states-utils` (around lines 18-21)
- Tax Year `<Select>`: `value={paystubData.taxYear.toString()}` (line 188), `onValueChange` parses via `parseInt(value, 10)` (lines 189-194), `<SelectContent>` (line 199) with `<SelectItem value="2024">2024</SelectItem>` (line 200) and `<SelectItem value="2023">2023</SelectItem>` (line 201) - BOTH to remove
- State `<Select>`: No State Income Tax optgroup maps `getNoIncomeTaxStates()` (lines 215-219); State Income Tax optgroup maps `getIncomeTaxStates()` (lines 223-227) the `.map()` idiom to mirror for years

From src/hooks/use-paystub-form.ts (current):
- `INITIAL_PAYSTUB` has `taxYear: 2024` (line 13) - change to 2025

From src/hooks/use-paystub-generator.ts (current):
- The URL-restore effect (lines 50-78) applies `taxYear: urlState.year ?? prev.taxYear` (line 70) and `if (urlState.state) { formState.setSelectedState(urlState.state) }` (lines 73-75). The state branch is where the PAYSTUB-10 intersect goes. `urlState.state` is the raw nuqs string (e.g. `'AL'`).

From src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx (current):
- `<ToolPageLayout ... description="Generate accurate payroll breakdowns with federal and state tax calculations for any pay period" ...>` (line 168) over-promises "accurate" + "for any pay period"

From src/app/(public)/tools/paystub-calculator/page.tsx (current, SERVER component, owns metadata):
- `export const metadata: Metadata` with `description:` (line 13): the current detailed-breakdown string; `openGraph.description` (line 16)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the Tax Year dropdown from getSupportedTaxYears(); remove the 2023 and 2024 items; default taxYear to 2025 (PAYSTUB-02)</name>
  <files>src/components/paystub/PaystubForm.tsx, src/hooks/use-paystub-form.ts</files>
  <read_first>
    - src/components/paystub/PaystubForm.tsx (file under edit; Tax Year Select lines 186-203; State optgroup .map idiom lines 213-228; imports around lines 9-23)
    - src/hooks/use-paystub-form.ts (file under edit; INITIAL_PAYSTUB taxYear at line 13)
    - .planning/phases/11-paystub-tax-accuracy/11-01-SUMMARY.md (confirms getSupportedTaxYears returns [2025])
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (PAYSTUB-02: remove BOTH 2023 and 2024 items, offer 2025, default taxYear 2025, lines 32-36)
    - .planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md (year-render mirroring the state .map idiom, lines 212-251)
  </read_first>
  <action>
    1. In PaystubForm.tsx: add `import { getSupportedTaxYears } from '@/lib/paystub-calculator/tax-data'` alongside the existing states-utils import, keeping the paystub utils grouped. REPLACE both hardcoded `<SelectItem value="2024">2024</SelectItem>` (line 200) and `<SelectItem value="2023">2023</SelectItem>` (line 201) inside the Tax Year `<SelectContent>` with `{getSupportedTaxYears().map(y => (<SelectItem key={y} value={String(y)}>{y}</SelectItem>))}`, mirroring the `getIncomeTaxStates().map(...)` render in the State optgroup. The `onValueChange` `parseInt(value, 10)` handler stays unchanged. Leave the State `<Select>` UNCHANGED (its `getIncomeTaxStates()` call auto-narrows to CA/NY/IL/PA/MA via 11-03).

    2. In use-paystub-form.ts: change `INITIAL_PAYSTUB.taxYear` from `2024` to `2025` (line 13) so the default matches the only data-backed year.

    Optional (CONTEXT.md discretion): a short dash-free plain-text note under the State `<Select>` such as "State income tax supported for CA, NY, IL, PA, MA." If added, NO em/en-dash; skip if it complicates layout (not required for any acceptance criterion).

    Tabs, single quotes, no semicolons (Biome). 'use client' file - do not add a metadata export here.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/business-website && [ "$(grep -c 'value="2023"' src/components/paystub/PaystubForm.tsx)" -eq 0 ] && [ "$(grep -c 'value="2024"' src/components/paystub/PaystubForm.tsx)" -eq 0 ] && grep -q 'getSupportedTaxYears' src/components/paystub/PaystubForm.tsx && grep -q 'taxYear: 2025' src/hooks/use-paystub-form.ts && [ "$(grep -c 'taxYear: 2024' src/hooks/use-paystub-form.ts)" -eq 0 ] && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - Both year items removed: `grep -c 'value="2023"'` == 0 and `grep -c 'value="2024"'` == 0 in PaystubForm.tsx.
    - `getSupportedTaxYears` imported/used in PaystubForm.tsx.
    - Default updated: `taxYear: 2025` present and `taxYear: 2024` absent in use-paystub-form.ts.
    - `bun run typecheck && bun run lint` pass; no U+2014/U+2013 in PaystubForm.tsx.
  </acceptance_criteria>
  <done>The Tax Year dropdown renders only 2025 from getSupportedTaxYears(); the 2023 and 2024 items are gone; the default taxYear is 2025; the State optgroup auto-renders the derived 5-state list; typecheck + lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Intersect the URL-restored ?state= with the supported codes so a stale value cannot reach the defensive $0 (PAYSTUB-10)</name>
  <files>src/hooks/use-paystub-generator.ts</files>
  <read_first>
    - src/hooks/use-paystub-generator.ts (file under edit; URL-restore effect lines 50-78, state branch lines 73-75)
    - src/lib/paystub-calculator/states-utils.ts (read-only; getIncomeTaxStates + getNoIncomeTaxStates value shape `{ value, label }`)
    - .planning/phases/11-paystub-tax-accuracy/11-03-SUMMARY.md (confirms getIncomeTaxStates derives CA/NY/IL/PA/MA and getNoIncomeTaxStates is unchanged)
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (PAYSTUB-10 URL-state hardening, lines 57-58)
    - .planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md ("Runtime State Inventory" + "Library Behavior" stale-URL semantics + Open Question 1 recommendation, lines ~537-560, ~696-698)
  </read_first>
  <action>
    Harden the URL-restore state branch (PAYSTUB-10). In use-paystub-generator.ts:

    1. Add `import { getIncomeTaxStates, getNoIncomeTaxStates } from '@/lib/paystub-calculator/states-utils'` (these are the public helpers; NO_INCOME_TAX_CODES is module-private). Build a supported-code set once - e.g. `const supportedStateCodes = new Set([...getIncomeTaxStates(), ...getNoIncomeTaxStates()].map(s => s.value))`. Place it at module scope or memoize inside the hook (do not recompute every render unnecessarily).

    2. In the restore effect (lines 73-75), replace the unconditional `if (urlState.state) { formState.setSelectedState(urlState.state) }` with an intersect: uppercase the restored code and only call `setSelectedState` if `supportedStateCodes.has(restoredCode)`. If the restored code is NOT supported (e.g. `AL`), do NOT set it - leave `selectedState` at its default so the calculation path never receives an unsupported code and never produces the defensive silent $0 for it. (A supported no-income-tax code like TX still restores correctly and correctly yields $0 via the no-tax group.)

    Do not change the `taxYear` restore (line 70) - the tightened validation from 11-03 is the year gate. Keep the `hasInitializedFromUrl` ref logic and the rest of the effect intact. No `any`. Tabs, single quotes, no semicolons (Biome).
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/business-website && grep -qE 'getIncomeTaxStates|getNoIncomeTaxStates' src/hooks/use-paystub-generator.ts && grep -q 'setSelectedState' src/hooks/use-paystub-generator.ts && bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - The supported-code intersect uses `getIncomeTaxStates` and/or `getNoIncomeTaxStates` (imported), not a new hardcoded state list.
    - `setSelectedState` is called only when the uppercased restored code is in the supported set (verify by reading the conditional); an unsupported code like `AL` does not call `setSelectedState`.
    - The `taxYear` restore line is unchanged.
    - `bun run typecheck && bun run lint` pass.
  </acceptance_criteria>
  <done>A URL-restored ?state= is intersected with the supported state codes; an unsupported code (AL) is dropped rather than applied, so it can never reach calculateStateTax's defensive $0; supported codes (incl. no-tax TX) still restore; typecheck + lint pass.</done>
</task>

<task type="auto">
  <name>Task 3: Reframe paystub hero + metadata copy as a 2025 estimate (dash-free) and run the full phase gate (PAYSTUB-09)</name>
  <files>src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx, src/app/(public)/tools/paystub-calculator/page.tsx</files>
  <read_first>
    - src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx (file under edit; description prop line 168)
    - src/app/(public)/tools/paystub-calculator/page.tsx (file under edit; metadata.description line 13, openGraph.description line 16; server component)
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (PAYSTUB-09 estimate framing, no over-promise, dash-free, reference 2025, lines 54-55)
    - .planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md (Methodology "Copy verdict" + Pitfall 6 dash ban + exact current strings)
    - CLAUDE.md (NO em/en-dash in user-facing text; metadata description 120-160 chars; 'use client' cannot export metadata)
  </read_first>
  <action>
    Two copy edits (PAYSTUB-09), then the full phase gate:

    1. In PaystubCalculatorClient.tsx (line 168): REPLACE the `description=` string with an estimate-framed, dash-free string that drops "accurate" and the universal-coverage framing, references the 2025 tax year, and is honest about scope (federal tax for all filers; state income tax for the supported states; an estimate that excludes W-4 allowances, the standard deduction, pre-tax deductions, and credits). Final wording is discretion; example direction: "Estimate your 2025 federal and state income tax breakdown by pay period. State income tax is supported for CA, NY, IL, PA, and MA." Use commas, periods, hyphens, or "to" - never an em-dash (U+2014) or en-dash (U+2013).

    2. In page.tsx: REPLACE `metadata.description` (line 13) with an estimate-framed, dash-free string in the 120-160 character SEO range that drops the over-promise and references 2025 (e.g. "Estimate 2025 payroll tax breakdowns with our free paystub calculator. Federal tax for all filers and state income tax for CA, NY, IL, PA, and MA."). Confirm the final string is 120-160 characters. Also update `openGraph.description` (line 16) to a short dash-free estimate-framed string (e.g. "Estimate 2025 payroll tax breakdowns with our free paystub calculator."). page.tsx is the server component that owns metadata.

    3. Run the full phase gate: `bun run lint && bun run typecheck && bun run test:unit && bun run build`. All must pass. If an existing paystub e2e spec is present in e2e/ (e.g. tools.spec.ts), optionally add a smoke assertion that the year dropdown shows only 2025 and the state list lacks an unsupported label, but only if a paystub e2e already exists (CONTEXT.md: add e2e only if one exists).

    No em-dash/en-dash in either copy file.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/business-website && [ "$(grep -c 'for any pay period' 'src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx')" -eq 0 ] && [ "$(grep -c 'accurate' 'src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx')" -eq 0 ] && grep -qi 'estimate' 'src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx' && grep -qi 'estimate' 'src/app/(public)/tools/paystub-calculator/page.tsx' && ! grep -nP "[\x{2013}\x{2014}]" 'src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx' 'src/app/(public)/tools/paystub-calculator/page.tsx' && bun run lint && bun run typecheck && bun run test:unit && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - Over-promise removed: `grep -c 'for any pay period'` == 0 and `grep -c 'accurate'` == 0 in PaystubCalculatorClient.tsx.
    - Estimate framing present in both copy files; both reference 2025.
    - The new page.tsx metadata.description is 120-160 characters (verify the string length when reading it back).
    - No em-dash/en-dash in either copy file.
    - Full gate green: `bun run lint && bun run typecheck && bun run test:unit && bun run build` all pass.
  </acceptance_criteria>
  <done>The hero description and page metadata (and openGraph) describe a 2025 estimate, drop "accurate" and the universal-coverage framing, contain no em/en-dash, and the metadata stays 120-160 chars; the full lint + typecheck + test:unit + build gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale nuqs URL -> form select + calculation | A shared `?year=2024` / `?state=AL` URL restores into form state on mount. nuqs `parseAsInteger`/`parseAsString` pass these through unchanged, so the UI render set (years) and the restore-path intersect (states) plus validation are the gates. This plan owns the UI render and the state-intersect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering/Information disclosure | Tax Year `<Select>` offering a value with no backing data | mitigate | The dropdown renders from `getSupportedTaxYears()` (Task 1), offering only 2025; default is 2025; a stale `?year=2024`/`2023` has no matching option and is rejected by validatePaystubInputs (11-03, T-11-05). |
| T-11-08 | Information disclosure | A stale shared `?state=AL` reaching `calculateStateTax`'s defensive `return 0` and presenting a confident silent $0 | mitigate | Intersect the URL-restored state with the supported codes (`getIncomeTaxStates` + `getNoIncomeTaxStates`); an unsupported code is dropped, never applied, so it cannot reach the defensive $0 (Task 2). This is the PAYSTUB-10 control. |
| T-11-09 | Information disclosure (misleading UI copy) | Hero/metadata advertising "accurate" coverage the gross-to-marginal engine lacks | mitigate | Reframe the copy as a 2025 estimate (Task 3) per the IRS Pub 15-T methodology gap; prevents claiming accuracy the engine does not deliver. |
| T-11-SC | Tampering | npm/bun installs | accept | No package installs in this phase (RESEARCH "## Package Legitimacy Audit": Not applicable, zero new deps). |

Input-validation / honest-output scope only. No auth, network, secret surface. nuqs stale-URL is the only untrusted input; states are now intersected and years are render-gated + validated. Reuses the existing shadcn `Select` primitive.
</threat_model>

<verification>
- `bun run lint && bun run typecheck && bun run test:unit && bun run build` all green (the phase gate).
- No U+2014/U+2013 in the copy files; metadata 120-160 chars.

## Manual smoke (operator, non-blocking)
- Load `/tools/paystub-calculator`: Tax Year dropdown shows only 2025; State Income Tax group lists exactly CA, NY, IL, PA, MA; hero copy reads as a 2025 estimate with no dash.
- Load `/tools/paystub-calculator?year=2024&state=AL`: no crash; the year is rejected/defaulted and the unsupported state is dropped (no silent $0 for AL).
</verification>

<success_criteria>
- Tax Year dropdown offers only 2025 (rendered from getSupportedTaxYears()); no 2023/2024 item; default taxYear 2025.
- State Income Tax optgroup lists exactly CA, NY, IL, PA, MA.
- URL-restored ?state= is intersected with supported codes; an unsupported value never reaches the defensive $0.
- Hero + metadata + openGraph copy describe a 2025 estimate, drop "accurate", are dash-free; metadata 120-160 chars.
- Full lint + typecheck + test:unit + build gate passes.
</success_criteria>

<output>
Create `.planning/phases/11-paystub-tax-accuracy/11-04-SUMMARY.md` when done. Record: the final copy strings (hero, metadata.description with char count, openGraph.description), the default-year change, the state-intersect implementation, confirmation both year items were removed and years render from getSupportedTaxYears(), and the full-gate result.
</output>

---
phase: 11-paystub-tax-accuracy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/paystub-calculator/state-tax-data.ts
autonomous: true
requirements: [PAYSTUB-04, PAYSTUB-06, PAYSTUB-07, PAYSTUB-08, PAYSTUB-01]

must_haves:
  truths:
    - "CA 2025 brackets equal the official FTB Schedule X/Y/Z values (single 1% ceiling 11079, 11.3% to 742953) plus the 1% MHS surtax band over 1000000 (effective 13.3% top)"
    - "NY 2025 brackets equal the official DTF IT-201-I values including the 9.65% / 10.3% / 10.9% high-income brackets"
    - "MA is a flat 5.0% (0.05) on income up to 1083150 plus a 4% surtax band over 1083150 (effective 9% top)"
    - "IL stays 0.0495 and PA stays 0.0307 (unchanged for 2025)"
    - "stateTaxDataByYear is keyed 2025; TX, FL, WA flat-0 rows are absent from the income-tax bracket table"
    - "getSupportedIncomeTaxStateCodes() returns CA, NY, IL, PA, MA derived from stateTaxDataByYear keys"
  artifacts:
    - path: "src/lib/paystub-calculator/state-tax-data.ts"
      provides: "Official 2025 CA/NY/MA brackets with surtax top-brackets keyed 2025, TX/FL/WA removed, IL/PA unchanged, getSupportedIncomeTaxStateCodes() derivation helper"
      contains: "getSupportedIncomeTaxStateCodes"
  key_links:
    - from: "src/lib/paystub-calculator/state-tax-data.ts"
      to: "stateTaxDataByYear keys"
      via: "getSupportedIncomeTaxStateCodes unions Object.keys over year tables"
      pattern: "getSupportedIncomeTaxStateCodes"
---

<objective>
Re-key the state income-tax table to OFFICIAL 2025, correct California, New York, and Massachusetts to their official published 2025 values (including the >$1M CA MHS surtax and the >$1,083,150 MA 4% surtax, encoded as additional top brackets so no calculation path changes), delete the redundant flat-0 TX/FL/WA rows, and expose the supported state-code set as a data-derived helper.

Purpose: Three of the five "supported" states (CA, NY, MA) currently ship stale or pre-2020 values, the table is keyed to the wrong year, and TX/FL/WA flat-0 rows pollute the data-derived selectable list. This plan replaces those numbers with the official 2025 figures transcribed in 11-RESEARCH.md and exports `getSupportedIncomeTaxStateCodes()` so 11-03 can tie the dropdown to the data (PAYSTUB-01). REQUIREMENTS.md PAYSTUB-06/08 mandate the CA MHS and MA surtaxes, and CONTEXT.md (line 15) directs implementing the >$1M tiers at full fidelity, so the surtaxes are in scope, not deferred.

Output: `state-tax-data.ts` keyed 2025 with official CA/NY/MA values, surtaxes as top brackets, TX/FL/WA removed, IL/PA unchanged, and `getSupportedIncomeTaxStateCodes()` exported. This plan exclusively owns `state-tax-data.ts`; 11-01 exclusively owns `tax-data.ts`, so the two run in parallel in Wave 1. The state-tax-calculations math is NOT touched here (surtax-as-top-bracket needs no new math path); the tests live in 11-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md
@.planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md
@.planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md
@.planning/phases/11-paystub-tax-accuracy/11-VALIDATION.md

<interfaces>
<!-- Verified contracts the executor needs. Extracted from the codebase 2026-06-02. No exploration required. -->

From src/lib/paystub-calculator/state-tax-data.ts (current):
- `type StateTaxBrackets = Record<string, TaxData['federalBrackets']>` (line 4)
- `const stateTaxDataByYear: Record<number, StateTaxBrackets>` (line 6) keyed year -> stateCode -> brackets; only the `2024` key exists today (re-key to 2025)
- Each state value is keyed by filing status (`single`, `marriedJoint`, `marriedSeparate`, `headOfHousehold`, `qualifyingSurvivingSpouse`); each is an ascending array of `{ limit: number, rate: number }` terminating in `{ limit: Infinity, rate }`
- CA block lines 8-64 (stale 2023 values); NY block lines 65-116 (pre-2023 rates, missing high brackets); flat rows IL line 117, PA line 118, MA line 119 (0.0535 stale), TX line 120, FL line 121, WA line 122
- `getStateTaxBrackets(state, year?)` (lines 126-136) the existing data-derived lookup (uppercases state, `?? Math.max(...Object.keys(...))`) DO NOT change
- `flatBrackets(rate)` (lines 138-146) hoisted factory returning a single `{ limit: Infinity, rate }` per filing status; used by IL/PA, KEEP

From src/lib/paystub-calculator/state-tax-calculations.ts (math, NOT modified here):
- `calculateStateTax(grossPay, ytdGross, state, filingStatus, year?)` walks brackets against cumulative income `ytdGross + grossPay`, applying each bracket rate only to the portion of income between the previous limit and the bracket limit. A surtax expressed as an additional top bracket with the combined rate (base + surtax) over the threshold therefore works with NO math change. (Verified against the loop at lines 25-60.)
</interfaces>

<official_2025_values>
<!-- TRANSCRIBE exactly from 11-RESEARCH.md "## Official 2025 Tables (TARGET YEAR)": "### California 2025" (Sch X/Y/Z), "### New York 2025", "### Flat-rate states 2025" + the MA surtax note. Ascending upper limits. Do NOT use the 2024 reference section. -->

CALIFORNIA 2025 (FTB 2025 Schedules X/Y/Z) base bands, then add the 1% MHS surtax band over 1000000:
- Schedule X (single AND marriedSeparate): 1% to 11079; 2% to 26264; 4% to 41452; 6% to 57542; 8% to 72724; 9.3% to 371479; 10.3% to 445771; 11.3% to 742953; 12.3% over (Infinity).
- Schedule Y (marriedJoint AND qualifyingSurvivingSpouse): 1% to 22158; 2% to 52528; 4% to 82904; 6% to 115084; 8% to 145448; 9.3% to 742958; 10.3% to 891542; 11.3% to 1485906; 12.3% over (Infinity).
- Schedule Z (headOfHousehold): 1% to 22173; 2% to 52530; 4% to 67716; 6% to 83805; 8% to 98990; 9.3% to 505208; 10.3% to 606251; 11.3% to 1010417; 12.3% over (Infinity).

CA MHS SURTAX ENCODING RULE (per schedule; surtax is +1% on income over 1000000, so insert a 1000000 limit boundary; the band that contains 1000000 splits into "up to 1000000 at base rate" and "over 1000000 at base+0.01"). Concrete tails to write:
- Schedule X (single/MFS): 1000000 sits inside the 12.3% band (over 742953) -> tail = `{ limit: 742953, rate: 0.113 }, { limit: 1000000, rate: 0.123 }, { limit: Infinity, rate: 0.133 }`.
- Schedule Y (MFJ/QSS): 1000000 sits inside the 11.3% band (891542..1485906) -> tail = `{ limit: 891542, rate: 0.103 }, { limit: 1000000, rate: 0.113 }, { limit: 1485906, rate: 0.123 }, { limit: Infinity, rate: 0.133 }`.
- Schedule Z (HoH): 1000000 sits inside the 11.3% band (606251..1010417) -> tail = `{ limit: 606251, rate: 0.103 }, { limit: 1000000, rate: 0.113 }, { limit: 1010417, rate: 0.123 }, { limit: Infinity, rate: 0.133 }`.
(Verify after writing: over 1000000 every schedule's rate is base+0.01; arrays stay strictly ascending in limit.)

NEW YORK 2025 (DTF IT-201-I 2025; unchanged from 2024 since NY does not index these; NO surtax band to add, just the missing high brackets):
- single AND marriedSeparate: 4% to 8500; 4.5% to 11700; 5.25% to 13900; 5.5% to 80650; 6% to 215400; 6.85% to 1077550; 9.65% to 5000000; 10.3% to 25000000; 10.9% over (Infinity).
- marriedJoint AND qualifyingSurvivingSpouse: 4% to 17150; 4.5% to 23600; 5.25% to 27900; 5.5% to 161550; 6% to 323200; 6.85% to 2155350; 9.65% to 5000000; 10.3% to 25000000; 10.9% over (Infinity). (Note MFJ 5.25% ceiling is 27900 and 5.5% ceiling is 161550 - the official 2025 values that supersede the code's stale 27950/43000.)
- headOfHousehold: 4% to 12800; 4.5% to 17650; 5.25% to 20900; 5.5% to 107650; 6% to 269300; 6.85% to 1616450; 9.65% to 5000000; 10.3% to 25000000; 10.9% over (Infinity).

FLAT-RATE STATES 2025:
- IL: flatBrackets(0.0495) (unchanged).
- PA: flatBrackets(0.0307) (unchanged).
- MA: flat 5.0% plus 4% surtax over 1083150 (2025 threshold) = effective 9% top. Encode as a two-band array for ALL five filing statuses: `{ limit: 1083150, rate: 0.05 }, { limit: Infinity, rate: 0.09 }`. Do NOT use flatBrackets() for MA (it emits a single Infinity band only).
</official_2025_values>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-key state table to 2025; CA/NY official 2025 + surtaxes; MA flat 0.05 + surtax; delete TX/FL/WA; keep IL/PA (PAYSTUB-06, -07, -08, -04)</name>
  <files>src/lib/paystub-calculator/state-tax-data.ts</files>
  <read_first>
    - src/lib/paystub-calculator/state-tax-data.ts (file under edit; CA lines 8-64, NY lines 65-116, flat rows 117-122, getStateTaxBrackets 126-136, flatBrackets 138-146)
    - src/lib/paystub-calculator/state-tax-calculations.ts (read-only; confirm the bracket loop applies each rate to the slice between previous limit and bracket limit so surtax-as-top-bracket needs no math change)
    - .planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md "## Official 2025 Tables (TARGET YEAR)" -> "### California 2025", "### New York 2025", "### Flat-rate states 2025" + MA surtax note ($1,083,150). AUTHORITATIVE numeric source; do NOT transcribe from the 2024 reference section.
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (PAYSTUB-06/07/08 full-fidelity surtax decision incl. >$1M tiers, lines 44-52; PAYSTUB-04 delete TX/FL/WA, line 42; surtax-as-extra-bracket preference, line 51)
    - .planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md (state-tax-data target shape, flat vs bracket encoding, lines 35-83)
  </read_first>
  <behavior>
    - CA single: 1% on the slice from 0 to 11079; the 12.3% band terminates at 1000000 and income over 1000000 is taxed at 13.3% (MHS surtax). MFJ/QSS surtax boundary sits inside the 11.3% band (891542..1485906) per the encoding rule.
    - NY single: top three brackets are 9.65% (to 5000000), 10.3% (to 25000000), 10.9% (over), present for all five statuses; MFJ 5.25% ceiling is 27900 and 5.5% ceiling is 161550.
    - MA single: 5.0% up to 1083150, 9.0% (5% + 4% surtax) over 1083150, for all five statuses.
    - IL 0.0495 and PA 0.0307 unchanged. TX/FL/WA absent. stateTaxDataByYear keyed 2025.
  </behavior>
  <action>
    Edit state-tax-data.ts per the LOCKED decisions and the transcribed official 2025 values in `<official_2025_values>`:

    1. Re-key the table: change the `2024:` key to `2025:` (PAYSTUB-02 state side; the year set is now 2025).

    2. CALIFORNIA (PAYSTUB-06): replace all five CA filing-status arrays (lines 8-64) with the official 2025 FTB Schedule X/Y/Z upper limits in `<official_2025_values>`. Single + marriedSeparate use Schedule X; marriedJoint + qualifyingSurvivingSpouse use Schedule Y; headOfHousehold uses Schedule Z. Then encode the 1% MHS surtax over 1000000 as additional top bands per the CA MHS SURTAX ENCODING RULE (the concrete per-schedule tails are spelled out in `<official_2025_values>`). Keep each array strictly ascending in limit.

    3. NEW YORK (PAYSTUB-07): replace all five NY filing-status arrays (lines 65-116) with the official 2025 DTF IT-201-I upper limits in `<official_2025_values>`, INCLUDING the 9.65% (to 5000000), 10.3% (to 25000000), and 10.9% (over) high-income brackets. Single + marriedSeparate share one schedule; marriedJoint + qualifyingSurvivingSpouse share another; headOfHousehold has its own. No surtax band for NY.

    4. MASSACHUSETTS (PAYSTUB-08): replace the `MA: flatBrackets(0.0535)` line (119) with an explicit two-band structure for all five filing statuses: `{ limit: 1083150, rate: 0.05 }` then `{ limit: Infinity, rate: 0.09 }` (flat 5% plus the 4% surtax over 1083150 = 9% effective). Write the five per-status arrays inline OR add a small local two-band builder helper; do NOT reuse `flatBrackets` for MA. Leave `IL: flatBrackets(0.0495)` and `PA: flatBrackets(0.0307)` untouched.

    5. DELETE the three lines `TX: flatBrackets(0)`, `FL: flatBrackets(0)`, `WA: flatBrackets(0)` (lines 120-122) (PAYSTUB-04). Do NOT touch the `flatBrackets` factory or `getStateTaxBrackets`.

    6. ADD an exported `getSupportedIncomeTaxStateCodes(): string[]` (PAYSTUB-01 source) that unions `Object.keys` over every `Object.values(stateTaxDataByYear)` year table into a `Set<string>` and returns the spread array. After the deletions this equals CA, NY, IL, PA, MA.

    No `any`; derive types from the existing tables (`TaxData['federalBrackets']`). Tabs, single quotes, no semicolons (Biome).
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/business-website && grep -q '2025:' src/lib/paystub-calculator/state-tax-data.ts && grep -q 'limit: 11079' src/lib/paystub-calculator/state-tax-data.ts && grep -q 'limit: 742953' src/lib/paystub-calculator/state-tax-data.ts && grep -q 'rate: 0.133' src/lib/paystub-calculator/state-tax-data.ts && grep -q 'limit: 1000000' src/lib/paystub-calculator/state-tax-data.ts && grep -q 'rate: 0.0965' src/lib/paystub-calculator/state-tax-data.ts && grep -q 'rate: 0.109' src/lib/paystub-calculator/state-tax-data.ts && grep -q 'limit: 5000000' src/lib/paystub-calculator/state-tax-data.ts && grep -q 'limit: 1083150' src/lib/paystub-calculator/state-tax-data.ts && grep -q 'flatBrackets(0.0495)' src/lib/paystub-calculator/state-tax-data.ts && grep -q 'flatBrackets(0.0307)' src/lib/paystub-calculator/state-tax-data.ts && grep -q 'getSupportedIncomeTaxStateCodes' src/lib/paystub-calculator/state-tax-data.ts && ! grep -q 'flatBrackets(0)' src/lib/paystub-calculator/state-tax-data.ts && [ "$(grep -v '^[[:space:]]*//' src/lib/paystub-calculator/state-tax-data.ts | grep -c '0.0535')" -eq 0 ] && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Table keyed 2025: `grep -q '2025:'` matches; no `2024:` key remains.
    - CA single 1% ceiling 2025 value: `limit: 11079` present; CA 11.3% band ceiling `limit: 742953` present; CA top surtax band `rate: 0.133` present and `limit: 1000000` present.
    - NY high brackets present: `rate: 0.0965`, `rate: 0.109`, `limit: 5000000` all present.
    - MA corrected to 2025: `grep -v '^[[:space:]]*//' ... | grep -c '0.0535'` == 0 (comments excluded so prose cannot self-invalidate); `limit: 1083150` present.
    - IL/PA intact: `flatBrackets(0.0495)` and `flatBrackets(0.0307)` present.
    - TX/FL/WA flat-0 removed: `flatBrackets(0)` absent.
    - `getSupportedIncomeTaxStateCodes` exported.
    - `bun run typecheck` passes (no `any`, imports resolve).
  </acceptance_criteria>
  <done>state-tax-data.ts keyed 2025 holds official 2025 CA/NY/MA values with the CA MHS (over 1000000 -> 13.3%) and MA 4% (over 1083150 -> 9%) surtaxes encoded as top brackets and NY's 9.65/10.3/10.9 brackets; TX/FL/WA flat-0 removed; IL/PA unchanged; getSupportedIncomeTaxStateCodes() exported; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale nuqs URL -> form state | A bookmarked/shared `?state=AL` URL is restored into form state on mount; untrusted, user-controllable input that crosses into the calculation path. This plan tightens the data-derived selectable surface (`getSupportedIncomeTaxStateCodes`) that 11-03 wires into the dropdown and 11-04 uses to intersect the restored state (PAYSTUB-10). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Spoofing/Information disclosure | `state-tax-data.ts` presenting stale (2023/pre-2020) CA/NY/MA figures | mitigate | Replace CA/NY/MA with the official 2025 published values transcribed from primary government sources (FTB/DTF/Mass.gov) in 11-RESEARCH.md; CA MHS and MA surtaxes encoded as top brackets (Task 1). Value-assertion tests lock the corrected boundaries in 11-03. |
| T-11-04 | Tampering | `getIncomeTaxStates()` drifting from the bracket data (independent allow-list) | mitigate | Expose `getSupportedIncomeTaxStateCodes()` over `stateTaxDataByYear` (Task 1); 11-03 derives the income-tax state list from it and a bidirectional parity test prevents future drift. |
| T-11-SC | Tampering | npm/bun installs | accept | No package installs in this phase (RESEARCH "## Package Legitimacy Audit": Not applicable, zero new deps). |

Data-correctness / input-validation scope only. No auth, session, network, crypto, secret, or persistence surface. `Infinity` bracket limits live only in the static table and are never user-controllable.
</threat_model>

<verification>
- `bun run typecheck` green (no `any`, imports resolve).
- Grep gates above confirm 2025 re-key, official CA/NY/MA values, surtax top-bands, TX/FL/WA removal, IL/PA intact, helper exported.
- NOTE: the state-tax golden/parity/surtax unit tests are written in 11-03 (which owns the test files). Existing MA assertions at the old 0.0535 rate will be RED until 11-03 recomputes them to 0.05; expected at the Wave-1 boundary. Do NOT edit test files here.
</verification>

<success_criteria>
- Table keyed 2025; CA/NY/MA hold official 2025 values; CA MHS (over 1000000 -> 13.3%) and MA 4% (over 1083150 -> 9%) surtaxes encoded as top brackets; NY includes 9.65/10.3/10.9.
- IL 0.0495 and PA 0.0307 unchanged; TX/FL/WA flat-0 removed.
- `getSupportedIncomeTaxStateCodes()` exported and returns CA, NY, IL, PA, MA.
- `bun run typecheck` passes.
</success_criteria>

<output>
Create `.planning/phases/11-paystub-tax-accuracy/11-02-SUMMARY.md` when done. Record: the exact CA/NY/MA 2025 arrays written (so verification can diff against the official values), the surtax-encoding tail chosen for each CA schedule and MA, confirmation the table is keyed 2025 and TX/FL/WA were deleted, and the new `getSupportedIncomeTaxStateCodes` export so 11-03 can reference it.
</output>

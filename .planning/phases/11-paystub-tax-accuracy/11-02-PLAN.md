---
phase: 11-paystub-tax-accuracy
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/paystub/PaystubForm.tsx
  - src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx
  - src/app/(public)/tools/paystub-calculator/page.tsx
autonomous: true
requirements: [PAYSTUB-01, PAYSTUB-02]

must_haves:
  truths:
    - "The Tax Year dropdown offers only data-backed years (2024); the 2023 SelectItem no longer exists"
    - "The State Income Tax optgroup renders exactly the data-derived list (CA, NY, IL, PA, MA) via getIncomeTaxStates()"
    - "The paystub hero copy and page metadata no longer over-promise universal state coverage ('for any pay period' framing removed)"
    - "No em-dash or en-dash appears in any user-facing paystub string"
    - "The full gate chain (lint, typecheck, unit, build) passes after all Phase 11 edits"
  artifacts:
    - path: "src/components/paystub/PaystubForm.tsx"
      provides: "Tax Year dropdown rendered from getSupportedTaxYears(); no hardcoded 2023 item"
      contains: "getSupportedTaxYears"
    - path: "src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx"
      provides: "Softened ToolPageLayout description (no over-promise, no dash)"
      contains: "description="
    - path: "src/app/(public)/tools/paystub-calculator/page.tsx"
      provides: "Softened metadata.description (no over-promise, no dash, 120-160 chars)"
      contains: "description:"
  key_links:
    - from: "src/components/paystub/PaystubForm.tsx"
      to: "src/lib/paystub-calculator/tax-data.ts"
      via: "import getSupportedTaxYears"
      pattern: "getSupportedTaxYears"
    - from: "src/components/paystub/PaystubForm.tsx"
      to: "src/lib/paystub-calculator/states-utils.ts"
      via: "getIncomeTaxStates().map render in the State Income Tax optgroup"
      pattern: "getIncomeTaxStates"
---

<objective>
Wire the data-derived helpers from Plan 01 into the form UI and soften the over-promising marketing copy. Remove the dead 2023 year `SelectItem`, render the Tax Year dropdown from `getSupportedTaxYears()`, let the State Income Tax optgroup auto-narrow to the derived list, and adjust the hero/metadata copy so it no longer implies universal coverage now that state support is explicitly 5 states. End with the full Phase 11 gate chain.

Purpose: Make the visible inputs match the data Plan 01 locked down, and stop the page copy from advertising coverage the calculator does not have. This plan owns the form component and the two UI-copy files (zero file overlap with Plan 01).
Output: Updated `PaystubForm.tsx` (data-driven year dropdown), softened copy in `PaystubCalculatorClient.tsx` + `page.tsx`, and a green `bun run lint && bun run typecheck && bun run test:unit && bun run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md
@.planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md
@.planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md
@.planning/phases/11-paystub-tax-accuracy/11-01-SUMMARY.md

<interfaces>
<!-- Verified contracts the executor needs. Extracted from the codebase 2026-06-01. -->

From src/lib/paystub-calculator/tax-data.ts (created in Plan 01):
- `getSupportedTaxYears(): number[]` — returns data-backed years sorted descending (today: [2024])

From src/lib/paystub-calculator/states-utils.ts:
- `getIncomeTaxStates(): { value: string; label: string }[]` — now derives from the bracket data (CA, NY, IL, PA, MA)
- `getNoIncomeTaxStates(): { value: string; label: string }[]` — UNCHANGED (8 no-tax states)

From src/components/paystub/PaystubForm.tsx (current):
- 'use client' file — CANNOT export metadata
- Tax Year `<Select>` at lines 185-204; hardcoded `<SelectItem value="2024">2024</SelectItem>` + `<SelectItem value="2023">2023</SelectItem>` (lines 200-201) inside `<SelectContent>` (line 199)
- onValueChange parses via `parseInt(value, 10)` and sets `paystubData.taxYear` (lines 189-194)
- State `<Select>` at lines 206-231 already maps `getNoIncomeTaxStates()` (lines 215-219) and `getIncomeTaxStates()` (lines 223-227) into `<SelectItem>` — the year render must mirror this `.map()` idiom
- Existing imports: `getIncomeTaxStates`, `getNoIncomeTaxStates` from `@/lib/paystub-calculator/states-utils` (lines 18-21)

From src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx (current):
- `description="Generate accurate payroll breakdowns with federal and state tax calculations for any pay period"` passed to `<ToolPageLayout>` (line 168) — over-promises "for any pay period" + implied all-state coverage

From src/app/(public)/tools/paystub-calculator/page.tsx (current, server component):
- `export const metadata: Metadata` with `description:` at line 13: `'Generate detailed payroll breakdowns with federal and state tax calculations. Free paystub calculator for employers and employees.'` (CLAUDE.md: keep 120-160 chars for SEO)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the Tax Year dropdown from getSupportedTaxYears(); remove the dead 2023 item (PAYSTUB-02)</name>
  <files>src/components/paystub/PaystubForm.tsx</files>
  <read_first>
    - src/components/paystub/PaystubForm.tsx (file being modified; Tax Year Select lines 185-204; State optgroup .map idiom lines 213-228; imports lines 9-23)
    - .planning/phases/11-paystub-tax-accuracy/11-01-SUMMARY.md (confirms getSupportedTaxYears export name + that getIncomeTaxStates now derives CA/NY/IL/PA/MA)
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (PAYSTUB-02: remove 2023, every offered year must be data-backed, lines 32-35)
    - .planning/phases/11-paystub-tax-accuracy/11-PATTERNS.md (year-render target mirroring the state .map idiom, lines 212-251)
  </read_first>
  <action>
    Edit the Tax Year `<Select>` in `PaystubForm.tsx` (per CONTEXT.md PAYSTUB-02, mirroring the existing State optgroup `.map()` render):

    1. Add `import { getSupportedTaxYears } from '@/lib/paystub-calculator/tax-data'` alongside the existing `states-utils` import block (lines 18-21), following the import-ordering convention (paystub utils together).
    2. REPLACE the two hardcoded `<SelectItem value="2024">2024</SelectItem>` and `<SelectItem value="2023">2023</SelectItem>` lines (200-201) inside the Tax Year `<SelectContent>` with `{getSupportedTaxYears().map(y => (<SelectItem key={y} value={String(y)}>{y}</SelectItem>))}` — exactly mirroring the `getIncomeTaxStates().map(...)` render in the State optgroup (lines 223-227). The `onValueChange` `parseInt(value, 10)` handler is unchanged.
    3. Leave the State `<Select>` (lines 206-231) UNCHANGED — its `getIncomeTaxStates()` call auto-narrows to the derived CA/NY/IL/PA/MA list via Plan 01.

    Optional (CONTEXT.md:52 discretion): a short, dash-free note under the State `<Select>` such as a plain-text line "State income tax supported for CA, NY, IL, PA, MA." If added, it must contain NO em-dash or en-dash. Skip if it complicates layout; not required for any acceptance criterion.

    Tabs, single quotes, no semicolons (Biome). 'use client' file — do not add a metadata export here.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'value="2023"' src/components/paystub/PaystubForm.tsx` returns nothing (2023 item removed)
    - `grep -n "getSupportedTaxYears" src/components/paystub/PaystubForm.tsx` shows the import + the `.map()` render
    - `bun run typecheck && bun run lint` pass
    - No new em-dash/en-dash: `grep -nP "[\x{2013}\x{2014}]" src/components/paystub/PaystubForm.tsx` returns nothing
  </acceptance_criteria>
  <done>The Tax Year dropdown renders only data-backed years from `getSupportedTaxYears()` (just 2024); the hardcoded 2023 item is gone; the State optgroup auto-renders the derived 5-state list; typecheck + lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Soften over-promising paystub copy; run the full Phase 11 gate chain (PAYSTUB-01 honesty + verification)</name>
  <files>src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx, src/app/(public)/tools/paystub-calculator/page.tsx</files>
  <read_first>
    - src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx (file being modified; description prop at line 168)
    - src/app/(public)/tools/paystub-calculator/page.tsx (file being modified; metadata.description at line 13; server component)
    - .planning/phases/11-paystub-tax-accuracy/11-CONTEXT.md (UI copy decision: do not over-promise now coverage is limited, line 47-48)
    - .planning/phases/11-paystub-tax-accuracy/11-RESEARCH.md (Pitfall 5 dash ban + exact current strings, lines 202-204)
    - CLAUDE.md (NO em/en-dash in user-facing text; metadata description 120-160 chars; 'use client' cannot export metadata)
  </read_first>
  <action>
    Two copy edits, then the phase verification gate:

    1. In `PaystubCalculatorClient.tsx` (line 168): REPLACE the `description=` string `"Generate accurate payroll breakdowns with federal and state tax calculations for any pay period"` with a softer, honest, dash-free string that does NOT over-promise universal state coverage or "any pay period" universality. Keep it concise and accurate to the actual scope (federal tax for all filers; state income tax for the 5 supported states). Example direction (final wording is discretion, must be dash-free): "Generate payroll breakdowns with federal tax and state income tax for supported states." Do not introduce any em-dash (U+2014) or en-dash (U+2013).

    2. In `page.tsx` (line 13, server `metadata.description`): REPLACE the description with a softer, dash-free string in the 120-160 character SEO range that drops the implied universal coverage while keeping it descriptive (e.g. "Generate detailed payroll breakdowns with federal tax and state income tax for supported states. Free paystub calculator for employers and employees."). Confirm length stays 120-160 chars. The `'use client'` restriction does not apply here (page.tsx is the server component that owns metadata).

    3. Run the full Phase 11 gate chain and confirm green: `bun run lint && bun run typecheck && bun run test:unit && bun run build`. The `bun run build` step is required because tax-data.ts edits fan out to federal/SS/Medicare and the paystub page is statically built; a green build proves no regression reached the rendered output.
  </action>
  <verify>
    <automated>bun run lint && bun run typecheck && bun run test:unit && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "for any pay period" src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx` returns nothing (over-promise removed)
    - The new `page.tsx` metadata.description is 120-160 characters (verify by reading the string length)
    - No em-dash/en-dash in either file: `grep -nP "[\x{2013}\x{2014}]" "src/app/(public)/tools/paystub-calculator/PaystubCalculatorClient.tsx" "src/app/(public)/tools/paystub-calculator/page.tsx"` returns nothing
    - `bun run lint && bun run typecheck && bun run test:unit && bun run build` all pass (full gate chain green)
  </acceptance_criteria>
  <done>Both the hero description and the page metadata are honest about scope and contain no em/en-dash; the full Phase 11 gate chain (lint + typecheck + unit + build) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale nuqs URL -> form select state | A shared `?year=2023` URL restores `taxYear: 2023` into form state; the Tax Year `<Select>` now has no matching `SelectItem`, and validation (Plan 01) rejects it. The boundary is the same untrusted-URL surface; this plan ensures the UI side of it cannot offer an unbacked value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering/Information disclosure | Tax Year `<Select>` offering a value with no backing data | mitigate | The dropdown is rendered from `getSupportedTaxYears()` (Task 1), so it can only ever offer data-backed years. A stale `?year=2023` restored into state has no matching option and is rejected by `validatePaystubInputs` (Plan 01, T-11-01); it degrades to a validation error, never a silent fake figure. |
| T-11-05 | Information disclosure (misleading UI copy) | Hero/metadata advertising coverage the calculator lacks | mitigate | Soften the copy (Task 2) so it no longer implies universal state coverage now that support is explicitly 5 states; prevents the UI from making a false promise about what it computes. |

Input-validation / honest-output scope only. No auth, network, secret, or persistence surface is touched. Reuses the existing shadcn `Select` primitive; introduces no new UI primitive.
</threat_model>

<verification>
- `bun run lint` — Biome clean (no dash violations, formatting)
- `bun run typecheck` — no `any`, imports resolve (getSupportedTaxYears from Plan 01)
- `bun run test:unit` — full suite green (parity + rejected-year from Plan 01 still pass)
- `bun run build` — production build green (paystub page renders, no federal-calc regression in static output)

## Manual smoke (operator, pre-PR — not blocking automated gates)
- Load `/tools/paystub-calculator?year=2023&state=AL` in dev: confirm it falls back to a supported default or shows a clear validation error, never a silent $0 or a 2024-as-2023 figure (RESEARCH Open Q1 / A4). The Plan 01 validation + this plan's data-driven dropdown together make this non-crashing.
- Read the rendered page: hero + metadata copy no longer over-promise and contain no em/en-dash.
</verification>

<success_criteria>
- Tax Year dropdown offers only 2024; no 2023 item in the DOM
- State Income Tax optgroup lists exactly CA, NY, IL, PA, MA
- Hero + metadata copy honest about coverage, dash-free, metadata 120-160 chars
- `bun run lint && bun run typecheck && bun run test:unit && bun run build` green
</success_criteria>

<output>
Create `.planning/phases/11-paystub-tax-accuracy/11-02-SUMMARY.md` when done. Record the final copy strings (both files) with their character counts, confirmation the 2023 item is removed and years render from `getSupportedTaxYears()`, and the gate-chain result.
</output>

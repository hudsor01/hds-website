---
phase: 34-update-references
plan: 01
type: execute
---

<objective>
Update all internal links from /portfolio and /case-studies to /showcase.

Purpose: Ensure consistent navigation to the new unified showcase route.
Output: All internal links updated, no broken references.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md

**Prerequisite:** Phase 33 complete (/showcase routes exist)

**Files likely needing updates:**
- src/components/layout/Navbar*.tsx
- src/components/layout/Footer.tsx
- src/app/page.tsx (homepage)
- src/app/testimonials/page.tsx
- src/app/pricing/page.tsx
- src/app/not-found.tsx
- src/app/global-not-found.tsx
- src/components/ui/card.tsx (if it has portfolio links)
- src/app/sitemap.ts
- Any other pages linking to portfolio or case-studies
</context>

<tasks>

<task type="auto">
  <name>Task 1: Find all portfolio/case-studies references</name>
  <files>None (search operation)</files>
  <action>
Search for all references to /portfolio and /case-studies in the codebase:

grep -r "/portfolio" src/ --include="*.tsx" --include="*.ts"
grep -r "/case-studies" src/ --include="*.tsx" --include="*.ts"
grep -r "case-studies" src/ --include="*.tsx" --include="*.ts"

Document all files that need updating.
  </action>
  <verify>Complete list of files with old route references</verify>
  <done>All references identified</done>
</task>

<task type="auto">
  <name>Task 2: Update navigation components</name>
  <files>src/components/layout/Navbar*.tsx, src/components/layout/Footer.tsx</files>
  <action>
Update navigation links:
- Change /portfolio → /showcase
- Change /case-studies → /showcase
- Update link text if needed (e.g., "Portfolio" → "Showcase" or "Our Work")
  </action>
  <verify>Navigation links point to /showcase</verify>
  <done>Navigation updated</done>
</task>

<task type="auto">
  <name>Task 3: Update all other references</name>
  <files>Various (based on Task 1 findings)</files>
  <action>
Update all remaining references:
- Homepage CTAs and featured projects links
- Pricing page work examples
- Testimonials page project links
- Not-found pages suggestions
- Sitemap entries
- Any component Card links

Replace:
- href="/portfolio" → href="/showcase"
- href="/portfolio/${slug}" → href="/showcase/${slug}"
- href="/case-studies" → href="/showcase"
- href="/case-studies/${slug}" → href="/showcase/${slug}"
  </action>
  <verify>grep -r "/portfolio\|/case-studies" src/ returns no results (except old route files)</verify>
  <done>All references updated</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] No references to /portfolio or /case-studies remain (except route files to be deleted)
- [ ] Navigation links work correctly
- [ ] `bun run typecheck` passes
- [ ] `bun run build` succeeds
</verification>

<success_criteria>
- All internal links point to /showcase
- Navigation consistent throughout site
- No broken links
- Ready for cleanup in Phase 35-36
</success_criteria>

<output>
After completion, create `.planning/phases/34-update-references/34-01-SUMMARY.md`
</output>

---
phase: 58-core-component-polish
plan: 04
type: execute
wave: 3
depends_on: [58-02, 58-03]
files_modified: []
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]

must_haves:
  truths:
    - "Full test suite passes with zero failures"
    - "Build succeeds with no TypeScript or Biome errors"
    - "User confirms button variants look polished — not generic shadcn defaults"
    - "User confirms navbar hover is neutral (muted), not amber fill"
    - "User confirms footer background matches the dark token — no hsl() regression"
    - "User confirms card default variant is visually distinct from page background"
  artifacts:
    - path: "tests/unit/components.test.tsx"
      provides: "36 passing assertions for COMP-01 through COMP-03"
      contains: "36 pass"
    - path: "tests/unit/navigation.test.tsx"
      provides: "25 passing assertions for COMP-04"
      contains: "25 pass"
  key_links:
    - from: "src/components/ui/button.tsx"
      to: "src/app/page.tsx"
      via: "Button variant='accent' CTA in hero — must still render correctly"
      pattern: "variant.*accent"
    - from: "src/components/layout/Footer.tsx"
      to: "src/app/globals.css"
      via: "bg-background-dark token class"
      pattern: "background-dark"
---

<objective>
Automated verification followed by a human visual checkpoint. Confirm all COMP requirements are met, full test suite is green, build is clean, and the component polish is visually confirmed in the browser.

Purpose: Close Phase 58 with confident quality gate — automated tests confirm structural correctness; human check confirms visual quality meets Resend/Linear/Clerk bar.
Output: Phase 58 verified and closed.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/58-core-component-polish/58-02-SUMMARY.md
@.planning/phases/58-core-component-polish/58-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification suite</name>
  <files></files>
  <action>
Run the full automated verification sequence. All checks must pass before the checkpoint task.

1. Full unit test suite:
   ```bash
   bun test /Users/richard/Developer/business-website/tests/
   ```
   Expected: All tests pass. Zero failures. Specifically confirm:
   - `components.test.tsx`: 36 pass (24 original + 12 new COMP-01/02/03)
   - `navigation.test.tsx`: 25 pass (21 original + 4 new COMP-04)

2. TypeScript check:
   ```bash
   bun run typecheck
   ```
   Expected: Zero errors.

3. Biome lint:
   ```bash
   bun run lint
   ```
   Expected: Zero errors or warnings.

4. Production build:
   ```bash
   bun run build
   ```
   Expected: Build succeeds. Check that `bg-surface-raised`, `bg-surface-sunken`, `bg-background-dark`, `border-border-subtle` are recognized (no unknown utility warnings that cause build failure).

5. Grep safety checks — confirm anti-patterns are gone:
   ```bash
   grep -n "hover:bg-accent" /Users/richard/Developer/business-website/src/components/layout/Navbar.tsx
   ```
   Expected: Zero matches (all nav link hover states should use hover:bg-muted now).

   ```bash
   grep -n "backgroundColor" /Users/richard/Developer/business-website/src/components/layout/Footer.tsx
   ```
   Expected: Zero matches (inline style hack removed).

   ```bash
   grep -n "shadow-xs" /Users/richard/Developer/business-website/src/components/ui/card.tsx
   ```
   Expected: Zero matches in the default variant (shadow-xs removed, shadow-sm added).

If any check fails, fix the issue before proceeding to the checkpoint. Document any fallbacks used (e.g., bracket syntax instead of direct token utility) in the SUMMARY.
  </action>
  <verify>
    <automated>bun test /Users/richard/Developer/business-website/tests/ 2>&1 | tail -5</automated>
    <manual>All grep checks return zero matches for the removed patterns.</manual>
    <sampling_rate>all checks must pass before checkpoint is presented to user</sampling_rate>
  </verify>
  <done>
    - Full test suite: zero failures
    - TypeScript: zero errors
    - Biome: zero violations
    - Build: succeeds
    - Anti-pattern greps: all return zero matches
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Component polish for all four COMP requirements:
    - COMP-01: Button variants — default/accent/destructive/success now have shadow-sm + active states; ghost remains text-only
    - COMP-02: Input has aria-invalid error state matching textarea; bg-surface-sunken for visual depth
    - COMP-03: Card default variant uses bg-surface-raised + shadow-sm + border-border-subtle — visually distinct from page background
    - COMP-04: Navbar hover uses neutral bg-muted (not amber fill); footer uses bg-background-dark token class
  </what-built>
  <how-to-verify>
    Run `bun run dev` in the project directory and open http://localhost:3000

    **Check 1 — Buttons (COMP-01):**
    Visit the homepage. The "Start Shipping Faster" CTA in the navbar uses `variant="default"`. Hover it — should have a subtle shadow and responsive hover. The hero "Get My Free Roadmap" uses `variant="accent"` (amber). Both should look polished — not flat shadcn defaults.
    Visit any page with a ghost button (secondary CTAs). Ghost should appear text-only at rest, with a subtle muted hover.

    **Check 2 — Navbar hover (COMP-04):**
    Hover over the "Services", "Portfolio", "Tools", "About", "Pricing" nav links. They should show a NEUTRAL muted background on hover — NOT an amber/orange fill. The active page link (whichever page you're on) keeps its amber accent indicator.

    **Check 3 — Footer background (COMP-04):**
    Scroll to the footer. The footer background should be dark (matching the design token). Inspect element in DevTools — the `<footer>` element should have `bg-background-dark` class and NO inline `style` attribute with `backgroundColor`.

    **Check 4 — Card surface (COMP-03):**
    Visit /services or any page with default cards. Default cards should appear slightly elevated from the page background — a subtle but visible distinction. Glass cards (testimonials, pricing) should be unchanged.

    **Check 5 — Form inputs (COMP-02):**
    Visit /contact — the form inputs should appear slightly sunken (bg-surface-sunken). If you inspect DevTools, the `<input>` element should have `bg-surface-sunken` class and `aria-invalid:border-destructive` class present in its className string.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 58 is complete when:
- All 61 new tests pass (36 in components.test.tsx + 25 in navigation.test.tsx)
- Build is clean
- User approves visual quality in browser
- REQUIREMENTS.md updated: COMP-01, COMP-02, COMP-03, COMP-04 marked [x]
</verification>

<success_criteria>
- COMP-01: Button polish confirmed — shadow, active states, variant differentiation
- COMP-02: Input/textarea error state parity — aria-invalid classes on both; sunken surface
- COMP-03: Card default visually distinct — surface-raised, shadow-sm
- COMP-04: Navbar neutral hover — no amber fill; footer uses design token
- 61 total tests pass across components.test.tsx and navigation.test.tsx
- User visual sign-off obtained
- Phase 59 (Tool Page Polish) unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/58-core-component-polish/58-04-SUMMARY.md`

Also update:
- `.planning/REQUIREMENTS.md` — mark COMP-01, COMP-02, COMP-03, COMP-04 as [x]
- `.planning/STATE.md` — Phase 58 complete
- `.planning/ROADMAP.md` — Phase 58 plans count updated
</output>

---
phase: 58-core-component-polish
plan: 02
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/label.tsx
autonomous: true
requirements: [COMP-01, COMP-02]

must_haves:
  truths:
    - "Button primary (default) variant has shadow-sm and active state classes"
    - "Button accent variant has shadow-sm and active state classes"
    - "Button ghost variant is visually distinct — no solid background at rest"
    - "Input has aria-invalid error state classes matching textarea"
    - "Input uses bg-surface-sunken for visual depth"
    - "Textarea maintains its existing error state — no regression"
    - "Label duplicate 'use client' directive removed"
    - "All 12 COMP-01/02 tests pass (green)"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Polished button variants — default, accent, ghost, destructive, success, outline, secondary, muted, link"
      contains: "shadow-sm"
    - path: "src/components/ui/input.tsx"
      provides: "Input with error state parity — aria-invalid:border-destructive, aria-invalid:ring-destructive/20, bg-surface-sunken"
      contains: "aria-invalid:border-destructive"
    - path: "src/components/ui/textarea.tsx"
      provides: "Textarea unchanged — existing aria-invalid classes confirmed intact"
      contains: "aria-invalid:border-destructive"
    - path: "src/components/ui/label.tsx"
      provides: "Label with duplicate 'use client' removed"
      contains: "flex items-center gap-tight"
  key_links:
    - from: "tests/unit/components.test.tsx"
      to: "src/components/ui/button.tsx"
      via: "render(Button) + className assertion"
      pattern: "shadow-sm.*active:bg-primary"
    - from: "tests/unit/components.test.tsx"
      to: "src/components/ui/input.tsx"
      via: "render(Input) + className assertion"
      pattern: "aria-invalid:border-destructive"
---

<objective>
Polish button.tsx CVA variant strings and input.tsx base class string to satisfy COMP-01 and COMP-02 requirements. Zero API changes — purely CSS class updates within existing `cva()` definitions.

Purpose: Turn the RED assertions from Plan 01 GREEN for buttons and form primitives. No callsite changes needed.
Output: button.tsx with shadow/active polish on all variants; input.tsx with error state parity and sunken surface; label.tsx without duplicate directive.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/58-core-component-polish/58-RESEARCH.md
@.planning/phases/58-core-component-polish/58-01-SUMMARY.md
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/textarea.tsx
@src/components/ui/label.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish button.tsx CVA variants (COMP-01)</name>
  <files>src/components/ui/button.tsx</files>
  <action>
Update the `buttonVariants` CVA definition in `src/components/ui/button.tsx`. No prop API changes, no new variants, no callsite changes.

**Changes to the `variant` map:**

1. `default` — add `shadow-sm`, `active:bg-primary/70`, strengthen focus ring:
   ```
   default: 'bg-primary text-primary-foreground shadow-sm hover:bg-primary/80 active:bg-primary/70 focus-visible:ring-primary/30',
   ```

2. `destructive` — add `shadow-sm`, `active:bg-destructive/80` (keep existing dark mode ring classes):
   ```
   destructive: 'bg-destructive text-destructive-foreground shadow-sm hover:bg-destructive/80 active:bg-destructive/75 focus-visible:ring-destructive/20 dark:focus-visible:ring-destructive/40 dark:bg-destructive/60',
   ```

3. `success` — add `shadow-sm`, `active:` state:
   ```
   success: 'bg-success text-success-foreground shadow-sm hover:bg-success-dark active:bg-success-dark/90',
   ```

4. `accent` — add `shadow-sm`, `active:bg-accent/75`:
   ```
   accent: 'bg-accent text-accent-foreground shadow-sm hover:bg-accent/85 active:bg-accent/75',
   ```

5. `outline` — add `active:bg-accent/10` (keep existing classes):
   ```
   outline: 'border border-border bg-background text-foreground shadow-xs hover:bg-accent hover:text-accent-foreground active:bg-accent/15 dark:bg-input/30 dark:border-input dark:hover:bg-input/50',
   ```

6. `secondary` — add `shadow-xs`, `active:bg-secondary/70`:
   ```
   secondary: 'bg-secondary text-secondary-foreground shadow-xs hover:bg-secondary/80 active:bg-secondary/70',
   ```

7. `muted` — add `active:bg-muted/70`:
   ```
   muted: 'bg-muted text-muted-foreground hover:bg-muted/80 active:bg-muted/70',
   ```

8. `ghost` — no change needed (already has no solid bg at rest, has hover:bg-muted/50). COMP-01 test verifies ghost has no bg-primary/bg-accent/bg-secondary.

9. `link` — no change needed.

**Do not change:** The base `cva()` string, `size` variants, `defaultVariants`, component props, or the component render function.

After editing, run Biome to ensure no formatting violations: `bunx biome check --write /Users/richard/Developer/business-website/src/components/ui/button.tsx`
  </action>
  <verify>
    <automated>bun test /Users/richard/Developer/business-website/tests/unit/components.test.tsx 2>&1 | grep -E "Button Polish|pass|fail"</automated>
    <manual>The 5 "Button Polish — COMP-01" describe block tests must now pass. Run full suite to check no regressions in existing Button tests.</manual>
    <sampling_rate>run after this task, before Task 2</sampling_rate>
  </verify>
  <done>
    - All 5 Button Polish assertions pass (shadow-sm present, active states present, ghost has no solid bg)
    - All original 24 components.test.tsx tests still pass
    - `bun run lint` clean
    - `bun run typecheck` clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish input.tsx error state + surface token; clean label.tsx duplicate directive (COMP-02)</name>
  <files>src/components/ui/input.tsx, src/components/ui/label.tsx</files>
  <action>
**File 1: src/components/ui/input.tsx**

Update the `inputVariants` base class string (the first argument to `cva()`). The current base string is:
```
'flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-muted-foreground focus-visible:outline-hidden focus-visible:ring-2 focus-visible:ring-ring disabled:cursor-not-allowed disabled:opacity-50'
```

Replace with (one continuous string — no line breaks inside the template literal):
- Replace `bg-background` with `bg-surface-sunken` (adds visual depth — sunken surface for inputs)
- Add after the `focus-visible:ring-ring` class: `aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive`

Resulting base string:
```
'flex h-10 w-full rounded-md border border-input bg-surface-sunken px-3 py-2 text-sm file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-muted-foreground focus-visible:outline-hidden focus-visible:ring-2 focus-visible:ring-ring aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive disabled:cursor-not-allowed disabled:opacity-50'
```

Do not change the variant definitions (`default: ''`, `currency: 'pl-7'`), props, the wrapper div, or the currency span.

**Verify `bg-surface-sunken` is a valid Tailwind v4 utility:** globals.css has `--color-surface-sunken` in `@theme {}` — Tailwind v4 auto-generates `bg-surface-sunken`. If the build fails due to unknown class, fall back to `bg-[var(--color-surface-sunken)]` bracket syntax and note this in the SUMMARY.

---

**File 2: src/components/ui/label.tsx**

The file has two `'use client'` directives (lines 1 and 3 — a copy-paste artifact per the research). Remove the duplicate. The file should have exactly one `'use client'` at the top, followed by the imports.

After editing both files: `bunx biome check --write /Users/richard/Developer/business-website/src/components/ui/input.tsx /Users/richard/Developer/business-website/src/components/ui/label.tsx`
  </action>
  <verify>
    <automated>bun test /Users/richard/Developer/business-website/tests/unit/components.test.tsx 2>&1 | grep -E "Input Polish|pass|fail"</automated>
    <manual>All 4 "Input Polish — COMP-02" tests pass. Confirm textarea COMP-02 parity test also passes (it already has aria-invalid — should be green already). Confirm no regressions in FloatingInput or FloatingTextarea tests.</manual>
    <sampling_rate>run after this task and before Plan 03 begins</sampling_rate>
  </verify>
  <done>
    - All 12 new COMP-01/02 assertions pass (green)
    - All original 24 tests still pass — total 36 passing
    - input.tsx has `bg-surface-sunken` (or fallback bracket syntax) and three `aria-invalid:*` classes
    - label.tsx has exactly one 'use client' directive
    - `bun run lint` clean
    - `bun run typecheck` clean
  </done>
</task>

</tasks>

<verification>
Run full component test suite after both tasks complete:

```bash
bun test /Users/richard/Developer/business-website/tests/unit/components.test.tsx
```

Expected result: 36 pass, 0 fail (24 original + 12 new COMP-01/02 blocks).

Also run:
```bash
bun run /Users/richard/Developer/business-website typecheck
bun run /Users/richard/Developer/business-website lint
```
Both must be clean.
</verification>

<success_criteria>
- Button: `shadow-sm` present on default, accent, destructive, success variants; `active:*` classes on all solid variants
- Input: `aria-invalid:border-destructive`, `aria-invalid:ring-destructive/20`, `dark:aria-invalid:ring-destructive/40` present; `bg-surface-sunken` replaces `bg-background`
- Textarea: existing `aria-invalid:*` classes intact (no regression)
- Label: exactly one `'use client'` directive
- 36/36 tests pass in components.test.tsx
- TypeScript and Biome both clean
</success_criteria>

<output>
After completion, create `.planning/phases/58-core-component-polish/58-02-SUMMARY.md`
</output>

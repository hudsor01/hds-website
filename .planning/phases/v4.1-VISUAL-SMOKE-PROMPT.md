# v4.1 Visual Smoke Test — Copy-Paste Prompt

Paste this into a fresh Claude session (browser-enabled or with mailbox/dev access) OR walk through it manually. Each section has a clear pass criterion. Mark the checkbox when verified.

---

You are verifying PR #164 (`feat/v4.1-nextjs16-brand-consistency`) on the Hudson Digital Solutions website. The PR shipped 5 phases: brand-token codegen, React-PDF migration, React Email v6 migration, Cache Components, and `after()` for fire-and-forget side effects.

The brand colour is **slate blue `#064180`** (oklch(0.38 0.12 255) — derived from `--color-primary` in `src/app/globals.css`). The pre-PR brand was cyan `#0891b2` / `#06b6d4` / `#0e7490` — verify NONE of those appear anywhere.

## Setup

```bash
cd /Users/richard/Developer/business-website
git checkout feat/v4.1-nextjs16-brand-consistency
bun install
bun run brand:generate              # confirm regenerates src/lib/_generated/brand.ts cleanly
bun run dev                          # if port 3000 is busy: bun run dev -- -p 3002
```

The dev server needs `POSTGRES_URL` (and optionally `RESEND_API_KEY` for live email tests) in `.env.local`.

---

## Phase 61 — Brand SoT codegen + error/meta/manifest

### 61-A: 404 page (`global-not-found.tsx`)
- [ ] Visit `http://localhost:3000/some-bogus-path-that-does-not-exist`
- [ ] **Expect:** 404 page with the "Go Home" button in slate-blue `#064180` (NOT cyan), "Contact Us" outline button with slate-blue border, popular-page link colours all slate blue.
- [ ] **Pass:** No cyan anywhere. Page renders with Tailwind classes (no inline `style={{...}}`). Inspect element on the "Go Home" button — should have class `bg-primary`, not inline `backgroundColor: '#06b6d4'`.

### 61-B: Global error page (`global-error.tsx`)
- [ ] Trigger by editing any page temporarily: add `throw new Error('test')` at the top of `src/app/page.tsx` then visit `/`. Revert immediately after testing.
- [ ] **Expect:** Application Error page with "Try again" button slate blue, destructive icon (red), "Go home" secondary button.
- [ ] **Pass:** Brand-coherent. No cyan.

### 61-C: Meta theme-color
- [ ] On any page (e.g., `/`), View Page Source and find:
  ```html
  <meta name="theme-color" content="#064180"/>
  <meta name="msapplication-TileColor" content="#064180"/>
  ```
- [ ] **Pass:** Both meta tags use `#064180`, NOT `#0891b2`.

### 61-D: Mobile browser tab tint
- [ ] Open `/` in **mobile Safari (iOS)** or **Chrome Android** OR use Chrome DevTools device emulation (toggle device toolbar, select iPhone 14)
- [ ] **Expect:** Browser address bar / status bar tint is slate-blue (`#064180`).
- [ ] **Pass:** Tint matches design palette, not cyan.

### 61-E: Web App Manifest
- [ ] Visit `http://localhost:3000/manifest.json` (or open browser dev tools → Application → Manifest)
- [ ] **Expect:** `theme_color: "#064180"`, `background_color: "#fafaf9"`, plus a `_comment_theme_color` field documenting the contract.
- [ ] **Pass:** Both colours match BRAND tokens. PWA install prompt would now use slate blue.

---

## Phase 62 — React-PDF templates

For each PDF type, navigate to the relevant tool, fill the form with test data, click Generate (downloads or previews the PDF), and inspect.

### 62-A: Contract PDF
- [ ] Visit `/tools/contract-generator` → fill any test data → Download PDF
- [ ] **Expect:** Brand bar at top in slate blue, section headings in slate blue, neutral grey body text.
- [ ] **Pass:** Header and headings are `#064180`, NOT cyan.

### 62-B: Proposal PDF
- [ ] Visit `/tools/proposal-generator` → fill → Download PDF
- [ ] **Expect:** Same as contract. Section headers, dividers, accent rows in slate blue.
- [ ] **Pass:** No cyan anywhere.

### 62-C: Invoice PDF
- [ ] Visit `/tools/invoice-generator` → fill → Download PDF
- [ ] **Expect:** Header bar slate blue, total row top border slate blue, "Invoice #" heading slate blue. Stale `// cyan-600` code comments removed (won't be visible in PDF but check git diff).
- [ ] **Pass:** Slate blue throughout.

### 62-D: Paystub PDF (sanity check)
- [ ] Visit `/tools/paystub-calculator` → fill → Download PDF
- [ ] **Expect:** Generic neutral colours (white background, light grey table headers `#f0f0f0`). NO brand colour was applied here intentionally — paystub uses generic paper aesthetic.
- [ ] **Pass:** Renders cleanly, no brand-colour drift either way.

---

## Phase 63 — React Email v6 (8 templates)

Need `RESEND_API_KEY` set in `.env.local` for live sends. Each test triggers a real email; verify in **Apple Mail (macOS or iOS)** AND **Gmail web**. Outlook desktop is best-effort (Word renderer ignores gradients + flexbox; PR added solid bg fallbacks but you may see a flat header instead of a gradient).

### 63-A: Newsletter welcome
- [ ] Submit the newsletter form on the homepage (footer or anywhere it appears) with your real email address
- [ ] **Expect (Apple Mail / Gmail):** Subject "Welcome to Hudson Digital Solutions Newsletter". Body: heading "Welcome to Our Newsletter!" in slate blue. Bulleted list of "what you'll receive". Unsubscribe link in footer (slate blue). Footer with business contact info.
- [ ] **Pass:** Slate-blue brand throughout. No cyan. Unsubscribe URL contains `email=` query param matching your email.

### 63-B: Newsletter admin notification
- [ ] Same trigger as 63-A — admin notification arrives at `hello@hudsondigitalsolutions.com` shortly after.
- [ ] **Expect:** "New Newsletter Subscriber" heading slate blue, labelled-row layout (Email / Source / Subscribed at).
- [ ] **Pass:** Brand consistency.

### 63-C: Contact form admin notification
- [ ] Submit `/contact` form with realistic test data (firstName, lastName, email, message; optional: company, phone, service, budget, timeline)
- [ ] **Expect at admin inbox:** Subject `New Project Inquiry - <Name> (Score: NN)`. Body: "New Contact Form Submission" heading slate blue, contact info section, **lead intelligence section if score >= 1** (with category label like HIGH PRIORITY/QUALIFIED/NURTURE), message section (white-space: pre-wrap), footer with submission timestamp.
- [ ] **Pass:** All brand. Multi-line message text preserves line breaks.

### 63-D: Contact form welcome (sender)
- [ ] Same trigger as 63-C — welcome email sent to the address you submitted with.
- [ ] **Expect:** Multiple paragraphs separated by visible vertical gaps (this was the bug introduced + fixed in review iteration 4 — paragraph spacing must NOT be collapsed). Sequence content with `{{firstName}}` etc replaced with your data.
- [ ] **Pass:** Paragraphs visually separated, content reads naturally.

### 63-E: Testimonial admin notification
- [ ] Visit `/tools/testimonial-collector` (or however the public submit form is reached) → submit a test testimonial
- [ ] **Expect at admin:** "New Testimonial Received" slate blue, labelled-row layout (Name, Company, Role, Rating /5, Service, Submitted via, Submitted at), testimonial content in muted-bg box.
- [ ] **Pass:** Brand consistent.

### 63-F: Calculator admin notification
- [ ] Submit any calculator form (ROI, Cost Estimator, Performance, TTL) at `/tools/`
- [ ] **Expect at admin:** "New Calculator Lead" slate blue, labelled rows including Calculator type (NOT "Calculator" — should be the proper full name like "Texas TTL Calculator"; this was a review-iteration-1 fix).
- [ ] **Pass:** Calculator name correct.

### 63-G: Calculator results email (to user)
- [ ] Same trigger as 63-F — results email sent to the address provided
- [ ] **Expect:** **Gradient header** (slate-blue → darker slate). In Outlook desktop, you'll see a flat slate-blue header (gradient fallback to solid bg — added in review iteration 3). Value boxes for each result. "Next Steps" callout with slate-blue left border. CTA button "Schedule Free Consultation" slate blue.
- [ ] **Pass:** Modern clients show gradient; Outlook shows solid slate. Layout intact.

### 63-H: TTL calculator results email
- [ ] Submit `/tools/ttl-calculator` (or texas-ttl-calculator) with vehicle data + provide your email
- [ ] **Expect:** Gradient header (135deg slate → primaryDeep). Vehicle Details section. **TTL Breakdown rendered as a 2-column table** (label left, value right) — NOT flexbox (which Outlook would have broken). Total TTL row in slate blue. Monthly Payment box (solid slate-blue background, white text). View Full Results CTA button.
- [ ] **Pass:** Two-column layout aligns correctly even in Outlook desktop. Total TTL prominent.

### 63-I: Scheduled drip campaign
- [ ] Trigger a drip dispatch (admin/cron endpoint OR simulate by inserting a row in `scheduled_emails` with `scheduled_for` in the past, then call `/api/process-emails` with the `CRON_SECRET` Bearer token)
- [ ] **Expect:** Multi-block email with proper paragraph rendering. **Bullets render as bullets** (this was a pre-existing bug — `\\n\\n` literal escapes — fixed in review iteration 1). **Headings (`**bold**` markdown) render in slate blue**, not as literal asterisks. Signature with email link slate blue. Unsubscribe link in footer.
- [ ] **Pass:** Markdown-like content parses correctly into structured blocks.

---

## Phase 64 — Cache Components

These pages use `'use cache'` directive + `cacheLife()` + `cacheTag()` in their data layer. Build mode (`bun run build && bun run start`) will show their cache classification.

### 64-A: Build output classification
- [ ] Run `bun run build` — at the end of the route table, look for the legend.
- [ ] **Expect:** Three categories present: `○ (Static)`, `◐ (Partial Prerender)`, `ƒ (Dynamic)`. The `/blog/[slug]`, `/portfolio/[slug]`, `/showcase`, `/help/[category]/[slug]` should be Static or Partial Prerender (not Dynamic).
- [ ] **Pass:** Build completes; expected pages are marked Static or Partial Prerender.

### 64-B: Blog list page
- [ ] Visit `/blog` (after dev or prod server start)
- [ ] **Expect:** Posts render correctly. Refresh — second load is faster (cached).
- [ ] **Pass:** No 500 errors. Content renders.

### 64-C: Blog post detail
- [ ] Visit `/blog/<a-real-slug>`
- [ ] **Expect:** Post content with sanitized HTML (DOMPurify still runs but inside the cached `BlogPostContent` component). The `prose-cyan` class was REMOVED — prose accents (link colours, blockquote borders) should now use design tokens, not cyan.
- [ ] **Pass:** Content renders, no cyan in prose links.

### 64-D: Showcase page
- [ ] Visit `/showcase`
- [ ] **Expect:** Showcase grid renders. The Suspense fallback may flash briefly on first load.
- [ ] **Pass:** Items render.

### 64-E: Portfolio detail
- [ ] Visit `/portfolio/<a-real-slug>`
- [ ] **Expect:** Project page renders. Note: previously had `export const revalidate = 3600` (now removed; data layer caches via `cacheLife('days')` for items, `cacheLife('hours')` for lists). Fallback gradient (when no `gradientClass` in DB row) should be `from-primary to-foreground` (slate-blue → near-black), NOT `from-cyan-500 to-blue-600`.
- [ ] **Pass:** Brand-coherent fallback gradient.

### 64-F: Help category page
- [ ] Visit `/help/<a-category-slug>` (e.g. `/help/getting-started`)
- [ ] **Expect:** Category info + article list. Render works without errors.
- [ ] **Pass:** Renders.

### 64-G: Help article page
- [ ] Visit `/help/<category>/<article-slug>`
- [ ] **Expect:** Article markdown rendered. Previously was `force-dynamic` (now removed; data layer caches with `cacheTag('help-articles', \`help-article:${slug}\`)`).
- [ ] **Pass:** Renders, no force-dynamic visible in network tab.

### 64-H: Testimonial submit-via-token (Suspense boundary)
- [ ] Need a real token from the DB. Either create one via `createTestimonialRequest` in `src/lib/testimonials.ts` OR use admin tools. Then visit `/testimonials/submit/<token>`.
- [ ] **Expect:** Initial spinner (`<Suspense fallback={<LoadingFallback />}>` — added in Phase 64 because of Cache Components requirement), then either the form (valid token), "Already Submitted" (used token), "Link Expired" (past expiration), or 404 (invalid token).
- [ ] **Pass:** Suspense fallback renders briefly; correct branch shown.

### 64-I: Cache invalidation — double-submit guard
- [ ] Submit a valid testimonial via `/testimonials/submit/<token>` once → should succeed.
- [ ] Try to submit AGAIN with the same token → should fail with "This link has already been used".
- [ ] **Pass:** Second submission blocked. (This was a critical fix — the `revalidateTag(token, 'max')` call after `markRequestSubmitted` purges the cached `submitted: false` state.)

---

## Phase 65 — `after()` for side effects

### 65-A: Contact form response time
- [ ] Open Chrome DevTools Network tab. Submit `/contact` form. Look at the timing for the POST to `/api/contact`.
- [ ] **Expect:** Total response time ≤ 200–300ms. Previously the response waited for: admin email + welcome email + lead notifications (Slack/Discord) + follow-up scheduling. All now deferred to `after()` post-response. The response is just the success ack.
- [ ] **Pass:** Response is visibly faster than the wall-clock time it takes for the welcome email to arrive in your inbox.

### 65-B: Newsletter form response time
- [ ] Same approach for `/api/newsletter/subscribe` — response should return before the welcome and admin notification emails complete.
- [ ] **Pass:** Sub-second response.

### 65-C: Calculator submit response time
- [ ] Submit any calculator. The user-facing response includes `lead_id`, `lead_score`, `lead_quality` and returns immediately. The 4 deferred side effects (results email, admin email, Slack/Discord, follow-up scheduling) run after.
- [ ] **Pass:** Sub-second response.

### 65-D: Web vitals beacon
- [ ] Open any page. The web vitals reporter automatically POSTs to `/api/web-vitals` after the page loads. In DevTools Network, the response should be 204 with empty body.
- [ ] **Pass:** Beacon fires; response immediate.

---

## Code-quality spot checks (no browser needed)

```bash
# Brand cyan eliminated entirely
grep -rnE "#0891b2|#06b6d4|#0e7490|cyan-[0-9]" src/   # → ZERO matches

# All emails use react: prop
grep -rn "html: '\|html: \`" src/app/api/ src/app/actions/ src/lib/scheduled-emails.tsx src/lib/contact-service.tsx   # → ZERO

# No dead @react-email/render
grep -rn "@react-email/render" src/ package.json   # → ZERO

# Codegen + tests + build all green
bun run brand:generate
bun run typecheck
bun run lint
bun run test:unit       # → 403 / 0
bun run build           # → succeeds with cacheComponents enabled
```

---

## Final sign-off

When all checkboxes above are checked (or items genuinely don't apply because the relevant flow isn't reachable in your environment), the PR is verified end-to-end and ready to merge.

If you find any issue not covered by the four review iterations already applied, post the finding back into the conversation and the developer will iterate.

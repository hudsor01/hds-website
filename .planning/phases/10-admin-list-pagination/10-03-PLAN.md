---
phase: 10-admin-list-pagination
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/lib/admin/blog-queries.ts
  - src/app/(admin)/admin/blog/page.tsx
autonomous: true
requirements: [P10]
must_haves:
  truths:
    - "listBlogPostsForAdmin accepts an options object { q?, cursor?, direction? }; positional-args call sites no longer exist (no compat shim)."
    - "Return shape is { rows: AdminBlogListRow[], hasMore, prevCursor, nextCursor }; tag-id lookup still runs for ONLY the page slice (not the full table)."
    - "Search matches blog_posts.title OR slug OR excerpt via case-insensitive ILIKE with %/_ escaped."
    - "The page renders <SearchInput> + <Pagination>; PublishToggle per row keeps working byte-equal."
    - "Empty-q-result state shows a 'Clear search' link that drops q from the URL."
  artifacts:
    - path: src/lib/admin/blog-queries.ts
      provides: "Cursor-paginated + search-aware list helper for admin blog"
      exports: ["listBlogPostsForAdmin"]
      contains: "hasMore"
    - path: src/app/(admin)/admin/blog/page.tsx
      provides: "Admin blog list with search + pagination"
      contains: "SearchInput"
  key_links:
    - from: "src/app/(admin)/admin/blog/page.tsx"
      to: "src/lib/admin/blog-queries.ts::listBlogPostsForAdmin"
      via: "listBlogPostsForAdmin({ q, cursor })"
      pattern: "listBlogPostsForAdmin\\("
---
> **API ADDENDUM (2026-05-27, post-Wave-1):** SearchInput was rewritten to use `nuqs` (`useQueryState`) after Wave 1 shipped. The current API is `<SearchInput placeholder="..." />` ONLY. Drop any `baseHref`, `q`, or `preservedParams` props from your SearchInput JSX. Read `/tmp/v5-audit/src/components/admin/SearchInput.tsx` and `/tmp/v5-audit/tests/unit/admin/search-input.test.tsx` to confirm. `Pagination` props are unchanged (still `baseHref`, `prevCursor`, `nextCursor`, `preservedParams`). Pages still read `q` from `searchParams` server-side and pass it to the query helper; only the CLIENT-side input is now nuqs-driven. See `@.planning/phases/10-admin-list-pagination/10-CONTEXT.md` §4 for the locked decision.

> **API ADDENDUM 2 (2026-05-27, shadcn-first revision):** The custom `src/components/admin/Pagination.tsx` wrapper was deleted. Wave 2 pages compose shadcn `<Pagination>` primitives directly from `@/components/ui/pagination` (Pagination, PaginationContent, PaginationItem, PaginationPrevious, PaginationNext). Hrefs come from `buildPaginationHref(baseHref, cursor, preservedParams)` exported from `@/lib/admin/list-cursor`. Disabled state on Prev/Next: pass `aria-disabled="true" className="pointer-events-none opacity-50"` instead of a href. See `10-01-SUMMARY.md` -> "Pagination -- composed directly from shadcn primitives" for the canonical JSX block. **ALSO:** Replace raw `<table>` / `<thead>` / `<tbody>` / `<tr>` / `<th>` / `<td>` markup with shadcn `<Table>` / `<TableHeader>` / `<TableBody>` / `<TableRow>` / `<TableHead>` / `<TableCell>` from `@/components/ui/table`. Drop the per-page `overflow-x-auto rounded-xl border border-border bg-surface-raised` div wrapper -- shadcn `<Table>` already wraps in `<div className="relative w-full overflow-auto">`. Drop the per-page `<thead>` className -- shadcn `<TableHeader>` styles the bottom border. Drop the per-column `<th scope="col" className="px-4 py-3 ...">` -- shadcn `<TableHead>` handles padding + alignment. Keep `<caption className="sr-only">` content; render it via `<TableCaption>` if you keep it (or move to an aria-label on `<Table>`).




<objective>
Convert `/admin/blog` from "list every post sorted by (publishedAt DESC NULLS LAST, createdAt DESC)" to a cursor-paginated + searchable list. The helper joins blog_authors and lazy-loads tag IDs per post; both behaviors are preserved, but the tag-id lookup runs only for the post-id slice that will actually be returned (not the full table). Cursor key is `(publishedAt, createdAt, id)` with `NULLS LAST` semantics encoded into the comparison.

Output: One updated query helper, one updated page. One atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/10-admin-list-pagination/10-CONTEXT.md
@.planning/phases/10-admin-list-pagination/10-01-SUMMARY.md
@src/lib/admin/blog-queries.ts
@src/app/(admin)/admin/blog/page.tsx
@src/lib/admin/list-cursor.ts
@src/components/admin/SearchInput.tsx
@src/components/admin/Pagination.tsx
@src/lib/schemas/blog.ts

<interfaces>
New signature for src/lib/admin/blog-queries.ts:

```typescript
export type ListBlogPostsOptions = {
  q?: string
  cursor?: string
  direction?: Direction
}

export type ListBlogPostsResult = {
  rows: AdminBlogListRow[]
  hasMore: boolean
  prevCursor: string | null
  nextCursor: string | null
}

export async function listBlogPostsForAdmin(
  opts?: ListBlogPostsOptions
): Promise<ListBlogPostsResult>
```

Sort order: `(publishedAt DESC NULLS LAST, createdAt DESC, id ASC)`.
Cursor tuple: `[publishedAt-or-sentinel, createdAt.toISOString(), id]` where unpublished rows (publishedAt=null) get the sentinel `'\x00'` so they sort AFTER any real timestamp. Practically: encode `publishedAt?.toISOString() ?? ''` and treat empty string as the NULLS-LAST sentinel when decoding.

Search columns: `blogPosts.title`, `blogPosts.slug`, `blogPosts.excerpt`.

Page input:
```typescript
interface AdminBlogPageProps {
  searchParams: Promise<{ q?: string; cursor?: string }>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rewrite listBlogPostsForAdmin to options-object + cursor + search</name>
  <read_first>
    - src/lib/admin/blog-queries.ts (preserve every other export: getBlogPostForAdmin, createBlogPost, updateBlogPost, deleteBlogPost, toggleBlogPostPublished, loadTagIdsForPosts, computePublishedAtTransition — all byte-equal)
    - src/lib/admin/list-cursor.ts
    - src/lib/schemas/blog.ts (blogPosts.publishedAt is nullable timestamptz; createdAt has defaultNow)
    - src/lib/admin/showcase-queries.ts (Plan 10-02 output — copy the structural pattern of the new listShowcasesForAdmin)
  </read_first>
  <files>src/lib/admin/blog-queries.ts</files>
  <behavior>
    - listBlogPostsForAdmin() returns at most PAGE_SIZE rows joined with author and with tagIds populated.
    - loadTagIdsForPosts is called exactly once with the trimmed page slice of post ids (length ≤ PAGE_SIZE), not the full table.
    - q='foo' filters to posts whose title OR slug OR excerpt contains 'foo' (case-insensitive).
    - Cursor handles NULLS LAST: a row with publishedAt=null sorts AFTER any row with a real publishedAt. The cursor tuple uses the literal string '\x00' (or empty string — pick one and document) as the sentinel for null.
    - hasMore signaling and cursor encoding match Plan 10-02 contract.
    - DB error returns { rows: [], hasMore: false, prevCursor: null, nextCursor: null }; logger.error called.
  </behavior>
  <action>
    Step A (RED). Create `tests/unit/admin/blog-queries.test.ts` (new file, no existing test). Use the chainable-mock pattern from `tests/unit/showcase.test.ts`. Tests required:
      - returns hasMore=true and trims to PAGE_SIZE when DB yields PAGE_SIZE + 1.
      - empty result yields the zero shape.
      - q='foo' triggers an OR of three ilike predicates against (title, slug, excerpt).
      - loadTagIdsForPosts is called with the trimmed page-slice id list, not the full DB result (mock the inArray helper call to capture).
      - On DB error: returns the zero shape; logger.error called once.

    Step B (GREEN). Rewrite `listBlogPostsForAdmin` in `src/lib/admin/blog-queries.ts`. Updates:
      - Extend the drizzle-orm import: add `and, asc, eq, gt, ilike, lt, or` to the current `{ desc, eq, inArray, sql }`. Resulting line: `import { and, asc, desc, eq, gt, ilike, inArray, lt, or, sql } from 'drizzle-orm'`.
      - Add `import { PAGE_SIZE, type Direction, decodeCursor, encodeCursor, escapeLikePattern } from '@/lib/admin/list-cursor'`.
      - Replace the function body with the same control flow as Plan 10-02 listShowcasesForAdmin, but with these per-table differences:
        - Sort tuple is `(publishedAt, createdAt, id)` with NULLS LAST. Drizzle's `sql\`${blogPosts.publishedAt} desc nulls last\`` is the existing ORDER BY; the new helper keeps that for forward direction. For 'before' direction, flip to `sql\`${blogPosts.publishedAt} asc nulls first\`` plus `asc(blogPosts.createdAt), desc(blogPosts.id)`.
        - Cursor encoding: `encodeCursor('after', [row.publishedAt?.toISOString() ?? '', row.createdAt!.toISOString(), row.post.id])`. The empty string sentinel means "null". On decode, treat parts[0] === '' as null when building the comparison predicate.
        - Comparison predicate for forward direction (NULLS LAST):
          - When cursor parts[0] is a real timestamp:
            ```
            publishedAt IS NULL  -- nulls come after, so any null is "after" this row
            OR publishedAt < $1
            OR (publishedAt = $1 AND createdAt < $2)
            OR (publishedAt = $1 AND createdAt = $2 AND id > $3)
            ```
          - When cursor parts[0] is the empty sentinel (cursor row had null publishedAt):
            ```
            publishedAt IS NULL AND (createdAt < $2 OR (createdAt = $2 AND id > $3))
            ```
          For 'before' direction, invert each comparator and swap the NULL-handling branch.
          Express in drizzle with `and(...)`, `or(...)`, `eq`, `lt`, `gt`, `isNull`, `isNotNull`. Add `isNull, isNotNull` to the import line if used.
      - Tag lookup: after computing `pageRows` (the trimmed + display-ordered slice), call `loadTagIdsForPosts(pageRows.map(r => r.post.id))` (the existing helper is unchanged). Map results onto `pageRows` exactly as today.
      - Wrap the whole new body in try/catch returning the zero shape on error.

    KEEP byte-equal: `getBlogPostForAdmin`, `loadTagIdsForPosts`, `createBlogPost`, `updateBlogPost`, `deleteBlogPost`, `toggleBlogPostPublished`, `computePublishedAtTransition`.

    Step C. `bun test tests/unit/admin/blog-queries.test.ts` green. `bun run lint && bun run typecheck` clean.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/blog-queries.test.ts</automated>
  </verify>
  <done>listBlogPostsForAdmin returns the new shape with NULLS-LAST cursor handling; tests pass; non-list exports are byte-equal vs origin/main.</done>
</task>

<task type="auto">
  <name>Task 2: rewrite /admin/blog page to consume the new helper + render SearchInput + Pagination</name>
  <read_first>
    - src/app/(admin)/admin/blog/page.tsx (current)
    - src/app/(admin)/admin/showcase/page.tsx (Plan 10-02 output; copy the structural pattern)
    - src/components/admin/SearchInput.tsx, Pagination.tsx
    - src/components/admin/ResourceListPage.tsx
  </read_first>
  <files>src/app/(admin)/admin/blog/page.tsx</files>
  <action>
    Apply the same transformation as Plan 10-02 page rewrite, with these per-page strings:
      - Page interface: `interface AdminBlogPageProps { searchParams: Promise<{ q?: string; cursor?: string }> }`.
      - SearchInput placeholder: `Search posts`.
      - Empty-q-result copy: `No posts matching {q}.`.
      - Clear-search link: `href="/admin/blog"`.
      - SearchInput baseHref: `"/admin/blog"`. Pagination baseHref: `"/admin/blog"`.
      - ResourceListPage `emptyMessage="No posts yet. Create your first one."` (unchanged).
      - `isEmpty={rows.length === 0 && !q}`.
      - Keep `metadata` export byte-equal. Keep the existing `<Suspense>` wrapper + fallback string.
      - Keep the existing `<table>` block (every th/td/PublishToggle/Link) byte-equal. Keep `formatPublishedAt` + `dateFormatter` const declarations byte-equal.

    NO new tests in this task.

    Run `bun run lint && bun run typecheck`.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Page accepts searchParams Promise<{ q?, cursor? }>; SearchInput above, Pagination below; existing table + PublishToggle byte-equal; metadata untouched; lint + typecheck clean.</done>
</task>

</tasks>

<verification>
- `bun run lint && bun run typecheck` exit 0.
- `bun test tests/unit/admin/blog-queries.test.ts` passes.
- `grep -nE '—|–' src/lib/admin/blog-queries.ts src/app/\(admin\)/admin/blog/page.tsx` returns nothing.
- `git diff origin/main -- src/lib/admin/blog-queries.ts` confined to imports + listBlogPostsForAdmin body.
</verification>

<success_criteria>
- listBlogPostsForAdmin: options-object signature, NULLS LAST cursor, hasMore + prev/next cursors.
- /admin/blog: SearchInput + Pagination wired; existing table + PublishToggle byte-equal.
- One commit: `feat(10-03): cursor + search for /admin/blog`.
</success_criteria>

<output>
Create `.planning/phases/10-admin-list-pagination/10-03-SUMMARY.md`. Note the NULLS LAST sentinel value chosen and whether the tag-id lookup now runs over PAGE_SIZE ids instead of the full table.
</output>

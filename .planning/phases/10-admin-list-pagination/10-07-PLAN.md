---
phase: 10-admin-list-pagination
plan: 07
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/lib/admin/newsletter-queries.ts
  - src/app/(admin)/admin/newsletter/page.tsx
autonomous: true
requirements: [P10]
must_haves:
  truths:
    - "listSubscribersForAdmin accepts an options object { status?, q?, cursor?, direction? }."
    - "Return shape is { rows, hasMore, prevCursor, nextCursor }; sort is `subscribedAt DESC, id ASC`."
    - "Search matches newsletter_subscribers.email OR name (case-insensitive, %/_ escaped)."
    - "Existing status chip filter byte-equal; composes with q + cursor."
    - "Page renders <SearchInput> + existing <StatusFilterBar> + <Pagination>."
  artifacts:
    - path: src/lib/admin/newsletter-queries.ts
      provides: "Cursor-paginated + search-aware admin newsletter list helper"
      exports: ["listSubscribersForAdmin"]
      contains: "hasMore"
    - path: src/app/(admin)/admin/newsletter/page.tsx
      provides: "Admin newsletter list with status filter + search + pagination"
      contains: "SearchInput"
  key_links:
    - from: "src/app/(admin)/admin/newsletter/page.tsx"
      to: "src/lib/admin/newsletter-queries.ts::listSubscribersForAdmin"
      via: "listSubscribersForAdmin({ status, q, cursor })"
      pattern: "listSubscribersForAdmin\\("
---
> **API ADDENDUM (2026-05-27, post-Wave-1):** SearchInput was rewritten to use `nuqs` (`useQueryState`) after Wave 1 shipped. The current API is `<SearchInput placeholder="..." />` ONLY. Drop any `baseHref`, `q`, or `preservedParams` props from your SearchInput JSX. Read `/tmp/v5-audit/src/components/admin/SearchInput.tsx` and `/tmp/v5-audit/tests/unit/admin/search-input.test.tsx` to confirm. `Pagination` props are unchanged (still `baseHref`, `prevCursor`, `nextCursor`, `preservedParams`). Pages still read `q` from `searchParams` server-side and pass it to the query helper; only the CLIENT-side input is now nuqs-driven. See `@.planning/phases/10-admin-list-pagination/10-CONTEXT.md` §4 for the locked decision.

> **API ADDENDUM 2 (2026-05-27, shadcn-first revision):** The custom `src/components/admin/Pagination.tsx` wrapper was deleted. Wave 2 pages compose shadcn `<Pagination>` primitives directly from `@/components/ui/pagination` (Pagination, PaginationContent, PaginationItem, PaginationPrevious, PaginationNext). Hrefs come from `buildPaginationHref(baseHref, cursor, preservedParams)` exported from `@/lib/admin/list-cursor`. Disabled state on Prev/Next: pass `aria-disabled="true" className="pointer-events-none opacity-50"` instead of a href. See `10-01-SUMMARY.md` -> "Pagination -- composed directly from shadcn primitives" for the canonical JSX block. **ALSO:** Replace raw `<table>` / `<thead>` / `<tbody>` / `<tr>` / `<th>` / `<td>` markup with shadcn `<Table>` / `<TableHeader>` / `<TableBody>` / `<TableRow>` / `<TableHead>` / `<TableCell>` from `@/components/ui/table`. Drop the per-page `overflow-x-auto rounded-xl border border-border bg-surface-raised` div wrapper -- shadcn `<Table>` already wraps in `<div className="relative w-full overflow-auto">`. Drop the per-page `<thead>` className -- shadcn `<TableHeader>` styles the bottom border. Drop the per-column `<th scope="col" className="px-4 py-3 ...">` -- shadcn `<TableHead>` handles padding + alignment. Keep `<caption className="sr-only">` content; render it via `<TableCaption>` if you keep it (or move to an aria-label on `<Table>`).




<objective>
Convert `/admin/newsletter` from "200 newest subscribers, optional status filter" to cursor-paginated + searchable list. Existing status chip filter stays byte-equal. Cursor key is `(subscribedAt, id)`. Note: `subscribedAt` is nullable in the schema (`defaultNow()` only), so the cursor uses NULLS LAST handling like Plan 10-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/10-admin-list-pagination/10-CONTEXT.md
@.planning/phases/10-admin-list-pagination/10-01-SUMMARY.md
@.planning/phases/10-admin-list-pagination/10-05-SUMMARY.md
@.planning/phases/10-admin-list-pagination/10-03-SUMMARY.md
@src/lib/admin/newsletter-queries.ts
@src/app/(admin)/admin/newsletter/page.tsx
@src/components/admin/StatusFilterBar.tsx
@src/lib/admin/list-cursor.ts
@src/components/admin/SearchInput.tsx
@src/components/admin/Pagination.tsx
@src/lib/schemas/emails.ts

<interfaces>
```typescript
export type ListSubscribersOptions = {
  status?: SubscriberStatus | null
  q?: string
  cursor?: string
  direction?: Direction
}

export type ListSubscribersResult = {
  rows: SubscriberRow[]
  hasMore: boolean
  prevCursor: string | null
  nextCursor: string | null
}

export async function listSubscribersForAdmin(
  opts?: ListSubscribersOptions
): Promise<ListSubscribersResult>
```

Sort order: `(subscribedAt DESC NULLS LAST, id ASC)`.
Cursor tuple: `[subscribedAt?.toISOString() ?? '', id]` — empty string is the NULLS LAST sentinel (matches Plan 10-03 convention).
Search columns: `newsletterSubscribers.email`, `newsletterSubscribers.name`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rewrite listSubscribersForAdmin to options-object + cursor + search</name>
  <read_first>
    - src/lib/admin/newsletter-queries.ts (preserve every other export byte-equal: getSubscriberById, setSubscriberStatus, deleteSubscriber, SUBSCRIBER_STATUSES tuple)
    - src/lib/admin/blog-queries.ts (Plan 10-03 output — NULLS LAST cursor template)
    - src/lib/admin/leads-queries.ts (Plan 10-05 output — status + q + cursor combination template)
    - src/lib/schemas/emails.ts (newsletter_subscribers.subscribedAt is nullable timestamptz; email notNull, name nullable)
  </read_first>
  <files>src/lib/admin/newsletter-queries.ts</files>
  <behavior>
    - listSubscribersForAdmin() returns at most PAGE_SIZE rows newest-first by subscribedAt.
    - status='unsubscribed' applies eq(newsletterSubscribers.status, 'unsubscribed').
    - q='alice' filters to rows whose email OR name ILIKE the escaped pattern.
    - Cursor handles NULLS LAST exactly like Plan 10-03.
    - DB error returns the zero shape; logger.error called.
  </behavior>
  <action>
    Step A (RED). Create `tests/unit/admin/newsletter-queries.test.ts`:
      - returns hasMore=true, trims to PAGE_SIZE.
      - empty result yields zero shape.
      - status='unsubscribed' adds eq() predicate.
      - q='alice' triggers an OR of two ilike predicates against (email, name).
      - On DB error: zero shape; logger.error called.

    Step B (GREEN). Rewrite `listSubscribersForAdmin`:
      - Imports: extend to `import { and, asc, desc, eq, gt, ilike, isNull, isNotNull, lt, or, sql } from 'drizzle-orm'` (only the symbols actually used after the rewrite).
      - Add `import { PAGE_SIZE, type Direction, decodeCursor, encodeCursor, escapeLikePattern } from '@/lib/admin/list-cursor'`.
      - Cursor + sort template: reuse Plan 10-03's NULLS LAST handling but with `(subscribedAt, id)` as the 2-part tuple (NO createdAt in the middle — keep it simple).
      - Status + q + cursor compose via `and(...conditions)`.
      - Wrap in try/catch; return zero shape on error; `logger.error('newsletter-queries.listSubscribersForAdmin failed', error)`.

    KEEP byte-equal: getSubscriberById, setSubscriberStatus, deleteSubscriber, SUBSCRIBER_STATUSES + SubscriberStatus exports.

    Step C. `bun test tests/unit/admin/newsletter-queries.test.ts` green. Lint + typecheck.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/newsletter-queries.test.ts</automated>
  </verify>
  <done>Helper returns the new shape with NULLS-LAST cursor + status + q composing; tests pass; non-list exports byte-equal.</done>
</task>

<task type="auto">
  <name>Task 2: rewrite /admin/newsletter page to consume the new helper</name>
  <read_first>
    - src/app/(admin)/admin/newsletter/page.tsx (current)
    - src/app/(admin)/admin/leads/page.tsx (Plan 10-05 output — same status-aware param-preservation pattern)
  </read_first>
  <files>src/app/(admin)/admin/newsletter/page.tsx</files>
  <action>
    Apply the Plan 10-05 transformation with these per-page details:
      - Page interface: `interface AdminNewsletterPageProps { searchParams: Promise<{ status?: string; q?: string; cursor?: string }> }`.
      - Helper call: `listSubscribersForAdmin({ status, q: q || undefined, cursor })`.
      - SearchInput placeholder: `Search subscribers`.
      - Empty-q-result copy: `No subscribers matching {q}.`.
      - Clear-search link: `status ? '/admin/newsletter?status=' + status : '/admin/newsletter'` (build with a const).
      - SearchInput baseHref + Pagination baseHref: `"/admin/newsletter"`.

    KEEP byte-equal: metadata, FILTER_OPTIONS, Suspense fallback, page heading, the table block.

    NO new tests. Lint + typecheck.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Page accepts searchParams Promise<{ status?, q?, cursor? }>; StatusFilterBar byte-equal + SearchInput + Pagination wired; lint + typecheck clean.</done>
</task>

</tasks>

<verification>
- `bun run lint && bun run typecheck` exit 0.
- `bun test tests/unit/admin/newsletter-queries.test.ts` passes.
- `grep -nE '—|–' src/lib/admin/newsletter-queries.ts src/app/\(admin\)/admin/newsletter/page.tsx` returns nothing.
- `git diff origin/main -- src/lib/admin/newsletter-queries.ts` confined to imports + listSubscribersForAdmin body.
</verification>

<success_criteria>
- listSubscribersForAdmin: options-object signature with status + q + cursor + direction.
- /admin/newsletter: StatusFilterBar + SearchInput + Pagination all composing.
- One commit: `feat(10-07): cursor + search for /admin/newsletter`.
</success_criteria>

<output>
Create `.planning/phases/10-admin-list-pagination/10-07-SUMMARY.md`.
</output>

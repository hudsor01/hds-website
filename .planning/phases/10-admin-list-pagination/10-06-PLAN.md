---
phase: 10-admin-list-pagination
plan: 06
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/lib/admin/calculator-leads-queries.ts
  - src/app/(admin)/admin/leads/calculator/page.tsx
autonomous: true
requirements: [P10]
must_haves:
  truths:
    - "listCalculatorLeadsForAdmin accepts an options object { quality?, q?, cursor?, direction? }."
    - "Return shape is { rows, hasMore, prevCursor, nextCursor }."
    - "Search matches calculator_leads.email OR name (case-insensitive, %/_ escaped)."
    - "Existing quality chip filter composes with q + cursor (URL param stays `status` per the existing convention; helper key is `quality` to match the DB column name)."
    - "Page renders <SearchInput> + existing <StatusFilterBar> + <Pagination>."
  artifacts:
    - path: src/lib/admin/calculator-leads-queries.ts
      provides: "Cursor-paginated + search-aware admin calculator-leads list helper"
      exports: ["listCalculatorLeadsForAdmin"]
      contains: "hasMore"
    - path: src/app/(admin)/admin/leads/calculator/page.tsx
      provides: "Admin calculator-leads list with quality filter + search + pagination"
      contains: "SearchInput"
  key_links:
    - from: "src/app/(admin)/admin/leads/calculator/page.tsx"
      to: "src/lib/admin/calculator-leads-queries.ts::listCalculatorLeadsForAdmin"
      via: "listCalculatorLeadsForAdmin({ quality, q, cursor })"
      pattern: "listCalculatorLeadsForAdmin\\("
---
> **API ADDENDUM (2026-05-27, post-Wave-1):** SearchInput was rewritten to use `nuqs` (`useQueryState`) after Wave 1 shipped. The current API is `<SearchInput placeholder="..." />` ONLY. Drop any `baseHref`, `q`, or `preservedParams` props from your SearchInput JSX. Read `/tmp/v5-audit/src/components/admin/SearchInput.tsx` and `/tmp/v5-audit/tests/unit/admin/search-input.test.tsx` to confirm. `Pagination` props are unchanged (still `baseHref`, `prevCursor`, `nextCursor`, `preservedParams`). Pages still read `q` from `searchParams` server-side and pass it to the query helper; only the CLIENT-side input is now nuqs-driven. See `@.planning/phases/10-admin-list-pagination/10-CONTEXT.md` §4 for the locked decision.

> **API ADDENDUM 2 (2026-05-27, shadcn-first revision):** The custom `src/components/admin/Pagination.tsx` wrapper was deleted. Wave 2 pages compose shadcn `<Pagination>` primitives directly from `@/components/ui/pagination` (Pagination, PaginationContent, PaginationItem, PaginationPrevious, PaginationNext). Hrefs come from `buildPaginationHref(baseHref, cursor, preservedParams)` exported from `@/lib/admin/list-cursor`. Disabled state on Prev/Next: pass `aria-disabled="true" className="pointer-events-none opacity-50"` instead of a href. See `10-01-SUMMARY.md` -> "Pagination -- composed directly from shadcn primitives" for the canonical JSX block. **ALSO:** Replace raw `<table>` / `<thead>` / `<tbody>` / `<tr>` / `<th>` / `<td>` markup with shadcn `<Table>` / `<TableHeader>` / `<TableBody>` / `<TableRow>` / `<TableHead>` / `<TableCell>` from `@/components/ui/table`. Drop the per-page `overflow-x-auto rounded-xl border border-border bg-surface-raised` div wrapper -- shadcn `<Table>` already wraps in `<div className="relative w-full overflow-auto">`. Drop the per-page `<thead>` className -- shadcn `<TableHeader>` styles the bottom border. Drop the per-column `<th scope="col" className="px-4 py-3 ...">` -- shadcn `<TableHead>` handles padding + alignment. Keep `<caption className="sr-only">` content; render it via `<TableCaption>` if you keep it (or move to an aria-label on `<Table>`).




<objective>
Convert `/admin/leads/calculator` from "200 newest rows, optional quality filter" to cursor-paginated + searchable list. Existing quality chip filter (`?status=hot|warm|cold` per the existing URL convention) stays byte-equal and composes alongside SearchInput + Pagination. Cursor key is `(createdAt, id)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/10-admin-list-pagination/10-CONTEXT.md
@.planning/phases/10-admin-list-pagination/10-01-SUMMARY.md
@.planning/phases/10-admin-list-pagination/10-05-SUMMARY.md
@src/lib/admin/calculator-leads-queries.ts
@src/app/(admin)/admin/leads/calculator/page.tsx
@src/components/admin/StatusFilterBar.tsx
@src/lib/admin/list-cursor.ts
@src/components/admin/SearchInput.tsx
@src/components/admin/Pagination.tsx
@src/lib/schemas/leads.ts

<interfaces>
```typescript
export type ListCalculatorLeadsOptions = {
  quality?: CalculatorLeadQuality | null
  q?: string
  cursor?: string
  direction?: Direction
}

export type ListCalculatorLeadsResult = {
  rows: CalculatorLeadRow[]
  hasMore: boolean
  prevCursor: string | null
  nextCursor: string | null
}

export async function listCalculatorLeadsForAdmin(
  opts?: ListCalculatorLeadsOptions
): Promise<ListCalculatorLeadsResult>
```

Sort order: `(createdAt DESC, id ASC)`.
Cursor tuple: `[createdAt.toISOString(), id]`.
Search columns: `calculatorLeads.email`, `calculatorLeads.name`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rewrite listCalculatorLeadsForAdmin to options-object + cursor + search</name>
  <read_first>
    - src/lib/admin/calculator-leads-queries.ts (preserve every other export byte-equal: getCalculatorLeadById, markCalculatorLeadContacted, markCalculatorLeadConverted, deleteCalculatorLead, the CALCULATOR_LEAD_QUALITIES tuple)
    - src/lib/admin/leads-queries.ts (Plan 10-05 output — structural template with a single-status filter)
    - src/lib/schemas/leads.ts (calculator_leads columns: email notNull, name nullable, leadQuality nullable text, createdAt defaultNow)
  </read_first>
  <files>src/lib/admin/calculator-leads-queries.ts</files>
  <behavior>
    - listCalculatorLeadsForAdmin() returns at most PAGE_SIZE rows newest-first.
    - quality='hot' applies eq(calculatorLeads.leadQuality, 'hot').
    - q='alice' filters to rows whose email OR name ILIKE the escaped pattern.
    - Cursor handling same as Plan 10-04.
    - DB error returns the zero shape; logger.error called.
  </behavior>
  <action>
    Step A (RED). Create `tests/unit/admin/calculator-leads-queries.test.ts` mirroring `tests/unit/admin/leads-queries.test.ts` shape:
      - returns hasMore=true, trims to PAGE_SIZE.
      - empty result yields the zero shape.
      - quality='hot' adds eq() predicate on leadQuality.
      - q='alice' triggers an OR of two ilike predicates against (email, name).
      - quality + q combine via and().
      - On DB error: zero shape; logger.error called.

    Step B (GREEN). Rewrite `listCalculatorLeadsForAdmin`:
      - Import line: replace `import { desc, eq } from 'drizzle-orm'` with `import { and, asc, desc, eq, gt, ilike, lt, or } from 'drizzle-orm'`.
      - Add `import { PAGE_SIZE, type Direction, decodeCursor, encodeCursor, escapeLikePattern } from '@/lib/admin/list-cursor'`.
      - Use the same 2-part cursor template from Plan 10-04 + 10-05; the quality filter slots in like the leads.status filter.
      - Wrap in try/catch returning zero shape; `logger.error('calculator-leads-queries.listCalculatorLeadsForAdmin failed', error)`.

    KEEP byte-equal: getCalculatorLeadById, markCalculatorLeadContacted, markCalculatorLeadConverted, deleteCalculatorLead, the CALCULATOR_LEAD_QUALITIES + CalculatorLeadQuality exports.

    Step C. `bun test tests/unit/admin/calculator-leads-queries.test.ts` green. Lint + typecheck.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/calculator-leads-queries.test.ts</automated>
  </verify>
  <done>Helper returns the new shape; tests pass; non-list exports byte-equal vs origin/main.</done>
</task>

<task type="auto">
  <name>Task 2: rewrite /admin/leads/calculator page to consume the new helper</name>
  <read_first>
    - src/app/(admin)/admin/leads/calculator/page.tsx (current)
    - src/app/(admin)/admin/leads/page.tsx (Plan 10-05 output)
  </read_first>
  <files>src/app/(admin)/admin/leads/calculator/page.tsx</files>
  <action>
    Apply the Plan 10-05 transformation with these per-page details:
      - Page interface: `interface AdminCalculatorLeadsPageProps { searchParams: Promise<{ status?: string; q?: string; cursor?: string }> }` (URL param remains `status` to match the existing filter convention; the helper key is `quality`).
      - Inside `CalculatorLeadsList`: parse `quality` exactly as today (from `rawStatus`), then add q + cursor parsing identical to Plan 10-05. Call:
        ```typescript
        const { rows, prevCursor, nextCursor } = await listCalculatorLeadsForAdmin({
          quality,
          q: q || undefined,
          cursor
        })
        ```
      - Preserved-params for SearchInput: `{ status: quality }` when quality is set.
      - Preserved-params for Pagination: include `status` (from quality) AND `q` when set. Per useBlockStatements, build with explicit-brace if statements.
      - SearchInput placeholder: `Search calculator leads`.
      - Empty-q-result copy: `No calculator leads matching {q}.`.
      - Clear-search link: `href={quality ? '/admin/leads/calculator?status=' + quality : '/admin/leads/calculator'}` (build with a const, do not inline a template literal in JSX).
      - SearchInput baseHref + Pagination baseHref: `"/admin/leads/calculator"`.

    KEEP byte-equal: metadata, FILTER_OPTIONS, Suspense fallback, page heading + "Back to leads" link, the full table block.

    NO new tests. Lint + typecheck.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Page accepts searchParams Promise<{ status?, q?, cursor? }>; StatusFilterBar byte-equal + SearchInput + Pagination wired; lint + typecheck clean.</done>
</task>

</tasks>

<verification>
- `bun run lint && bun run typecheck` exit 0.
- `bun test tests/unit/admin/calculator-leads-queries.test.ts` passes.
- `grep -nE '—|–' src/lib/admin/calculator-leads-queries.ts src/app/\(admin\)/admin/leads/calculator/page.tsx` returns nothing.
- `git diff origin/main -- src/lib/admin/calculator-leads-queries.ts` confined to imports + listCalculatorLeadsForAdmin body.
</verification>

<success_criteria>
- listCalculatorLeadsForAdmin: options-object signature with quality + q + cursor + direction.
- /admin/leads/calculator: quality chip + SearchInput + Pagination all composing.
- One commit: `feat(10-06): cursor + search for /admin/leads/calculator`.
</success_criteria>

<output>
Create `.planning/phases/10-admin-list-pagination/10-06-SUMMARY.md`.
</output>

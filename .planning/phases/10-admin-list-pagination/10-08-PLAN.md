---
phase: 10-admin-list-pagination
plan: 08
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/lib/admin/emails-queries.ts
  - src/app/(admin)/admin/emails/page.tsx
autonomous: true
requirements: [P10]
must_haves:
  truths:
    - "listScheduledEmailsForAdmin accepts an options object { status?, q?, cursor?, direction? }."
    - "Return shape is { rows, hasMore, prevCursor, nextCursor }; sort is `scheduledFor DESC, id ASC`."
    - "Search matches scheduled_emails.recipientEmail OR recipientName OR stepId (case-insensitive, %/_ escaped)."
    - "Stat-card grid at the top of the page stays UNFILTERED — counts always reflect the full queue regardless of status / q / cursor."
    - "Existing status chip filter byte-equal; composes with q + cursor."
    - "Page renders stat cards + <StatusFilterBar> + <SearchInput> + <Pagination>."
  artifacts:
    - path: src/lib/admin/emails-queries.ts
      provides: "Cursor-paginated + search-aware admin scheduled-emails list helper"
      exports: ["listScheduledEmailsForAdmin"]
      contains: "hasMore"
    - path: src/app/(admin)/admin/emails/page.tsx
      provides: "Admin emails list with stat cards (unfiltered) + status filter + search + pagination"
      contains: "SearchInput"
  key_links:
    - from: "src/app/(admin)/admin/emails/page.tsx"
      to: "src/lib/admin/emails-queries.ts::listScheduledEmailsForAdmin"
      via: "listScheduledEmailsForAdmin({ status, q, cursor })"
      pattern: "listScheduledEmailsForAdmin\\("
    - from: "src/app/(admin)/admin/emails/page.tsx"
      to: "src/lib/admin/emails-queries.ts::getQueueCounts"
      via: "getQueueCounts() — called without any filter to keep stat cards reflecting full queue"
      pattern: "getQueueCounts\\(\\)"
---
> **API ADDENDUM (2026-05-27, post-Wave-1):** SearchInput was rewritten to use `nuqs` (`useQueryState`) after Wave 1 shipped. The current API is `<SearchInput placeholder="..." />` ONLY. Drop any `baseHref`, `q`, or `preservedParams` props from your SearchInput JSX. Read `/tmp/v5-audit/src/components/admin/SearchInput.tsx` and `/tmp/v5-audit/tests/unit/admin/search-input.test.tsx` to confirm. `Pagination` props are unchanged (still `baseHref`, `prevCursor`, `nextCursor`, `preservedParams`). Pages still read `q` from `searchParams` server-side and pass it to the query helper; only the CLIENT-side input is now nuqs-driven. See `@.planning/phases/10-admin-list-pagination/10-CONTEXT.md` §4 for the locked decision.

> **API ADDENDUM 2 (2026-05-27, shadcn-first revision):** The custom `src/components/admin/Pagination.tsx` wrapper was deleted. Wave 2 pages compose shadcn `<Pagination>` primitives directly from `@/components/ui/pagination` (Pagination, PaginationContent, PaginationItem, PaginationPrevious, PaginationNext). Hrefs come from `buildPaginationHref(baseHref, cursor, preservedParams)` exported from `@/lib/admin/list-cursor`. Disabled state on Prev/Next: pass `aria-disabled="true" className="pointer-events-none opacity-50"` instead of a href. See `10-01-SUMMARY.md` -> "Pagination -- composed directly from shadcn primitives" for the canonical JSX block. **ALSO:** Replace raw `<table>` / `<thead>` / `<tbody>` / `<tr>` / `<th>` / `<td>` markup with shadcn `<Table>` / `<TableHeader>` / `<TableBody>` / `<TableRow>` / `<TableHead>` / `<TableCell>` from `@/components/ui/table`. Drop the per-page `overflow-x-auto rounded-xl border border-border bg-surface-raised` div wrapper -- shadcn `<Table>` already wraps in `<div className="relative w-full overflow-auto">`. Drop the per-page `<thead>` className -- shadcn `<TableHeader>` styles the bottom border. Drop the per-column `<th scope="col" className="px-4 py-3 ...">` -- shadcn `<TableHead>` handles padding + alignment. Keep `<caption className="sr-only">` content; render it via `<TableCaption>` if you keep it (or move to an aria-label on `<Table>`).




<objective>
Convert `/admin/emails` from "100 newest rows, optional status filter" to cursor-paginated + searchable list. The 4 queue-health stat cards stay UNFILTERED — `getQueueCounts()` is byte-equal and called without arguments. Existing status chip filter stays byte-equal. Cursor key is `(scheduledFor, id)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/10-admin-list-pagination/10-CONTEXT.md
@.planning/phases/10-admin-list-pagination/10-01-SUMMARY.md
@.planning/phases/10-admin-list-pagination/10-05-SUMMARY.md
@src/lib/admin/emails-queries.ts
@src/app/(admin)/admin/emails/page.tsx
@src/components/admin/StatusFilterBar.tsx
@src/lib/admin/list-cursor.ts
@src/components/admin/SearchInput.tsx
@src/components/admin/Pagination.tsx
@src/lib/schemas/emails.ts

<interfaces>
```typescript
export type ListScheduledEmailsOptions = {
  status?: EmailStatus | null
  q?: string
  cursor?: string
  direction?: Direction
}

export type ListScheduledEmailsResult = {
  rows: ScheduledEmail[]
  hasMore: boolean
  prevCursor: string | null
  nextCursor: string | null
}

export async function listScheduledEmailsForAdmin(
  opts?: ListScheduledEmailsOptions
): Promise<ListScheduledEmailsResult>
```

Sort order: `(scheduledFor DESC, id ASC)`. `scheduledFor` is `.notNull()` in the schema (see src/lib/schemas/emails.ts), so NO NULLS LAST handling needed — straight 2-part cursor like Plan 10-04 / 10-05.
Cursor tuple: `[scheduledFor.toISOString(), id]`.
Search columns: `scheduledEmails.recipientEmail`, `scheduledEmails.recipientName`, `scheduledEmails.stepId`. recipientName is nullable; ilike on a NULL column returns NULL (not true), so the OR predicate naturally skips null-name rows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rewrite listScheduledEmailsForAdmin to options-object + cursor + search</name>
  <read_first>
    - src/lib/admin/emails-queries.ts (preserve every other export byte-equal: getQueueCounts, getScheduledEmailById, retryScheduledEmail, cancelScheduledEmail, deleteScheduledEmail, EMAIL_STATUSES, QueueCounts, RetryResult)
    - src/lib/admin/leads-queries.ts (Plan 10-05 output — status + q + cursor template)
    - src/lib/schemas/emails.ts (scheduled_emails: recipientEmail notNull, recipientName nullable, stepId notNull, scheduledFor notNull, sentAt nullable, status nullable defaults to 'pending')
  </read_first>
  <files>src/lib/admin/emails-queries.ts</files>
  <behavior>
    - listScheduledEmailsForAdmin() returns at most PAGE_SIZE rows newest-first by scheduledFor.
    - status='failed' applies eq(scheduledEmails.status, 'failed').
    - q='abc' filters to rows whose recipientEmail OR recipientName OR stepId ILIKE the escaped pattern.
    - Cursor handling: straight 2-part (Plan 10-04 template) — scheduledFor is notNull, no sentinel.
    - DB error returns the zero shape; logger.error called.
    - getQueueCounts() is NOT changed (and must keep its existing call signature so the page can call it without args to keep counts unfiltered).
  </behavior>
  <action>
    Step A (RED). Create `tests/unit/admin/emails-queries.test.ts`:
      - returns hasMore=true, trims to PAGE_SIZE.
      - empty result yields zero shape.
      - status='failed' adds eq() predicate.
      - q='abc' triggers an OR of three ilike predicates against (recipientEmail, recipientName, stepId).
      - status + q combine via and().
      - On DB error: zero shape; logger.error called.
      - getQueueCounts is NOT touched: assert by importing it AND asserting its function.length and the return key set are identical to the current implementation (sanity: `typeof getQueueCounts === 'function'` and `getQueueCounts.length === 0`).

    Step B (GREEN). Rewrite `listScheduledEmailsForAdmin`:
      - Imports: extend to `import { and, asc, count, desc, eq, gt, ilike, lt, or } from 'drizzle-orm'` (keep `count` for getQueueCounts).
      - Add `import { PAGE_SIZE, type Direction, decodeCursor, encodeCursor, escapeLikePattern } from '@/lib/admin/list-cursor'`.
      - Use Plan 10-05 (leads) template with the per-table column substitutions above.
      - Wrap in try/catch returning zero shape; `logger.error('emails-queries.listScheduledEmailsForAdmin failed', error)`.
      - The function is no longer `$dynamic()`-based; conditions are composed up front. Remove the `.$dynamic()` call from the body.

    KEEP byte-equal: getQueueCounts, getScheduledEmailById, retryScheduledEmail, cancelScheduledEmail, deleteScheduledEmail, EMAIL_STATUSES, QueueCounts, RetryResult, EmailStatus.

    Step C. `bun test tests/unit/admin/emails-queries.test.ts` green. Lint + typecheck.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/emails-queries.test.ts</automated>
  </verify>
  <done>Helper returns the new shape with status + q + cursor composing; tests pass; getQueueCounts + all other exports byte-equal vs origin/main.</done>
</task>

<task type="auto">
  <name>Task 2: rewrite /admin/emails page to consume the new helper + keep stat cards unfiltered</name>
  <read_first>
    - src/app/(admin)/admin/emails/page.tsx (current)
    - src/app/(admin)/admin/leads/page.tsx (Plan 10-05 output)
  </read_first>
  <files>src/app/(admin)/admin/emails/page.tsx</files>
  <action>
    Apply the Plan 10-05 transformation with these per-page details + the unfiltered-stats requirement:
      - Page interface: `interface AdminEmailsPageProps { searchParams: Promise<{ status?: string; q?: string; cursor?: string }> }`.
      - Inside `EmailsList`: parse status as today, then parse q + cursor.
      - Helper calls: change from `Promise.all([getQueueCounts(), listScheduledEmailsForAdmin(status, 100)])` to `Promise.all([getQueueCounts(), listScheduledEmailsForAdmin({ status, q: q || undefined, cursor })])`. CRITICAL: `getQueueCounts()` is called with NO arguments so the stat cards stay reflecting the full queue regardless of q / status / cursor.
      - SearchInput placeholder: `Search emails`.
      - Empty-q-result copy: `No emails matching {q}.`.
      - Clear-search link: `status ? '/admin/emails?status=' + status : '/admin/emails'`.
      - SearchInput baseHref + Pagination baseHref: `"/admin/emails"`.
      - Render order:
        ```jsx
        <>
          {/* existing 4-card stat grid — byte-equal, uses `counts` */}
          <StatusFilterBar ... />  {/* byte-equal */}
          <SearchInput baseHref="/admin/emails" q={q} placeholder="Search emails" preservedParams={preservedForSearch} />
          {rows.length === 0 ? (empty-state / no-match-state) : (
            <>
              {/* existing <div className="overflow-x-auto ..."> table — byte-equal */}
              <Pagination ... />
            </>
          )}
        </>
        ```

    KEEP byte-equal: metadata, EmailsList outer Suspense fallback, the 4-card stat grid block (including its mapping over EMAIL_STATUSES and the `counts[s].toLocaleString('en-US')` rendering), the table block.

    The stat cards must remain ABOVE the StatusFilterBar so the operator gets queue health regardless of filter — same physical layout as today.

    NO new tests. Lint + typecheck.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Page accepts searchParams Promise<{ status?, q?, cursor? }>; stat cards UNFILTERED + StatusFilterBar byte-equal + SearchInput + Pagination wired; lint + typecheck clean.</done>
</task>

</tasks>

<verification>
- `bun run lint && bun run typecheck` exit 0.
- `bun test tests/unit/admin/emails-queries.test.ts` passes.
- `grep -nE '—|–' src/lib/admin/emails-queries.ts src/app/\(admin\)/admin/emails/page.tsx` returns nothing.
- `git diff origin/main -- src/lib/admin/emails-queries.ts` confined to imports + listScheduledEmailsForAdmin body. getQueueCounts / retryScheduledEmail / cancelScheduledEmail / deleteScheduledEmail bodies are byte-equal.
- `git diff origin/main -- src/app/api/process-emails/route.ts` is empty.
</verification>

<success_criteria>
- listScheduledEmailsForAdmin: options-object signature with status + q + cursor + direction.
- /admin/emails: stat cards unfiltered, StatusFilterBar + SearchInput + Pagination all composing.
- One commit: `feat(10-08): cursor + search for /admin/emails`.
</success_criteria>

<output>
Create `.planning/phases/10-admin-list-pagination/10-08-SUMMARY.md`. Explicitly confirm in the SUMMARY that getQueueCounts was NOT modified and that the stat cards visually reflect the full queue under filtered conditions.
</output>

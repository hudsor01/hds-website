---
phase: 10-admin-list-pagination
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/lib/admin/testimonials-queries.ts
  - src/app/(admin)/admin/testimonials/page.tsx
autonomous: true
requirements: [P10]
must_haves:
  truths:
    - "listTestimonialsForAdmin accepts an options object { q?, cursor?, direction? }."
    - "Return shape is { rows, hasMore, prevCursor, nextCursor }."
    - "Search matches testimonials.name OR company OR content via ILIKE (case-insensitive, %/_ escaped)."
    - "Page renders <SearchInput> + <Pagination>; PublishToggle on each row keeps working byte-equal."
    - "Empty-q-result state shows 'Clear search' link."
  artifacts:
    - path: src/lib/admin/testimonials-queries.ts
      provides: "Cursor-paginated + search-aware admin testimonials list helper"
      exports: ["listTestimonialsForAdmin"]
      contains: "hasMore"
    - path: src/app/(admin)/admin/testimonials/page.tsx
      provides: "Admin testimonials list with search + pagination"
      contains: "SearchInput"
  key_links:
    - from: "src/app/(admin)/admin/testimonials/page.tsx"
      to: "src/lib/admin/testimonials-queries.ts::listTestimonialsForAdmin"
      via: "listTestimonialsForAdmin({ q, cursor })"
      pattern: "listTestimonialsForAdmin\\("
---
> **API ADDENDUM (2026-05-27, post-Wave-1):** SearchInput was rewritten to use `nuqs` (`useQueryState`) after Wave 1 shipped. The current API is `<SearchInput placeholder="..." />` ONLY. Drop any `baseHref`, `q`, or `preservedParams` props from your SearchInput JSX. Read `/tmp/v5-audit/src/components/admin/SearchInput.tsx` and `/tmp/v5-audit/tests/unit/admin/search-input.test.tsx` to confirm. `Pagination` props are unchanged (still `baseHref`, `prevCursor`, `nextCursor`, `preservedParams`). Pages still read `q` from `searchParams` server-side and pass it to the query helper; only the CLIENT-side input is now nuqs-driven. See `@.planning/phases/10-admin-list-pagination/10-CONTEXT.md` §4 for the locked decision.

> **API ADDENDUM 2 (2026-05-27, shadcn-first revision):** The custom `src/components/admin/Pagination.tsx` wrapper was deleted. Wave 2 pages compose shadcn `<Pagination>` primitives directly from `@/components/ui/pagination` (Pagination, PaginationContent, PaginationItem, PaginationPrevious, PaginationNext). Hrefs come from `buildPaginationHref(baseHref, cursor, preservedParams)` exported from `@/lib/admin/list-cursor`. Disabled state on Prev/Next: pass `aria-disabled="true" className="pointer-events-none opacity-50"` instead of a href. See `10-01-SUMMARY.md` -> "Pagination -- composed directly from shadcn primitives" for the canonical JSX block. **ALSO:** Replace raw `<table>` / `<thead>` / `<tbody>` / `<tr>` / `<th>` / `<td>` markup with shadcn `<Table>` / `<TableHeader>` / `<TableBody>` / `<TableRow>` / `<TableHead>` / `<TableCell>` from `@/components/ui/table`. Drop the per-page `overflow-x-auto rounded-xl border border-border bg-surface-raised` div wrapper -- shadcn `<Table>` already wraps in `<div className="relative w-full overflow-auto">`. Drop the per-page `<thead>` className -- shadcn `<TableHeader>` styles the bottom border. Drop the per-column `<th scope="col" className="px-4 py-3 ...">` -- shadcn `<TableHead>` handles padding + alignment. Keep `<caption className="sr-only">` content; render it via `<TableCaption>` if you keep it (or move to an aria-label on `<Table>`).




<objective>
Convert `/admin/testimonials` from "list everything sorted by createdAt DESC" to cursor-paginated + searchable list. Helper signature changes; non-list exports stay byte-equal (the `deleteTestimonial` re-export from `@/lib/testimonials` is untouched).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/10-admin-list-pagination/10-CONTEXT.md
@.planning/phases/10-admin-list-pagination/10-01-SUMMARY.md
@.planning/phases/10-admin-list-pagination/10-02-SUMMARY.md
@src/lib/admin/testimonials-queries.ts
@src/app/(admin)/admin/testimonials/page.tsx
@src/lib/admin/list-cursor.ts
@src/components/admin/SearchInput.tsx
@src/components/admin/Pagination.tsx
@src/lib/schemas/content.ts

<interfaces>
```typescript
export type ListTestimonialsOptions = {
  q?: string
  cursor?: string
  direction?: Direction
}

export type ListTestimonialsResult = {
  rows: TestimonialRow[]
  hasMore: boolean
  prevCursor: string | null
  nextCursor: string | null
}

export async function listTestimonialsForAdmin(
  opts?: ListTestimonialsOptions
): Promise<ListTestimonialsResult>
```

Sort order: `(createdAt DESC, id ASC)`.
Cursor tuple: `[createdAt.toISOString(), id]` (2 parts).
Search columns: `testimonials.name`, `testimonials.company`, `testimonials.content`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rewrite listTestimonialsForAdmin to options-object + cursor + search</name>
  <read_first>
    - src/lib/admin/testimonials-queries.ts (preserve every other export byte-equal: getTestimonialById, createTestimonial, updateTestimonial, deleteTestimonial re-export, toggleTestimonialPublished)
    - src/lib/admin/showcase-queries.ts (Plan 10-02 output — structural template)
    - src/lib/admin/list-cursor.ts
    - src/lib/schemas/content.ts (testimonials columns: name notNull, company nullable, content notNull, createdAt timestamptz defaultNow)
  </read_first>
  <files>src/lib/admin/testimonials-queries.ts</files>
  <behavior>
    - listTestimonialsForAdmin() returns at most PAGE_SIZE rows newest-first.
    - q='Acme' filters to rows whose name OR company OR content contains 'acme' (case-insensitive).
    - Cursor 'after' goes forward (older rows); 'before' goes backward (newer rows) and reverses the slice back to display order.
    - DB error returns the zero shape; logger.error called.
  </behavior>
  <action>
    Step A (RED). Create `tests/unit/admin/testimonials-queries.test.ts` using chainable-mock pattern from `tests/unit/showcase.test.ts`. Tests required:
      - returns hasMore=true, trims to PAGE_SIZE when DB yields PAGE_SIZE + 1.
      - empty result yields the zero shape.
      - q='acme' triggers an OR of three ilike predicates against (name, company, content).
      - On DB error: zero shape; logger.error called once.

    Step B (GREEN). Rewrite `listTestimonialsForAdmin`:
      - Import line: replace `import { desc, eq } from 'drizzle-orm'` with `import { and, asc, desc, eq, gt, ilike, lt, or } from 'drizzle-orm'`.
      - Add `import { PAGE_SIZE, type Direction, decodeCursor, encodeCursor, escapeLikePattern } from '@/lib/admin/list-cursor'`.
      - Replace function body. Use the simplified 2-part cursor comparison (no NULLS LAST gymnastics, no second ASC/DESC mix):
        - Forward ('after' or no cursor):
          ```
          createdAt < $1 OR (createdAt = $1 AND id > $2)
          ```
          orderBy: `[desc(testimonials.createdAt), asc(testimonials.id)]`.
        - Backward ('before'):
          ```
          createdAt > $1 OR (createdAt = $1 AND id < $2)
          ```
          orderBy: `[asc(testimonials.createdAt), desc(testimonials.id)]`, then reverse the result slice in JS.
      - Cursor encoding: `encodeCursor('after', [row.createdAt!.toISOString(), row.id])`.
      - Wrap in try/catch; on error return the zero shape and call `logger.error('testimonials-queries.listTestimonialsForAdmin failed', error)`.

    KEEP byte-equal: getTestimonialById, createTestimonial, updateTestimonial, deleteTestimonial re-export, toggleTestimonialPublished.

    Step C. `bun test tests/unit/admin/testimonials-queries.test.ts` green. Lint + typecheck.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/testimonials-queries.test.ts</automated>
  </verify>
  <done>Helper returns the new shape; tests pass; non-list exports byte-equal vs origin/main.</done>
</task>

<task type="auto">
  <name>Task 2: rewrite /admin/testimonials page to consume the new helper</name>
  <read_first>
    - src/app/(admin)/admin/testimonials/page.tsx (current)
    - src/app/(admin)/admin/showcase/page.tsx (Plan 10-02 output)
  </read_first>
  <files>src/app/(admin)/admin/testimonials/page.tsx</files>
  <action>
    Apply the same Plan 10-02 transformation with these per-page strings:
      - Page interface: `interface AdminTestimonialsPageProps { searchParams: Promise<{ q?: string; cursor?: string }> }`.
      - SearchInput placeholder: `Search testimonials`.
      - Empty-q-result copy: `No testimonials matching {q}.`.
      - Clear-search link: `href="/admin/testimonials"`.
      - SearchInput baseHref + Pagination baseHref: `"/admin/testimonials"`.
      - ResourceListPage `emptyMessage="No testimonials yet. Create your first one."` (unchanged).
      - `isEmpty={rows.length === 0 && !q}`.

    KEEP byte-equal: metadata, dateFormatter, Suspense fallback string, the full table block + PublishToggle/Edit Link markup.

    NO new tests. Lint + typecheck.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Page accepts searchParams; SearchInput + Pagination wired; table byte-equal; lint + typecheck clean.</done>
</task>

</tasks>

<verification>
- `bun run lint && bun run typecheck` exit 0.
- `bun test tests/unit/admin/testimonials-queries.test.ts` passes.
- `grep -nE '—|–' src/lib/admin/testimonials-queries.ts src/app/\(admin\)/admin/testimonials/page.tsx` returns nothing.
- `git diff origin/main -- src/lib/admin/testimonials-queries.ts` confined to imports + listTestimonialsForAdmin body.
</verification>

<success_criteria>
- listTestimonialsForAdmin: options-object signature + new return shape.
- /admin/testimonials: SearchInput + Pagination wired; PublishToggle byte-equal.
- One commit: `feat(10-04): cursor + search for /admin/testimonials`.
</success_criteria>

<output>
Create `.planning/phases/10-admin-list-pagination/10-04-SUMMARY.md`.
</output>

---
phase: 13-admin-error-observability
plan: 08
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/admin/dashboard-queries.ts
  - src/components/admin/widgets/VisitorsChart.tsx
  - src/components/admin/widgets/TopPagesTable.tsx
  - src/components/admin/widgets/TrafficSourcesPie.tsx
  - src/components/admin/widgets/WebVitalsCards.tsx
  - src/components/admin/widgets/RecentLeadsPanel.tsx
  - src/app/(admin)/admin/dashboard/page.tsx
  - tests/unit/admin/dashboard-queries.test.ts
autonomous: true
requirements: [ADMINERR-02]

must_haves:
  truths:
    - "When one dashboard widget query throws, that widget shows an error in its own card"
    - "When one widget query throws, the other widgets and the page still render (no whole-page blank)"
    - "When a widget query returns zero rows, the widget shows its existing empty state"
  artifacts:
    - path: "src/lib/admin/dashboard-queries.ts"
      provides: "all 5 widget queries return AdminQueryResult (incl. getWebVitalsP75 per research A1)"
      contains: "AdminQueryResult"
    - path: "src/app/(admin)/admin/dashboard/page.tsx"
      provides: "passes each AdminQueryResult to its widget; Promise.all never rejects"
      contains: "Promise.all"
    - path: "tests/unit/admin/dashboard-queries.test.ts"
      provides: "5 widget queries x error/ok-empty cases (file scaffolded in plan 13-01)"
      contains: "ok: false"
  key_links:
    - from: "src/app/(admin)/admin/dashboard/page.tsx"
      to: "VisitorsChart / TopPagesTable / TrafficSourcesPie / WebVitalsCards / RecentLeadsPanel"
      via: "passes result prop (AdminQueryResult)"
      pattern: "result="
    - from: "widgets"
      to: "AdminErrorState"
      via: "renders inline error on !result.ok"
      pattern: "result\\.ok"
---

<objective>
Migrate the five dashboard widget queries to `AdminQueryResult` and teach each widget to render its own inline `AdminErrorState` on failure, so a single failing widget shows an error in its own card while the other widgets and the page still render. Includes `getWebVitalsP75` per RESEARCH assumption A1 (the only widget that today can't distinguish "no samples" from "DB down").

Purpose: ADMINERR-02 (dashboard widgets) with v4 resilience preserved (`Promise.all` never rejects because errors are returned, not thrown).
Output: migrated `dashboard-queries.ts`, all 5 widgets accept a result prop with an error branch, dashboard page passes the results, populated `dashboard-queries.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-admin-error-observability/13-RESEARCH.md

<interfaces>
From src/lib/admin/query-result.ts (plan 13-01): `AdminQueryResult<T>`, `ok`, `err`.
From src/components/admin/AdminErrorState.tsx (plan 13-01): `AdminErrorState({ resource?, message?, inline? })` — use `inline` for widget bodies.

Current dashboard contracts (src/lib/admin/dashboard-queries.ts):
- `getVisitorsByDay(days): Promise<VisitorsByDayRow[]>` (line 45)
- `getTopPages(limit, days): Promise<TopPagesRow[]>` (line 77)
- `getTrafficSources(days): Promise<TrafficSourceRow[]>` (line 114)
- `getWebVitalsP75(days): Promise<WebVitalsP75Row[]>` (line 155)
- `getRecentLeads(limit): Promise<RecentLeadRow[]>` (line 189)
All five catch -> return `[]` today (no try/catch distinction).

Current widget props:
- `VisitorsChart({ data })` ('use client', recharts) — src/components/admin/widgets/VisitorsChart.tsx:39
- `TopPagesTable({ rows })` (server) — :16
- `TrafficSourcesPie({ data })` ('use client', recharts) — :34
- `WebVitalsCards({ rows })` (server) — :62
- `RecentLeadsPanel({ leads })` (server) — :73

Dashboard page (src/app/(admin)/admin/dashboard/page.tsx): `DashboardWidgets` does `await connection()` then `Promise.all([getVisitorsByDay(30), getTopPages(10,30), getTrafficSources(30), getWebVitalsP75(7), getRecentLeads(10)])` and renders `<VisitorsChart data={visitors} />`, `<WebVitalsCards rows={vitals} />`, `<TopPagesTable rows={topPages} />`, `<TrafficSourcesPie data={sources} />`, `<RecentLeadsPanel leads={recentLeads} />`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the 5 dashboard widget queries + populate dashboard-queries.test.ts</name>
  <read_first>
    - src/lib/admin/dashboard-queries.ts (full file — all 5 queries; each wraps a `db.select()...groupBy()/orderBy()/limit()` with `sql` exprs)
    - tests/unit/admin/dashboard-queries.test.ts (the harness scaffolded in plan 13-01 — extend it; the mock chain already includes `groupBy`)
    - 13-RESEARCH.md Pattern 2, Assumptions A1 (include `getWebVitalsP75`), Wave 0 Gaps
  </read_first>
  <behavior>
    - Each of the 5 queries: catch returns `err()` (`{ ok: false, error: true }`) and logs once via `logger.error`; success returns `ok(rows)` (rows may be empty)
    - A zero-row success is distinguishable from a thrown error
  </behavior>
  <action>
    Wrap each of `getVisitorsByDay`, `getTopPages`, `getTrafficSources`, `getWebVitalsP75`, `getRecentLeads` in a try/catch (some may already have one — preserve existing logging if present, add `logger.error('dashboard-queries.<fn> failed', error)` otherwise) and change each return type to `Promise<AdminQueryResult<<RowType>[]>>`: success returns `ok(rows)`, catch returns `err()`. Import `ok`/`err`/`AdminQueryResult` from `@/lib/admin/query-result`. Then populate `tests/unit/admin/dashboard-queries.test.ts` (scaffolded in plan 13-01): for each of the 5 queries add an error case (mock db throw -> `{ ok: false, error: true }`, `logger.error` called) and an ok-empty case (zero rows -> `result.ok === true`, `result.data` empty). Remove the placeholder smoke test added in plan 13-01.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/dashboard-queries.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 5 queries return `AdminQueryResult<RowType[]>`; catch returns `err()`; `logger.error` on each catch
    - `getWebVitalsP75` included (A1)
    - Test file covers 5 x (error + ok-empty); placeholder smoke removed; all green
  </acceptance_criteria>
  <done>All 5 widget queries return the union; tests cover error vs ok-empty for each; query-level typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Update the 5 widgets + the dashboard page</name>
  <read_first>
    - All 5 widget files (the card chrome + the existing `data.length === 0`/`rows.length === 0` empty branch each renders)
    - src/app/(admin)/admin/dashboard/page.tsx (`DashboardWidgets`; the `Promise.all` + the 5 widget render lines + the docblock that describes the old `[]`-on-failure behavior)
    - 13-RESEARCH.md Pattern 2 (per-widget isolation; client widgets receive the result object as a serializable prop)
  </read_first>
  <action>
    Change each widget's prop from the bare array (`data`/`rows`/`leads`) to `result: AdminQueryResult<<RowType>[]>`. Inside each widget, keep the card chrome unchanged; branch the BODY: `!result.ok ? <AdminErrorState inline resource="<widget label>" /> : <existing empty-or-content branch using result.data>`. Use resource labels from RESEARCH (e.g. "page analytics", "traffic data", "traffic source data", "web vitals", "recent leads"). For the two `'use client'` widgets (`VisitorsChart`, `TrafficSourcesPie`), the result is a plain serializable object so passing it as a prop is fine; import `AdminErrorState` (client-compatible). In `dashboard/page.tsx`: keep `await connection()` and `Promise.all` unchanged in shape, then pass each result through: `<VisitorsChart result={visitors} />`, `<TopPagesTable result={topPages} />`, `<TrafficSourcesPie result={sources} />`, `<WebVitalsCards result={vitals} />`, `<RecentLeadsPanel result={recentLeads} />`. Update the stale docblock that says queries "return [] on failure" to describe the returned-error-variant behavior. Confirm `Promise.all` still never rejects (errors are returned, not thrown) so one failing widget cannot blank the page.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint src/components/admin/widgets/ "src/app/(admin)/admin/dashboard/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 widgets accept `result: AdminQueryResult<...>` and render `<AdminErrorState inline ... />` on `!result.ok`, existing empty branch on ok-empty, content on ok-nonempty
    - Dashboard page passes `result={...}` to all 5; `Promise.all` shape unchanged (never rejects)
    - Stale docblock updated; no widget throws
    - `bun run typecheck` green across the dashboard slice; lint clean
  </acceptance_criteria>
  <done>Each widget owns its error card; the page passes results; one failing widget cannot blank the dashboard; typecheck + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard-queries -> widgets | Caught DB error returned as a variant; never rendered as exception text; client widgets receive a plain serializable result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Information disclosure | dashboard widgets | mitigate | Widgets render `AdminErrorState` (fixed copy); exception stays in `logger.error`; the result prop carries no error message. |
| T-13-13 | Denial of service / availability | dashboard page | mitigate | Errors are RETURNED not thrown -> `Promise.all` never rejects -> one failing widget cannot blank the page (preserves v4 resilience). |
</threat_model>

<verification>
- `bun test tests/unit/admin/dashboard-queries.test.ts` green (5 x error + ok-empty)
- `bun run typecheck` green across the dashboard slice
- `bun run lint` clean on all 5 widgets + the page
- No widget query throws (grep gate: each query body contains `return err()` in its catch)
</verification>

<success_criteria>
- Each widget distinguishes failure from empty in its own card; other widgets still render on one failure
- `getWebVitalsP75` included; `logger.error` retained on all 5 catch paths
- `Promise.all` resilience preserved
</success_criteria>

<output>
Create `.planning/phases/13-admin-error-observability/13-08-SUMMARY.md` when done.
</output>

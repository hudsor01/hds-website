---
phase: 13-admin-error-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/admin/query-result.ts
  - src/components/admin/AdminErrorState.tsx
  - tests/unit/admin/query-result.test.ts
  - tests/unit/admin/dashboard-queries.test.ts
autonomous: true
requirements: [ADMINERR-01, ADMINERR-02, ADMINERR-03, ADMINERR-04]

must_haves:
  truths:
    - "A failed admin query is representable as a value distinct from a successful-but-empty result"
    - "A detail loader can distinguish not-found from a DB error from a found row"
    - "Admin surfaces have one shared, accessible error component to render on failure"
  artifacts:
    - path: "src/lib/admin/query-result.ts"
      provides: "AdminQueryResult<T> (2-way) + AdminDetailResult<T> (3-way) + ok/err/found/notFoundResult/errResult constructors, NO server-only"
      contains: "AdminDetailResult"
    - path: "src/components/admin/AdminErrorState.tsx"
      provides: "Shared error card on shadcn Alert (role=alert, destructive), generic dash-free copy, never renders the raw exception"
      contains: "AdminErrorState"
    - path: "tests/unit/admin/dashboard-queries.test.ts"
      provides: "Wave-0 test scaffold for the 5 dashboard widget queries (created here, populated in plan 08)"
      contains: "describe"
  key_links:
    - from: "src/components/admin/AdminErrorState.tsx"
      to: "src/components/ui/alert.tsx"
      via: "imports Alert/AlertTitle/AlertDescription"
      pattern: "from '@/components/ui/alert'"
---

<objective>
Create the two shared primitives the entire phase depends on: the discriminated-union result types (in a tier-agnostic module, no `server-only`) and the single shared admin error-state component built on the already-vendored shadcn `Alert`. Also scaffold the missing `dashboard-queries.test.ts` file so plan 08 has a place to add the 5 widget-query regression tests.

Purpose: Every later plan (leads, calculator-leads, newsletter, showcase, testimonials, blog, dashboard, emails) imports these. Wave 1 owns these files exclusively so Wave 2 can parallelize per-resource without file conflicts.
Output: `src/lib/admin/query-result.ts`, `src/components/admin/AdminErrorState.tsx`, a passing `tests/unit/admin/query-result.test.ts`, and an empty-shell `tests/unit/admin/dashboard-queries.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-admin-error-observability/13-RESEARCH.md
@.planning/phases/13-admin-error-observability/13-CONTEXT.md

<interfaces>
<!-- The shadcn Alert primitive this component wraps. Already vendored, 'use client', role="alert", destructive variant. Use directly; no registry pull. -->

From src/components/ui/alert.tsx:
- `Alert(props: HTMLAttributes<HTMLDivElement> & VariantProps)` — renders `<div role="alert">`, accepts `variant: 'default' | 'destructive'`
- `AlertTitle(props: HTMLAttributes<HTMLHeadingElement>)` — renders `<h5>`
- `AlertDescription(props: HTMLAttributes<HTMLParagraphElement>)` — renders `<div>`
- All three exported from `@/components/ui/alert`

Existing precedent discriminated union to MATCH stylistically (from src/lib/admin/emails-queries.ts:96):
- `RetryResult = { ok: true; row: ... } | { ok: false; reason: ... }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared result-type module + unit test</name>
  <read_first>
    - src/lib/admin/emails-queries.ts:96-106 (the existing `RetryResult` union — match this hand-rolled idiom, no neverthrow dep)
    - 13-RESEARCH.md Pattern 1 (the exact union shapes + constructors) and Pitfall 5 (the module MUST NOT be `server-only` — client widgets `VisitorsChart`/`TrafficSourcesPie` import the type)
  </read_first>
  <behavior>
    - `ok(data)` produces `{ ok: true, data }`; type-narrows so `result.ok === true` exposes `result.data`
    - `err()` produces `{ ok: false, error: true }`; carries no payload
    - `found(data)` produces `{ status: 'found', data }`
    - `notFoundResult()` produces `{ status: 'not-found' }`
    - `errResult()` produces `{ status: 'error' }`
    - The three detail statuses are mutually exclusive; switching on `result.status` is exhaustive over `'found' | 'not-found' | 'error'`
  </behavior>
  <action>
    Create `src/lib/admin/query-result.ts` exporting two discriminated unions and their constructors, with NO `import 'server-only'` (it is imported by client widgets — see RESEARCH Pitfall 5). Define `AdminQueryResult<T> = { ok: true; data: T } | { ok: false; error: true }` for list/widget/queue reads, and `AdminDetailResult<T> = { status: 'found'; data: T } | { status: 'not-found' } | { status: 'error' }` for `get*ById` reads. Add tiny constructors: `ok<T>(data: T)`, `err()` (returns `AdminQueryResult<never>`), `found<T>(data: T)`, `notFoundResult()` (returns `AdminDetailResult<never>`), `errResult()` (returns `AdminDetailResult<never>`). No `any`; use generics. Match the hand-rolled style of `RetryResult` in `emails-queries.ts` (no external Result lib). Then create `tests/unit/admin/query-result.test.ts` (bun:test) covering the behaviors above: each constructor returns the right shape, and an exhaustive `switch (result.status)` over the detail union compiles and routes correctly.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/query-result.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/admin/query-result.ts` exports `AdminQueryResult`, `AdminDetailResult`, `ok`, `err`, `found`, `notFoundResult`, `errResult`
    - The file contains NO `import 'server-only'` line
    - `tests/unit/admin/query-result.test.ts` passes (constructor shapes + 3-way narrowing)
    - `bun run typecheck` green, no `any`
  </acceptance_criteria>
  <done>Both files exist; the types narrow correctly; unit test passes; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Create the shared AdminErrorState component</name>
  <read_first>
    - src/components/ui/alert.tsx (the full file — the primitive to wrap; already `role="alert"`, `destructive` variant)
    - 13-RESEARCH.md "Existing Empty-State Pattern + Shared Error Component" (the proposed API + exact dash-free copy) and Security Domain V7 (the copy must NOT leak DB internals)
    - src/components/admin/ResourceListPage.tsx (the card-chrome idiom the list empty-states use, to match visual chrome for the non-inline variant)
  </read_first>
  <action>
    Create `src/components/admin/AdminErrorState.tsx` wrapping `Alert`/`AlertTitle`/`AlertDescription` from `@/components/ui/alert` with `variant="destructive"` and a lucide `CircleAlert` (or `TriangleAlert`) icon (lucide is the configured icon library per components.json). Props: `resource?: string` (interpolated into the title), `message?: string` (optional override body copy), `inline?: boolean` (when true, drop the outer `rounded-xl border ... p-...` card wrapper so it sits inside a widget card body; when false/absent, wrap in the admin card chrome matching the empty-state cards). Default title: "Could not load {resource}" (fall back to "this data" when `resource` is absent so the sentence reads cleanly). Default body: "Something went wrong loading this data. Refresh to try again." The component MUST NEVER accept or render a raw `Error`/exception object or `error.message` (threat model: no info-leak; the exception only lives in `logger.error` server-side). All copy is em/en-dash free and emoji free. No `any`. The component does not itself need `'use client'` (the `Alert` it renders is the client boundary).
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint src/components/admin/AdminErrorState.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/admin/AdminErrorState.tsx` exports `AdminErrorState` with props `{ resource?, message?, inline? }`
    - Renders the shadcn `Alert` (inherits `role="alert"`) with `variant="destructive"` + a lucide icon
    - Copy contains no em-dash (U+2014), no en-dash (U+2013), no emoji
    - No prop accepts an `Error`/exception; raw exception text is never rendered
    - `bun run typecheck` + `bun run lint` green
  </acceptance_criteria>
  <done>Component renders a destructive Alert with generic copy and an `inline` variant; typecheck + lint green; no info-leak surface.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the missing dashboard-queries test file</name>
  <read_first>
    - tests/unit/admin/leads-queries.test.ts (the chainable-db-mock harness to copy — `state.shouldThrow`, `state.rowsToReturn`, `beforeEach` mock re-registration)
    - 13-RESEARCH.md Wave 0 Gaps (the note that dashboard widget queries use `.groupBy().orderBy().limit()` + `sql` exprs, so the mock chain must include `groupBy`)
    - tests/setup.ts (auto-mocks `@/env` + `@/lib/logger`, runs `mock.restore()` per test)
  </read_first>
  <action>
    Create `tests/unit/admin/dashboard-queries.test.ts` as a working bun:test shell that imports from `@/lib/admin/dashboard-queries`, sets up the chainable-db-mock harness copied from `leads-queries.test.ts`, and ensures the mock chain includes `groupBy` (the widget queries call `.groupBy().orderBy().limit()`). Include ONE placeholder smoke test (e.g. the module imports and `getRecentLeads` is callable) so the file is green and committable now. The 5 widget-query found-empty/error cases are added in plan 08 — leave a clear comment block listing the queries to cover (`getVisitorsByDay`, `getTopPages`, `getTrafficSources`, `getWebVitalsP75`, `getRecentLeads`). This file is created here in Wave 1 so plan 08 (Wave 2) only extends it; the file is owned by plan 08 thereafter.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/dashboard-queries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/admin/dashboard-queries.test.ts` exists, imports `@/lib/admin/dashboard-queries`, and passes
    - The db mock chain includes `groupBy`
    - A comment block enumerates the 5 widget queries plan 08 must cover
  </acceptance_criteria>
  <done>The test file exists and is green; the harness supports the widget query chain; plan 08 can extend it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query layer -> operator UI | A caught DB exception must not cross into the rendered error state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information disclosure | AdminErrorState.tsx | mitigate | Component API accepts only `resource`/`message`/`inline`; never an `Error`. Fixed generic copy. Raw exception stays in `logger.error` (server) only. |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | N/A this phase installs zero packages (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `bun run typecheck` green (the unions narrow; no `any`)
- `bun test tests/unit/admin/query-result.test.ts tests/unit/admin/dashboard-queries.test.ts` green
- `bun run lint` clean on the two new source files
- `query-result.ts` has no `import 'server-only'` (grep gate: `grep -c "server-only" src/lib/admin/query-result.ts` returns 0)
</verification>

<success_criteria>
- The two shared primitives exist and compile; the result types narrow both 2-way and 3-way
- `AdminErrorState` is accessible (inherits `role="alert"`), dash-free, never leaks an exception
- `dashboard-queries.test.ts` exists and is green, ready for plan 08 to extend
- No external package installed
</success_criteria>

<output>
Create `.planning/phases/13-admin-error-observability/13-01-SUMMARY.md` when done.
</output>

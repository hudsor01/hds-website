---
phase: 13-admin-error-observability
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/admin/leads-queries.ts
  - src/app/(admin)/admin/leads/page.tsx
  - src/app/(admin)/admin/leads/[id]/page.tsx
  - tests/unit/admin/leads-queries.test.ts
autonomous: true
requirements: [ADMINERR-01, ADMINERR-04]

must_haves:
  truths:
    - "When the leads list query throws, /admin/leads shows the error state, not an empty list"
    - "When the leads list query returns zero rows, /admin/leads shows the existing empty state"
    - "When getLeadById throws, the detail page shows the error state, not a 404"
    - "When the lead row genuinely does not exist, the detail page still 404s"
  artifacts:
    - path: "src/lib/admin/leads-queries.ts"
      provides: "listLeadsForAdmin -> AdminQueryResult<ListLeadsResult>; getLeadById -> AdminDetailResult<LeadDetail>"
      contains: "AdminDetailResult"
    - path: "src/app/(admin)/admin/leads/page.tsx"
      provides: "renders AdminErrorState on !ok"
      contains: "AdminErrorState"
    - path: "src/app/(admin)/admin/leads/[id]/page.tsx"
      provides: "switch on status: not-found -> notFound(), error -> AdminErrorState"
      contains: "AdminErrorState"
  key_links:
    - from: "src/app/(admin)/admin/leads/page.tsx"
      to: "listLeadsForAdmin"
      via: "narrows result.ok before destructuring data"
      pattern: "result\\.ok"
    - from: "src/app/(admin)/admin/leads/[id]/page.tsx"
      to: "getLeadById"
      via: "switch on result.status"
      pattern: "status === 'not-found'"
---

<objective>
Migrate the leads read layer to the discriminated unions and teach its two consumers to render the error state: the list page shows `AdminErrorState` on failure (empty state reserved for genuine zero rows), and the detail loader 404s only on not-found while rendering `AdminErrorState` on DB error.

Purpose: ADMINERR-01 (list) + ADMINERR-04 (detail) for the leads resource.
Output: migrated `leads-queries.ts`, both leads consumers updated, leads test asserting the error variant + 3-way detail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-admin-error-observability/13-RESEARCH.md

<interfaces>
From src/lib/admin/query-result.ts (created in plan 13-01):
- `AdminQueryResult<T> = { ok: true; data: T } | { ok: false; error: true }`
- `AdminDetailResult<T> = { status: 'found'; data: T } | { status: 'not-found' } | { status: 'error' }`
- constructors `ok`, `err`, `found`, `notFoundResult`, `errResult`

From src/components/admin/AdminErrorState.tsx (created in plan 13-01):
- `AdminErrorState({ resource?, message?, inline? })`

Current leads contracts (src/lib/admin/leads-queries.ts):
- `listLeadsForAdmin(opts?: ListLeadsOptions): Promise<ListLeadsResult>` (line 97) — catch returns `EMPTY_RESULT` (line 189); `ListLeadsResult = { rows, hasMore, prevCursor, nextCursor }` (line 61)
- `getLeadById(id: string): Promise<LeadDetail | null>` (line 203)
- `EMPTY_RESULT` const at line 68 (DELETE after wrapping)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate leads-queries reads + update tests</name>
  <read_first>
    - src/lib/admin/leads-queries.ts (full file — `listLeadsForAdmin` try/catch + cursor math; `getLeadById` Promise.all over three selects; `EMPTY_RESULT`)
    - tests/unit/admin/leads-queries.test.ts (the existing "DB error safety" case to rewrite + the chainable mock harness)
    - 13-RESEARCH.md Pattern 1, Pattern 3, and Code Examples (the exact list + 3-way test cases)
  </read_first>
  <behavior>
    - `listLeadsForAdmin` catch path returns `err()` (`{ ok: false, error: true }`), NOT `EMPTY_RESULT`; still calls `logger.error` once
    - `listLeadsForAdmin` zero-rows returns `ok({ rows: [], hasMore: false, prevCursor, nextCursor })`
    - `getLeadById` returns `found(detail)` when the lead row exists, `notFoundResult()` when no row, `errResult()` on throw (logs once)
  </behavior>
  <action>
    Change `listLeadsForAdmin` to return `Promise<AdminQueryResult<ListLeadsResult>>`: wrap the success payload in `ok(...)`, replace the `catch` `return EMPTY_RESULT` with `return err()` (keep the `logger.error('leads-queries.listLeadsForAdmin failed', error)` line). Delete the now-unused `EMPTY_RESULT` const (its only purpose was the swallowed-error default). Change `getLeadById` to return `Promise<AdminDetailResult<LeadDetail>>`: in the try, after destructuring the lead row, `return lead ? found({...}) : notFoundResult()`; in the catch, keep `logger.error('leads-queries.getLeadById failed', error, { metadata: { id } })` and `return errResult()`. Import the constructors + types from `@/lib/admin/query-result`. Then update `tests/unit/admin/leads-queries.test.ts`: rewrite the existing DB-error case to assert `listLeadsForAdmin()` returns `{ ok: false, error: true }` (and `logger.error` called once); add a case asserting zero rows returns `ok` with empty `data.rows`; add three `getLeadById` cases (found / not-found / error) per RESEARCH Code Examples.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/leads-queries.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `listLeadsForAdmin` returns `AdminQueryResult<ListLeadsResult>`; catch returns `err()`; `EMPTY_RESULT` deleted
    - `getLeadById` returns `AdminDetailResult<LeadDetail>` with all three branches
    - `logger.error` retained on both catch paths
    - Test file asserts error variant (not EMPTY_RESULT), ok-empty, and 3-way detail; all green
    - `bun run typecheck` green (consumers updated in task 2 — see note)
  </acceptance_criteria>
  <done>Both leads reads return the unions; tests assert all branches; query-level typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Update both leads consumers to render AdminErrorState</name>
  <read_first>
    - src/app/(admin)/admin/leads/page.tsx (line ~87 destructures `{ rows, prevCursor, nextCursor }`; empty-state card at lines ~108-125)
    - src/app/(admin)/admin/leads/[id]/page.tsx (`LeadDetailLoader` ~line 68; `if (!detail) notFound()` at ~69-71; the synchronous `if (id === BUILD_PLACEHOLDER_ID) notFound()` short-circuit BEFORE `connection()`)
    - 13-RESEARCH.md Pattern 1 (consumer), Pattern 3 (detail loader), and Pitfall 2 (do NOT touch the placeholder short-circuit)
  </read_first>
  <action>
    In `leads/page.tsx`: replace `const { rows, prevCursor, nextCursor } = await listLeadsForAdmin(...)` with `const result = await listLeadsForAdmin(...); if (!result.ok) { return <AdminErrorState resource="leads" /> } const { rows, prevCursor, nextCursor } = result.data`. Leave the existing `rows.length === 0 ? <empty> : <Table>` branch unchanged (it now only runs on genuine success). Import `AdminErrorState` from `@/components/admin/AdminErrorState`. In `leads/[id]/page.tsx` `LeadDetailLoader`: keep the synchronous `if (id === BUILD_PLACEHOLDER_ID) notFound()` line and `await connection()` EXACTLY as-is (Pitfall 2 — do not reintroduce the `$~` bug). Replace `if (!detail) notFound()` with: `const result = await getLeadById(id); if (result.status === 'not-found') { notFound() } if (result.status === 'error') { return <AdminErrorState resource="lead" /> }` then read `const detail = result.data` and render unchanged.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint "src/app/(admin)/admin/leads/page.tsx" "src/app/(admin)/admin/leads/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `leads/page.tsx` returns `<AdminErrorState resource="leads" />` on `!result.ok`; empty/table branch unchanged otherwise
    - `leads/[id]/page.tsx` 404s only on `'not-found'`, renders `<AdminErrorState resource="lead" />` on `'error'`
    - The `if (id === BUILD_PLACEHOLDER_ID) notFound()` short-circuit and `await connection()` are byte-unchanged
    - `bun run typecheck` green for the whole leads slice; lint clean
  </acceptance_criteria>
  <done>Both consumers narrow the unions and render the error state; placeholder short-circuit untouched; typecheck + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leads-queries -> leads pages | Caught DB error returned as a variant; never rendered as exception text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Information disclosure | leads pages | mitigate | Pages render `AdminErrorState` (fixed copy); the caught error stays in `logger.error`. |
| T-13-04 | Repudiation / availability | leads/[id] | mitigate | A DB error renders the error state, never a misleading 404; genuine missing row still 404s. |
</threat_model>

<verification>
- `bun test tests/unit/admin/leads-queries.test.ts` green (error variant + ok-empty + 3-way)
- `bun run typecheck` green across `leads-queries.ts` + both consumers
- `bun run lint` clean on the three touched source files
- grep gate: `grep -c "EMPTY_RESULT" src/lib/admin/leads-queries.ts` returns 0
</verification>

<success_criteria>
- Leads list distinguishes failure from empty; detail distinguishes error from 404
- `logger.error` retained; no exception text reaches the UI
- Placeholder PPR short-circuit intact
</success_criteria>

<output>
Create `.planning/phases/13-admin-error-observability/13-02-SUMMARY.md` when done.
</output>

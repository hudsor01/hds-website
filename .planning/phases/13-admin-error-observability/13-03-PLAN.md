---
phase: 13-admin-error-observability
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/admin/calculator-leads-queries.ts
  - src/app/(admin)/admin/leads/calculator/page.tsx
  - src/app/(admin)/admin/leads/calculator/[id]/page.tsx
  - tests/unit/admin/calculator-leads-queries.test.ts
autonomous: true
requirements: [ADMINERR-01, ADMINERR-04]

must_haves:
  truths:
    - "When the calculator-leads list query throws, the page shows the error state, not an empty list"
    - "When it returns zero rows, the page shows the existing empty state"
    - "When getCalculatorLeadById throws, the detail page shows the error state, not a 404"
    - "When the row genuinely does not exist, the detail page still 404s"
  artifacts:
    - path: "src/lib/admin/calculator-leads-queries.ts"
      provides: "listCalculatorLeadsForAdmin -> AdminQueryResult; getCalculatorLeadById -> AdminDetailResult"
      contains: "AdminDetailResult"
    - path: "src/app/(admin)/admin/leads/calculator/page.tsx"
      provides: "renders AdminErrorState on !ok"
      contains: "AdminErrorState"
    - path: "src/app/(admin)/admin/leads/calculator/[id]/page.tsx"
      provides: "switch on status"
      contains: "AdminErrorState"
  key_links:
    - from: "src/app/(admin)/admin/leads/calculator/page.tsx"
      to: "listCalculatorLeadsForAdmin"
      via: "narrows result.ok"
      pattern: "result\\.ok"
    - from: "src/app/(admin)/admin/leads/calculator/[id]/page.tsx"
      to: "getCalculatorLeadById"
      via: "switch on result.status"
      pattern: "status === 'not-found'"
---

<objective>
Migrate the calculator-leads read layer to the discriminated unions and update its list page + detail loader to render `AdminErrorState`.

Purpose: ADMINERR-01 + ADMINERR-04 for the calculator-leads resource.
Output: migrated `calculator-leads-queries.ts`, both consumers updated, test asserting the error variant + 3-way detail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-admin-error-observability/13-RESEARCH.md

<interfaces>
From src/lib/admin/query-result.ts (plan 13-01): `AdminQueryResult<T>`, `AdminDetailResult<T>`, `ok`, `err`, `found`, `notFoundResult`, `errResult`.
From src/components/admin/AdminErrorState.tsx (plan 13-01): `AdminErrorState({ resource?, message?, inline? })`.

Current calculator-leads contracts (src/lib/admin/calculator-leads-queries.ts):
- `listCalculatorLeadsForAdmin(opts?): Promise<ListCalculatorLeadsResult>` (line 85) — catch returns `EMPTY_RESULT` (line 185); `ListCalculatorLeadsResult` at line 49
- `getCalculatorLeadById(id): Promise<... | null>` (line 197)
- `EMPTY_RESULT` const at line 56 (DELETE after wrapping)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate calculator-leads-queries reads + update tests</name>
  <read_first>
    - src/lib/admin/calculator-leads-queries.ts (full file — `listCalculatorLeadsForAdmin` try/catch at line 85; `getCalculatorLeadById` at line 197; `EMPTY_RESULT` line 56)
    - tests/unit/admin/calculator-leads-queries.test.ts (DB-error case to rewrite + mock harness)
    - 13-RESEARCH.md Pattern 1, Pattern 3, Code Examples
  </read_first>
  <behavior>
    - `listCalculatorLeadsForAdmin` catch returns `err()`; zero rows returns `ok` with empty `data.rows`; `logger.error` once on catch
    - `getCalculatorLeadById` returns `found(row)` / `notFoundResult()` / `errResult()`; `logger.error` once on throw
  </behavior>
  <action>
    Change `listCalculatorLeadsForAdmin` to `Promise<AdminQueryResult<ListCalculatorLeadsResult>>`: wrap the success payload in `ok(...)`, replace `return EMPTY_RESULT` in the catch with `return err()` (keep `logger.error`). Delete the unused `EMPTY_RESULT` const. Change `getCalculatorLeadById` to `Promise<AdminDetailResult<CalculatorLeadRow>>` (use the existing row type the consumer reads): `return row ? found(row) : notFoundResult()` in the try, `logger.error(...)` + `return errResult()` in the catch. Import constructors/types from `@/lib/admin/query-result`. Update `tests/unit/admin/calculator-leads-queries.test.ts`: rewrite the DB-error case to assert `{ ok: false, error: true }`; add ok-empty; add `getCalculatorLeadById` found/not-found/error cases.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/calculator-leads-queries.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `listCalculatorLeadsForAdmin` -> `AdminQueryResult`; catch returns `err()`; `EMPTY_RESULT` deleted
    - `getCalculatorLeadById` -> `AdminDetailResult` with all three branches; `logger.error` retained
    - Tests assert error variant + ok-empty + 3-way; green
  </acceptance_criteria>
  <done>Both reads return the unions; tests cover all branches; query-level typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Update both calculator-leads consumers</name>
  <read_first>
    - src/app/(admin)/admin/leads/calculator/page.tsx (line ~89 destructure; empty-state ~114-134)
    - src/app/(admin)/admin/leads/calculator/[id]/page.tsx (`if (!row) notFound()` ~96-98; placeholder short-circuit + `connection()` BEFORE the read)
    - 13-RESEARCH.md Pattern 1, Pattern 3, Pitfall 2
  </read_first>
  <action>
    In `leads/calculator/page.tsx`: replace the destructure with `const result = await listCalculatorLeadsForAdmin(...); if (!result.ok) { return <AdminErrorState resource="calculator submissions" /> } const { rows, prevCursor, nextCursor } = result.data`. Keep the empty/table branch. In `leads/calculator/[id]/page.tsx`: keep the placeholder `notFound()` short-circuit and `await connection()` unchanged; replace `if (!row) notFound()` with the 3-way `switch`/guards (`'not-found'` -> `notFound()`, `'error'` -> `return <AdminErrorState resource="calculator submission" />`, else `const row = result.data`). Import `AdminErrorState`.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint "src/app/(admin)/admin/leads/calculator/page.tsx" "src/app/(admin)/admin/leads/calculator/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - List page renders `AdminErrorState` on `!ok`; detail 404s only on not-found, error-states on error
    - Placeholder short-circuit + `connection()` byte-unchanged
    - `bun run typecheck` green across the calculator-leads slice; lint clean
  </acceptance_criteria>
  <done>Both consumers narrow the unions; placeholder intact; typecheck + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calculator-leads-queries -> pages | Caught DB error returned as a variant; never rendered as exception text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Information disclosure | calculator-leads pages | mitigate | `AdminErrorState` fixed copy; exception stays in `logger.error`. |
| T-13-04b | Repudiation / availability | calculator/[id] | mitigate | DB error -> error state, never a 404; missing row still 404s. |
</threat_model>

<verification>
- `bun test tests/unit/admin/calculator-leads-queries.test.ts` green
- `bun run typecheck` green across the slice
- grep gate: `grep -c "EMPTY_RESULT" src/lib/admin/calculator-leads-queries.ts` returns 0
</verification>

<success_criteria>
- Calculator-leads list distinguishes failure from empty; detail distinguishes error from 404
- `logger.error` retained; placeholder short-circuit intact
</success_criteria>

<output>
Create `.planning/phases/13-admin-error-observability/13-03-SUMMARY.md` when done.
</output>

---
phase: 13-admin-error-observability
plan: 09
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/admin/emails-queries.ts
  - src/app/(admin)/admin/emails/page.tsx
  - src/app/(admin)/admin/emails/[id]/page.tsx
  - tests/unit/admin/emails-queries.test.ts
autonomous: true
requirements: [ADMINERR-01, ADMINERR-03, ADMINERR-04]

must_haves:
  truths:
    - "When getQueueCounts throws, /admin/emails shows an error state, not a healthy all-zero queue"
    - "When the scheduled-emails list query throws, the page shows the error state, not an empty list"
    - "A getQueueCounts failure does not blank the list, and vice versa (independent variants)"
    - "When getScheduledEmailById throws, the detail page shows the error state, not a 404; a missing row still 404s"
    - "retryScheduledEmail keeps its existing RetryResult contract after getScheduledEmailById changes"
  artifacts:
    - path: "src/lib/admin/emails-queries.ts"
      provides: "getQueueCounts -> AdminQueryResult<QueueCounts>; listScheduledEmailsForAdmin -> AdminQueryResult; getScheduledEmailById -> AdminDetailResult; retryScheduledEmail internal caller narrowed"
      contains: "AdminQueryResult<QueueCounts>"
    - path: "src/app/(admin)/admin/emails/page.tsx"
      provides: "one grid-spanning AdminErrorState on queue-counts error; separate error/empty for the list"
      contains: "AdminErrorState"
    - path: "src/app/(admin)/admin/emails/[id]/page.tsx"
      provides: "switch on status"
      contains: "AdminErrorState"
  key_links:
    - from: "src/app/(admin)/admin/emails/page.tsx"
      to: "getQueueCounts"
      via: "narrows counts.ok before reading the 4 stat cards"
      pattern: "counts\\.ok"
    - from: "retryScheduledEmail"
      to: "getScheduledEmailById"
      via: "narrows result.status === 'found' before reading data"
      pattern: "status === 'found'"
---

<objective>
Migrate the emails read layer: `getQueueCounts` returns `AdminQueryResult<QueueCounts>` (no more falsely-healthy all-zero record on error), `listScheduledEmailsForAdmin` returns `AdminQueryResult`, and `getScheduledEmailById` returns `AdminDetailResult`. Migrate the internal write-helper caller `retryScheduledEmail` in lockstep. Update the `/admin/emails` page to render one grid-spanning `AdminErrorState` on a queue-counts failure (RESEARCH open-Q1 recommendation) while keeping the list error/empty independent, and update the detail loader to 3-way switch. ALSO update the stale v4-era comment blocks that now contradict the new contract: the `getQueueCounts`/`listScheduledEmailsForAdmin` NOTE in `emails-queries.ts` (~line 159) and the docblock in `emails/page.tsx` (~lines 5-25) that claim queue counts are "intentionally NOT touched" / "returns the empty result shape; caller renders the empty state" — rewrite them to describe the returned-error-variant behavior (mirror how plan 08 updates its dashboard docblock). Leave no comment asserting the old return-[]/zero contract.

Purpose: ADMINERR-03 (queue) + ADMINERR-01 (scheduled-emails list) + ADMINERR-04 (detail) for emails. The list and counts share a `Promise.all` but are independent variants (Pitfall 4). Internal-caller hazard: `retryScheduledEmail` calls `getScheduledEmailById` and relies on the `null` contract.
Output: migrated `emails-queries.ts` (incl. the internal call site), both consumers updated, test asserting the queue error variant + list error variant + 3-way detail with the retry path still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-admin-error-observability/13-RESEARCH.md

<interfaces>
From src/lib/admin/query-result.ts (plan 13-01): `AdminQueryResult<T>`, `AdminDetailResult<T>`, `ok`, `err`, `found`, `notFoundResult`, `errResult`.
From src/components/admin/AdminErrorState.tsx (plan 13-01): `AdminErrorState({ resource?, message?, inline? })`.

Current emails contracts (src/lib/admin/emails-queries.ts):
- `getQueueCounts(): Promise<QueueCounts>` (line 108) — catch returns the all-zero record (~line 130, after `logger.error`); `QueueCounts = Record<EmailStatus, number>` (line 63)
- `listScheduledEmailsForAdmin(opts?): Promise<ListScheduledEmailsResult>` (line 163); `ListScheduledEmailsResult` line 72
- `getScheduledEmailById(id): Promise<... | null>` (line 269)
- existing precedent union `RetryResult = { ok: true; row } | { ok: false; reason }` (line 96)
- INTERNAL CALLER `retryScheduledEmail(id): Promise<RetryResult>` (line 301): `const existing = await getScheduledEmailById(id)` (line 302); `if (!existing) return { ok:false, reason:'not_found' }`

Consumers:
- emails/page.tsx (line ~88): `Promise.all([getQueueCounts(), listScheduledEmailsForAdmin(...)])`; renders 4 stat cards reading `counts[s]` (~96-128); a `filterOptions` builder at ~96-103 reads `counts[s]` for chip counts; list empty-state ~139-156
- emails/[id]/page.tsx (line ~75): `EmailDetailLoader`; `if (!row) notFound()` ~76-78; placeholder short-circuit + `connection()` before the read
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate emails reads (queue + list + detail) + the retry internal caller + update tests</name>
  <read_first>
    - src/lib/admin/emails-queries.ts (full file — `getQueueCounts` line 108 incl. its all-zero catch; `listScheduledEmailsForAdmin` line 163; `getScheduledEmailById` line 269; `retryScheduledEmail` line 301 calling line 302; the `RetryResult` union line 96)
    - tests/unit/admin/emails-queries.test.ts (the existing `getQueueCounts` sanity test + the `retryScheduledEmail` write-helper case + mock harness)
    - 13-RESEARCH.md Pattern 1, Pattern 3, Pitfall 4 (independent variants), §Internal-Caller Hazard (option 1), Code Examples (the `getQueueCounts` error-variant test)
  </read_first>
  <behavior>
    - `getQueueCounts` catch returns `err()` (`{ ok: false, error: true }`), NOT the `{pending:0,sent:0,failed:0,cancelled:0}` record; `logger.error` once
    - `listScheduledEmailsForAdmin` catch returns `err()`; zero rows returns `ok` empty
    - `getScheduledEmailById` returns `found(row)` / `notFoundResult()` / `errResult()`; `logger.error` once on throw
    - `retryScheduledEmail` keeps its EXISTING `RetryResult` contract (still returns `{ ok:false, reason:'not_found' }` when the row is absent/error) — existing retry test still passes
  </behavior>
  <action>
    Change `getQueueCounts` to `Promise<AdminQueryResult<QueueCounts>>`: wrap the computed counts record in `ok(...)`; replace the catch's all-zero `return` with `return err()` (keep `logger.error('emails-queries.getQueueCounts failed', error)`). Change `listScheduledEmailsForAdmin` to `Promise<AdminQueryResult<ListScheduledEmailsResult>>`: wrap success in `ok(...)`, catch returns `err()` (keep `logger.error`); delete any `EMPTY_RESULT`-style default if present. Change `getScheduledEmailById` to `Promise<AdminDetailResult<...>>`: `return row ? found(row) : notFoundResult()`, catch `logger.error(...)` + `return errResult()`. THEN migrate `retryScheduledEmail` (line ~302) in the SAME task: replace `const existing = await getScheduledEmailById(id); if (!existing) return { ok:false, reason:'not_found' }` with `const result = await getScheduledEmailById(id); const existing = result.status === 'found' ? result.data : null; if (!existing) return { ok:false, reason:'not_found' }`; its `RetryResult` return stays byte-equal. Keep the `RetryResult` union as-is (it is a separate, pre-existing mutation contract, NOT replaced by `AdminQueryResult`). Import the new constructors/types from `@/lib/admin/query-result`. Update `tests/unit/admin/emails-queries.test.ts`: rewrite the `getQueueCounts` test to assert `{ ok: false, error: true }` on throw (not the zero record) + `logger.error` once, plus an ok-counts success case; rewrite the list DB-error to `{ ok:false, error:true }` + ok-empty; add `getScheduledEmailById` found/not-found/error; verify the existing `retryScheduledEmail` test still passes.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/emails-queries.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `getQueueCounts` -> `AdminQueryResult<QueueCounts>`; catch returns `err()` (NOT the zero record)
    - `listScheduledEmailsForAdmin` -> `AdminQueryResult`; `getScheduledEmailById` -> `AdminDetailResult`
    - `retryScheduledEmail` narrows via `result.status === 'found' ? result.data : null`; `RetryResult` contract unchanged
    - `logger.error` retained on all three read catch paths
    - Tests assert queue error variant + list error variant + ok-empty + 3-way detail; existing retry test still passes
    - `bun run typecheck` green
  </acceptance_criteria>
  <done>Queue/list/detail reads return the unions; the retry helper narrowed in lockstep; tests green incl. retry path.</done>
</task>

<task type="auto">
  <name>Task 2: Update both emails consumers (independent queue + list variants)</name>
  <read_first>
    - src/app/(admin)/admin/emails/page.tsx (the `Promise.all([getQueueCounts(), listScheduledEmailsForAdmin(...)])` ~88; the 4 stat cards reading `counts[s]` ~96-128; the `filterOptions` builder ~96-103; the list empty-state ~139-156)
    - src/app/(admin)/admin/emails/[id]/page.tsx (`EmailDetailLoader` ~75; `if (!row) notFound()` ~76-78; placeholder short-circuit + `connection()`)
    - 13-RESEARCH.md Pitfall 4 (narrow each independently), Open Question 1 (one grid-spanning `AdminErrorState` recommended), Pattern 3, Pitfall 2
  </read_first>
  <action>
    In `emails/page.tsx`: keep the `Promise.all` shape; both results are now independent `AdminQueryResult`s (it still resolves — neither throws). Render the stat-card region from `counts`: when `counts.ok` render the 4 stat cards from `counts.data`, else render ONE grid-spanning `<AdminErrorState inline resource="queue health" />` in place of the 4-card row (research Open-Q1 recommendation). Guard the `filterOptions` builder that reads `counts[s]` for chip counts: when `!counts.ok`, omit the per-status counts (build the chips without count badges rather than reading off the error variant). Render the list region from the list result independently: `if (!list.ok) { /* render <AdminErrorState resource="scheduled emails" /> in the list area */ } else { const { rows, prevCursor, nextCursor } = list.data; /* existing empty/table branch */ }`. A counts failure must not blank the list and vice-versa. In `emails/[id]/page.tsx` `EmailDetailLoader`: keep the placeholder `notFound()` short-circuit + `await connection()` unchanged; replace `if (!row) notFound()` with the 3-way guards (`'not-found'` -> `notFound()`, `'error'` -> `return <AdminErrorState resource="scheduled email" />`, else `const row = result.data`). Import `AdminErrorState`.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint "src/app/(admin)/admin/emails/page.tsx" "src/app/(admin)/admin/emails/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Queue-counts failure renders ONE grid-spanning `AdminErrorState` (not 4 falsely-healthy zero cards); success renders the 4 stat cards from `counts.data`
    - `filterOptions` chip builder guarded when `!counts.ok` (no read off the error variant)
    - List failure renders `AdminErrorState` independently; a counts failure does not blank the list and vice-versa
    - Detail loader 404s only on not-found, error-states on error; placeholder short-circuit + `connection()` byte-unchanged
    - `bun run typecheck` green across the emails slice; lint clean
  </acceptance_criteria>
  <done>Queue + list narrow independently; queue error is not a healthy zero; detail distinguishes error from 404; placeholder intact; typecheck + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| emails-queries -> emails pages | Caught DB error returned as a variant; never rendered as exception text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Repudiation / availability | getQueueCounts / emails page | mitigate | Queue error renders an explicit error state, NOT a falsely-healthy all-zero queue (the ADMINERR-03 fix). |
| T-13-15 | Information disclosure | emails pages | mitigate | `AdminErrorState` fixed copy; exception stays in `logger.error`. |
| T-13-04g | Repudiation / availability | emails/[id] | mitigate | DB error -> error state, never a 404; missing row still 404s. |
| T-13-16 | Tampering | retryScheduledEmail | accept | `RetryResult` contract unchanged; internal narrow preserves "no row -> not_found". Out of scope per CONTEXT. |
</threat_model>

<verification>
- `bun test tests/unit/admin/emails-queries.test.ts` green (queue error variant + list error variant + ok-empty + 3-way + retry path)
- `bun run typecheck` green across the slice
- grep gate: the `getQueueCounts` catch no longer returns a zero record (`grep -A4 "getQueueCounts failed" src/lib/admin/emails-queries.ts` shows `return err()`, not `pending: 0`)
</verification>

<success_criteria>
- Queue health distinguishes failure from a healthy zero (ADMINERR-03); list distinguishes failure from empty (ADMINERR-01); detail distinguishes error from 404 (ADMINERR-04)
- Queue and list fail independently; `RetryResult` mutation contract intact
- `logger.error` retained; placeholder short-circuit intact
</success_criteria>

<output>
Create `.planning/phases/13-admin-error-observability/13-09-SUMMARY.md` when done.
</output>

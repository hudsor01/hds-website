---
phase: 13-admin-error-observability
plan: 07
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/admin/blog-queries.ts
  - src/app/(admin)/admin/blog/page.tsx
  - src/app/(admin)/admin/blog/[id]/edit/page.tsx
  - tests/unit/admin/blog-queries.test.ts
autonomous: true
requirements: [ADMINERR-01, ADMINERR-04]

must_haves:
  truths:
    - "When the blog list query throws, the page shows the error state, not an empty list"
    - "When getBlogPostForAdmin throws, the edit page shows the error state, not a 404"
    - "When the blog post genuinely does not exist, the edit page still 404s"
    - "updateBlogPost and toggleBlogPostPublished keep their existing return contract after getBlogPostForAdmin changes"
  artifacts:
    - path: "src/lib/admin/blog-queries.ts"
      provides: "listBlogPostsForAdmin -> AdminQueryResult; getBlogPostForAdmin -> AdminDetailResult; 2 internal callers narrowed"
      contains: "AdminDetailResult"
    - path: "src/app/(admin)/admin/blog/page.tsx"
      provides: "renders AdminErrorState on !ok"
      contains: "AdminErrorState"
    - path: "src/app/(admin)/admin/blog/[id]/edit/page.tsx"
      provides: "switch on status; getAuthors/getTags branch unaffected"
      contains: "AdminErrorState"
  key_links:
    - from: "updateBlogPost"
      to: "getBlogPostForAdmin"
      via: "narrows result.status === 'found' before reading data"
      pattern: "status === 'found'"
    - from: "src/app/(admin)/admin/blog/[id]/edit/page.tsx"
      to: "getBlogPostForAdmin"
      via: "switch on result.status"
      pattern: "status === 'not-found'"
---

<objective>
Migrate the blog read layer to the unions, update the list page + edit loader to render `AdminErrorState`, AND migrate the two internal write-helper callers of `getBlogPostForAdmin` (`updateBlogPost`, `toggleBlogPostPublished`) in lockstep. The edit loader calls `getBlogPostForAdmin` inside a `Promise.all` with `getAuthors`/`getTags` (not in scope) — only the post result becomes the 3-way union.

Purpose: ADMINERR-01 + ADMINERR-04 for blog. Internal-caller hazard: two write helpers call `getBlogPostForAdmin` and rely on the `null` contract.
Output: migrated `blog-queries.ts` (incl. both internal call sites), both consumers updated, test asserting the error variant + 3-way detail with write-helper paths still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-admin-error-observability/13-RESEARCH.md

<interfaces>
From src/lib/admin/query-result.ts (plan 13-01): `AdminQueryResult<T>`, `AdminDetailResult<T>`, `ok`, `err`, `found`, `notFoundResult`, `errResult`.
From src/components/admin/AdminErrorState.tsx (plan 13-01): `AdminErrorState({ resource?, message?, inline? })`.

Current blog contracts (src/lib/admin/blog-queries.ts):
- `listBlogPostsForAdmin(opts?): Promise<ListBlogPostsResult>` (line 126) — catch returns `EMPTY_RESULT` (line 304); `EMPTY_RESULT` const line 74
- `getBlogPostForAdmin(id): Promise<... | null>` (line 308) — also calls `loadTagIdsForPosts` inside the try (the blog test mock already handles the join select)
- INTERNAL CALLER `updateBlogPost` (line 382): `const existing = await getBlogPostForAdmin(id)` (line 386); `if (!existing) return null`
- INTERNAL CALLER `toggleBlogPostPublished` (line 446): `const existing = await getBlogPostForAdmin(id)` (line 449); same pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate blog reads + the 2 internal callers + update tests</name>
  <read_first>
    - src/lib/admin/blog-queries.ts (full file — `listBlogPostsForAdmin` line 126; `getBlogPostForAdmin` line 308 incl. its `loadTagIdsForPosts` await; `updateBlogPost` line 382 calling line 386; `toggleBlogPostPublished` line 446 calling line 449)
    - tests/unit/admin/blog-queries.test.ts (DB-error case + the `updateBlogPost`/`toggleBlogPostPublished` write-helper cases + the join-select mock)
    - 13-RESEARCH.md §Internal-Caller Hazard (option 1 local narrow) + Pitfall 3, Pattern 1, Pattern 3, Code Examples (the note that `getBlogPostForAdmin` additionally awaits `loadTagIdsForPosts` inside the try, so a throw there also yields `errResult()`)
  </read_first>
  <behavior>
    - `listBlogPostsForAdmin` catch returns `err()`; zero rows returns `ok` empty; `logger.error` once
    - `getBlogPostForAdmin` returns `found(post)` / `notFoundResult()` / `errResult()`; a throw in either the post select OR the `loadTagIdsForPosts` await yields `errResult()`; `logger.error` once
    - `updateBlogPost` and `toggleBlogPostPublished` keep their EXISTING return contracts — existing write-helper tests still pass
  </behavior>
  <action>
    Change `listBlogPostsForAdmin` to `Promise<AdminQueryResult<ListBlogPostsResult>>`: wrap success in `ok(...)`, catch returns `err()` (keep `logger.error`); delete `EMPTY_RESULT`. Change `getBlogPostForAdmin` to `Promise<AdminDetailResult<...>>` (use the existing post-detail type the consumer reads): keep the whole try (post select + `loadTagIdsForPosts`) inside; `return post ? found({...}) : notFoundResult()`; catch `logger.error(...)` + `return errResult()`. THEN migrate both internal callers in the SAME task: in `updateBlogPost` (line ~386) and `toggleBlogPostPublished` (line ~449) replace `const existing = await getBlogPostForAdmin(id); if (!existing) return null` with `const result = await getBlogPostForAdmin(id); const existing = result.status === 'found' ? result.data : null; if (!existing) return null`; return contracts byte-equal. Import constructors/types from `@/lib/admin/query-result`. Update `tests/unit/admin/blog-queries.test.ts`: rewrite DB-error to `{ ok: false, error: true }`; add ok-empty; add `getBlogPostForAdmin` found/not-found/error (the existing join-select mock covers the `loadTagIdsForPosts` await); verify the two write-helper tests still pass.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/blog-queries.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `listBlogPostsForAdmin` -> `AdminQueryResult`; `getBlogPostForAdmin` -> `AdminDetailResult`; `EMPTY_RESULT` deleted
    - Both internal callers narrow via `result.status === 'found' ? result.data : null`; contracts unchanged
    - `logger.error` retained on both read catch paths
    - Tests assert error variant + ok-empty + 3-way; both write-helper tests still pass
    - `bun run typecheck` green
  </acceptance_criteria>
  <done>Reads return the unions; both write helpers narrowed in lockstep; tests green incl. write-helper paths.</done>
</task>

<task type="auto">
  <name>Task 2: Update both blog consumers</name>
  <read_first>
    - src/app/(admin)/admin/blog/page.tsx (line ~77 destructure; uses `ResourceListPage` shell + inline empty ~94-105 — do NOT modify `ResourceListPage.tsx`)
    - src/app/(admin)/admin/blog/[id]/edit/page.tsx (line ~49 `Promise.all` with `getAuthors`/`getTags`; `if (!row) notFound()` ~53-55; placeholder short-circuit + `connection()`)
    - 13-RESEARCH.md Pattern 1, Pattern 3, Pitfall 2
  </read_first>
  <action>
    In `blog/page.tsx`: replace the destructure with `const result = await listBlogPostsForAdmin(...); if (!result.ok) { return <AdminErrorState resource="blog posts" /> } const { rows, prevCursor, nextCursor } = result.data`; keep the `ResourceListPage` empty/table branch. In `blog/[id]/edit/page.tsx`: keep the placeholder `notFound()` short-circuit + `await connection()` unchanged; keep the `Promise.all([getBlogPostForAdmin(id), getAuthors(), getTags()])` but the FIRST element is now an `AdminDetailResult`. After awaiting: `if (postResult.status === 'not-found') { notFound() } if (postResult.status === 'error') { return <AdminErrorState resource="blog post" /> } const post = postResult.data` then use `authors`/`tags` (unchanged) for the render. Import `AdminErrorState`. Do NOT modify `ResourceListPage.tsx`, `getAuthors`, or `getTags`.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint "src/app/(admin)/admin/blog/page.tsx" "src/app/(admin)/admin/blog/[id]/edit/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - List page renders `AdminErrorState` on `!ok`; edit loader 404s only on not-found, error-states on error
    - The `getAuthors`/`getTags` elements of the `Promise.all` are unchanged
    - `ResourceListPage.tsx` untouched; placeholder short-circuit + `connection()` byte-unchanged
    - `bun run typecheck` green across the blog slice; lint clean
  </acceptance_criteria>
  <done>Both consumers narrow the unions; authors/tags untouched; shared shell untouched; placeholder intact; typecheck + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blog-queries -> pages | Caught DB error returned as a variant; never rendered as exception text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Information disclosure | blog pages | mitigate | `AdminErrorState` fixed copy; exception stays in `logger.error`. |
| T-13-04f | Repudiation / availability | blog/[id]/edit | mitigate | DB error -> error state, never a 404; missing post still 404s. |
| T-13-11 | Tampering | updateBlogPost / toggleBlogPostPublished | accept | Write-helper contracts unchanged; internal narrow preserves "no row -> null". Out of scope per CONTEXT. |
</threat_model>

<verification>
- `bun test tests/unit/admin/blog-queries.test.ts` green incl. write-helper cases
- `bun run typecheck` green across the slice
- grep gate: `grep -c "EMPTY_RESULT" src/lib/admin/blog-queries.ts` returns 0
- `git diff --stat src/components/admin/ResourceListPage.tsx` shows no change
</verification>

<success_criteria>
- Blog list distinguishes failure from empty; edit page distinguishes error from 404 (authors/tags unaffected)
- Both internal write-helper callers migrated in lockstep, contracts intact
- `logger.error` retained; placeholder short-circuit + shared shell intact
</success_criteria>

<output>
Create `.planning/phases/13-admin-error-observability/13-07-SUMMARY.md` when done.
</output>

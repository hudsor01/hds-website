---
phase: 13-admin-error-observability
plan: 05
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/admin/showcase-queries.ts
  - src/app/(admin)/admin/showcase/page.tsx
  - src/app/(admin)/admin/showcase/[id]/edit/page.tsx
  - tests/unit/admin/showcase-queries.test.ts
autonomous: true
requirements: [ADMINERR-01, ADMINERR-04]

must_haves:
  truths:
    - "When the showcase list query throws, the page shows the error state, not an empty list"
    - "When getShowcaseById throws, the edit page shows the error state, not a 404"
    - "When the showcase row genuinely does not exist, the edit page still 404s"
    - "updateShowcase and toggleShowcasePublished keep their existing return contract after getShowcaseById changes"
  artifacts:
    - path: "src/lib/admin/showcase-queries.ts"
      provides: "listShowcasesForAdmin -> AdminQueryResult; getShowcaseById -> AdminDetailResult; 2 internal callers narrowed"
      contains: "AdminDetailResult"
    - path: "src/app/(admin)/admin/showcase/page.tsx"
      provides: "renders AdminErrorState on !ok"
      contains: "AdminErrorState"
    - path: "src/app/(admin)/admin/showcase/[id]/edit/page.tsx"
      provides: "switch on status"
      contains: "AdminErrorState"
  key_links:
    - from: "updateShowcase"
      to: "getShowcaseById"
      via: "narrows result.status === 'found' before reading data"
      pattern: "status === 'found'"
    - from: "src/app/(admin)/admin/showcase/[id]/edit/page.tsx"
      to: "getShowcaseById"
      via: "switch on result.status"
      pattern: "status === 'not-found'"
---

<objective>
Migrate the showcase read layer to the unions, update the list page + edit loader to render `AdminErrorState`, AND migrate the two internal write-helper callers of `getShowcaseById` (`updateShowcase`, `toggleShowcasePublished`) in lockstep so typecheck stays green.

Purpose: ADMINERR-01 + ADMINERR-04 for showcase. The internal-caller hazard (RESEARCH §Internal-Caller Hazard) is load-bearing: `getShowcaseById` is called by two write helpers that rely on the old `null` contract; changing it without updating them breaks typecheck.
Output: migrated `showcase-queries.ts` (incl. the two internal call sites), both consumers updated, test asserting the error variant + 3-way detail with write-helper paths still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-admin-error-observability/13-RESEARCH.md

<interfaces>
From src/lib/admin/query-result.ts (plan 13-01): `AdminQueryResult<T>`, `AdminDetailResult<T>`, `ok`, `err`, `found`, `notFoundResult`, `errResult`.
From src/components/admin/AdminErrorState.tsx (plan 13-01): `AdminErrorState({ resource?, message?, inline? })`.

Current showcase contracts (src/lib/admin/showcase-queries.ts):
- `listShowcasesForAdmin(opts?): Promise<ListShowcasesResult>` (line 82) — catch returns `EMPTY_RESULT` (line 194); `EMPTY_RESULT` const line 57
- `getShowcaseById(id): Promise<ShowcaseRow | null>` (line 206)
- INTERNAL CALLER `updateShowcase` (line 247): `const existing = await getShowcaseById(id)` (line 251); `if (!existing) return null`
- INTERNAL CALLER `toggleShowcasePublished` (line 293): `const existing = await getShowcaseById(id)` (line 296); same pattern; reads `existing.published`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate showcase reads + the 2 internal callers + update tests</name>
  <read_first>
    - src/lib/admin/showcase-queries.ts (full file — `listShowcasesForAdmin` line 82; `getShowcaseById` line 206; `updateShowcase` line 247 calling line 251; `toggleShowcasePublished` line 293 calling line 296)
    - tests/unit/admin/showcase-queries.test.ts (DB-error case + the existing `updateShowcase`/`toggleShowcasePublished` write-helper cases + mock harness)
    - 13-RESEARCH.md §Internal-Caller Hazard (option 1: local narrow `result.status === 'found' ? result.data : null`) + Pitfall 3, Pattern 1, Pattern 3
  </read_first>
  <behavior>
    - `listShowcasesForAdmin` catch returns `err()`; zero rows returns `ok` empty; `logger.error` once
    - `getShowcaseById` returns `found(row)` / `notFoundResult()` / `errResult()`; `logger.error` once on throw
    - `updateShowcase` and `toggleShowcasePublished` keep their EXISTING return contracts (still return `null` when the row is absent/error) — proven by the existing write-helper tests still passing
  </behavior>
  <action>
    Change `listShowcasesForAdmin` to `Promise<AdminQueryResult<ListShowcasesResult>>`: wrap success in `ok(...)`, catch returns `err()` (keep `logger.error`); delete `EMPTY_RESULT`. Change `getShowcaseById` to `Promise<AdminDetailResult<ShowcaseRow>>`: `return row ? found(row) : notFoundResult()`, catch `logger.error(...)` + `return errResult()`. THEN migrate the two internal callers in the SAME task (RESEARCH option 1, minimal diff): in `updateShowcase` (line ~251) replace `const existing = await getShowcaseById(id); if (!existing) return null` with `const result = await getShowcaseById(id); const existing = result.status === 'found' ? result.data : null; if (!existing) return null`; do the identical narrow in `toggleShowcasePublished` (line ~296) so `existing.published` still type-checks. Write-helper return contracts stay byte-equal (still out of scope per CONTEXT). Import constructors/types from `@/lib/admin/query-result`. Update `tests/unit/admin/showcase-queries.test.ts`: rewrite the list DB-error case to `{ ok: false, error: true }`; add ok-empty; add `getShowcaseById` found/not-found/error; verify the existing `updateShowcase`/`toggleShowcasePublished` tests still pass (the real `getShowcaseById` runs against the mocked db that returns a row, producing `found(row)`).
  </action>
  <verify>
    <automated>bun test tests/unit/admin/showcase-queries.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `listShowcasesForAdmin` -> `AdminQueryResult`; `getShowcaseById` -> `AdminDetailResult`; `EMPTY_RESULT` deleted
    - Both internal callers narrow via `result.status === 'found' ? result.data : null`; their return contracts unchanged
    - `logger.error` retained on both read catch paths
    - Test file asserts error variant + ok-empty + 3-way; existing write-helper tests still pass
    - `bun run typecheck` green (no `if (!existing)` against a `{status}` object remaining)
  </acceptance_criteria>
  <done>Reads return the unions; both write helpers narrowed in lockstep; tests green incl. write-helper paths.</done>
</task>

<task type="auto">
  <name>Task 2: Update both showcase consumers</name>
  <read_first>
    - src/app/(admin)/admin/showcase/page.tsx (line ~60 destructure; uses `ResourceListPage` shell + inline empty at ~77-89 — do NOT modify `ResourceListPage.tsx`, only this page)
    - src/app/(admin)/admin/showcase/[id]/edit/page.tsx (`EditLoader` ~45; `if (!row) notFound()` ~46-48; placeholder short-circuit + `connection()`)
    - 13-RESEARCH.md Pattern 1, Pattern 3, Pitfall 2
  </read_first>
  <action>
    In `showcase/page.tsx`: replace `const { rows, prevCursor, nextCursor } = await listShowcasesForAdmin(...)` with `const result = await listShowcasesForAdmin(...); if (!result.ok) { return <AdminErrorState resource="showcase entries" /> } const { rows, prevCursor, nextCursor } = result.data`; keep the `ResourceListPage` empty/table branch unchanged. In `showcase/[id]/edit/page.tsx` `EditLoader`: keep the placeholder `notFound()` short-circuit + `await connection()` unchanged; replace `if (!row) notFound()` with the 3-way guards (`'not-found'` -> `notFound()`, `'error'` -> `return <AdminErrorState resource="showcase entry" />`, else `const row = result.data`). Import `AdminErrorState`. Do NOT modify `src/components/admin/ResourceListPage.tsx` (shared, read-only this phase).
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint "src/app/(admin)/admin/showcase/page.tsx" "src/app/(admin)/admin/showcase/[id]/edit/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - List page renders `AdminErrorState` on `!ok`; edit loader 404s only on not-found, error-states on error
    - `ResourceListPage.tsx` untouched; placeholder short-circuit + `connection()` byte-unchanged
    - `bun run typecheck` green across the showcase slice; lint clean
  </acceptance_criteria>
  <done>Both consumers narrow the unions; shared shell untouched; placeholder intact; typecheck + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| showcase-queries -> pages | Caught DB error returned as a variant; never rendered as exception text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Information disclosure | showcase pages | mitigate | `AdminErrorState` fixed copy; exception stays in `logger.error`. |
| T-13-04d | Repudiation / availability | showcase/[id]/edit | mitigate | DB error -> error state, never a 404; missing row still 404s. |
| T-13-07 | Tampering | updateShowcase / toggleShowcasePublished | accept | Write-helper contracts unchanged; internal narrow preserves "no row -> null" behavior. Out of scope per CONTEXT. |
</threat_model>

<verification>
- `bun test tests/unit/admin/showcase-queries.test.ts` green incl. write-helper cases
- `bun run typecheck` green across the slice
- grep gate: `grep -c "EMPTY_RESULT" src/lib/admin/showcase-queries.ts` returns 0
- `git diff --stat src/components/admin/ResourceListPage.tsx` shows no change
</verification>

<success_criteria>
- Showcase list distinguishes failure from empty; edit page distinguishes error from 404
- Both internal write-helper callers migrated in lockstep, contracts intact
- `logger.error` retained; placeholder short-circuit + shared shell intact
</success_criteria>

<output>
Create `.planning/phases/13-admin-error-observability/13-05-SUMMARY.md` when done.
</output>

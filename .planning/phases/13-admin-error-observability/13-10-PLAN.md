---
phase: 13-admin-error-observability
plan: 10
type: execute
wave: 3
depends_on: ["13-02", "13-03", "13-04", "13-05", "13-06", "13-07", "13-08", "13-09"]
files_modified: []
autonomous: true
requirements: [ADMINERR-01, ADMINERR-02, ADMINERR-03, ADMINERR-04]

must_haves:
  truths:
    - "The full gate chain (lint + typecheck + test:unit + build) is green across the whole phase"
    - "next build stays green with no $~ / Neon-prerender errors (PPR placeholder path intact)"
    - "No EMPTY_RESULT default and no all-zero queue-counts default remains in any admin query file"
    - "query-result.ts is not server-only (client widgets compile)"
  artifacts:
    - path: ".planning/phases/13-admin-error-observability/13-10-SUMMARY.md"
      provides: "phase-wide gate result + per-requirement confirmation"
  key_links:
    - from: "all admin queries"
      to: "AdminQueryResult / AdminDetailResult"
      via: "every read returns a discriminated union; no swallowed default remains"
      pattern: "AdminQueryResult|AdminDetailResult"
---

<objective>
Run the full phase gate chain after all Wave 2 resource plans complete, and confirm every requirement is satisfied across the whole admin read surface. This is the build-safety net: many server components changed, so `next build` (PPR / cacheComponents) must stay green and the `$~` placeholder bug must not have been reintroduced.

Purpose: phase exit gate for ADMINERR-01..04 + the F5 build regression guard (RESEARCH Validation Architecture).
Output: a green gate chain and `13-10-SUMMARY.md` recording the result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-admin-error-observability/13-RESEARCH.md
@.planning/phases/13-admin-error-observability/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full phase gate chain</name>
  <read_first>
    - 13-VALIDATION.md (Gates row + Per-Requirement Verification Map)
    - 13-RESEARCH.md Validation Architecture (F1-F5 failure modes; F5 = `next build` green, no `$~`/prerender errors)
  </read_first>
  <action>
    Run, in order, `bun run lint`, `bun run typecheck`, `bun run test:unit`, and `bun run build`. All four must pass. For `test:unit`, the only acceptable failures are the documented pre-existing `homepage.test.tsx` + `navigation.test.tsx` cross-file RTL pollution (21 fail, 0 net-new vs the v6 baseline noted in STATE.md) — verify the count is unchanged and that ALL `tests/unit/admin/*-queries.test.ts` are green. For `build`, confirm it compiles successfully with no Neon "during prerendering, fetch() rejects" errors and the admin `[id]` routes are not stuck on a `$~`/Loading marker (Pitfall 2). If any gate fails on phase-owned code, fix the offending file (it belongs to one of the Wave 2 plans) and re-run; do not work around a real failure.
  </action>
  <verify>
    <automated>bun run lint && bun run typecheck && bun run test:unit && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `bun run lint` clean
    - `bun run typecheck` clean
    - `bun run test:unit`: all `tests/unit/admin/*` green; only the documented pre-existing homepage/navigation pollution failures remain (0 net-new)
    - `bun run build` compiles successfully; no `$~`/Neon-prerender errors
  </acceptance_criteria>
  <done>All four gates pass; admin tests green; build clean with the PPR placeholder path intact.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the phase invariants by grep + write the summary</name>
  <read_first>
    - All 8 `src/lib/admin/*-queries.ts` files (confirm no swallowed default remains)
    - src/lib/admin/query-result.ts (confirm no `server-only`)
  </read_first>
  <action>
    Run grep gates to confirm the migration is complete and no swallowed default survived: (1) no `EMPTY_RESULT` const remains in any list-query file (`grep -rln "EMPTY_RESULT" src/lib/admin/*-queries.ts` returns nothing); (2) the `getQueueCounts` catch returns `err()` not a zero record; (3) `query-result.ts` has no `import 'server-only'`; (4) every `get*ById` consumer 404s only on `'not-found'` (`grep -rl "status === 'not-found'" "src/app/(admin)/admin"` lists all 7 detail loaders). Then write `13-10-SUMMARY.md` recording: the gate-chain result, the grep-invariant results, and a per-requirement confirmation line for ADMINERR-01 (6 list pages + emails list), ADMINERR-02 (5 widgets), ADMINERR-03 (queue counts), ADMINERR-04 (7 detail loaders + 6 internal write-helper callers migrated). Note the manual-only verification from 13-VALIDATION.md (forced-DB-failure visual sweep) as an operator follow-up for `/gsd:verify-work`.
  </action>
  <verify>
    <automated>test -z "$(grep -rln 'EMPTY_RESULT' src/lib/admin/*-queries.ts)" && test "$(grep -c 'server-only' src/lib/admin/query-result.ts)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - No `EMPTY_RESULT` const remains in any admin query file
    - `getQueueCounts` catch returns `err()` (no zero record)
    - `query-result.ts` has no `server-only` import
    - All 7 detail loaders 404 only on `'not-found'`
    - `13-10-SUMMARY.md` written with the gate result + per-requirement confirmation + the manual visual-sweep follow-up
  </acceptance_criteria>
  <done>All grep invariants hold; the summary documents the phase-wide result and the operator visual-sweep follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase exit | The build gate confirms no info-leak surface and no PPR regression shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-17 | Tampering / availability | next build (PPR placeholder) | mitigate | `bun run build` gate confirms the `$~` placeholder bug was not reintroduced (Pitfall 2). |
| T-13-18 | Information disclosure | phase-wide | mitigate | Grep + review confirm no `AdminErrorState` call site passes a raw exception; `logger.error` retained on all catch paths. |
</threat_model>

<verification>
- `bun run lint && bun run typecheck && bun run test:unit && bun run build` all green
- No `EMPTY_RESULT` in any admin query file; `getQueueCounts` returns `err()`; `query-result.ts` not server-only
- All 7 detail loaders 404 only on `'not-found'`
</verification>

<success_criteria>
- The full gate chain is green; admin tests pass; build clean (PPR intact)
- Every ADMINERR requirement confirmed across the whole read surface
- The migration left no swallowed-error default behind
</success_criteria>

<output>
Create `.planning/phases/13-admin-error-observability/13-10-SUMMARY.md` when done.
</output>

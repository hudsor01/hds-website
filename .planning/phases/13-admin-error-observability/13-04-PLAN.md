---
phase: 13-admin-error-observability
plan: 04
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/admin/newsletter-queries.ts
  - src/app/(admin)/admin/newsletter/page.tsx
  - src/app/(admin)/admin/newsletter/[id]/page.tsx
  - tests/unit/admin/newsletter-queries.test.ts
autonomous: true
requirements: [ADMINERR-01, ADMINERR-04]

must_haves:
  truths:
    - "When the subscribers list query throws, the page shows the error state, not an empty list"
    - "When it returns zero rows, the page shows the existing empty state"
    - "When getSubscriberById throws, the detail page shows the error state, not a 404"
    - "When the subscriber genuinely does not exist, the detail page still 404s"
  artifacts:
    - path: "src/lib/admin/newsletter-queries.ts"
      provides: "listSubscribersForAdmin -> AdminQueryResult; getSubscriberById -> AdminDetailResult"
      contains: "AdminDetailResult"
    - path: "src/app/(admin)/admin/newsletter/page.tsx"
      provides: "renders AdminErrorState on !ok"
      contains: "AdminErrorState"
    - path: "src/app/(admin)/admin/newsletter/[id]/page.tsx"
      provides: "switch on status"
      contains: "AdminErrorState"
  key_links:
    - from: "src/app/(admin)/admin/newsletter/page.tsx"
      to: "listSubscribersForAdmin"
      via: "narrows result.ok"
      pattern: "result\\.ok"
    - from: "src/app/(admin)/admin/newsletter/[id]/page.tsx"
      to: "getSubscriberById"
      via: "switch on result.status"
      pattern: "status === 'not-found'"
---

<objective>
Migrate the newsletter read layer to the discriminated unions and update its list page + `SubscriberLoader` to render `AdminErrorState`.

Purpose: ADMINERR-01 + ADMINERR-04 for the newsletter resource.
Output: migrated `newsletter-queries.ts`, both consumers updated, test asserting the error variant + 3-way detail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-admin-error-observability/13-RESEARCH.md

<interfaces>
From src/lib/admin/query-result.ts (plan 13-01): `AdminQueryResult<T>`, `AdminDetailResult<T>`, `ok`, `err`, `found`, `notFoundResult`, `errResult`.
From src/components/admin/AdminErrorState.tsx (plan 13-01): `AdminErrorState({ resource?, message?, inline? })`.

Current newsletter contracts (src/lib/admin/newsletter-queries.ts):
- `listSubscribersForAdmin(opts?): Promise<ListSubscribersResult>` (line 125) — catch returns `EMPTY_RESULT` (line 261); `ListSubscribersResult` at line 68
- `getSubscriberById(id): Promise<... | null>` (line 269)
- `EMPTY_RESULT` const at line 75 (DELETE after wrapping)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate newsletter-queries reads + update tests</name>
  <read_first>
    - src/lib/admin/newsletter-queries.ts (full file — `listSubscribersForAdmin` line 125; `getSubscriberById` line 269; `EMPTY_RESULT` line 75)
    - tests/unit/admin/newsletter-queries.test.ts (DB-error case + mock harness)
    - 13-RESEARCH.md Pattern 1, Pattern 3, Code Examples
  </read_first>
  <behavior>
    - `listSubscribersForAdmin` catch returns `err()`; zero rows returns `ok` with empty `data.rows`; `logger.error` once
    - `getSubscriberById` returns `found(row)` / `notFoundResult()` / `errResult()`; `logger.error` once on throw
  </behavior>
  <action>
    Change `listSubscribersForAdmin` to `Promise<AdminQueryResult<ListSubscribersResult>>`: wrap success in `ok(...)`, catch returns `err()` (keep `logger.error`). Delete `EMPTY_RESULT`. Change `getSubscriberById` to `Promise<AdminDetailResult<SubscriberRow>>`: `return row ? found(row) : notFoundResult()` in the try, `logger.error(...)` + `return errResult()` in the catch. Import from `@/lib/admin/query-result`. Update `tests/unit/admin/newsletter-queries.test.ts`: rewrite DB-error to assert `{ ok: false, error: true }`; add ok-empty; add `getSubscriberById` found/not-found/error cases.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/newsletter-queries.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `listSubscribersForAdmin` -> `AdminQueryResult`; `getSubscriberById` -> `AdminDetailResult`; `EMPTY_RESULT` deleted
    - `logger.error` retained on both catch paths
    - Tests assert error variant + ok-empty + 3-way; green
  </acceptance_criteria>
  <done>Both reads return the unions; tests cover all branches; query-level typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Update both newsletter consumers</name>
  <read_first>
    - src/app/(admin)/admin/newsletter/page.tsx (line ~86 destructure; empty-state ~110-125)
    - src/app/(admin)/admin/newsletter/[id]/page.tsx (`SubscriberLoader` ~77; `if (!row) notFound()` ~78-80; placeholder short-circuit + `connection()`)
    - 13-RESEARCH.md Pattern 1, Pattern 3, Pitfall 2
  </read_first>
  <action>
    In `newsletter/page.tsx`: replace destructure with `const result = await listSubscribersForAdmin(...); if (!result.ok) { return <AdminErrorState resource="subscribers" /> } const { rows, prevCursor, nextCursor } = result.data`; keep the empty/table branch. In `newsletter/[id]/page.tsx` `SubscriberLoader`: keep the placeholder `notFound()` short-circuit + `await connection()` unchanged; replace `if (!row) notFound()` with the 3-way guards (`'not-found'` -> `notFound()`, `'error'` -> `return <AdminErrorState resource="subscriber" />`, else `const row = result.data`). Import `AdminErrorState`.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint "src/app/(admin)/admin/newsletter/page.tsx" "src/app/(admin)/admin/newsletter/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - List page renders `AdminErrorState` on `!ok`; detail 404s only on not-found, error-states on error
    - Placeholder short-circuit + `connection()` byte-unchanged
    - `bun run typecheck` green across the newsletter slice; lint clean
  </acceptance_criteria>
  <done>Both consumers narrow the unions; placeholder intact; typecheck + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| newsletter-queries -> pages | Caught DB error returned as a variant; never rendered as exception text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Information disclosure | newsletter pages | mitigate | `AdminErrorState` fixed copy; exception stays in `logger.error`. |
| T-13-04c | Repudiation / availability | newsletter/[id] | mitigate | DB error -> error state, never a 404; missing row still 404s. |
</threat_model>

<verification>
- `bun test tests/unit/admin/newsletter-queries.test.ts` green
- `bun run typecheck` green across the slice
- grep gate: `grep -c "EMPTY_RESULT" src/lib/admin/newsletter-queries.ts` returns 0
</verification>

<success_criteria>
- Newsletter list distinguishes failure from empty; detail distinguishes error from 404
- `logger.error` retained; placeholder short-circuit intact
</success_criteria>

<output>
Create `.planning/phases/13-admin-error-observability/13-04-SUMMARY.md` when done.
</output>

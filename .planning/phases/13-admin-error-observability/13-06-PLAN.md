---
phase: 13-admin-error-observability
plan: 06
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/lib/admin/testimonials-queries.ts
  - src/app/(admin)/admin/testimonials/page.tsx
  - src/app/(admin)/admin/testimonials/[id]/edit/page.tsx
  - tests/unit/admin/testimonials-queries.test.ts
autonomous: true
requirements: [ADMINERR-01, ADMINERR-04]

must_haves:
  truths:
    - "When the testimonials list query throws, the page shows the error state, not an empty list"
    - "When getTestimonialById throws, the edit page shows the error state, not a 404"
    - "When the testimonial genuinely does not exist, the edit page still 404s"
    - "toggleTestimonialPublished keeps its existing return contract after getTestimonialById changes"
  artifacts:
    - path: "src/lib/admin/testimonials-queries.ts"
      provides: "listTestimonialsForAdmin -> AdminQueryResult; getTestimonialById -> AdminDetailResult; 1 internal caller narrowed"
      contains: "AdminDetailResult"
    - path: "src/app/(admin)/admin/testimonials/page.tsx"
      provides: "renders AdminErrorState on !ok"
      contains: "AdminErrorState"
    - path: "src/app/(admin)/admin/testimonials/[id]/edit/page.tsx"
      provides: "switch on status"
      contains: "AdminErrorState"
  key_links:
    - from: "toggleTestimonialPublished"
      to: "getTestimonialById"
      via: "narrows result.status === 'found' before reading data"
      pattern: "status === 'found'"
    - from: "src/app/(admin)/admin/testimonials/[id]/edit/page.tsx"
      to: "getTestimonialById"
      via: "switch on result.status"
      pattern: "status === 'not-found'"
---

<objective>
Migrate the testimonials read layer to the unions, update the list page + edit loader to render `AdminErrorState`, AND migrate the single internal write-helper caller of `getTestimonialById` (`toggleTestimonialPublished`) in lockstep.

Purpose: ADMINERR-01 + ADMINERR-04 for testimonials. Internal-caller hazard: `toggleTestimonialPublished` calls `getTestimonialById` and relies on the `null` contract.
Output: migrated `testimonials-queries.ts` (incl. the internal call site), both consumers updated, test asserting the error variant + 3-way detail with the write-helper path still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-admin-error-observability/13-RESEARCH.md

<interfaces>
From src/lib/admin/query-result.ts (plan 13-01): `AdminQueryResult<T>`, `AdminDetailResult<T>`, `ok`, `err`, `found`, `notFoundResult`, `errResult`.
From src/components/admin/AdminErrorState.tsx (plan 13-01): `AdminErrorState({ resource?, message?, inline? })`.

Current testimonials contracts (src/lib/admin/testimonials-queries.ts):
- `listTestimonialsForAdmin(opts?): Promise<ListTestimonialsResult>` (line 79) — catch returns `EMPTY_RESULT` (line 173); `EMPTY_RESULT` const line 53
- `getTestimonialById(id): Promise<... | null>` (line 181)
- INTERNAL CALLER `toggleTestimonialPublished` (line 251): `const existing = await getTestimonialById(id)` (line 254); `if (!existing) return null`; reads `existing.published`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate testimonials reads + the internal caller + update tests</name>
  <read_first>
    - src/lib/admin/testimonials-queries.ts (full file — `listTestimonialsForAdmin` line 79; `getTestimonialById` line 181; `toggleTestimonialPublished` line 251 calling line 254)
    - tests/unit/admin/testimonials-queries.test.ts (DB-error case + the existing `toggleTestimonialPublished` write-helper case + mock harness)
    - 13-RESEARCH.md §Internal-Caller Hazard (option 1 local narrow) + Pitfall 3, Pattern 1, Pattern 3
  </read_first>
  <behavior>
    - `listTestimonialsForAdmin` catch returns `err()`; zero rows returns `ok` empty; `logger.error` once
    - `getTestimonialById` returns `found(row)` / `notFoundResult()` / `errResult()`; `logger.error` once on throw
    - `toggleTestimonialPublished` keeps its EXISTING return contract (still returns `null` when the row is absent/error) — existing write-helper test still passes
  </behavior>
  <action>
    Change `listTestimonialsForAdmin` to `Promise<AdminQueryResult<ListTestimonialsResult>>`: wrap success in `ok(...)`, catch returns `err()` (keep `logger.error`); delete `EMPTY_RESULT`. Change `getTestimonialById` to `Promise<AdminDetailResult<TestimonialRow>>`: `return row ? found(row) : notFoundResult()`, catch `logger.error(...)` + `return errResult()`. THEN migrate the internal caller in the SAME task: in `toggleTestimonialPublished` (line ~254) replace `const existing = await getTestimonialById(id); if (!existing) return null` with `const result = await getTestimonialById(id); const existing = result.status === 'found' ? result.data : null; if (!existing) return null` so `existing.published` still type-checks; return contract byte-equal. Import constructors/types from `@/lib/admin/query-result`. Update `tests/unit/admin/testimonials-queries.test.ts`: rewrite DB-error to `{ ok: false, error: true }`; add ok-empty; add `getTestimonialById` found/not-found/error; verify the existing `toggleTestimonialPublished` test still passes.
  </action>
  <verify>
    <automated>bun test tests/unit/admin/testimonials-queries.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `listTestimonialsForAdmin` -> `AdminQueryResult`; `getTestimonialById` -> `AdminDetailResult`; `EMPTY_RESULT` deleted
    - `toggleTestimonialPublished` narrows via `result.status === 'found' ? result.data : null`; contract unchanged
    - `logger.error` retained on both read catch paths
    - Tests assert error variant + ok-empty + 3-way; existing write-helper test still passes
    - `bun run typecheck` green
  </acceptance_criteria>
  <done>Reads return the unions; the write helper narrowed in lockstep; tests green incl. write-helper path.</done>
</task>

<task type="auto">
  <name>Task 2: Update both testimonials consumers</name>
  <read_first>
    - src/app/(admin)/admin/testimonials/page.tsx (line ~65 destructure; uses `ResourceListPage` shell + inline empty ~82-93 — do NOT modify `ResourceListPage.tsx`)
    - src/app/(admin)/admin/testimonials/[id]/edit/page.tsx (`EditLoader` ~46; `if (!row) notFound()` ~47-49; placeholder short-circuit + `connection()`)
    - 13-RESEARCH.md Pattern 1, Pattern 3, Pitfall 2
  </read_first>
  <action>
    In `testimonials/page.tsx`: replace the destructure with `const result = await listTestimonialsForAdmin(...); if (!result.ok) { return <AdminErrorState resource="testimonials" /> } const { rows, prevCursor, nextCursor } = result.data`; keep the `ResourceListPage` empty/table branch. In `testimonials/[id]/edit/page.tsx` `EditLoader`: keep the placeholder `notFound()` short-circuit + `await connection()` unchanged; replace `if (!row) notFound()` with the 3-way guards (`'not-found'` -> `notFound()`, `'error'` -> `return <AdminErrorState resource="testimonial" />`, else `const row = result.data`). Import `AdminErrorState`. Do NOT modify `ResourceListPage.tsx`.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint "src/app/(admin)/admin/testimonials/page.tsx" "src/app/(admin)/admin/testimonials/[id]/edit/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - List page renders `AdminErrorState` on `!ok`; edit loader 404s only on not-found, error-states on error
    - `ResourceListPage.tsx` untouched; placeholder short-circuit + `connection()` byte-unchanged
    - `bun run typecheck` green across the testimonials slice; lint clean
  </acceptance_criteria>
  <done>Both consumers narrow the unions; shared shell untouched; placeholder intact; typecheck + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testimonials-queries -> pages | Caught DB error returned as a variant; never rendered as exception text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Information disclosure | testimonials pages | mitigate | `AdminErrorState` fixed copy; exception stays in `logger.error`. |
| T-13-04e | Repudiation / availability | testimonials/[id]/edit | mitigate | DB error -> error state, never a 404; missing row still 404s. |
| T-13-09 | Tampering | toggleTestimonialPublished | accept | Write-helper contract unchanged; internal narrow preserves "no row -> null". Out of scope per CONTEXT. |
</threat_model>

<verification>
- `bun test tests/unit/admin/testimonials-queries.test.ts` green incl. write-helper case
- `bun run typecheck` green across the slice
- grep gate: `grep -c "EMPTY_RESULT" src/lib/admin/testimonials-queries.ts` returns 0
- `git diff --stat src/components/admin/ResourceListPage.tsx` shows no change
</verification>

<success_criteria>
- Testimonials list distinguishes failure from empty; edit page distinguishes error from 404
- The internal write-helper caller migrated in lockstep, contract intact
- `logger.error` retained; placeholder short-circuit + shared shell intact
</success_criteria>

<output>
Create `.planning/phases/13-admin-error-observability/13-06-SUMMARY.md` when done.
</output>

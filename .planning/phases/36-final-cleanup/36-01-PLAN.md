---
phase: 36-final-cleanup
plan: 01
type: execute
---

<objective>
Delete old portfolio and case-studies routes, remove case-studies data layer, verify complete migration.

Purpose: Complete the showcase migration by removing all legacy routes and data layers.
Output: Clean codebase with only /showcase routes, all tests pass, build succeeds.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md

**Prerequisite:** Phase 34 complete (all references point to /showcase)

**Files to delete:**
- src/app/portfolio/ (entire directory)
- src/app/case-studies/ (entire directory)
- src/lib/case-studies.ts (data layer replaced by showcase.ts)

**Files to keep:**
- src/lib/schema/content.ts (case_studies table definition - keep for now, drop later if desired)
- src/lib/showcase.ts (new unified data layer)
- src/app/showcase/ (new unified routes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old portfolio routes</name>
  <files>src/app/portfolio/</files>
  <action>
Delete the entire portfolio directory:
- src/app/portfolio/page.tsx
- src/app/portfolio/[slug]/page.tsx
- src/app/portfolio/loading.tsx (if exists)

Verify no imports reference these files.
  </action>
  <verify>rm -rf src/app/portfolio && bun run typecheck passes</verify>
  <done>Portfolio routes deleted</done>
</task>

<task type="auto">
  <name>Task 2: Delete old case-studies routes</name>
  <files>src/app/case-studies/</files>
  <action>
Delete the entire case-studies directory:
- src/app/case-studies/page.tsx
- src/app/case-studies/[slug]/page.tsx
- src/app/case-studies/loading.tsx (if exists)

Verify no imports reference these files.
  </action>
  <verify>rm -rf src/app/case-studies && bun run typecheck passes</verify>
  <done>Case-studies routes deleted</done>
</task>

<task type="auto">
  <name>Task 3: Delete old case-studies data layer</name>
  <files>src/lib/case-studies.ts</files>
  <action>
Delete the case-studies data layer file:
- src/lib/case-studies.ts

This is safe because:
1. All routes now use showcase.ts
2. Data has been migrated to projects table
3. No other files should import from case-studies.ts

First verify no imports exist:
grep -r "from.*case-studies" src/ --include="*.ts" --include="*.tsx"
  </action>
  <verify>No imports found, file deleted, typecheck passes</verify>
  <done>Case-studies data layer deleted</done>
</task>

<task type="auto">
  <name>Task 4: Final verification</name>
  <files>None (verification only)</files>
  <action>
Run full verification suite:
1. bun run typecheck - all types valid
2. bun run build - production build succeeds
3. bun run test - unit tests pass (if applicable)

Verify:
- /showcase route exists in build output
- /showcase/[slug] route exists in build output
- No /portfolio or /case-studies in build output
- No dead code warnings
  </action>
  <verify>All verification checks pass</verify>
  <done>Migration complete and verified</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] src/app/portfolio/ deleted
- [ ] src/app/case-studies/ deleted
- [ ] src/lib/case-studies.ts deleted
- [ ] No orphaned imports remain
- [ ] `bun run typecheck` passes
- [ ] `bun run build` succeeds
- [ ] /showcase routes work correctly
</verification>

<success_criteria>
- All legacy routes removed
- All legacy data layers removed
- Clean codebase with unified showcase
- All tests pass
- Build succeeds
- v3.0 Showcase migration complete
</success_criteria>

<output>
After completion, create `.planning/phases/36-final-cleanup/36-01-SUMMARY.md`
Update ROADMAP.md progress table
</output>

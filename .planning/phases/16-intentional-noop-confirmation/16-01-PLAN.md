---
phase: 16-intentional-noop-confirmation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/db.test.ts
  - tests/unit/error-tracking.test.ts
  - tests/unit/notifications.test.ts
  - .planning/v6-AUDIT-FINDINGS.md
autonomous: true
requirements: [NOOP-01, NOOP-02]

must_haves:
  truths:
    - "`.planning/v6-AUDIT-FINDINGS.md` Section 2 reads as an accurate closed v6 record: each intentional no-op has a VERIFIED-INTENTIONAL or RESOLVED-IN-PHASE-NN disposition with a one-line rationale"
    - "The four formerly-DECIDE/CLEANUP items are reconciled to their resolving phase (admin silent-error-swallow -> Phase 13, admin pageTitle -> Phase 14, ErrorBoundary finding #3 -> Phase 12, logger group/groupEnd/table + ttl processingFees -> Phase 15) per the merged origin/main state"
    - "The four env-gated no-ops (db mock proxy, ad-conversions sendAdConversion, error-tracking reportError, Slack/Discord webhooks) are locked by a passing regression test asserting the documented no-op contract"
    - "No production source file is changed; no source export is added (Slack/Discord private fns stay private)"
  artifacts:
    - path: "tests/unit/db.test.ts"
      provides: "db mock-proxy no-op regression test (resolves [] when POSTGRES_URL unset)"
      contains: "@/lib/db"
    - path: "tests/unit/error-tracking.test.ts"
      provides: "reportError no-op regression test (no Sentry.captureException, no throw when SENTRY_DSN unset)"
      contains: "reportError"
    - path: "tests/unit/notifications.test.ts"
      provides: "notifyHighValueLead public-seam no-op regression test (no fetch when webhooks unset; low-score early-return)"
      contains: "notifyHighValueLead"
    - path: ".planning/v6-AUDIT-FINDINGS.md"
      provides: "Reconciled Section 2 closed v6 record"
      contains: "VERIFIED-INTENTIONAL"
  key_links:
    - from: "tests/unit/db.test.ts"
      to: "src/lib/db.ts"
      via: "import { db } from '@/lib/db' (module-load mock proxy under unset POSTGRES_URL)"
      pattern: "from '@/lib/db'"
    - from: "tests/unit/error-tracking.test.ts"
      to: "src/lib/error-tracking.ts"
      via: "import reportError + spy on @sentry/nextjs captureException"
      pattern: "reportError"
    - from: "tests/unit/notifications.test.ts"
      to: "src/lib/notifications.ts"
      via: "import notifyHighValueLead (only exported seam); assert fetch not called"
      pattern: "notifyHighValueLead"
---

<objective>
Close the v6 audit loop. Two halves, one plan:

1. **NOOP-02 (tests):** Add three cheap regression tests that lock the documented no-op behavior of the env-gated integrations so a future change that accidentally makes them throw or do real work without config is caught. The fourth target (sendAdConversion) is already covered by the existing `tests/unit/ad-conversions.test.ts` no-op block — verify it stays green, do not duplicate.
2. **NOOP-01 (doc):** Reconcile `.planning/v6-AUDIT-FINDINGS.md` Section 2 so every intentional no-op carries a final disposition (VERIFIED-INTENTIONAL or RESOLVED-IN-PHASE-NN) with a one-line rationale, matching what phases 11-15 actually shipped on `origin/main`.

Purpose: The audit produced 50 "intentional no-op" findings. The 6 genuine stubs were fixed in phases 11-15. This phase finalizes the rest so the next audit recognizes them as intentional-by-record (NOOP-01) and intentional-by-contract (NOOP-02), not re-flagged.

Output: 3 new test files under `tests/unit/`, an updated `.planning/v6-AUDIT-FINDINGS.md`. No production source behavior change. No new source exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-intentional-noop-confirmation/16-CONTEXT.md
@.planning/phases/16-intentional-noop-confirmation/16-RESEARCH.md
@.planning/v6-AUDIT-FINDINGS.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from source + tests/setup.ts. No codebase exploration required. -->

tests/setup.ts (the test harness — read it first):
- Registers `mock.module('@/env', () => ({ env: TEST_ENV }))`. TEST_ENV contains ONLY:
  NODE_ENV, CSRF_SECRET, RESEND_API_KEY, NEXT_PUBLIC_GA_MEASUREMENT_ID, npm_package_version,
  BASE_URL, NEXT_PUBLIC_BASE_URL, ADMIN_SECRET, CRON_SECRET.
  -> GOOGLE_ADS_*, POSTGRES_URL, SENTRY_DSN, SLACK_WEBHOOK_URL, DISCORD_WEBHOOK_URL are ALL ABSENT
     by default. The "creds unset" / no-op path is the DEFAULT in every test — no env manipulation needed.
- `globalThis.fetch` is mocked globally; `beforeEach`/`afterEach` both call `mock.restore()` then re-apply
  ONLY the critical mocks (@/env, logger, next/cache, next/server). A `mock.module('@sentry/nextjs', ...)`
  or a global-fetch spy registered at file top is CLEARED between tests. Re-register inside the test
  file's own `beforeEach` (which runs AFTER setup's beforeEach), or use a local per-test fetch spy.
- `@/lib/logger` is mocked to a no-op logger instance. Do not assert on logger calls.

src/lib/db.ts — db mock proxy (Target 1):
- `const hasNoDatabase = !env.POSTGRES_URL` evaluated at MODULE LOAD (line 21).
- `export const db` is `createMockDb()` when POSTGRES_URL unset (line 70). The proxy: every property
  access returns a function returning a chainable Proxy; the chainable is a thenable whose then/catch/finally
  resolve `Promise.resolve([])`. So `await db.select()` -> `[]`; `await db.select().from({}).where({})` -> `[]`.
  Args are ignored by the mock (passing `{}` stands in for a table). No `server-only` guard — imports cleanly.

src/lib/error-tracking.ts — reportError (Target 2):
  `export function reportError(error: unknown, tags: Record<string, string>): void`
  Body: `if (!env.SENTRY_DSN) return` BEFORE `Sentry.captureException(error, { tags })`.
  Imports `import * as Sentry from '@sentry/nextjs'` (^10.55.0 installed).

src/lib/notifications.ts — public seam (Target 4):
  `export async function notifyHighValueLead(lead: LeadNotification): Promise<void>` is the ONLY export.
  `sendSlackNotification` / `sendDiscordNotification` are MODULE-PRIVATE — DO NOT import them, DO NOT add exports.
  Behavior: early-return (no fetch) if `lead.leadScore < NOTIFICATION_MINIMUM_THRESHOLD` (= 70, from
  src/lib/constants/lead-scoring.ts). For leadScore >= 70 it `Promise.allSettled([sendSlack, sendDiscord])`;
  with both webhooks unset, each inner fn `logger.warn(...)` + `return false` BEFORE any fetch -> resolves undefined.
  `LeadNotification` required fields: leadId, firstName, lastName, email, leadScore, leadQuality, source
  (optionals: phone, company, service, budget, timeline, calculatorType).

tests/unit/ad-conversions.test.ts (existing — Target 3, verify only):
  `describe('sendAdConversion (no-op gates)')` at line 202 already has the unset-env case
  ("does nothing when GOOGLE_ADS_* env is unset", lines 226-233) + 3 more no-op gates. NOOP-02 for
  sendAdConversion is already satisfied — verify the block is green, cite it; add NOTHING unless a gap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NOOP-02 regression tests for the env-gated no-ops</name>
  <files>tests/unit/db.test.ts, tests/unit/error-tracking.test.ts, tests/unit/notifications.test.ts</files>
  <read_first>
    - tests/setup.ts (the harness: TEST_ENV omits all gating vars; mock.restore in beforeEach/afterEach)
    - src/lib/db.ts, src/lib/error-tracking.ts, src/lib/notifications.ts (the modules under test)
    - tests/unit/ad-conversions.test.ts lines 202-281 (the existing no-op-gate idiom: local fetch spy + assert-not-called)
    - 16-RESEARCH.md "NOOP-02 Targets" + "Code Examples" + "Common Pitfalls"
  </read_first>
  <action>
    First, VERIFY (do not duplicate) the ad-conversions coverage: run the ad-conversions test file and confirm the `sendAdConversion (no-op gates)` describe block (the "does nothing when GOOGLE_ADS_* env is unset" case + the three other gates) is green. If green, NOOP-02 for sendAdConversion is satisfied — cite it in the SUMMARY. Add a new assertion ONLY if a genuine gap exists.

    Then create three NEW test files, each driving its module's no-op path via the DEFAULT unset env (TEST_ENV omits every gating var, so no env manipulation is needed). Each test asserts the documented no-op contract: no throw + the documented outcome ([] / no Sentry call / no fetch). Assert OUTSIDE any mocked callback (these functions swallow throws, so an expect() inside a mock callback would falsely pass — see ad-conversions.test.ts:344-345 and RESEARCH anti-patterns).

    tests/unit/db.test.ts — POSTGRES_URL unset means `db` IS createMockDb() at module load. Import `{ db } from '@/lib/db'`. Assert: `await db.select()` deep-equals `[]`; a representative chained query `await db.select().from({}).where({})` resolves to `[]` (args ignored by the mock); neither throws. Do NOT mock `@neondatabase/serverless` or `drizzle-orm` — the mock proxy IS the behavior under test.

    tests/unit/error-tracking.test.ts — SENTRY_DSN unset. Spy on `@sentry/nextjs` `captureException` via `mock.module('@sentry/nextjs', () => ({ captureException }))`. CRITICAL: re-register that mock.module inside this file's own `beforeEach` (setup.ts `mock.restore()` clears it between tests — RESEARCH Pitfall 2); import `reportError` lazily (`await import('@/lib/error-tracking')`) inside the test so it binds to the live mock. Assert: `reportError(new Error('boom'), { route: '/x' })` does not throw AND `captureException` `not.toHaveBeenCalled()`.

    tests/unit/notifications.test.ts — drive the PUBLIC seam `notifyHighValueLead` (Slack/Discord are NOT exported — do NOT import or export them). Install a LOCAL fetch spy in `beforeEach`, restore in `afterEach` (the ad-conversions idiom; do not rely on the global spy). Build a `LeadNotification` with the required fields (leadId, firstName, lastName, email, leadScore, leadQuality, source). Case 1 (high score, webhooks unset): `leadScore: 100` -> `notifyHighValueLead(lead)` resolves to undefined AND the local fetch spy `not.toHaveBeenCalled()` (both webhook gates no-op without reaching private fns). Case 2 (threshold early-return): `leadScore: 0` -> resolves to undefined AND fetch not called.

    No production source change. No `any` types — type the fetch spy as `ReturnType<typeof mock>` cast to the fetch signature exactly as ad-conversions.test.ts does. Run `bun run lint:fix` on the new files before committing.
  </action>
  <verify>
    <automated>bun test tests/unit/db.test.ts tests/unit/error-tracking.test.ts tests/unit/notifications.test.ts tests/unit/ad-conversions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/db.test.ts` asserts `await db.select()` toEqual `[]` and a chained query resolves `[]`; no throw. Green in isolation.
    - `tests/unit/error-tracking.test.ts` asserts `Sentry.captureException` not called + `reportError(...)` does not throw; Sentry mock re-registered in `beforeEach`. Green in isolation.
    - `tests/unit/notifications.test.ts` asserts (a) high-score lead resolves undefined with no fetch, (b) low-score lead resolves undefined with no fetch. Uses the public `notifyHighValueLead` seam only; no import of `sendSlackNotification`/`sendDiscordNotification`; no source export added. Green in isolation.
    - The existing `tests/unit/ad-conversions.test.ts` no-op-gate block runs green and is cited (not duplicated).
    - No production source file modified; no `any` types in the new tests.
  </acceptance_criteria>
  <done>All four target test files pass (`bun test` on the four files green). The three new files each assert the documented no-op contract (no throw + [] / no-Sentry-call / no-fetch). No source behavior change, no new source export.</done>
</task>

<task type="auto">
  <name>Task 2: NOOP-01 — reconcile v6-AUDIT-FINDINGS.md Section 2 to origin/main reality</name>
  <files>.planning/v6-AUDIT-FINDINGS.md</files>
  <read_first>
    - .planning/v6-AUDIT-FINDINGS.md (current Section 2 — the target; KEEP/CLEANUP/DECIDE buckets)
    - .planning/ROADMAP.md (phase numbering: 12=errorboundary, 13=admin-error-observability, 14=admin-page-title, 15=dead-code-cleanup) + STATE.md (phase 11-15 outcomes)
    - 16-RESEARCH.md "Common Pitfalls -> Pitfall 1" (the stale-local-tree hazard + the verified origin/main merge-state table)
  </read_first>
  <action>
    GIT-STATE GATE FIRST (RESEARCH Pitfall 1 — the local working tree is divergent from origin/main; do NOT be misled by on-disk `processingFees` / logger `group/groupEnd/table` / `pageTitle`). Run `git fetch origin main` then re-verify each item against `origin/main` (not the local tree). The verified merge state at research time was: Phase 12 error-report route MERGED (PR #333), Phase 13 AdminQueryResult seam MERGED (PR #334), Phase 14 pageTitle removed MERGED (PR #337), Phase 15 logger-methods/processingFees removed MERGED (PR #338, `f4d8ab13`). Confirm each with a targeted check on origin/main before writing its disposition:
      - `git cat-file -e origin/main:src/app/api/error-report/route.ts` -> exists => Phase 12 resolved.
      - `git grep -l "AdminQueryResult" origin/main -- 'src/lib/admin/'` -> hits => Phase 13 resolved.
      - `git grep -n "pageTitle" origin/main -- 'src/app/(admin)/admin/layout.tsx'` -> no hit => Phase 14 resolved.
      - `git show origin/main:src/lib/logger.ts | grep -nE 'group\(|groupEnd\(|table\('` -> no method hits => logger methods removed; `git grep -n "processingFees" origin/main -- 'src/lib/ttl-calculator/calculator.ts'` -> no hit => Phase 15 resolved.
    If any phase-15 item is unexpectedly still present on origin/main at execute time, write "RESOLVED-IN-PHASE-15 (merge pending)" for that item and do NOT claim it is gone. Otherwise mark RESOLVED-IN-PHASE-15.

    Then edit `.planning/v6-AUDIT-FINDINGS.md` Section 2 so every line carries a final disposition + one-line rationale. Reconcile the formerly-DECIDE/CLEANUP items to their resolving phase (do NOT leave them as standing no-ops):
      - Admin query silent-error-swallow (25 fns, was DECIDE) -> RESOLVED-IN-PHASE-13 (full error states everywhere; AdminQueryResult/AdminDetailResult seam supersedes the v4 return-[] lock).
      - admin `pageTitle="Admin"` (layout.tsx, was DECIDE) -> RESOLVED-IN-PHASE-14 (prop removed; canonical Next.js 16 per-route title).
      - ErrorBoundary finding #3 (the Section-1 stub; note its resolution here for a closed record) -> RESOLVED-IN-PHASE-12 (real /api/error-report route + Sonner; no alert()).
      - logger `group`/`groupEnd`/`table` (was CLEANUP) -> RESOLVED-IN-PHASE-15 (removed).
      - ttl `processingFees` always-0 (was CLEANUP-minor) -> RESOLVED-IN-PHASE-15 (removed).
      - contact-welcome `PARAGRAPH_STYLE.whiteSpace` (was CLEANUP) -> VERIFIED-INTENTIONAL (kept + documented in Phase 15).
    Confirm the genuinely env-gated no-ops as VERIFIED-INTENTIONAL with rationale (graceful degradation / documented groundwork; they remain by design): ad-conversions `sendAdConversion` + the unused `value`/`currency`/`occurredAt` params; error-tracking `reportError`/SENTRY_DSN gate; notifications Slack/Discord webhook gates; `db.ts` createMockDb proxy; rate-limiter Redis empty-catch fallback; logger prod log-level drops; attribution `writeStore` quota empty-catch; use-blob-upload `probeUploadDisabled` returns-false fallback; the `isResendConfigured()`-gated email success paths; admin images upload `onUploadCompleted` logs-only.
    For the four env-gated no-ops now locked by Task 1's tests (db proxy, sendAdConversion, reportError, Slack/Discord-via-notifyHighValueLead), append a short "locked by regression test: tests/unit/{db,error-tracking,notifications}.test.ts + tests/unit/ad-conversions.test.ts" note so the doc records the intentional-by-contract status.

    The doc must read as a closed, accurate v6 record: the 6 stubs fixed (which phase each, per Section 4's phase map + STATE.md), the formerly-DECIDE/CLEANUP items resolved (which phase), and the standing env-gated no-ops confirmed intentional. This is `.planning`-only — NO code change. Do NOT use em/en dashes in any prose you add (keep doc prose dash-free for consistency — use hyphen, comma, or "to").
  </action>
  <verify>
    <automated>grep -v '^#' .planning/v6-AUDIT-FINDINGS.md | grep -q "VERIFIED-INTENTIONAL" && grep -q "RESOLVED-IN-PHASE-13" .planning/v6-AUDIT-FINDINGS.md && grep -q "RESOLVED-IN-PHASE-14" .planning/v6-AUDIT-FINDINGS.md && grep -q "RESOLVED-IN-PHASE-15" .planning/v6-AUDIT-FINDINGS.md && grep -q "RESOLVED-IN-PHASE-12" .planning/v6-AUDIT-FINDINGS.md && ! grep -nP '[\x{2013}\x{2014}]' .planning/v6-AUDIT-FINDINGS.md && echo "PASS: dispositions present, dash-free"</automated>
  </verify>
  <acceptance_criteria>
    - Each item in Section 2 has a final disposition (VERIFIED-INTENTIONAL or RESOLVED-IN-PHASE-NN) plus a one-line rationale.
    - The four formerly-DECIDE/CLEANUP items reconcile to: admin silent-error-swallow -> Phase 13, admin pageTitle -> Phase 14, logger group/groupEnd/table -> Phase 15, ttl processingFees -> Phase 15; ErrorBoundary finding #3 noted RESOLVED-IN-PHASE-12; contact-welcome whiteSpace -> VERIFIED-INTENTIONAL.
    - Dispositions match a re-verified `origin/main` (the git-state gate ran; no claim contradicts the grep on origin/main).
    - The four test-locked no-ops carry an "intentional-by-contract / locked by regression test" note.
    - No code file changed. No em/en dash added to the doc.
  </acceptance_criteria>
  <done>`.planning/v6-AUDIT-FINDINGS.md` Section 2 is a closed, accurate v6 record: every intentional no-op disposed VERIFIED-INTENTIONAL or RESOLVED-IN-PHASE-NN with a rationale, reconciled to origin/main, dash-free. No source change.</done>
</task>

<task type="auto">
  <name>Task 3: Full phase gate</name>
  <files>(no files changed — gate only)</files>
  <read_first>
    - .planning/STATE.md (the documented pre-existing ~21-failure baseline: homepage.test.tsx + navigation.test.tsx cross-file RTL pollution; new tests must add 0 net-new failures)
    - 16-RESEARCH.md "Pitfall 4" (do NOT fix the pollution failures — out of scope)
  </read_first>
  <action>
    Run the full phase gate in order: `bun run lint` (clean), `bun run typecheck` (clean), `bun run test:unit` (the 3 new files + the verified ad-conversions block pass; full-suite failure count is unchanged from the documented ~21 pre-existing homepage/navigation pollution baseline — 0 net-new), `bun run build` (compiles). Do NOT touch the pre-existing pollution failures (RESEARCH Pitfall 4 — out of scope). If lint flags the new test files, fix the test files only (no source change). Record the pass/fail counts in the SUMMARY and confirm 0 net-new failures vs baseline.
  </action>
  <verify>
    <automated>bun run lint && bun run typecheck && bun run test:unit && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `bun run lint` clean; `bun run typecheck` clean; `bun run build` compiles successfully.
    - `bun run test:unit`: the 3 new files + the ad-conversions no-op block pass; full-suite failure count equals the documented ~21 pre-existing baseline (0 net-new failures).
    - No production source file changed by this task.
  </acceptance_criteria>
  <done>The full gate (lint + typecheck + test:unit + build) passes with 0 net-new test failures beyond the documented pre-existing baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds three unit test files and edits one `.planning` doc. It introduces no new code path, endpoint, input handler, network surface, or trust boundary. The behavior under test is the EXISTING graceful-degradation no-op (services do not fire without configured creds) — NOOP-02 locks that property by contract, which is a net security positive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | `tests/unit/*` (test-only change) + `.planning/v6-AUDIT-FINDINGS.md` (doc-only) | accept | No production source behavior change; no new surface. Phase gate (lint + typecheck + test:unit + build) confirms the existing behavior is unchanged. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "Package Legitimacy Audit": not applicable). No slopcheck checkpoint required. |
</threat_model>

<verification>
- All four target test files green: `bun test tests/unit/db.test.ts tests/unit/error-tracking.test.ts tests/unit/notifications.test.ts tests/unit/ad-conversions.test.ts`.
- `.planning/v6-AUDIT-FINDINGS.md` Section 2 disposed and reconciled to origin/main; dash-free.
- Full gate: `bun run lint && bun run typecheck && bun run test:unit && bun run build` — 0 net-new test failures vs the documented ~21 pre-existing homepage/navigation pollution baseline.
- No production source file modified; no new source export (Slack/Discord stay private).
</verification>

<success_criteria>
- Three new regression tests (`db`, `error-tracking`, `notifications`) assert the documented no-op contract (no throw + [] / no-Sentry-call / no-fetch) and pass.
- The existing `sendAdConversion` no-op coverage is verified green and cited (not duplicated).
- `.planning/v6-AUDIT-FINDINGS.md` is a closed, accurate v6 record: 6 stubs fixed (which phase each), formerly-DECIDE/CLEANUP items resolved (which phase), env-gated no-ops confirmed VERIFIED-INTENTIONAL, the four test-locked ones noted intentional-by-contract.
- Full phase gate passes with 0 net-new failures.
- No production source behavior change.
</success_criteria>

<output>
Create `.planning/phases/16-intentional-noop-confirmation/16-01-SUMMARY.md` when done. Record: the three new test files + their assertions, the verified ad-conversions citation, the git-state re-verification result for the NOOP-01 reconciliation, and the final gate pass/fail counts (confirming 0 net-new vs the ~21 baseline).
</output>

---
phase: 33-showcase-routes
plan: 01
type: execute
---

<objective>
Create unified /showcase route that displays both quick portfolio items and detailed case studies.

Purpose: Replace separate /portfolio and /case-studies with single /showcase route that adapts display based on showcaseType.
Output: Working /showcase and /showcase/[slug] routes.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/lib/showcase.ts
@src/app/portfolio/page.tsx
@src/app/portfolio/[slug]/page.tsx
@src/app/case-studies/page.tsx
@src/app/case-studies/[slug]/page.tsx

**Prerequisite:** Phase 32 complete (showcase data layer exists)

**Route behavior:**
- /showcase - List all items, optionally filter by type
- /showcase/[slug] - Detail page that adapts UI based on showcaseType:
  - 'quick': Show portfolio-style view (image, stats, tech stack)
  - 'detailed': Show case-study-style view (challenge/solution/results narrative)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /showcase listing page</name>
  <files>src/app/showcase/page.tsx, src/app/showcase/loading.tsx</files>
  <action>
Create src/app/showcase/page.tsx:
1. Import from showcase.ts data layer
2. Display all showcase items in unified grid
3. Use Card component with variant="project" for consistency
4. Add optional filter tabs: All | Portfolio | Case Studies
5. Use ISR with revalidate = 3600

Create src/app/showcase/loading.tsx with spinner fallback.

Base the design on portfolio/page.tsx but support both item types.
  </action>
  <verify>bun run build succeeds, /showcase route exists in output</verify>
  <done>Showcase listing page created</done>
</task>

<task type="auto">
  <name>Task 2: Create /showcase/[slug] detail page</name>
  <files>src/app/showcase/[slug]/page.tsx</files>
  <action>
Create src/app/showcase/[slug]/page.tsx:
1. Fetch item by slug from showcase.ts
2. Check showcaseType to determine rendering:
   - If 'quick': Render portfolio-style (from portfolio/[slug])
   - If 'detailed': Render case-study-style (challenge/solution/results sections)
3. Include generateStaticParams for static generation
4. Include generateMetadata for SEO
5. Schema markup for rich results
6. Related items section

Combine the best of both portfolio/[slug] and case-studies/[slug] detail pages.
  </action>
  <verify>bun run build succeeds, /showcase/[slug] route works</verify>
  <done>Showcase detail page created with type-aware rendering</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>New /showcase routes with unified display</what-built>
  <how-to-verify>
    1. Run: bun run dev
    2. Visit: http://localhost:3000/showcase
    3. Verify: All items display (both portfolio and case study types)
    4. Click a portfolio-type item: Verify portfolio-style detail view
    5. Click a case-study-type item: Verify challenge/solution/results narrative view
    6. Verify: Navigation, loading states, and responsiveness work
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] /showcase listing page renders all items
- [ ] /showcase/[slug] adapts UI based on showcaseType
- [ ] Quick items show portfolio-style view
- [ ] Detailed items show case-study-style view
- [ ] SEO metadata works
- [ ] `bun run build` succeeds
</verification>

<success_criteria>
- /showcase route displays unified content
- Detail pages adapt to content type
- Design consistent with existing site style
- Ready for reference updates in Phase 34
</success_criteria>

<output>
After completion, create `.planning/phases/33-showcase-routes/33-01-SUMMARY.md`
</output>

---
phase: 28-remove-resources
plan: 01
type: execute
---

<objective>
Delete lead magnet resource pages and their orphaned API route.

Purpose: Continue v3.0 Website Consolidation - remove unused marketing lead-magnet pages.
Output: Cleaner codebase with 3 fewer files (2 resource pages + 1 API route).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Prior Phase:**
Phase 27 complete - deleted industries/locations (~1,509 LOC removed).

**Files to Delete:**
- `src/app/resources/conversion-optimization-toolkit/page.tsx` (~298 lines)
- `src/app/resources/website-performance-checklist/page.tsx` (~358 lines)
- `src/app/api/lead-magnet/route.ts` - orphaned API only used by resource pages

**Verification:**
- No navigation links to /resources
- Download files referenced don't exist (public/resources/downloads/ missing)
- lead-magnet API only used by these two pages
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete resources pages and lead-magnet API</name>
  <files>src/app/resources/, src/app/api/lead-magnet/</files>
  <action>
Delete the resource pages and orphaned API:
1. Delete src/app/resources/ directory (2 lead magnet pages)
2. Delete src/app/api/lead-magnet/ directory (orphaned API)
  </action>
  <verify>
ls src/app/resources 2>/dev/null || echo "resources deleted"
ls src/app/api/lead-magnet 2>/dev/null || echo "lead-magnet API deleted"
  </verify>
  <done>All files deleted: 2 resource pages + 1 API route</done>
</task>

<task type="auto">
  <name>Task 2: Verify build passes</name>
  <files>None</files>
  <action>Run typecheck and build to ensure no broken references.</action>
  <verify>bun run typecheck && bun run build</verify>
  <done>TypeScript compiles, build succeeds</done>
</task>

</tasks>

<verification>
- [ ] `ls src/app/resources` returns "No such file or directory"
- [ ] `ls src/app/api/lead-magnet` returns "No such file or directory"
- [ ] `bun run typecheck` passes
- [ ] `bun run build` succeeds
</verification>

<success_criteria>
- All target files deleted
- Build succeeds
- Ready for Phase 29 (Tools Cleanup)
</success_criteria>

---
phase: 64-cache-components
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/blog.ts
  - src/app/api/blog/n8n/route.ts
autonomous: true
requirements:
  - CACHE-01

must_haves:
  truths:
    - "All read functions in src/lib/blog.ts are wrapped with 'use cache' + cacheLife() + cacheTag()"
    - "List-level reads use cacheTag('blog-posts'); item-level reads use cacheTag('blog-posts', `blog-post:${slug}`)"
    - "If an n8n auto-publish webhook handler exists in src/app/api/blog/, it calls updateTag('blog-posts') after a successful insert"
    - "src/app/blog/[slug]/page.tsx and src/app/blog/page.tsx no longer call any data-layer function with non-serialisable arguments"
  artifacts: []
  key_links:
    - from: "src/lib/blog.ts"
      to: "next/cache"
      via: "import { cacheLife, cacheTag, updateTag }"
      pattern: "'use cache'"
---

<objective>
Migrate the blog data layer (`src/lib/blog.ts`) to function-level Cache Components: `'use cache'` directive on every read, `cacheLife('hours')` for lists / `cacheLife('days')` for items, `cacheTag()` keyed on domain + slug. If an n8n auto-publish webhook exists, add `updateTag('blog-posts')` to invalidate after successful inserts.

Purpose: Demonstrate the full Cache Components pattern on the highest-traffic, most-write-active domain. This sets the template for plans 02 and 03 (showcase + help).
Output: Updated `src/lib/blog.ts` with directives + tags. Optional one-line addition to the n8n webhook handler.
</objective>

<execution_context>
Standard GSD execute-plan flow.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-cache-components/64-CONTEXT.md
@.planning/phases/64-cache-components/64-RESEARCH.md

<interfaces>
<!-- Pattern reminder from RESEARCH.md -->

```typescript
import { cacheLife, cacheTag } from 'next/cache'

export async function getAllPosts() {
  'use cache'
  cacheLife('hours')
  cacheTag('blog-posts')

  return db.select().from(blogPosts).orderBy(desc(blogPosts.publishedAt))
}

export async function getPostBySlug(slug: string) {
  'use cache'
  cacheLife('days')
  cacheTag('blog-posts', `blog-post:${slug}`)

  const [post] = await db.select().from(blogPosts).where(eq(blogPosts.slug, slug))
  return post ?? null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Audit current shape (no edits)</name>
  <action>
Before editing, read `src/lib/blog.ts` end-to-end. List in the SUMMARY:
- Every exported function and its return type
- Whether any function takes a non-serialisable argument (object with methods, Date instance constructed by caller, etc.)
- Whether any function reads cookies/headers (would block caching)
- Confirm whether `src/app/api/blog/` contains an n8n auto-publish handler (path may be `src/app/api/blog/n8n/route.ts` or similar)

This audit informs the exact shape of Tasks 1-2.
  </action>
  <verify>
    <manual>Audit captured in SUMMARY draft notes.</manual>
  </verify>
  <done>Audit complete and documented.</done>
</task>

<task type="auto">
  <name>Task 1: Wrap every read function in src/lib/blog.ts with 'use cache'</name>
  <files>src/lib/blog.ts</files>
  <action>
Add `import { cacheLife, cacheTag } from 'next/cache'` at the top.

For each exported READ function (anything that doesn't INSERT/UPDATE/DELETE):
1. Add `'use cache'` as the first line of the function body
2. Add `cacheLife('hours')` for list-shaped reads (`getAllPosts`, `getPostsByTag`, `getPostsByAuthor`, `getRelatedPosts`)
3. Add `cacheLife('days')` for single-item reads (`getPostBySlug`)
4. Add `cacheTag()` calls:
   - List reads: `cacheTag('blog-posts')`. For tag/author lists, also include the scoped tag: `cacheTag('blog-posts', `blog-tag:${tag}`)`
   - Item reads: `cacheTag('blog-posts', `blog-post:${slug}`)`

Do NOT touch any write function (insert/update/delete) — those are handled in Task 2.

If the audit in Task 0 found a function that takes a non-serialisable argument, refactor the signature to accept primitive args only (string/number/boolean), or skip caching that function and document the reason in SUMMARY.
  </action>
  <verify>
    <automated>bun run typecheck && grep -c "'use cache'" src/lib/blog.ts</automated>
    <manual>Grep count should match the number of read functions identified in Task 0.</manual>
  </verify>
  <done>Every read function has 'use cache' + cacheLife + cacheTag; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add updateTag invalidation to blog write paths (if any)</name>
  <files>src/app/api/blog/n8n/route.ts (or similar — confirmed via Task 0 audit)</files>
  <action>
If Task 0 found an n8n auto-publish handler:
1. Add `import { updateTag } from 'next/cache'` at the top
2. After the successful insert, call `updateTag('blog-posts')` to flush the list cache
3. If the inserted row's slug is known, also call `updateTag(`blog-post:${insertedSlug}`)` to flush any pre-existing item cache for that slug (covers re-publishes)

If NO write path exists in src/app/api/, skip this task and note in SUMMARY: "No write path found — cache tags are reserved for future use."
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint 2>&1 | tail -5</automated>
    <manual>If a write path was found, manually trigger it (e.g., POST to the webhook with a test payload) and verify the next /blog request reflects the new post.</manual>
  </verify>
  <done>updateTag wired into the existing write path, OR documented as deferred because no write path exists.</done>
</task>

<task type="auto">
  <name>Task 3: Verify pages still work without page-level revalidate</name>
  <files>src/app/blog/page.tsx (read only), src/app/blog/[slug]/page.tsx (read only)</files>
  <action>
These pages currently do NOT have `export const revalidate` (audit confirmed earlier). Verify they still render and behave correctly with the new function-level caching.

If the audit reveals that ONLY the data layer has changed (no page-level directives existed), no edits are needed here — just run the verify step.

If a page DOES have `export const revalidate`, remove it (the data layer now owns caching).
  </action>
  <verify>
    <automated>bun run build 2>&1 | grep -A2 -E "/blog|blog/\[slug\]" | head -20</automated>
    <manual>Build output should show /blog as cached (look for "(Cache)" or static markers in the route table). No build errors.</manual>
  </verify>
  <done>Build succeeds; blog routes are cached as expected.</done>
</task>

</tasks>

<verification>
1. `bun run typecheck && bun run lint && bun run test:unit` all pass
2. `bun run build && bun run start` — visit `/blog` and `/blog/[a-real-slug]`, confirm fast TTFB on the second request
3. If write path was wired in Task 2: trigger a publish, confirm the new post appears immediately on `/blog` without a manual rebuild
4. `grep -n "export const revalidate" src/app/blog/` returns zero matches
</verification>

<success_criteria>
- src/lib/blog.ts uses 'use cache' on every read function with appropriate cacheLife + cacheTag
- updateTag wired into write path OR explicitly documented as deferred
- /blog and /blog/[slug] still render correctly and are cached
- Typecheck, lint, unit tests all green
- One manual cache-hit verification recorded in SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/64-cache-components/64-01-SUMMARY.md`. SUMMARY must include the Task 0 audit findings (function inventory + write-path status) so plans 02 and 03 can mirror the pattern accurately.
</output>

---
phase: 64-cache-components
plan: "02"
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - src/lib/showcase.ts
  - src/lib/help-articles.ts
  - src/app/portfolio/[slug]/page.tsx
  - src/app/showcase/page.tsx
  - src/app/help/[category]/[slug]/page.tsx
autonomous: true
requirements:
  - CACHE-02
  - CACHE-03

must_haves:
  truths:
    - "All read functions in src/lib/showcase.ts wrapped with 'use cache' + cacheLife() + cacheTag()"
    - "All read functions in src/lib/help-articles.ts wrapped with 'use cache' + cacheLife() + cacheTag()"
    - "src/app/portfolio/[slug]/page.tsx no longer has `export const revalidate = 3600`"
    - "src/app/showcase/page.tsx no longer has `export const revalidate = 3600`"
    - "src/app/help/[category]/[slug]/page.tsx — `export const dynamic = 'force-dynamic'` is either removed (preferred) or retained with an inline comment justifying why this route MUST render dynamically per request"
  artifacts: []
---

<objective>
Apply the pattern established in plan 01 to the showcase and help-articles data layers, then strip the page-level caching directives that the data layer now supersedes.

Purpose: Eliminate `export const revalidate` from this codebase by moving caching into the data layer where invalidation can be granular and tag-based.
Output: Two data-layer files cached; three pages cleaned up.
</objective>

<execution_context>
Standard GSD execute-plan flow.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-cache-components/64-CONTEXT.md
@.planning/phases/64-cache-components/64-RESEARCH.md
@.planning/phases/64-cache-components/64-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate src/lib/showcase.ts</name>
  <files>src/lib/showcase.ts</files>
  <action>
Mirror the plan 01 pattern. For each exported READ function:
- `'use cache'` first line
- `cacheLife('hours')` for list reads, `cacheLife('days')` for item reads
- `cacheTag('showcase-list')` for list reads
- `cacheTag('showcase-list', `showcase:${slug}`)` for item reads

Read the file end-to-end first; if any function takes non-serialisable args, refactor to primitives or skip with a documented reason.
  </action>
  <verify>
    <automated>bun run typecheck && grep -c "'use cache'" src/lib/showcase.ts</automated>
  </verify>
  <done>Every read wrapped; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate src/lib/help-articles.ts</name>
  <files>src/lib/help-articles.ts</files>
  <action>
Same pattern. Tags: `help-articles` (list), `help-articles` + `help-article:${slug}` (item), `help-articles` + `help-category:${categorySlug}` (category list).
  </action>
  <verify>
    <automated>bun run typecheck && grep -c "'use cache'" src/lib/help-articles.ts</automated>
  </verify>
  <done>Every read wrapped; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Drop export const revalidate from portfolio + showcase pages</name>
  <files>src/app/portfolio/[slug]/page.tsx, src/app/showcase/page.tsx</files>
  <action>
- `src/app/portfolio/[slug]/page.tsx:18` — delete `export const revalidate = 3600`
- `src/app/showcase/page.tsx:12` — delete `export const revalidate = 3600`

The data layer now owns caching for both routes via `cacheLife` on the read functions.
  </action>
  <verify>
    <automated>grep -n "export const revalidate" src/app/portfolio/ src/app/showcase/ 2>&1 | head -5</automated>
    <manual>Above grep returns zero matches.</manual>
  </verify>
  <done>Page-level directives removed; build still succeeds.</done>
</task>

<task type="auto">
  <name>Task 4: Decide on help/[category]/[slug] force-dynamic</name>
  <files>src/app/help/[category]/[slug]/page.tsx</files>
  <action>
Read the file end-to-end. Determine whether `export const dynamic = 'force-dynamic'` (line 31) is justified:

JUSTIFIED if the page reads cookies, headers, or per-request session data. (Help articles are public — almost certainly NOT the case here.)

NOT JUSTIFIED — remove the line. Help articles are now cached at the data layer with `cacheLife('days')` + `cacheTag('help-articles', `help-article:${slug}`)`.

If you remove it: also confirm no consumer of the page relies on per-request behaviour (e.g., a child component that reads cookies — unlikely on a public help page).

If you keep it: add an inline comment immediately above the directive explaining the constraint (e.g., `// must render per-request because XYZ`). Don't leave it unexplained.
  </action>
  <verify>
    <automated>bun run build 2>&1 | grep -A2 "/help/" | head -20</automated>
    <manual>If removed: build output shows the help route as cached. If kept: comment is present and reason is clear.</manual>
  </verify>
  <done>Decision made and documented; build succeeds either way.</done>
</task>

</tasks>

<verification>
1. `bun run typecheck && bun run lint && bun run test:unit && bun run test:e2e:fast` all pass
2. `bun run build` succeeds; route table shows /portfolio/[slug], /showcase, /help/[category]/[slug] as cached (or as dynamic-with-justification for the last one)
3. `grep -rn "export const revalidate" src/app/` returns zero matches
4. `grep -rn "export const dynamic" src/app/` returns zero matches OR only matches that are accompanied by an explanatory comment
5. Manual: visit `/showcase`, `/portfolio/[a-slug]`, `/help/[a-cat]/[a-slug]` in production-mode (`bun run start`); confirm cache hits on second request
</verification>

<success_criteria>
- showcase + help data layers fully cached
- All `export const revalidate` removed from src/app/
- `export const dynamic = 'force-dynamic'` either removed or documented
- Typecheck, lint, unit tests, E2E fast suite all green
- Cache-hit behaviour verified manually
</success_criteria>

<output>
After completion, create `.planning/phases/64-cache-components/64-02-SUMMARY.md` AND `.planning/phases/64-cache-components/64-VERIFICATION.md` (phase-level — consolidates plans 01 + 02).
</output>

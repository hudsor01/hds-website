---
phase: 59-tool-page-polish
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/unit/tool-page-layout.test.tsx
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03]

must_haves:
  truths:
    - "test file exists with failing assertions for ToolPageLayout header (no icon, title + description only)"
    - "test file has failing assertions for result card glass-card-light class"
    - "test file has failing assertions for action bar conditional rendering"
    - "test file has failing assertion for CalculatorInput label-above render"
    - "all existing 394 tests continue to pass"
  artifacts:
    - path: "tests/unit/tool-page-layout.test.tsx"
      provides: "TDD RED scaffold for TOOL-01, TOOL-02, TOOL-03"
      exports: []
  key_links:
    - from: "tests/unit/tool-page-layout.test.tsx"
      to: "src/components/layout/ToolPageLayout.tsx"
      via: "import (will fail until Plan 02 creates the file)"
      pattern: "from '@/components/layout/ToolPageLayout'"
---

<objective>
Write the RED-phase TDD test file for ToolPageLayout — failing assertions that define the contract for Plan 02's implementation.

Purpose: Establish verifiable acceptance criteria for TOOL-01 (header rendering), TOOL-03 (result card glass treatment, action bar), and TOOL-02 (CalculatorInput label-above pattern) before any implementation exists.
Output: tests/unit/tool-page-layout.test.tsx with failing tests, all existing tests green.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/59-tool-page-polish/59-RESEARCH.md
@tests/unit/components.test.tsx
</context>

<feature>
  <name>ToolPageLayout TDD Red Scaffold</name>
  <files>tests/unit/tool-page-layout.test.tsx</files>
  <behavior>
    This is the RED phase. Write tests that WILL FAIL because ToolPageLayout does not exist yet.
    Tests import from '@/components/layout/ToolPageLayout' — this import will fail until Plan 02.
    Use the same bun:test + @testing-library/react pattern as tests/unit/components.test.tsx.

    Required test cases:

    **TOOL-01: Header Rendering**
    - renders h1 with the provided title text
    - renders description paragraph with text-lead and text-muted-foreground classes
    - does NOT render any icon element in the header (no role="img" in header area)
    - header does NOT have hero-style background (no gradient/hero class)

    **TOOL-03: Result Card**
    - renders with glass-card-light class on result card container when resultSlot is provided and hasResult=true
    - renders placeholder text when hasResult=false and columns="two" (result pane always visible in two-column)
    - does NOT render placeholder pane when columns="single" (single-column renders results inline, no persistent pane)
    - renders action bar only when actions array is provided and non-empty
    - does NOT render action bar when no actions prop passed
    - renders Copy button when actions includes { type: 'copy', label: 'Copy' }
    - renders Download button when actions includes { type: 'download', label: 'Download' }

    **TOOL-02: CalculatorInput (extend components.test.tsx via this file OR add describe block there)**
    Import CalculatorInput from '@/components/calculators/CalculatorInput' and assert:
    - renders label element ABOVE the input (label appears before input in DOM order)
    - renders help text below input when helpText prop provided
    - renders error text in red (text-destructive class) below input when error prop provided

    Test structure (bun:test pattern from codebase):
    ```typescript
    import { afterEach, describe, expect, it } from 'bun:test'
    import { cleanup, render, screen } from '@testing-library/react'
    // imports for ToolPageLayout and CalculatorInput

    afterEach(() => { cleanup() })

    describe('ToolPageLayout — TOOL-01: Header', () => { ... })
    describe('ToolPageLayout — TOOL-03: Result Card', () => { ... })
    describe('CalculatorInput — TOOL-02: Label Pattern', () => { ... })
    ```

    For the ToolPageLayout tests, create a minimal stub or use the real component import.
    Since the file won't exist yet, the test file itself will compile-error/import-error.
    This is intentional RED state — document this in a comment at the top of the file.

    ToolPageLayout props to use in tests (from RESEARCH.md):
    ```typescript
    interface ToolPageLayoutProps {
      title: string
      description: string
      columns?: 'single' | 'two'
      formSlot: ReactNode
      resultSlot?: ReactNode
      hasResult?: boolean
      resultPlaceholder?: string
      actions?: ToolAction[]
    }
    ```
  </behavior>
  <implementation>
    RED-GREEN-REFACTOR cycle:
    RED: Write tests → run them → confirm they FAIL (import error or assertion failure)
    GREEN: Plan 02 creates ToolPageLayout → tests should pass
    REFACTOR: N/A for scaffold phase

    Run after writing:
    `bun test tests/unit/tool-page-layout.test.tsx 2>&1 | tail -20`
    Expected: errors (import failure or assertion failures) — this is correct RED state.

    Run to confirm no regressions:
    `bun test tests/ 2>&1 | grep -E "Tests:|Snapshots:|pass|fail" | tail -5`
    NOTE: The new test file will show failures/errors. All OTHER test files must still pass (394 tests).
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED-phase test file for ToolPageLayout and CalculatorInput</name>
  <files>tests/unit/tool-page-layout.test.tsx</files>
  <action>
    Create tests/unit/tool-page-layout.test.tsx with bun:test + @testing-library/react.

    Add comment at top: "RED PHASE — ToolPageLayout does not exist yet. Tests will fail until Plan 02 creates src/components/layout/ToolPageLayout.tsx"

    Import pattern (same as components.test.tsx):
    ```typescript
    import { afterEach, describe, expect, it } from 'bun:test'
    import { cleanup, render, screen } from '@testing-library/react'
    import type { ReactNode } from 'react'
    import { CalculatorInput } from '@/components/calculators/CalculatorInput'
    import { ToolPageLayout } from '@/components/layout/ToolPageLayout'
    ```

    Write the test cases listed in the behavior section. For ToolPageLayout render calls, pass minimal required props:
    - title="Test Tool"
    - description="Test description text"
    - formSlot={<div>form</div>}
    - columns="two" | "single" (per test case)
    - hasResult={true} | {false} (per test case)
    - resultPlaceholder="Fill in the form to see results"
    - actions={[{ type: 'copy', label: 'Copy', onClick: () => {} }]}

    After writing the file, run:
    `bun test tests/unit/tool-page-layout.test.tsx 2>&1 | tail -20`
    Confirm the test file produces errors (RED state). This is correct.

    Then run:
    `bun test tests/ --testPathPattern="(?!tool-page-layout)" 2>&1 | grep -E "pass|fail" | tail -5`
    If bun:test does not support --testPathPattern, run full suite and note tool-page-layout errors are expected:
    `bun test tests/ 2>&1 | tail -10`
    Confirm all 394 existing tests in OTHER files still pass.
  </action>
  <verify>
    <automated>bun test tests/unit/tool-page-layout.test.tsx 2>&1 | grep -E "error|fail|Error" | head -5</automated>
  </verify>
  <done>
    - tests/unit/tool-page-layout.test.tsx exists
    - Running it produces import errors or assertion failures (RED state confirmed)
    - All 394 tests in other test files continue to pass
    - Commit: test(59-01): add RED-phase scaffold for ToolPageLayout (TOOL-01/02/03)
  </done>
</task>

</tasks>

<verification>
After commit:
1. `bun test tests/unit/tool-page-layout.test.tsx` → shows errors/failures (RED confirmed)
2. `bun test tests/ 2>&1 | tail -5` → existing 394 tests still pass
</verification>

<success_criteria>
- test file committed with failing assertions for TOOL-01, TOOL-02, TOOL-03
- existing 394 tests unaffected
- RED state documented and verified
</success_criteria>

<output>
After completion, create .planning/phases/59-tool-page-polish/59-01-SUMMARY.md
</output>

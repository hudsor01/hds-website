# v9 Content Engine — REQUIREMENTS

**Goal:** produce and publish enough SEO + social optimized blog posts to fill a
29-day Facebook scheduling window at 3 posts/day (87 posts), evenly distributed
across the 10 canonical content pillars, with every post tracked through a PR and
verified against a fixed guardrail checklist so no drift accumulates.

## The math

- Window: 29 days x 3 posts/day = **87** Facebook posts = **87** blog posts (1:1).
- Inventory: **9** existing published posts (reused).
- To write: **78** new posts.

## Even distribution (target totals per pillar)

Phase numbers map pillar -> phase (23..32). Each pillar ends at 8 or 9 total.

| Phase | Pillar | Have | Write | Total |
|---|---|:--:|:--:|:--:|
| 23 | 1. Small-business web design | 2 | 6 | 8 |
| 24 | 2. Website performance / Core Web Vitals | 0 | 9 | 9 |
| 25 | 3. Local SEO / get found on Google | 0 | 9 | 9 |
| 26 | 4. Conversion optimization | 2 | 6 | 8 |
| 27 | 5. Booking, payments and automation | 2 | 7 | 9 |
| 28 | 6. Own your site / migrate off platforms | 0 | 9 | 9 |
| 29 | 7. Industry verticals (home/local services) | 0 | 9 | 9 |
| 30 | 8. What a website costs / buying guide | 2 | 6 | 8 |
| 31 | 9. Running the business (ops/finance) | 0 | 9 | 9 |
| 32 | 10. Technical / developer authority | 1 | 8 | 9 |
| | **Total** | **9** | **78** | **87** |

Pillar definitions, SME angles, and the topic backlog live in
`.planning/CONTENT-STRATEGY.md`.

## Architecture decisions (locked)

- **Source of truth:** markdown files at `content/blog/<slug>.md` with YAML
  frontmatter; synced to Neon by `scripts/publish-blog.ts` (idempotent upsert by
  slug). The 9 existing Neon posts are migrated to files in Phase 22.
- **Publish gating:** auto-publish on phase-PR merge. The publish script runs
  against Neon so merged posts go live and become linkable in a Facebook post.
- **Content generation:** drafts generated by the local LM Studio model
  `mistral-small-3.2-24b-instruct-2506-mlx` (OpenAI-compatible at
  `http://localhost:1234/v1`) via `scripts/generate-blog.ts`. Claude reviews and
  edits every draft for guardrails + brand voice before it is committed. Keeps
  Claude token usage low; the local model does the word generation.
- **Guardrails:** enforced by `scripts/validate-blog.ts` wired into the CI Code
  Quality job, in two tiers so strong content is never blocked on an arbitrary
  box (philosophy: push for quality AND volume, not box-fitting):
  - **ERROR (blocks the PR):** only what breaks publish, linkability, the
    absolute brand rule, or ships thin content. R0/R3/R4 (frontmatter, slug,
    taxonomy), R9 (no dash/emoji), R8-floor (min 1000 words, NO upper limit).
  - **WARN (reported, never blocks):** SEO/quality nudges Claude resolves at
    finalization. R1 (title length/keyword), R2 (excerpt length), R5 (link
    count), R6 (CTA), R7 (keyword placement). Run `--strict` to fail on these.

## Per-blog guardrails (Definition of Done, every post)

SEO
- R1 (warn): title aims <= 60 chars and contains the target keyword.
- R2 (warn): meta description (excerpt) ideally 120-160 chars (clamped at render).
- R3 (error): unique kebab-case slug.
- R4 (error): primary pillar tag from the 12-tag taxonomy (+ optional secondary).
- R5 (warn): aim for at least 2 internal links (a tool/service and a related post).
- R6 (warn): one clear CTA (free website plan, or the relevant tool).
- R7 (warn): H2/H3 structure; target keyword early (first 100 words).
- R8 (error): minimum 1000 words (floor pushes quality up; NO ceiling, depth is good); reading_time auto-set.
- R9 (error): NO em-dash or en-dash anywhere; no emojis.
- R10: Article (or BlogPosting) JSON-LD emitted on the post page.

Social
- R11: featureImage set (the Open Graph image for the Facebook/social link preview).
- R12: Open Graph + Twitter Card meta present on the post page (title, description, image).
- R13: a share-worthy hook in the first paragraph.

On-site / linkable
- R14: file present + published to Neon + renders HTTP 200 at /blog/<slug> + appears in the sitemap.

## Phases

- **Phase 22 content-pipeline-foundation:** build the `content/blog` file format,
  `publish-blog.ts`, `validate-blog.ts` (CI gate), `generate-blog.ts` (LM Studio),
  ensure the blog post page emits OG/Twitter meta + Article JSON-LD (R10-R12), and
  migrate the 9 existing posts to files. No new posts yet. Prerequisite for all
  pillar phases.
- **Phases 23-32:** one per pillar; generate + Claude-review + commit that pillar's
  allotment; `validate-blog` green; ship as a PR; auto-publish on merge.

## Milestone done when

87 posts are live (9 migrated + 78 new), evenly distributed per the table above,
every post passing `validate-blog`, all shipped via merged PRs, with sitemap + OG
preview verified, ready to schedule at 3/day for 29 days on Facebook.

#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint, typecheck, and unit tests before push
# E2E tests skipped temporarily due to middleware runtime issue
npm run lint && npm run typecheck && npm run test:unit
